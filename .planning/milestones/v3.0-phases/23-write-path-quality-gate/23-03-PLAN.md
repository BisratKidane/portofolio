---
phase: 23-write-path-quality-gate
plan: 03
type: execute
wave: 3
depends_on: ["23-01", "23-02"]
files_modified: []
autonomous: false
requirements: [QUAL-01]

must_haves:
  truths:
    - "The full npm test suite (backend + frontend) is run from the repo root; displayName.test.js, FamilyMember.test.js, familyMember.geez.test.js, and every new Phase 23 test are confirmed green."
    - "The two pre-existing, unrelated backend integration failures (verifyEmail.test.js VERIFY-04 admin-race, familyMember.dedup.test.js REL-06 TOCTOU) are explicitly identified by name and flagged as pre-existing/out-of-scope, not silently masked or falsely reported as passing (D-08)."
    - "A human has entered a real Tigrinya name via the newly-shipped Add relative / Edit member dialogs and visually confirmed correct rendering — including no fixed-card overflow — on /family and both /manage surfaces (D-07), closing the Phase 22 deferred visual sign-off (22-03-SUMMARY.md carry-forward)."
    - "The same human sign-off confirms the add-relative picker's Ge'ez match (FIND-02) and the clear-to-null round-trip (D-05/DATA-03) against real, freshly-entered data."
  artifacts: []
  key_links: []
---

<objective>
Close the v3.0 milestone's quality gate (QUAL-01): confirm the full test suite (backend + frontend) is green except two explicitly-named pre-existing failures unrelated to this milestone, re-confirm the `displayName`/`geezFullname` unit-test invariants from Phases 18/21 still hold, and obtain the human glyph/visual sign-off against real Tigrinya names entered through the write path Plans 23-01/23-02 just shipped — the exact sign-off Phase 22 deferred (`22-03-SUMMARY.md`) because no member had a Ge'ez name until now.

Purpose: This is the milestone-closing gate. jsdom cannot rasterize glyphs or measure real visual overflow, and a full-suite run is the only way to prove no regression was introduced across two workspaces — both require a combination of an automated run and a recorded human checkpoint, not either alone.
Output: A recorded full-suite test result (with the two known pre-existing failures explicitly named, not masked) and a recorded human sign-off (approved, or a list of issues routed to gap closure) closing SC3 and SC4 of Phase 23, and the deferred Phase 22 visual gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/23-write-path-quality-gate/23-CONTEXT.md
@.planning/phases/22-render-surfaces-read-path/22-03-SUMMARY.md

<locked_decisions>
- D-07: The manual glyph/visual sign-off is done by entering real Tigrinya names via the new dialog (no direct SQL/data seeding) — including the longest realistic name — then visually verifying rendering + fixed-card truncation/overflow on /family and both /manage surfaces. This closes the deferred Phase 22 VIEW-01 gate in one pass.
- D-08: Full `npm test` (backend + frontend) must be green at milestone close. Two pre-existing backend integration failures exist and are unrelated to v3.0 (`verifyEmail` VERIFY-04 admin-race; `familyMember.dedup` REL-06 TOCTOU dedup under REPEATABLE READ) — see Deferred in 23-CONTEXT.md. The gate must not be silently blocked by these, but they must be flagged explicitly rather than masked. This plan does NOT fix them — that is explicitly out of this phase's scope (23-CONTEXT.md Deferred Ideas).
- 22-03-SUMMARY.md: Phase 22 deferred its own visual sign-off because "96 family members total, 0 with any Ge'ez name" existed at the time — the write path (this phase) is what makes real data possible to enter, so this task closes that carry-forward.
- Do NOT modify `verifyEmail.test.js` or `familyMember.dedup.test.js` to make them pass — fixing VERIFY-04/REL-06 is explicitly deferred (23-CONTEXT.md Deferred Ideas: "Could be its own debug/fix task").
</locked_decisions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite quality gate run + pre-existing-failure triage (QUAL-01, D-08)</name>
  <files>None — verification-only, no files modified</files>
  <read_first>
    .planning/phases/23-write-path-quality-gate/23-CONTEXT.md (D-08, Deferred Ideas)
    backend/src/resolvers/verifyEmail.test.js (identify the exact VERIFY-04 test name(s) expected to fail)
    backend/src/services/familyMember.dedup.test.js (identify the exact REL-06 test name(s) expected to fail)
    frontend/src/utils/displayName.test.js (the helper invariants QUAL-01 re-confirms)
    backend/src/models/FamilyMember.test.js (geezFullname derivation invariants, Phase 18)
    backend/src/resolvers/familyMember.geez.test.js (geezFullname GraphQL round-trip invariants, Phase 19)
  </read_first>
  <acceptance_criteria>
    - Every failing test in a full `npm test` run (if any) belongs to `backend/src/resolvers/verifyEmail.test.js` and/or `backend/src/services/familyMember.dedup.test.js` — zero failures anywhere else, including every Phase 18/19/21/22/23 Ge'ez-related test file.
    - `cd frontend && npm test -- displayName` exits `0`.
    - `cd backend && npm test -- FamilyMember` exits `0`.
    - `cd backend && npm test -- familyMember.geez` exits `0`.
    - The SUMMARY.md for this plan names the exact failing test file(s)/test name(s), if any, with a one-line rationale citing D-08 — never silently omitted, never reported as "all green" if a failure exists anywhere.
  </acceptance_criteria>
  <action>
    Run `npm test` from the repo root (invokes `npm test --workspaces`, running both the backend and frontend suites). Capture the full output. Confirm `frontend/src/utils/displayName.test.js`, `backend/src/models/FamilyMember.test.js`, and `backend/src/resolvers/familyMember.geez.test.js` all pass with zero failures — these are the exact SC3 invariants ("the displayName helper and geezFullname derivation unit tests remain green"). Confirm every test file created or modified in Plans 23-01 and 23-02 passes. If failures exist ONLY inside `backend/src/resolvers/verifyEmail.test.js` (VERIFY-04 simultaneous-verify ADMIN race) and/or `backend/src/services/familyMember.dedup.test.js` (REL-06 TOCTOU dedup under REPEATABLE READ), record them explicitly by file and test name in this plan's SUMMARY as pre-existing and unrelated to v3.0 (backend source on these paths is unchanged since Phase 22) — per D-08, do NOT attempt to fix them as part of this task. If any OTHER, un-flagged test fails anywhere in the suite, treat the gate as NOT closed and report the failure — do not proceed to Task 2 until it is resolved or explicitly triaged with the user.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Full suite run recorded; displayName/geezFullname invariant tests (Phases 18/19/21) confirmed green; every Phase 23 test passes; any pre-existing VERIFY-04/REL-06 failures explicitly named and flagged per D-08 (not masked, not silently passed over); no other regression introduced by Phase 23.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual glyph/visual sign-off with a real Tigrinya name entered via the new write path (D-07, closes 22-03 carry-forward)</name>
  <files>None — verification-only checkpoint, no files modified</files>
  <action>
    No code changes in this task — Plans 23-01 and 23-02 already shipped the write-path implementation. The executor's procedural action is: start the app (`npm run dev` from the repo root), sign in, enter a real Tigrinya name via the Edit/Add-relative dialogs for a real family member, then present the checkpoint below to the user for manual visual and functional verification against /family, /manage, and the add-relative picker. Do not proceed past this task until the user responds via `<resume-signal>`.
  </action>
  <what-built>
    The write path for Ge'ez names: Plan 23-01 added the 3 Ge'ez inputs (Ge'ez first name (ስም), Ge'ez last name (ስም ኣቦ), Ge'ez mother's name (ስም ኣደ)) to the shared MemberFields form, wired into both the Add relative and Edit member dialogs, with full round-trip persistence and clear-to-null. Plan 23-02 made the add-relative "other parent" picker match typed Ge'ez text via a custom filterOptions, without changing its Latin-only visible label. This closes the loop with Phase 22's already-shipped render path (getGeezDisplay wired into /family, both /manage surfaces, and admin-table search).
  </what-built>
  <how-to-verify>
    1. Start the app locally (`npm run dev` from the repo root) and sign in.
    2. Open `/manage`, use "Edit" (or "+ Add [relation]") on a real family member and enter a real Tigrinya name into all three new fields — Ge'ez first name (ስም), Ge'ez last name (ስም ኣቦ), Ge'ez mother's name (ስም ኣደ) — using your own device keyboard/IME. Prefer the longest realistic name you have available, to match the original STATE.md blocker's ask.
    3. Save, then reopen the same Edit dialog and confirm all three values reload exactly as entered (SC1 round-trip).
    4. Go to `/family`, locate that member's card, and confirm: the Ge'ez name renders as a distinct line below the Latin name; the card's fixed size (252x120px) is unchanged; every populated row truncates with an ellipsis rather than overflowing — including the worst case of Head tag + Latin name + Ge'ez name + birthday + mother + address all present together.
    5. Back on `/manage`, confirm the same member's Ge'ez name appears correctly on both the admin member table row and the relationship-panel card.
    6. In the `/manage` admin search box, type a Ge'ez substring of the name you just entered and confirm the member's row stays visible.
    7. Open the Add-relative dialog for a different member (relation type "child" works well), click "or pick someone already in your family", and type a Ge'ez substring matching the member you just edited — confirm their option (labelled with their Latin name) surfaces in the picker (FIND-02).
    8. Go back to Edit that member, clear one of the three Ge'ez fields, save, then reopen and confirm it now reads blank — proving the clear-to-null path (D-05/DATA-03) holds end-to-end through the real UI.
  </how-to-verify>
  <resume-signal>Type "approved" if every check in steps 3-8 passes, or describe exactly what's wrong (e.g. "card overflowed for member X", "picker didn't surface member Y for Ge'ez substring Z", "clearing the field did not persist as blank").</resume-signal>
  <verify>
    <human-check>User responds "approved" to the resume-signal after completing steps 1-8 of how-to-verify.</human-check>
  </verify>
  <done>User has typed "approved", confirming a real Tigrinya name entered via the new write-path dialogs round-trips correctly, renders correctly without overflow on /family, appears correctly on both /manage surfaces and the admin search, is matched by the add-relative picker (FIND-02), and clears to blank correctly (D-05) — closing this phase's D-07 gate and the Phase 22 22-03-SUMMARY.md deferred carry-forward.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan makes zero code changes. Task 1 runs the existing test suite; Task 2 is a human visual/functional review of Plans 23-01/23-02's already-shipped, already-tested write-path code against real, freshly-entered data through already-authorized dialogs and already-scoped mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | accept | No applicable threats — this plan has zero file changes, zero new code, zero new input/network/persistence/dependency surface. It verifies (automated test run + human sign-off) what Plans 23-01 and 23-02 already shipped and threat-modeled. |
</threat_model>

<verification>
Task 1: `npm test` from the repo root — every failure (if any) is confirmed to be one of the two named pre-existing files, explicitly recorded in the SUMMARY.
Task 2: Human sign-off recorded via the `checkpoint:human-verify` task's resume-signal ("approved", or a list of concrete issues routed to a gap-closure plan before the milestone closes).
</verification>

<success_criteria>
- `npm test` (backend + frontend) passes with zero unflagged failures; any VERIFY-04/REL-06 failures are named explicitly, not masked.
- `displayName.test.js`, `FamilyMember.test.js`, and `familyMember.geez.test.js` are confirmed green.
- A human has approved the real-Tigrinya-name sign-off covering round-trip persistence, /family card rendering without overflow, both /manage surfaces, the add-relative picker's Ge'ez match, and clear-to-null.
- The v3.0 milestone's QUAL-01 requirement and Phase 22's deferred visual-sign-off carry-forward are both closed.
</success_criteria>

<output>
Create `.planning/phases/23-write-path-quality-gate/23-03-SUMMARY.md` when done, recording the full-suite result (including any explicitly-named pre-existing failures) and the human sign-off outcome (approved / issues found + routed to gap closure).
</output>
