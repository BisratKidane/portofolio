---
phase: 22-render-surfaces-read-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/FamilyTreePage.jsx
  - frontend/src/pages/ManagePage.jsx
autonomous: true
requirements: [VIEW-01, VIEW-02, FIND-01]

must_haves:
  truths:
    - "The /family tree page's GraphQL query fetches geezFullname for every member, so MemberNode.jsx has the data to render (VIEW-01)."
    - "The /manage page's GraphQL queries fetch geezFullname for every member on both the relationship-panel path and the admin-table path, so MemberCard.jsx and AdminMemberTable.jsx have the data to render and search (VIEW-02, FIND-01)."
    - "No admin-only field is newly exposed to non-admin viewers — geezFullname is added to the SAME visibility class as the existing fullname field, and the FAMILY_TREE_QUERY's existing D-14/Pitfall 6 comment/guarantee is untouched."
  artifacts:
    - path: "frontend/src/pages/FamilyTreePage.jsx"
      provides: "FAMILY_TREE_QUERY selection set including geezFullname"
      contains: "geezFullname"
    - path: "frontend/src/pages/ManagePage.jsx"
      provides: "EDITABLE_MEMBER_FIELDS and FAMILY_MEMBERS_QUERY both including geezFullname (edited independently)"
      contains: "geezFullname"
  key_links:
    - from: "frontend/src/pages/FamilyTreePage.jsx"
      to: "FAMILY_TREE_QUERY familyMembers selection"
      via: "GraphQL query string field list"
      pattern: "fullname geezFullname gender"
    - from: "frontend/src/pages/ManagePage.jsx"
      to: "EDITABLE_MEMBER_FIELDS + FAMILY_MEMBERS_QUERY"
      via: "GraphQL query string field list (two independent constants)"
      pattern: "fullname geezFullname gender"
---

<objective>
Add the `geezFullname` scalar field to the three GraphQL selection-set constants that feed this phase's render surfaces: `FAMILY_TREE_QUERY` (`FamilyTreePage.jsx`, feeds the `/family` tree card) and `EDITABLE_MEMBER_FIELDS` + `FAMILY_MEMBERS_QUERY` (`ManagePage.jsx`, feed the `/manage` relationship panels and admin table). This is a pure data-prerequisite wave — no rendering or search logic changes here. Without it, Plan 22-02's render and search wiring would silently render/match nothing, because the field simply wouldn't be fetched (ROADMAP SC4, CONTEXT.md D-05).

Purpose: Make the already-available `geezFullname` field (Phase 18/19, backend-complete) actually reach the frontend components that need to render and search it.
Output: Three GraphQL selection-set constants across two files, each including `geezFullname`, with zero other behavioral change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-render-surfaces-read-path/22-CONTEXT.md
@.planning/phases/22-render-surfaces-read-path/22-PATTERNS.md
@.planning/phases/22-render-surfaces-read-path/22-UI-SPEC.md

<locked_decisions>
- D-05 (CONTEXT.md): Add `geezFullname` to `FAMILY_TREE_QUERY`, `EDITABLE_MEMBER_FIELDS`, and `FAMILY_MEMBERS_QUERY`. Add raw `geezFirstname`/`geezLastname`/`geezMothersname` ONLY if a surface actually needs the raw parts — no surface this phase does, so do NOT add them anywhere.
- `EDITABLE_MEMBER_FIELDS` is interpolated into `MY_EDITABLE_MEMBERS_QUERY` and `FAMILY_MEMBER_FOCUS_QUERY` (and their nested relative selections) — editing this ONE constant propagates everywhere it's interpolated. `FAMILY_MEMBERS_QUERY` is a SEPARATE, non-interpolated constant that feeds `AdminMemberTable` — it must be edited independently.
- The `D-14/Pitfall 6` comment above `FAMILY_TREE_QUERY` (about never exposing the admin-only account-link field to this non-admin-reachable query) must be left untouched; `geezFullname` is not admin-only, so it does not violate that guarantee.
</locked_decisions>

<interfaces>
From `frontend/src/pages/FamilyTreePage.jsx` lines 16-27 (current, BEFORE this plan):
```javascript
// D-14/Pitfall 6: this query is reachable by any linked member
// (requireFamilyAccess, Plan 17-01), so it must never expose the
// admin-only account-link field that myEditableMembers/familyMember(id)
// select on ManagePage.
const FAMILY_TREE_QUERY = `
  query FamilyTree {
    familyMembers {
      id firstname lastname fullname gender birthdate isAlive photoUrl mothersname address
      mother { id fullname } father { id } spouses { id } children { id }
    }
  }
`;
```

From `frontend/src/pages/ManagePage.jsx` lines 24-51 (current, BEFORE this plan):
```javascript
const EDITABLE_MEMBER_FIELDS =
  'id firstname lastname fullname gender mothersname email birthdate isAlive phone address photoUrl';

const MY_EDITABLE_MEMBERS_QUERY = `
  query MyEditableMembers {
    myEditableMembers {
      ${EDITABLE_MEMBER_FIELDS}
      mother { id siblings { id fullname gender birthdate photoUrl } }
      father { id siblings { id fullname gender birthdate photoUrl } }
      spouses { id fullname } children { id fullname } siblings { id fullname }
      linkedUser { id }
    }
  }
`;

const FAMILY_MEMBERS_QUERY = `
  query FamilyMembersTable {
    familyMembers {
      id firstname lastname fullname gender birthdate isAlive photoUrl
      linkedUser { id name email }
      createdBy { id name } updatedBy { id name } createdAt updatedAt
    }
  }
`;
```

Existing tests mock `graphqlRequest` generically and assert with `expect.stringContaining('familyMembers')`/`'myEditableMembers'` (loose substring checks, not exact query strings) — `frontend/src/pages/FamilyTreePage.test.jsx` and `frontend/src/pages/ManagePage.test.jsx` will NOT break from this field addition.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add geezFullname to FAMILY_TREE_QUERY</name>
  <files>frontend/src/pages/FamilyTreePage.jsx</files>
  <read_first>
    frontend/src/pages/FamilyTreePage.jsx (lines 16-27 — the query constant and its D-14/Pitfall 6 comment)
    .planning/phases/22-render-surfaces-read-path/22-PATTERNS.md (FamilyTreePage.jsx section)
    .planning/phases/22-render-surfaces-read-path/22-UI-SPEC.md (Component Contract #4)
  </read_first>
  <action>
    In `FAMILY_TREE_QUERY`'s `familyMembers` selection, insert `geezFullname` immediately after `fullname` and before `gender`, changing the scalar field line from `id firstname lastname fullname gender birthdate isAlive photoUrl mothersname address` to `id firstname lastname fullname geezFullname gender birthdate isAlive photoUrl mothersname address`. Do not touch the `D-14/Pitfall 6` comment above the query, the nested relation selections (`mother`/`father`/`spouses`/`children`), or any other line in the file. Do not add `geezFirstname`/`geezLastname`/`geezMothersname` — the tree card only renders `geezFullname`.
  </action>
  <acceptance_criteria>
    - `grep -c "id firstname lastname fullname geezFullname gender birthdate isAlive photoUrl mothersname address" frontend/src/pages/FamilyTreePage.jsx` returns `1`.
    - `grep -c "D-14/Pitfall 6" frontend/src/pages/FamilyTreePage.jsx` returns `1` (comment untouched).
    - `grep -c "geezFirstname\|geezLastname\|geezMothersname" frontend/src/pages/FamilyTreePage.jsx` returns `0`.
    - `cd frontend && npm test -- FamilyTreePage` exits `0` (existing tests use loose `stringContaining` assertions, unaffected by the field addition).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- FamilyTreePage</automated>
  </verify>
  <done>FAMILY_TREE_QUERY's familyMembers selection includes geezFullname immediately after fullname; D-14 comment and nested relation selections are unchanged; FamilyTreePage.test.jsx passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add geezFullname to EDITABLE_MEMBER_FIELDS and FAMILY_MEMBERS_QUERY</name>
  <files>frontend/src/pages/ManagePage.jsx</files>
  <read_first>
    frontend/src/pages/ManagePage.jsx (lines 24-51 — EDITABLE_MEMBER_FIELDS and the separate FAMILY_MEMBERS_QUERY constant)
    .planning/phases/22-render-surfaces-read-path/22-PATTERNS.md (ManagePage.jsx section)
    .planning/phases/22-render-surfaces-read-path/22-UI-SPEC.md (Component Contract #4)
  </read_first>
  <action>
    Add `geezFullname` to `EDITABLE_MEMBER_FIELDS`, inserted immediately after `fullname` and before `gender`: change `'id firstname lastname fullname gender mothersname email birthdate isAlive phone address photoUrl'` to `'id firstname lastname fullname geezFullname gender mothersname email birthdate isAlive phone address photoUrl'`. This single edit automatically propagates to `MY_EDITABLE_MEMBERS_QUERY`, `FAMILY_MEMBER_FOCUS_QUERY`, and every nested relative selection that interpolates `${EDITABLE_MEMBER_FIELDS}` — do not edit those template literals directly. Separately and independently, add `geezFullname` to `FAMILY_MEMBERS_QUERY`'s `familyMembers` selection (this constant does NOT interpolate `EDITABLE_MEMBER_FIELDS`), changing `id firstname lastname fullname gender birthdate isAlive photoUrl` to `id firstname lastname fullname geezFullname gender birthdate isAlive photoUrl`. Do not add `geezFirstname`/`geezLastname`/`geezMothersname` anywhere — neither constant needs the raw parts this phase.
  </action>
  <acceptance_criteria>
    - `grep -c "id firstname lastname fullname geezFullname gender mothersname email birthdate isAlive phone address photoUrl" frontend/src/pages/ManagePage.jsx` returns `1` (EDITABLE_MEMBER_FIELDS).
    - `grep -c "id firstname lastname fullname geezFullname gender birthdate isAlive photoUrl" frontend/src/pages/ManagePage.jsx` returns `1` (FAMILY_MEMBERS_QUERY).
    - `grep -c "geezFirstname\|geezLastname\|geezMothersname" frontend/src/pages/ManagePage.jsx` returns `0`.
    - `cd frontend && npm test -- ManagePage` exits `0` (existing tests use loose `stringContaining` assertions, unaffected by the field addition).
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- ManagePage</automated>
  </verify>
  <done>EDITABLE_MEMBER_FIELDS and FAMILY_MEMBERS_QUERY both independently include geezFullname immediately after fullname; ManagePage.test.jsx passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Frontend GraphQL query → backend resolver | This plan widens three existing, already-authorized read queries by one additional scalar field. No new query, no new resolver, no new endpoint, no change to who can reach these queries (Phase 14's `requireFamilyAccess`/`requireAdmin` guards are unchanged). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure | `FAMILY_TREE_QUERY` (reachable by any linked member, per the file's own D-14/Pitfall 6 comment) | accept | `geezFullname` is in the same visibility class as the already-exposed `fullname` field (both are plain name data, not admin-only like the account-link field the D-14 comment guards against). Task 1's acceptance criteria re-verify the D-14 comment and guarantee are untouched, and no admin-only field is added. |

No other STRIDE categories apply: this plan adds zero new input-handling, zero new mutation, zero new persistence, and zero new dependency. It is a one-field-wider read on data already fetched and already authorized by prior phases.
</threat_model>

<verification>
Run `cd frontend && npm test -- FamilyTreePage` and `cd frontend && npm test -- ManagePage` — both pass.
Run `cd frontend && npm test` (full frontend suite) — stays green, confirming no regression.
`grep -c "geezFullname" frontend/src/pages/FamilyTreePage.jsx frontend/src/pages/ManagePage.jsx` shows the field present in all three targeted constants.
</verification>

<success_criteria>
- `FAMILY_TREE_QUERY`, `EDITABLE_MEMBER_FIELDS`, and `FAMILY_MEMBERS_QUERY` all include `geezFullname`.
- No raw `geezFirstname`/`geezLastname`/`geezMothersname` field is added anywhere.
- `FamilyTreePage.test.jsx` and `ManagePage.test.jsx` both stay green.
- The `D-14/Pitfall 6` comment/guarantee in `FamilyTreePage.jsx` is untouched.
</success_criteria>

<output>
Create `.planning/phases/22-render-surfaces-read-path/22-01-SUMMARY.md` when done
</output>
