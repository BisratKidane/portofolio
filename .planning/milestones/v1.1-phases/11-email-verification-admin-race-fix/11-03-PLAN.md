---
phase: 11-email-verification-admin-race-fix
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - backend/migrations/manual/011-add-email-verification-columns.sql
  - README.md
autonomous: true
requirements: [VERIFY-01]

must_haves:
  truths:
    - "A documented ALTER TABLE artifact exists for the three email-verification columns, plus a targeted UPDATE that backfills emailVerified=true for ONLY the existing ADMIN row (D-02, D-03) — no broad backfill"
  artifacts:
    - path: "backend/migrations/manual/011-add-email-verification-columns.sql"
      provides: "the exact, documented ALTER TABLE + backfill statements for SC-5"
      contains: "ALTER TABLE users ADD COLUMN emailVerified"
    - path: "README.md"
      provides: "a new Manual Database Migrations subsection documenting the boot-and-verify procedure for Phase 11"
      contains: "Add email verification columns to users (Phase 11 / VERIFY-01)"
  key_links:
    - from: "backend/migrations/manual/011-add-email-verification-columns.sql"
      to: "README.md"
      via: "README's new subsection names this exact file path as the artifact to run"
      pattern: "011-add-email-verification-columns\\.sql"
---

<objective>
Produce the one-time, hand-written schema migration for the three email-verification columns (mirroring Phase 9's exact pattern), including the D-03 grandfather backfill that protects the existing bootstrap ADMIN's session across deploy. The actual human boot-and-verify checkpoint (ROADMAP SC-5) is deliberately deferred to Plan 11-07's final task, which runs last in the phase (wave 4, after the full register/verifyEmail/login resolver stack and the frontend verify-email route exist) — so the human check can prove the real end-to-end flow, not just a schema boot.

Purpose: `sequelize.sync()` never alters an existing table's columns — CI's force-recreated test database cannot catch this failure mode, so a documented, reviewable artifact must exist before any human runs it.
Output: A checked-in migration SQL file and matching README documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-email-verification-admin-race-fix/11-CONTEXT.md

<interfaces>
<!-- Phase 9's exact precedent file (backend/migrations/manual/009-add-password-changed-at.sql, full contents): -->
<!--
-- Manual, one-time migration (Phase 9 / SESS-01).
-- ... (comment block explaining sync() never alters existing tables, nullable/no-backfill rationale, DATETIME(3) rationale)
ALTER TABLE users ADD COLUMN passwordChangedAt DATETIME(3) NULL DEFAULT NULL;
-->

<!-- README.md's existing "## Manual Database Migrations" section (verbatim structure to mirror): -->
<!--
## Manual Database Migrations

This project has no migration framework. sequelize.sync() creates tables for brand-new
databases, but it never alters an existing table's columns. Any phase that adds a column
to an already-provisioned database ships a hand-written SQL file under
backend/migrations/manual/ plus a documented boot-and-verify procedure...

### Add passwordChangedAt to users (Phase 9 / SESS-01)
1. Apply the migration against your database, using DB_USER/DB_PASSWORD/DB_NAME from the
   active env file:
   docker compose --env-file env/local.env exec -T mysql mysql -u"$DB_USER" -p"$DB_PASSWORD" "$DB_NAME" < backend/migrations/manual/009-add-password-changed-at.sql
   Or run the equivalent statement with any MySQL client pointed at DB_HOST/DB_PORT/DB_NAME.
2. Boot the backend against that same database and confirm: no Unknown column error;
   /health returns {"status":"ok"}; an existing pre-migration user can still log in and
   query me.
3. Reset that same user's password through the app, then immediately log in again.
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: migration SQL + README boot-and-verify documentation (SC-5 groundwork)</name>
  <files>backend/migrations/manual/011-add-email-verification-columns.sql, README.md</files>
  <read_first>backend/migrations/manual/009-add-password-changed-at.sql (full file, exact style template above); README.md (full current file — confirm the exact "## Manual Database Migrations" section and its existing "### Add passwordChangedAt..." subsection before editing); backend/src/models/User.js (post-Plan-11-01, to mirror the exact 3 column shapes committed there); .planning/phases/11-email-verification-admin-race-fix/11-CONTEXT.md D-01, D-02, D-03</read_first>
  <action>
    Create `backend/migrations/manual/011-add-email-verification-columns.sql` (new directory entry) with a comment block mirroring 009's style: (1) states this is a manual, one-time migration for Phase 11 / VERIFY-01, NOT applied by `sequelize.sync()`; (2) explains that `emailVerificationToken`/`emailVerificationExpiresAt` are nullable with no default so no unrelated behavior is impacted beyond the intended false-by-default verification gate; (3) explains that ONLY the existing `ADMIN` row is backfilled to `emailVerified = true` (D-03) — every other pre-existing user keeps the `false` default and must re-verify via `resendVerificationEmail` before their next login, which is the deliberate scope boundary, not an oversight. Then the statements, one per line: `ALTER TABLE users ADD COLUMN emailVerified BOOLEAN NOT NULL DEFAULT false;` then `ALTER TABLE users ADD COLUMN emailVerificationToken VARCHAR(255) NULL DEFAULT NULL;` then `ALTER TABLE users ADD COLUMN emailVerificationExpiresAt DATETIME NULL DEFAULT NULL;` then `UPDATE users SET emailVerified = true WHERE role = 'ADMIN';`.

    Insert a new `### Add email verification columns to users (Phase 11 / VERIFY-01)` subsection into `README.md`'s existing `## Manual Database Migrations` section, directly after the existing `### Add passwordChangedAt to users (Phase 9 / SESS-01)` subsection, following the identical 3-step structure: (1) the apply command pointed at the new file (`docker compose --env-file env/local.env exec -T mysql mysql -u"$DB_USER" -p"$DB_PASSWORD" "$DB_NAME" < backend/migrations/manual/011-add-email-verification-columns.sql`, or the equivalent via any MySQL client); (2) boot the backend against that same database and confirm zero `Unknown column 'emailVerified' in 'field list'` errors and `/health` still returns `{"status":"ok"}`; (3) note that the full functional boot-and-verify (login-gate + register/verify end-to-end flow) happens once the whole phase's resolvers land, and point to Plan 11-07's final manual-verification task rather than repeating it here.
  </action>
  <acceptance_criteria>
    `grep -n "ALTER TABLE users ADD COLUMN emailVerified BOOLEAN NOT NULL DEFAULT false;" backend/migrations/manual/011-add-email-verification-columns.sql` matches exactly once.
    `grep -n "ALTER TABLE users ADD COLUMN emailVerificationToken VARCHAR(255) NULL DEFAULT NULL;" backend/migrations/manual/011-add-email-verification-columns.sql` and `grep -n "ALTER TABLE users ADD COLUMN emailVerificationExpiresAt DATETIME NULL DEFAULT NULL;" backend/migrations/manual/011-add-email-verification-columns.sql` each match exactly once.
    `grep -n "UPDATE users SET emailVerified = true WHERE role = 'ADMIN';" backend/migrations/manual/011-add-email-verification-columns.sql` matches exactly once (D-03 scoped backfill, not a broad one).
    `grep -c "^### Add email verification columns to users (Phase 11 / VERIFY-01)" README.md` returns `1`.
    `grep -n "011-add-email-verification-columns.sql" README.md` returns at least one match.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "ALTER TABLE users ADD COLUMN emailVerified BOOLEAN NOT NULL DEFAULT false;" backend/migrations/manual/011-add-email-verification-columns.sql && grep -n "UPDATE users SET emailVerified = true WHERE role = 'ADMIN';" backend/migrations/manual/011-add-email-verification-columns.sql && grep -c "^### Add email verification columns to users (Phase 11 / VERIFY-01)" README.md</automated>
  </verify>
  <done>The documented ALTER TABLE + scoped backfill artifact and its matching README boot-and-verify procedure both exist and cross-reference each other.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `sequelize.sync()` vs. a real, already-provisioned dev/prod database | Schema changes to a live database are outside the ORM's control this milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03a | Tampering (unintentional) / Availability | `sequelize.sync()` vs. real DB schema drift | mitigate | Manual, documented `ALTER TABLE` file (this plan) plus a blocking human checkpoint boot-and-verify step (Plan 11-07's final task, deferred until the full resolver stack exists) — closes the `Unknown column` crash-on-first-query failure mode CI cannot catch. |
| T-11-03b | Elevation of Privilege (accepted residual, deliberate scope) | Backfill scope — only the ADMIN row | accept | Grandfathering exactly one row (D-03) is a locked, explicit scope boundary protecting the sole bootstrap admin's session across deploy without reopening the ADMIN slot or un-gating every other existing account. |
</threat_model>

<verification>
Run the grep checks in Task 1's `<verify>` block. The human boot-and-verify proof required by ROADMAP SC-5 is performed in Plan 11-07's Task 3, not here.
</verification>

<success_criteria>
- A documented `ALTER TABLE` + scoped `UPDATE` backfill SQL file exists, matching Plan 11-01's exact column shapes.
- Matching README procedure exists and cross-references the file.
</success_criteria>

<output>
Create `.planning/phases/11-email-verification-admin-race-fix/11-03-SUMMARY.md` when done
</output>
</content>
