---
phase: 10-rate-limiting-on-auth-mutations
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/config/rateLimits.js
  - backend/src/utils/rateLimitStore.js
  - backend/src/utils/rateLimitStore.test.js
autonomous: true
requirements: [RATE-04]

must_haves:
  truths:
    - "A per-key attempt counter allows up to a configured max within a configured window and rejects the next attempt in the same window (the mechanism RATE-01/02/03's thresholds are built on)"
    - "Two independent keys (e.g. two different client IPs, or two different operations for the same IP) never share a counter (D-01 key isolation, the mechanism RATE-04's 'me/dashboard unaffected by login' guarantee depends on)"
    - "A window that has fully elapsed resets the counter for that key, proven with an injectable clock, not wall-clock sleeps or fake timers (Claude's Discretion: deterministic time control)"
    - "A synchronous resetRateLimitStore() test hook clears all counters immediately, mirroring resetTables() (D-03, hard test-isolation requirement)"
  artifacts:
    - path: "backend/src/config/rateLimits.js"
      provides: "centralized, hardcoded per-operation threshold map (D-06/D-07) — the single edit point later plans and Phase 11 consume"
      contains: "export const RATE_LIMITS"
    - path: "backend/src/utils/rateLimitStore.js"
      provides: "module-level singleton Map + fixed-window checkAndIncrement + resetRateLimitStore (D-01/D-03)"
      contains: "export function checkAndIncrement"
    - path: "backend/src/utils/rateLimitStore.test.js"
      provides: "RED->GREEN TDD proof of the store's isolation, threshold, window-reset, and full-clear behavior"
      contains: "resetRateLimitStore"
  key_links:
    - from: "backend/src/utils/rateLimitStore.js (checkAndIncrement)"
      to: "backend/src/config/rateLimits.js (RATE_LIMITS)"
      via: "consumed together by the plugin built in 10-02 — this plan produces both halves of that contract but does not wire them to each other yet"
      pattern: "export function checkAndIncrement"
---

<objective>
Build the two foundational, framework-free modules the whole phase is built on: a centralized, hardcoded rate-limit threshold map (`rateLimits.js`, D-06/D-07) and an in-memory, fixed-window, per-key attempt counter (`rateLimitStore.js`, D-01/D-03) with an injectable clock and a synchronous full-clear reset hook. Neither module touches Apollo, Express, or GraphQL — both are pure, directly unit-testable primitives that the Apollo plugin in Plan 10-02 wires together.

Purpose: Interface-first ordering. The plugin (10-02) and the wiring (10-02/10-03) both depend on this contract; building and TDD-proving it first means later plans consume a known-correct mechanism instead of re-deriving counter semantics under pressure.
Output: `RATE_LIMITS` config object; `checkAndIncrement(key, max, windowMs, now)` and `resetRateLimitStore()` from `rateLimitStore.js`, proven by a RED->GREEN TDD cycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rate-limiting-on-auth-mutations/10-CONTEXT.md
@.planning/phases/10-rate-limiting-on-auth-mutations/10-PATTERNS.md

<interfaces>
<!-- Existing code the executor consumes directly — no exploration needed. -->

From backend/src/config/env.js (plain-exported-config convention to mirror, lines 18-38):
```javascript
export const env = {
  nodeEnv: process.env.NODE_ENV || 'development',
  port: Number(process.env.PORT || 4000),
  jwtSecret: process.env.JWT_SECRET || 'change-me',
  resetTokenExpiresMinutes: Number(process.env.RESET_TOKEN_EXPIRES_MINUTES || 30),
  ...
};
```
`rateLimits.js` follows this exact shape but is fully hardcoded — no `process.env` reads (D-06: no env-var surface this milestone).

From backend/src/models/index.js (module-level singleton convention to mirror, full file):
```javascript
import { sequelize } from '../config/database.js';
import { initUser } from './User.js';

const User = initUser(sequelize);

export const models = {
  User
};
```
`rateLimitStore.js` follows this exact shape: a private module-scope value (here, a `Map`) constructed once at import time, operated on only through exported functions — never a class instance passed around.

From backend/test/helpers.js (the test-reset-hook convention `resetRateLimitStore` must mirror, lines 22-24):
```javascript
export async function resetTables() {
  await models.User.destroy({ where: {}, truncate: true });
}
```
`resetRateLimitStore()` is the in-memory analog: synchronous (`store.clear()`), not async — there is no DB round-trip here — but it must be called the same way (`beforeEach(resetRateLimitStore)` or as one statement inside a multi-statement `beforeEach`), so later plans can slot it in next to `resetTables()` without ceremony.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the rate-limit config contract (RATE_LIMITS)</name>
  <files>backend/src/config/rateLimits.js</files>
  <read_first>backend/src/config/env.js (plain exported config convention, full file already read — reproduced above); .planning/phases/10-rate-limiting-on-auth-mutations/10-CONTEXT.md decisions D-06 and D-07 (centralized constants map, hardcoded thresholds, single edit point, absent-from-map = unlimited); .planning/phases/10-rate-limiting-on-auth-mutations/10-PATTERNS.md "backend/src/config/rateLimits.js" section</read_first>
  <action>
    Create `backend/src/config/rateLimits.js` exporting a single named `RATE_LIMITS` plain object, built once at module load with no `process.env` reads anywhere in the file (D-06). Keys are the exact lowercase GraphQL `Mutation` field names from `backend/src/schemas/user.schema.js` that this milestone protects: `login`, `register`, `requestPasswordReset`. Each value is an object with `max` (Number) and `windowMs` (Number, computed as an explicit `minutes * 60 * 1000` or `hours * 60 * 60 * 1000` expression, not a bare millisecond literal, so the window is self-documenting): `login: { max: 5, windowMs: 15 * 60 * 1000 }`, `register: { max: 5, windowMs: 60 * 60 * 1000 }`, `requestPasswordReset: { max: 5, windowMs: 60 * 60 * 1000 }`. Add a one-line comment above the object noting two things for future maintainers: (1) this is the single edit point for tuning any threshold, and (2) any GraphQL field name absent from this map is treated as unlimited by the consuming plugin (Plan 10-02) — do not add `logout`, `me`, or `dashboard` here. Also add a short comment noting Phase 11's `resendVerificationEmail` will be added as one more entry to this same object (per the ROADMAP Phase 11 dependency note) — do not add that entry now, it is out of scope for Phase 10.
  </action>
  <acceptance_criteria>
    `grep -n "export const RATE_LIMITS" backend/src/config/rateLimits.js` returns exactly 1 match.
    `grep -c "process.env" backend/src/config/rateLimits.js` returns 0 (fully hardcoded, D-06).
    `grep -n "login:" backend/src/config/rateLimits.js`, `grep -n "register:" backend/src/config/rateLimits.js`, and `grep -n "requestPasswordReset:" backend/src/config/rateLimits.js` each return exactly 1 match.
    `grep -n "max: 5" backend/src/config/rateLimits.js` returns exactly 3 matches (one per operation).
    `grep -n "15 \* 60 \* 1000" backend/src/config/rateLimits.js` returns exactly 1 match (login's 15-minute window); `grep -n "60 \* 60 \* 1000" backend/src/config/rateLimits.js` returns exactly 2 matches (register's and requestPasswordReset's 1-hour windows).
    `node --input-type=module -e "import('./backend/src/config/rateLimits.js').then(m => { if (m.RATE_LIMITS.login.max !== 5 || m.RATE_LIMITS.register.max !== 5 || m.RATE_LIMITS.requestPasswordReset.max !== 5) process.exit(1); })"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "import('./backend/src/config/rateLimits.js').then(m => console.log(JSON.stringify(m.RATE_LIMITS)))"</automated>
  </verify>
  <done>RATE_LIMITS exports exactly 3 keys (login, register, requestPasswordReset) each with the ROADMAP-mandated max/windowMs values, fully hardcoded with no env-var surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD the in-memory fixed-window rate-limit store</name>
  <files>backend/src/utils/rateLimitStore.js, backend/src/utils/rateLimitStore.test.js</files>
  <read_first>backend/src/models/index.js (module-level singleton convention, reproduced above); backend/test/helpers.js resetTables (reset-hook convention, reproduced above); .planning/phases/10-rate-limiting-on-auth-mutations/10-CONTEXT.md decisions D-01, D-02, D-03, and "Claude's Discretion" (window algorithm and store data-shape left open, provided isolation/expiry/deterministic-time-control are preserved); .planning/phases/10-rate-limiting-on-auth-mutations/10-PATTERNS.md "backend/src/utils/rateLimitStore.js" section</read_first>
  <behavior>
    - Test 1 (threshold): calling `checkAndIncrement('k1', 3, 1000, 1000)` three times in a row (same key, same `now`) returns `true` all three times; a fourth call with the same key/window (`now` still 1000, or any `now` within `1000 + 1000`) returns `false`.
    - Test 2 (key isolation, D-01): `checkAndIncrement('k1', 1, 1000, 1000)` returns `true`; a call with a DIFFERENT key `checkAndIncrement('k2', 1, 1000, 1000)` also returns `true` — the second key's budget is untouched by the first key's usage.
    - Test 3 (window reset via injectable clock, no fake timers): exhaust `k1`'s budget (`checkAndIncrement('k1', 1, 1000, 1000)` returns `true`, a second call at `now=1000` returns `false`), then call `checkAndIncrement('k1', 1, 1000, 2001)` (a `now` value `>= windowStart + windowMs`, i.e. `1000 + 1000 = 2000`) and assert it returns `true`, restarting the count at 1 for the new window.
    - Test 4 (resetRateLimitStore clears everything immediately): exhaust a key's budget, call `resetRateLimitStore()`, then call `checkAndIncrement` for that SAME key with the SAME `now` (still mid-old-window) and assert it returns `true` — the reset is a full, immediate clear, not a window-boundary-dependent reset.
    - Test 5 (default clock): calling `checkAndIncrement('k5', 1, 60000)` with NO explicit 4th argument does not throw and returns `true` (confirms the `now` parameter defaults to `Date.now()` for production use, without requiring every caller to pass it).
  </behavior>
  <action>
    Follow a strict RED -> GREEN sequence. Do NOT create `rateLimitStore.js` until step 3.

    **Step 1 (RED) — create ONLY `backend/src/utils/rateLimitStore.test.js`:** import `checkAndIncrement` and `resetRateLimitStore` from `./rateLimitStore.js` (a module that does not exist yet) and write the 5 tests from `<behavior>` above, following this codebase's flat `describe`/`it` + `expect` style (see `backend/src/utils/auth.test.js` or `backend/src/resolvers/login.test.js` for tone — plain Vitest, no custom matchers). Add a `beforeEach(() => resetRateLimitStore())` at the top so every test starts from a clean store.

    **Step 2 (RED gate — mandatory):** run `npm test --workspace backend -- rateLimitStore` and confirm it fails because the module `./rateLimitStore.js` cannot be resolved (import error). Commit the RED state: `test(10-01): add failing tests for rateLimitStore`.

    **Step 3 (GREEN) — create `backend/src/utils/rateLimitStore.js`:** implement a module-level singleton `const store = new Map();` (never exported directly — only through functions, matching the `models` singleton convention). Implement and export `checkAndIncrement(key, max, windowMs, now = Date.now())`: read `store.get(key)`; if there is no entry, OR `now - entry.windowStart >= windowMs` (the window has fully elapsed), set `store.set(key, { count: 1, windowStart: now })` and return `true` (this is the fixed-window algorithm — Claude's Discretion, chosen for simplicity and because CONTEXT.md only requires correct expiry + isolation, not sliding-window smoothing); otherwise, if `entry.count >= max`, return `false` without mutating the entry; otherwise increment `entry.count` in place and return `true`. Implement and export `resetRateLimitStore()` as a single synchronous `store.clear();` call.

    **Step 4 (GREEN gate):** re-run `npm test --workspace backend -- rateLimitStore`, confirm all 5 tests pass, then run the full backend suite `npm test --workspace backend` to confirm nothing else regressed (this module is not imported anywhere else yet, so this is a sanity check, not expected to reveal anything). Commit: `feat(10-01): implement fixed-window rate-limit store`.
  </action>
  <acceptance_criteria>
    The RED gate was observed: a `test(10-01): ...` commit precedes the `feat(10-01): ...` commit in `git log`, and the SUMMARY.md records that the RED run failed on module resolution before any implementation file existed.
    `grep -n "export function checkAndIncrement" backend/src/utils/rateLimitStore.js` returns exactly 1 match.
    `grep -n "export function resetRateLimitStore" backend/src/utils/rateLimitStore.js` returns exactly 1 match.
    `grep -n "now = Date.now()" backend/src/utils/rateLimitStore.js` returns exactly 1 match (confirms the injectable-clock-with-production-default contract).
    `grep -n "new Map()" backend/src/utils/rateLimitStore.js` returns exactly 1 match, and it is NOT exported directly (`grep -n "export.*store\b" backend/src/utils/rateLimitStore.js | grep -v "function"` returns 0 matches — only the two functions are exported).
    All 5 tests in `rateLimitStore.test.js` pass; `npm test --workspace backend` (full suite) is green.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- rateLimitStore</automated>
  </verify>
  <done>`checkAndIncrement` enforces a fixed-window, per-key attempt budget with an injectable clock (defaulting to `Date.now()`), and `resetRateLimitStore()` synchronously clears all counters — both proven by a RED->GREEN TDD cycle with no reliance on wall-clock sleeps or fake timers; full backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| In-process module state (this store) | Trusted server-side state. No external input reaches key composition at this layer — callers (the Plan 10-02 plugin) are responsible for building safe keys from trusted inputs; this store treats `key` as an opaque string. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01a | Denial of Service (unbounded memory growth) | `rateLimitStore.js` module-level `Map` | accept | Key space is bounded by a small fixed set of operations (3 this phase, 4 after Phase 11) times the number of distinct real client IPs seen; no TTL sweep/eviction is implemented this milestone. Accepted per D-02: single-instance app, in-memory-only is an explicit, documented milestone trade-off; a restart clears all state. Revisit only if this ships to a long-uptime, high-traffic deployment. |
| T-10-01b | Tampering (test-suite integrity — cross-test counter bleed) | `rateLimitStore.js` shared singleton, consumed by every test file that eventually imports it | mitigate | `resetRateLimitStore()` (D-03) gives every test file a way to fully clear state before each case; this plan's own test suite proves the hook works (Test 4). Wiring this hook into the global test harness happens in Plan 10-03. |
</threat_model>

<verification>
Run `npm test --workspace backend -- rateLimitStore` to confirm the TDD cycle landed correctly (RED commit before GREEN commit, all 5 tests passing). Run `npm test --workspace backend` (full suite) to confirm no regression — expected to be a no-op since nothing else imports this module yet.
</verification>

<success_criteria>
- `RATE_LIMITS` exports exactly the 3 phase-10 operations with the ROADMAP-mandated thresholds, fully hardcoded.
- `checkAndIncrement` enforces a correct fixed-window budget per opaque key, with independent counters per key and a working injectable clock.
- `resetRateLimitStore()` is a synchronous full clear.
- Both new behaviors are proven by a RED->GREEN TDD cycle, not written test-after.
- Full backend suite is green.
</success_criteria>

<output>
Create `.planning/phases/10-rate-limiting-on-auth-mutations/10-01-SUMMARY.md` when done
</output>
</content>
