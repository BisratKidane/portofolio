---
phase: 27-descendant-navigation-performance
plan: 04
type: execute
wave: 3
depends_on: ["27-02", "27-03"]
files_modified:
  - frontend/src/pages/DetailPage.jsx
  - frontend/src/pages/DetailPage.test.jsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, PERF-01, PERF-03]

must_haves:
  truths:
    - "Expanding the head's card loads and displays its direct children in a GenerationGrid, with a visible parent->child connector (NAV-01)"
    - "Re-clicking an expanded control collapses that person's children and hides all descendants beneath, chevron rotation visibly reflects state (NAV-02, D-02)"
    - "At no point are more than 3 generations' worth of PersonCards mounted simultaneously — proven by a DOM query returning null for any dropped 4th-generation card (NAV-03)"
    - "Expanding a childful grandchild forward-shifts the view without a full page reload; collapsing the promoted top restores the EXACT pre-shift view (NAV-04, D-03, D-04)"
    - "Opening /detail fires exactly the 2 initial-load graphqlRequest calls (familyHead + familyMember) and zero children-fetch calls until the first expand click (PERF-01, D-08)"
    - "A repeat expand from cache adds zero graphqlRequest calls and exactly the expected number of render commits — not fewer generic 'no crash' assertions (PERF-03, D-09)"
  artifacts:
    - path: "frontend/src/pages/DetailPage.jsx"
      provides: "Live onExpand wiring via useDescendantNav (Plan 27-03) + GenerationGrid (Plan 27-02) rendering gen1/gen2 rows beneath the head/promoted-top card"
    - path: "frontend/src/pages/DetailPage.test.jsx"
      provides: "End-to-end coverage of NAV-01..04/PERF-01/PERF-03 through the real component tree with mocked graphqlRequest, including a Profiler-based render-commit-count assertion"
  key_links:
    - from: "frontend/src/pages/DetailPage.jsx"
      to: "frontend/src/hooks/useDescendantNav.js"
      via: "const nav = useDescendantNav(mainPerson)"
      pattern: "useDescendantNav\\("
    - from: "frontend/src/pages/DetailPage.jsx"
      to: "frontend/src/components/person/GenerationGrid.jsx"
      via: "<GenerationGrid people={nav.gen1|nav.gen2} .../> rendered conditionally on topExpanded/expandedChildId"
      pattern: "<GenerationGrid"
---

<objective>
Wire the already-built, already-tested `useDescendantNav` hook (Plan 27-03) and
`GenerationGrid` component (Plan 27-02) into `DetailPage`, replacing the inert
`onExpand={() => {}}` head-card handler with the real navigation flow, and prove
the full NAV-01..04/PERF-01/PERF-03 behavior end-to-end through the real rendered
component tree.

Purpose: This is the phase's integration point — every prior plan (27-01 reducer,
27-02 grid, 27-03 hook) was built and unit-tested in isolation specifically so this
final wiring step is small, low-risk glue code. This plan is where the phase's
success criteria become observable in the actual UI.

Output: `frontend/src/pages/DetailPage.jsx` (extended), `DetailPage.test.jsx`
(extended with the full descendant-navigation test suite).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-descendant-navigation-performance/27-CONTEXT.md
@.planning/phases/27-descendant-navigation-performance/27-RESEARCH.md
@.planning/phases/27-descendant-navigation-performance/27-PATTERNS.md
@.planning/phases/27-descendant-navigation-performance/27-02-SUMMARY.md
@.planning/phases/27-descendant-navigation-performance/27-03-SUMMARY.md

<interfaces>
<!-- useDescendantNav return shape (Plan 27-03, frontend/src/hooks/useDescendantNav.js) -->
const nav = useDescendantNav(mainPerson);
// nav.topPerson        — full FamilyMember object currently at the view's top
//                         (mainPerson before any expand/shift, or the promoted
//                         parent's cached `self` object after a shift)
// nav.topExpanded       — bool, drives the top card's `expanded` prop
// nav.gen1              — array of direct children (card-ready), [] until fetched
// nav.expandedChildId   — id of the currently drilled-into gen1 child, or null
// nav.gen2              — array of that child's children (card-ready), [] until fetched
// nav.loadingId         — id of the person currently being expanded (in flight), or null
// nav.onExpandTop(person)        — pass as the top PersonCard's onExpand
// nav.onExpandChild(person)      — pass as each gen1 GenerationGrid's onExpand
// nav.onExpandGrandchild(person) — pass as the gen2 GenerationGrid's onExpand

<!-- GenerationGrid contract (Plan 27-02, frontend/src/components/person/GenerationGrid.jsx) -->
<GenerationGrid people={array} role={'Child'|'Grandchild'} expandedId={id|null}
  onExpand={handler} onEdit={handler} loadingId={id|null} />

<!-- Current inert head render this plan replaces (frontend/src/pages/DetailPage.jsx
     lines 120-129) -->
<Box sx={{ width: '100%', maxWidth: 420 }}>
  <PersonCard member={mainPerson} role="Head" spouse={mainPerson.spouses?.[0]}
    expanded={false} onExpand={() => {}} onEdit={() => {}} />
</Box>

<!-- CircularProgress already imported at DetailPage.jsx line 10 — no new import needed. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire useDescendantNav + GenerationGrid into DetailPage</name>
  <files>frontend/src/pages/DetailPage.jsx</files>
  <read_first>
    - frontend/src/pages/DetailPage.jsx (full file — the exact block to replace, lines 114-131)
    - frontend/src/hooks/useDescendantNav.js (Plan 27-03 — the hook's exact return shape, unchanged)
    - frontend/src/components/person/GenerationGrid.jsx (Plan 27-02 — the exact prop contract, unchanged)
    - .planning/phases/27-descendant-navigation-performance/27-CONTEXT.md (D-01..D-09 — the full locked behavior this wiring must realize)
  </read_first>
  <action>
    In `DetailPage.jsx`, add `import { useDescendantNav } from '../hooks/useDescendantNav.js';`
    and `import GenerationGrid from '../components/person/GenerationGrid.jsx';`
    alongside the existing imports. Inside the component body, after `mainPerson`
    is available, call `const nav = useDescendantNav(mainPerson);`. Replace the
    inert head-card block (the current `<Box sx={{ width: '100%', maxWidth: 420
    }}><PersonCard member={mainPerson} role="Head" ... expanded={false}
    onExpand={() => {}} .../></Box>`) with a `position: 'relative'` wrapper Box
    containing `<PersonCard member={nav.topPerson} role="Head"
    spouse={nav.topPerson.spouses?.[0]} expanded={nav.topExpanded}
    onExpand={nav.onExpandTop} onEdit={() => {}} />` plus, only when
    `nav.loadingId === nav.topPerson.id`, a small absolutely-positioned
    `<CircularProgress size={16} aria-hidden="true" data-testid="top-loading"
    sx={{ position: 'absolute', top: 8, right: 8 }} />` (mirrors GenerationGrid's
    own per-card loading badge). Immediately below that Box, conditionally render
    `{nav.topExpanded && nav.gen1.length > 0 && (<GenerationGrid people={nav.gen1}
    role="Child" expandedId={nav.expandedChildId} onExpand={nav.onExpandChild}
    onEdit={() => {}} loadingId={nav.loadingId} />)}`, and below THAT,
    `{nav.expandedChildId && nav.gen2.length > 0 && (<GenerationGrid
    people={nav.gen2} role="Grandchild" expandedId={null}
    onExpand={nav.onExpandGrandchild} onEdit={() => {}} loadingId={nav.loadingId}
    />)}`. `onEdit` stays a no-op on EVERY card (head/gen1/gen2) — Phase 28 wires
    real admin edit/add actions; this phase must not touch that. Role labels
    reflect POSITION IN THE CURRENT VIEW, not absolute genealogical generation
    (Claude's Discretion, consistent with NAV-04's "parent becomes the new top
    person" framing — after a shift the promoted parent still shows role="Head"
    since it now occupies the view's top slot). Do not modify `FAMILY_HEAD_QUERY`
    or `FAMILY_MEMBER_QUERY` — both stay exactly as they are (PERF-01, Pattern 3:
    the initial load must never be extended to fetch two levels deep).
  </action>
  <verify>
    <automated>npm test --workspace frontend -- DetailPage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useDescendantNav" frontend/src/pages/DetailPage.jsx` is `>= 1`.
    - `grep -c "<GenerationGrid" frontend/src/pages/DetailPage.jsx` equals `2`
      (one for gen1/Child, one for gen2/Grandchild).
    - `grep -c "FAMILY_MEMBER_QUERY" frontend/src/pages/DetailPage.jsx` is `1`
      (the constant declaration itself, unmodified — no new fields added to it).
    - `grep -c "onEdit={() => {}}" frontend/src/pages/DetailPage.jsx` is `>= 3`
      (head, gen1 grid, gen2 grid — all still inert; Phase 28 scope untouched).
  </acceptance_criteria>
  <done>DetailPage renders a live, expandable head card plus gen1/gen2 GenerationGrids driven entirely by useDescendantNav's state, with edit actions still fully inert.</done>
</task>

<task type="auto">
  <name>Task 2: Extend DetailPage tests to prove NAV-01..04/PERF-01/PERF-03 end-to-end</name>
  <files>frontend/src/pages/DetailPage.test.jsx</files>
  <read_first>
    - frontend/src/pages/DetailPage.test.jsx (full file — the existing mock/render harness AND the now-stale test at lines 122-134, which asserted the expand click was a no-op; this test must be UPDATED since the click now genuinely fires a fetch)
    - frontend/src/pages/DetailPage.jsx (as wired in Task 1 — the exact prop/handler wiring to exercise)
    - .planning/phases/27-descendant-navigation-performance/27-RESEARCH.md (Validation Architecture — the Profiler render-count technique, verbatim)
    - .planning/phases/27-descendant-navigation-performance/27-CONTEXT.md (D-01..D-09 — every locked behavior this suite must assert)
  </read_first>
  <behavior>
    - UPDATE the existing "passes no-op onExpand/onEdit to PersonCard so the head
      card never crashes when clicked" test (currently asserting `.not.toThrow()`
      for both the Edit click and the expand click): keep the Edit-click assertion
      as-is (still genuinely inert), but for the expand click, mock a resolved
      `graphqlRequest` response BEFORE the click and assert the click now leads to
      a rendered `GenerationGrid`'s child `PersonCard`, not merely "did not throw".
    - PERF-01 (lazy load): after the initial head render, assert `graphqlRequest`
      has been called exactly 2 times (familyHead + familyMember) — zero
      additional calls exist before any expand click.
    - NAV-01 (expand shows children grid): mock a resolved children array for the
      head's id, click "Show children of {head.fullname}", `await waitFor` a
      `GenerationGrid`-rendered child `PersonCard` (`getByTestId('person-card-
      {childId}')`) to appear, and assert a `generation-apex` element is present.
    - NAV-02 (collapse hides descendants): with a child already expanded (its own
      children mocked/fetched), click that child's "Hide children of ..." control
      and assert the grandchild `PersonCard` is no longer in the document
      (`queryByTestId` returns `null`).
    - D-01 (auto-collapse sibling): with two head-children both having their own
      children, expand child A (assert its grandchildren render), then expand
      child B, and assert A's grandchildren are GONE (`queryByTestId` null) while
      B's render — proving only one branch is ever open.
    - NAV-04 + D-04 (forward-shift then symmetric undo): build the 3-level fixture
      chain (head -> child with children -> grandchild with its OWN children ->
      great-grandchild), expand head, expand child, expand grandchild (triggers
      the shift), assert the DOM now shows the PROMOTED parent as the top card
      (`getByTestId('person-card-{childId}')` with role text "Head") and the
      great-grandchild as its child, THEN click collapse on the promoted top card
      and assert the ORIGINAL head + original child are restored exactly (the
      pre-shift child's own children/grandchild reappear identically — same ids,
      same expanded state) with NO additional `graphqlRequest` calls fired for
      the undo (the popped frame's data was already cached).
    - NAV-03 (never a 4th generation): at the peak of the shift scenario above,
      assert the total count of DOM elements matching `[data-testid^="person-
      card-"]` never exceeds 3 distinct generation slots' worth of cards at once
      (head/top + its spouse if any, its children row, its expanded child's
      children row) — explicitly assert the dropped grandparent's OTHER children
      (the promoted parent's siblings) are NOT present after the shift.
    - PERF-03 (cache + re-render proof): wrap the rendered tree in
      `<Profiler id="nav" onRender={onRenderSpy}>...</Profiler>`; expand a child,
      collapse it, then re-expand the SAME child — assert `graphqlRequest`'s total
      call count is unchanged between the first expand and the re-expand (cache
      hit), and separately assert `onRenderSpy`'s call count for the re-expand
      step is a small, exact, expected number (not merely "didn't grow
      unboundedly") — proving one commit for the toggle, not an extra one for a
      refetch that never happened.
  </behavior>
  <action>
    Extend `frontend/src/pages/DetailPage.test.jsx` using the existing
    `graphqlRequest`/`photoClient` mocks and `renderPage()` helper already in the
    file — add a `<Profiler>` variant of the render helper only for the PERF-03
    test (import `Profiler` from `'react'`, wrap `<DetailPage />` in it inline for
    that one test, do not change the shared `renderPage()` helper used by every
    other test). Build the 3-level fixture chain described in the behavior bullets
    (head id '1' with children:[{id:'2'}], CHILD id '2' with children:[{id:'3'}],
    GRANDCHILD id '3' with children:[{id:'4'}], GREAT_GRANDCHILD id '4' with no
    children) and queue `graphqlRequest.mockResolvedValueOnce({ familyMember: {
    children: [...] } })` calls in the exact click order each test needs. Use
    `fireEvent.click` + `await waitFor(...)` for each expand/collapse step,
    matching the existing file's async idiom. Update the stale no-op test per the
    first behavior bullet rather than deleting it.
  </action>
  <verify>
    <automated>npm test --workspace frontend -- DetailPage</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- DetailPage` exits 0, zero failing/skipped tests.
    - `npm test --workspace frontend` exits 0 (full suite green, no regressions).
    - The test file contains at least one assertion per NAV-01, NAV-02, NAV-03,
      NAV-04, PERF-01, PERF-03, and D-01, matched by an explicit code comment or
      test title referencing that ID (grep-visible, e.g. `it('... NAV-04 ...')`).
    - The `<Profiler>`-based test asserts an EXACT render-commit count for the
      cache-hit re-expand step (not an inequality like `toBeLessThan`).
  </acceptance_criteria>
  <done>DetailPage's full descendant-navigation behavior — expand/collapse, auto-collapse siblings, forward-shift + symmetric undo, the 3-generation cap, lazy fetch, and the session cache's no-refetch/no-extra-render guarantee — is proven end-to-end against the real rendered component tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (authenticated session) → `/detail`'s expand-triggered reads | Identical boundary to the existing initial-load path (Phase 26); this plan adds no new boundary, only wires already-gated reads into the UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05 | Elevation of Privilege | `onEdit` handlers passed to every card | mitigate | Every `onEdit` prop wired in this plan is `() => {}` (a true no-op), on the head card AND every gen1/gen2 card — no edit/add mutation path exists yet on `/detail` regardless of `canEdit` value returned by the backend. Admin-only mutation wiring is explicitly Phase 28's scope; this plan cannot introduce an unauthorized-write vector because it introduces no write path at all. |
| T-27-06 | Information Disclosure | Expand-triggered reads reusing `familyMember(id)` | accept | Same resolver/gate as Phase 24/26 (`requireFamilyAccess`), already proven bounded/N+1-free (PERF-02). No new field, no new query root, no new authorization logic introduced by this plan. |
</threat_model>

<verification>
- `npm test --workspace frontend -- DetailPage` passes.
- `npm test --workspace frontend` passes (full suite, no regressions).
- Manual smoke check (optional, not required for automated gate): confirm no MUI
  Grid `size`-prop deprecation warning appears in the test console output.
</verification>

<success_criteria>
NAV-01: expanding a card loads/shows direct children in a responsive grid with a visible connector.
NAV-02: re-clicking expand collapses children + hides all descendants; control reflects state.
NAV-03: never more than 3 generations shown at once, proven via DOM assertion.
NAV-04: expanding a childful grandchild forward-shifts the view with no full reload; collapse walks back up exactly (D-04).
PERF-01: opening /detail fetches only the main person; children load only on expand.
PERF-03: cached descendants are served with no refetch and a bounded, exact re-render count.
</success_criteria>

<output>
Create `.planning/phases/27-descendant-navigation-performance/27-04-SUMMARY.md` when done.
</output>
