---
phase: 27-descendant-navigation-performance
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - frontend/src/hooks/useDescendantNav.js
  - frontend/src/hooks/useDescendantNav.test.js
autonomous: true
requirements: [PERF-01, PERF-03]

must_haves:
  truths:
    - "Opening /detail never triggers this hook's expand-only fetch — a person's direct children are fetched ONLY when that person's expand handler is invoked (PERF-01, D-08)"
    - "A repeat expand/collapse of an already-cached id is served from the in-memory cache with ZERO additional graphqlRequest calls (PERF-03, D-09)"
    - "The session cache is a useRef Map, never React state, so a cache write alone never triggers a re-render (PERF-03)"
    - "A search-driven mainPerson change (RESET) clears only the view frame, never the cache — previously-loaded descendants stay cached across a main-person swap (D-09)"
    - "Expanding a childful grandchild fetches only that grandchild's children — the promoted parent's own children (new gen1) are already cached from the earlier expand, so zero extra fetch is needed for them (D-03/D-09)"
  artifacts:
    - path: "frontend/src/hooks/useDescendantNav.js"
      provides: "useDescendantNav(mainPerson) hook: navReducer (Plan 27-01) + useRef(Map) cache + EXPAND_CHILDREN_QUERY orchestration; exposes topPerson/topExpanded/gen1/expandedChildId/gen2/loadingId + onExpandTop/onExpandChild/onExpandGrandchild handlers"
      exports: ["useDescendantNav"]
    - path: "frontend/src/hooks/useDescendantNav.test.js"
      provides: "renderHook-based coverage: cache-miss fetches+caches, cache-hit skips fetch, RESET preserves cache, forward-shift resolves the promoted parent from cache with no extra fetch"
  key_links:
    - from: "frontend/src/pages/DetailPage.jsx"
      to: "frontend/src/hooks/useDescendantNav.js"
      via: "const nav = useDescendantNav(mainPerson) (consumed by Plan 27-04)"
      pattern: "useDescendantNav\\("
    - from: "frontend/src/hooks/useDescendantNav.js"
      to: "backend familyMember(id) resolver (Phase 24, unchanged)"
      via: "graphqlRequest(EXPAND_CHILDREN_QUERY, { id })"
      pattern: "EXPAND_CHILDREN_QUERY"
    - from: "frontend/src/hooks/useDescendantNav.js"
      to: "frontend/src/hooks/descendantNav.reducer.js"
      via: "useReducer(navReducer, mainPerson?.id, initial)"
      pattern: "navReducer"
---

<objective>
Build `useDescendantNav`, the first custom hook in this codebase: it owns the
navigation frame (via Plan 27-01's `navReducer`), a session-scoped in-memory cache
of already-fetched descendants, and the expand-triggered GraphQL fetch — exposing a
small, render-ready surface (`topPerson`, `gen1`, `gen2`, expand handlers) that
`DetailPage` (Plan 27-04) wires directly into `PersonCard`/`GenerationGrid`.

Purpose: Deliver PERF-01 (lazy per-generation fetch — never the whole tree) and
PERF-03 (session cache — no duplicate requests, no unnecessary re-renders) as an
isolated, hook-level contract, fully proven via `renderHook` before any page wiring
exists.

Output: `frontend/src/hooks/useDescendantNav.js` (+ test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-descendant-navigation-performance/27-CONTEXT.md
@.planning/phases/27-descendant-navigation-performance/27-RESEARCH.md
@.planning/phases/27-descendant-navigation-performance/27-PATTERNS.md
@.planning/phases/27-descendant-navigation-performance/27-01-SUMMARY.md

<interfaces>
<!-- navReducer contract (Plan 27-01, frontend/src/hooks/descendantNav.reducer.js) -->
initial(topId) -> { topId, topExpanded: false, expandedChildId: null, history: [] }
navReducer(state, action) -> newState
  actions: {type:'RESET', id}, {type:'EXPAND_TOP'}, {type:'EXPAND_CHILD', id}, {type:'EXPAND_GRANDCHILD', id}

<!-- graphqlRequest (frontend/src/api/graphqlClient.js lines 23-36) -->
graphqlRequest(query, variables) -> Promise<data>  # unwrapped response.data.data; throws Error on failure (message already joined); do not re-wrap

<!-- DetailPage's existing fetch idiom to mirror (frontend/src/pages/DetailPage.jsx lines 45-52) -->
const loadPersonById = useCallback((id) => {
  setLoading(true); setError('');
  return graphqlRequest(FAMILY_MEMBER_QUERY, { id })
    .then((data) => setMainPerson(data.familyMember))
    .catch((err) => setError(err.message))
    .finally(() => setLoading(false));
}, []);
<!-- ensureEntry below follows this exact .then/.catch/.finally shape, writing into
     a useRef cache entry instead of calling setState. -->

<!-- FamilyMember GraphQL fields available (backend/src/schemas/familyMember.schema.js
     lines 13-45) — the exact field set for EXPAND_CHILDREN_QUERY's children selection: -->
id, fullname, geezFullname, gender, isAlive, photoUrl, canEdit,
spouses { id fullname geezFullname gender isAlive photoUrl },
children { id }   # count-only nested selection — Phase 24 D-05 convention: no
                  # childCount field, derive .length; this lets each returned
                  # child's OWN PersonCard gate its expand control (CARD-04)
                  # without an extra round trip.

<!-- Cache-entry shape this plan MUST implement (project-specific design, not from
     an external source — resolves the "how do I get the promoted parent's full
     card-field object after a forward-shift" problem without a second Map): -->
cache.current: Map<personId, { self: FamilyMemberLite, children: FamilyMemberLite[] | undefined }>
  - `self` is ALWAYS available synchronously the moment ensureEntry(person) is
    called, because PersonCard's onExpand(member) callback (Phase 25, unchanged)
    already hands the caller the FULL member object being expanded — no lookup
    needed, no separate id->object cache required.
  - `children` is `undefined` until the fetch resolves (cache miss), then holds the
    fetched array (cache hit thereafter).
  - Deriving `topPerson`/`gen1`/`gen2` for render: `topPerson = cache.current.get(
    state.topId)?.self ?? mainPerson`; `gen1 = cache.current.get(state.topId)
    ?.children ?? []`; `gen2 = state.expandedChildId ? (cache.current.get(
    state.expandedChildId)?.children ?? []) : []`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing hook test suite (RED)</name>
  <files>frontend/src/hooks/useDescendantNav.test.js</files>
  <read_first>
    - frontend/src/pages/DetailPage.jsx (loadPersonById fetch idiom + FAMILY_MEMBER_QUERY field set, lines 21-52)
    - frontend/src/pages/DetailPage.test.jsx (graphqlRequest mock setup, call-count/args assertion idiom, lines 12-20, 76-88)
    - frontend/src/api/graphqlClient.js (graphqlRequest return/throw shape)
    - backend/src/schemas/familyMember.schema.js (FamilyMember field names — confirm every field named in EXPAND_CHILDREN_QUERY exists on the type)
    - frontend/src/hooks/descendantNav.reducer.js (Plan 27-01 — the reducer this hook must import and drive, unchanged)
    - .planning/phases/27-descendant-navigation-performance/27-RESEARCH.md (Pattern 2 — ref-vs-state cache split; Pattern 3 — the narrow expand-only query, why NOT to extend FAMILY_MEMBER_QUERY; Pitfall 2 — no redundant shift-eligibility guard; Pitfall 4 — call-count and re-render are TWO separate assertions)
  </read_first>
  <behavior>
    - Mount `renderHook(() => useDescendantNav(mainPerson))` with a `mainPerson`
      fixture `{ id: '1', ..., spouses: [], children: [{ id: '2' }] }`. Immediately
      after mount, assert `graphqlRequest` has been called ZERO times (PERF-01 —
      opening never fetches until an expand happens) and `result.current.topPerson`
      equals `mainPerson`, `result.current.gen1` is `[]`, `result.current.topExpanded`
      is `false`.
    - Calling `result.current.onExpandTop(mainPerson)` fires exactly ONE
      `graphqlRequest` call whose first arg matches `/children/i` (or a named
      `ExpandChildren` operation) and second arg equals `{ id: '1' }`. After the
      mocked promise resolves (`await waitFor`), `result.current.topExpanded` is
      `true` and `result.current.gen1` equals the mocked `children` array returned.
    - Calling `onExpandTop(mainPerson)` AGAIN (collapse, since already expanded)
      then a THIRD time (re-expand) fires NO additional `graphqlRequest` calls —
      the call count stays at 1 throughout (PERF-03 cache-hit, D-09).
    - Calling `result.current.onExpandChild(childMember)` for a gen1 child with its
      own `children: [{ id: '3' }]` fires exactly one more `graphqlRequest` call
      (`{ id: childMember.id }`); after resolving, `result.current.expandedChildId`
      equals `childMember.id` and `result.current.gen2` equals the mocked
      grandchildren array.
    - Calling `result.current.onExpandGrandchild(grandchildMember)` (the grandchild
      HAS its own children, eligible for shift) fires exactly ONE more
      `graphqlRequest` call for the grandchild's id — NOT a second call for the
      promoted parent's children, since those were already cached by the
      `onExpandChild` call above (D-03/D-09). After resolving,
      `result.current.topPerson.id` equals the PROMOTED parent's id (looked up from
      cache, full object with fullname/etc., not just an id string), and
      `result.current.gen1` equals the same array that was `gen2` before the shift.
    - RESET-preserves-cache: re-render the hook with a DIFFERENT `mainPerson` (new
      id), then call `onExpandTop` for the ORIGINAL first `mainPerson` object again
      — assert the total `graphqlRequest` call count is UNCHANGED from before the
      main-person swap (the original id's entry is still cached, D-09).
    - `loadingId` reflects the in-flight id: assert `result.current.loadingId`
      equals the expanding person's id while the mocked promise is pending, and is
      `null` again after it resolves.
  </behavior>
  <action>
    Create `frontend/src/hooks/useDescendantNav.test.js` using
    `@testing-library/react`'s `renderHook`/`act`/`waitFor` (already available via
    the installed `@testing-library/react` ^16.3.2 — no new dependency). Mock
    `../api/graphqlClient.js` exactly as `DetailPage.test.jsx` does (lines 12-14,
    20). Build small fixtures for a 3-generation chain: `mainPerson` (id '1'),
    `CHILD` (id '2', has its own `children: [{ id: '3' }]`), `GRANDCHILD` (id '3',
    has its own `children: [{ id: '4' }]`), `GREAT_GRANDCHILD` (id '4', no
    children). Chain `graphqlRequest.mockResolvedValueOnce({ familyMember: {
    children: [CHILD] } })` then `{ familyMember: { children: [GRANDCHILD] } }`
    then `{ familyMember: { children: [GREAT_GRANDCHILD] } }` for the three
    sequential expands. Write one `it`/`describe` block per behavior bullet above.
  </action>
  <verify>
    <automated>npm test --workspace frontend -- useDescendantNav</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- useDescendantNav` FAILS (module
      `./useDescendantNav.js` does not exist) — expected RED before Task 2.
    - Test file contains at least 7 `it(...)` blocks covering every behavior bullet,
      using `renderHook` (not a full component mount).
  </acceptance_criteria>
  <done>A complete, currently-failing test suite fully specifies useDescendantNav's cache/fetch/derive contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement useDescendantNav (GREEN)</name>
  <files>frontend/src/hooks/useDescendantNav.js</files>
  <read_first>
    - frontend/src/hooks/useDescendantNav.test.js (just written in Task 1 — the exact contract)
    - frontend/src/hooks/descendantNav.reducer.js (Plan 27-01 — import target, unchanged)
    - .planning/phases/27-descendant-navigation-performance/27-RESEARCH.md (Pattern 2 and Pattern 3 — the verbatim reference shapes for the cache/fetch split and the query string)
  </read_first>
  <action>
    Create `frontend/src/hooks/useDescendantNav.js` importing `{ useCallback,
    useEffect, useReducer, useRef, useState } from 'react'`, `{ graphqlRequest }
    from '../api/graphqlClient.js'`, and `{ navReducer, initial } from
    './descendantNav.reducer.js'`. Declare a module-scope
    `EXPAND_CHILDREN_QUERY` template literal named per this app's SCREAMING_SNAKE_
    CASE `_QUERY` convention (matching `FAMILY_MEMBER_QUERY` in `DetailPage.jsx`):
    `query ExpandChildren($id: ID!) { familyMember(id: $id) { children { id
    fullname geezFullname gender isAlive photoUrl canEdit spouses { id fullname
    geezFullname gender isAlive photoUrl } children { id } } } }`. Export
    `useDescendantNav(mainPerson)`: `const cache = useRef(new Map())`; `const
    [state, dispatch] = useReducer(navReducer, mainPerson?.id, initial)`; `const
    [loadingId, setLoadingId] = useState(null)`. Add a `useEffect` keyed on
    `mainPerson?.id` that dispatches `{ type: 'RESET', id: mainPerson?.id }` and
    seeds/refreshes `cache.current` for the new id WITHOUT clearing the rest of the
    Map: if the id has no existing entry, `cache.current.set(mainPerson.id, { self:
    mainPerson, children: undefined })`; if it already has an entry (this id was
    visited before this session), only refresh `self` while preserving the
    already-cached `children` (`cache.current.set(mainPerson.id, { self:
    mainPerson, children: existing?.children })`). Implement one internal
    `ensureEntry(person)` via `useCallback`: if `cache.current.get(person.id)
    ?.children` is already defined, `return Promise.resolve()` immediately (cache
    hit, zero network call); otherwise `cache.current.set(person.id, { self:
    person, children: cache.current.get(person.id)?.children })` (seed/refresh
    `self` synchronously before the async call), `setLoadingId(person.id)`, then
    `graphqlRequest(EXPAND_CHILDREN_QUERY, { id: person.id }).then((data) => {
    cache.current.set(person.id, { self: person, children: data.familyMember
    ?.children ?? [] }); }).finally(() => setLoadingId((current) => current ===
    person.id ? null : current))`. Implement three exported handlers, each shaped
    identically — `ensureEntry(person).then(() => dispatch({...}))` — so the
    reducer alone decides toggle/collapse/shift semantics (no duplicated
    conditional logic in the hook, per the reducer's already-tested behavior):
    `onExpandTop(person)` dispatches `{ type: 'EXPAND_TOP' }`; `onExpandChild(person)`
    dispatches `{ type: 'EXPAND_CHILD', id: person.id }`; `onExpandGrandchild(person)`
    dispatches `{ type: 'EXPAND_GRANDCHILD', id: person.id }`. Derive the render-ready
    values on every call exactly per the `<interfaces>` cache-entry formulas above
    (`topPerson`, `gen1`, `gen2`). Return `{ topPerson, topExpanded: state.topExpanded,
    gen1, expandedChildId: state.expandedChildId, gen2, loadingId, onExpandTop,
    onExpandChild, onExpandGrandchild }`.
  </action>
  <verify>
    <automated>npm test --workspace frontend -- useDescendantNav</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- useDescendantNav` exits 0, all tests from
      Task 1 green.
    - `grep -c "useRef(new Map" frontend/src/hooks/useDescendantNav.js` is `>= 1`
      (cache lives in a ref, never `useState` — PERF-03).
    - `grep -c "import.*navReducer.*from './descendantNav.reducer.js'"
      frontend/src/hooks/useDescendantNav.js` is `1` (reducer imported, not
      reimplemented inline).
    - `grep -c "FAMILY_MEMBER_QUERY" frontend/src/hooks/useDescendantNav.js` is `0`
      (this file declares its OWN `EXPAND_CHILDREN_QUERY`, never imports or extends
      `DetailPage`'s initial-load query — PERF-01, Pattern 3).
  </acceptance_criteria>
  <done>useDescendantNav is implemented, passes every test from Task 1, and structurally guarantees the cache lives outside React state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (authenticated session) → `familyMember(id)` via `EXPAND_CHILDREN_QUERY` | Same resolver/gate as the existing initial-load query — no new boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03 | Information Disclosure | `EXPAND_CHILDREN_QUERY` fired by `ensureEntry` with an arbitrary `id` | accept | `familyMember(id)` already calls `requireFamilyAccess(user)` (Phase 12/24, unchanged) before returning any data — this new query hits the IDENTICAL resolver/gate as `FAMILY_MEMBER_QUERY`, so it introduces no new attack surface. No new authorization logic is written here or should be. |
| T-27-04 | Information Disclosure | Session cache holding another person's card data across a search-driven main-person reset | accept | The cache is scoped to a single `useRef` inside one hook instance owned by the CURRENT authenticated user's own browser session (component unmounts on navigation away, and the ref is recreated fresh on remount) — it never crosses a user boundary; all cached data was already returned to this same authenticated, family-scoped viewer by the same gated resolver. No cross-user leak is possible. |
</threat_model>

<verification>
- `npm test --workspace frontend -- useDescendantNav` passes.
- `npm test --workspace frontend` passes (no regressions — new files only, depends only on Plan 27-01's already-merged reducer).
</verification>

<success_criteria>
PERF-01: children are fetched only on expand, proven by the zero-calls-before-expand assertion.
PERF-03: repeat expand/collapse of a cached id fires no additional graphqlRequest calls, and the cache is structurally a useRef (never triggers a re-render on write alone).
D-08/D-09 are each concretely implemented and unit-tested via renderHook.
</success_criteria>

<output>
Create `.planning/phases/27-descendant-navigation-performance/27-03-SUMMARY.md` when done.
</output>
