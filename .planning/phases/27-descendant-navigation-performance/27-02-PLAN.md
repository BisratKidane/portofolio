---
phase: 27-descendant-navigation-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/person/GenerationGrid.jsx
  - frontend/src/components/person/GenerationGrid.test.jsx
autonomous: true
requirements: [NAV-01]

must_haves:
  truths:
    - "Children of one generation render in a responsive grid — up to 3 cards/row desktop, fewer on tablet, 1 on mobile — via MUI Grid's size prop (D-05, NAV-01)"
    - "Exactly one inverted-V (∧) apex cue renders per generation container, never one line per child card (D-06)"
    - "Each rendered card's spouse passes through unchanged to PersonCard's existing dashed-connector pairing; the children group visually hangs beneath the couple (D-07)"
    - "A per-card loading indicator shows for exactly the person currently being expanded (Claude's Discretion — expand-in-flight feel), and clears on resolution"
  artifacts:
    - path: "frontend/src/components/person/GenerationGrid.jsx"
      provides: "Presentational children-grid wrapper reused for both the gen1 (Child) and gen2 (Grandchild) rows; renders unmodified PersonCard instances"
      exports: ["default (GenerationGrid)"]
    - path: "frontend/src/components/person/GenerationGrid.test.jsx"
      provides: "RTL coverage: card count, single apex per container, aria-hidden apex, size-prop grid cells, spouse passthrough, expanded/loading passthrough, onExpand/onEdit forwarding"
  key_links:
    - from: "frontend/src/pages/DetailPage.jsx"
      to: "frontend/src/components/person/GenerationGrid.jsx"
      via: "<GenerationGrid people=... role=... expandedId=... onExpand=... onEdit=... loadingId=... /> (consumed by Plan 27-04)"
      pattern: "GenerationGrid"
    - from: "frontend/src/components/person/GenerationGrid.jsx"
      to: "frontend/src/components/person/PersonCard.jsx"
      via: "renders one unmodified PersonCard per grid item — PersonCard itself is NOT touched by this plan"
      pattern: "<PersonCard"
---

<objective>
Build `GenerationGrid`, the new presentational layout wrapper that renders one
generation's people (a person's direct children) in a responsive MUI grid with a
single group-level inverted-V (∧) apex above it, communicating "these children
belong to the couple above" without drawing a line to each individual card.

Purpose: Deliver NAV-01's grid + visual-connector requirement as a standalone,
fully-tested component `DetailPage` (Plan 27-04) can drop in twice — once for gen1
(children) and once for gen2 (grandchildren) — with zero knowledge of fetch/cache/
state-machine internals. `PersonCard` (Phase 25) stays untouched; this wrapper only
lays existing cards into a grid and forwards props.

Output: `frontend/src/components/person/GenerationGrid.jsx` (+ test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-descendant-navigation-performance/27-CONTEXT.md
@.planning/phases/27-descendant-navigation-performance/27-RESEARCH.md
@.planning/phases/27-descendant-navigation-performance/27-PATTERNS.md

<interfaces>
<!-- PersonCard contract (frontend/src/components/person/PersonCard.jsx line 46) —
     UNCHANGED by this plan. GenerationGrid renders this exact component per person. -->
PersonCard({ member, role, spouse, isSpouse, expanded, onExpand, onEdit })
  - `expanded` gates the chevron rotation; `onExpand(member)` fires with the FULL
    member object (not just an id) when its footer expand control is clicked —
    only rendered at all when `member.children?.length >= 1` (Phase 25, unchanged).
  - `spouse` (optional): when supplied to a non-`isSpouse` card, PersonCard itself
    composes the anchor + a dashed connector + a second leaf PersonCard for the
    spouse. GenerationGrid only needs to pass `spouse={person.spouses?.[0]}` —
    the pairing visuals are entirely PersonCard's existing responsibility.
  - `data-testid={`person-card-${member.id}`}` — the counting/lookup hook to use
    in tests (mirrors PersonCard.test.jsx's own convention).

<!-- Theme tokens (frontend/src/theme.js lines 7-24) -->
colors.line = '#e6e8f0'   // USE THIS for the apex — restrained, distinct from
                          // colors.primary (spouse dashed connector) and
                          // colors.slate (/family's tree edges). D-06 explicitly
                          // wants something lighter than both.

<!-- Connector-as-plain-Box precedent (PersonCard.jsx lines 62-72) — the dashed
     spouse connector is the DIRECT style precedent for the new apex: a plain
     aria-hidden Box styled via `sx`, never SVG. -->
<Box aria-hidden="true" data-connector-style="dashed"
     sx={{ width: 32, alignSelf: 'center', borderTop: `2px dashed ${colors.primary}` }} />

<!-- MUI v6 Grid `size` prop (confirmed installed ^6.3.1, mui.com/material-ui/react-grid/) -->
<Grid container spacing={2}>
  <Grid size={{ xs: 12, sm: 6, md: 4 }}>...</Grid>
</Grid>
<!-- xs:12 = 1/row mobile, sm:6 = 2/row tablet (~600-900px), md:4 = 3/row desktop
     (>=900px) — Claude's Discretion breakpoint choice per RESEARCH.md Open Question 1. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write GenerationGrid's failing behavior tests (RED)</name>
  <files>frontend/src/components/person/GenerationGrid.test.jsx</files>
  <read_first>
    - frontend/src/components/person/PersonCard.jsx (full file — the component being wrapped, unmodified)
    - frontend/src/components/person/PersonCard.test.jsx (renderCard helper convention, data-testid/aria-hidden assertion idioms, lines 24-35, 179-208)
    - .planning/phases/27-descendant-navigation-performance/27-RESEARCH.md (Pattern 4 — GenerationGrid + ApexCue reference; Pitfall 3 — do not assert pixel widths/breakpoints in jsdom; Pitfall 5 — expanded must be a pure function of passed-in `expandedId`)
    - .planning/phases/27-descendant-navigation-performance/27-PATTERNS.md (`GenerationGrid.jsx`/`GenerationGrid.test.jsx` sections)
  </read_first>
  <behavior>
    - Renders exactly `people.length` `PersonCard`s — assert via
      `container.querySelectorAll('[data-testid^="person-card-"]')` count, for
      `people` arrays of length 1, 2, and 3.
    - Renders exactly ONE `data-testid="generation-apex"` element regardless of
      `people.length` (D-06 — group-level, never per-card).
    - The apex element has `aria-hidden="true"`.
    - Each person's card receives `expanded={person.id === expandedId}` — pass
      `expandedId` matching one person in a 3-person array and assert only that
      one card's expand button reads "Hide children of ..." while the others read
      "Show children of ...".
    - `spouse` passthrough: a person object with `spouses: [spouseObj]` renders
      BOTH the anchor and spouse `PersonCard`s (assert 2 `person-card-` testids for
      that one grid entry — reuses PersonCard's own spouse-pairing behavior,
      GenerationGrid is only proven to forward the prop, not re-verify PersonCard's
      internal spouse rendering).
    - `onExpand`/`onEdit` are forwarded verbatim: clicking a card's expand button
      calls the `onExpand` prop with that card's exact member object; clicking Edit
      (when `canEdit: true`) calls `onEdit` with that member object.
    - `loadingId` passthrough: a `data-testid="generation-loading-{id}"` element
      renders only for the person whose id matches `loadingId`, and is absent when
      `loadingId` is `null` or doesn't match any rendered person.
    - Does NOT assert on `getBoundingClientRect()`/computed pixel widths for the
      "3 per row" responsive claim (Pitfall 3) — trust MUI Grid's CSS.
  </behavior>
  <action>
    Create `frontend/src/components/person/GenerationGrid.test.jsx` mirroring
    `PersonCard.test.jsx`'s local `renderCard`-style helper convention (here:
    `renderGrid(overrides = {})` defaulting `people` to a 2-person array, `role:
    'Child'`, `expandedId: null`, `onExpand: vi.fn()`, `onEdit: vi.fn()`,
    `loadingId: null`). Mock `../../api/photoClient.js` exactly as
    `PersonCard.test.jsx` does (line 6-8) since `PersonCard` internally renders
    `MemberAvatarImage`. Write one `it` per behavior bullet above using
    `screen`/`container` queries, no `graphqlRequest` mock needed (this is a leaf
    presentational component — same as `PersonCard` itself).
  </action>
  <verify>
    <automated>npm test --workspace frontend -- GenerationGrid</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- GenerationGrid` FAILS (module
      `./GenerationGrid.jsx` does not exist) — expected RED before Task 2.
    - Test file contains at least 8 `it(...)` blocks covering every behavior bullet.
  </acceptance_criteria>
  <done>A complete, currently-failing test suite fully specifies GenerationGrid's contract before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GenerationGrid + ApexCue (GREEN)</name>
  <files>frontend/src/components/person/GenerationGrid.jsx</files>
  <read_first>
    - frontend/src/components/person/GenerationGrid.test.jsx (just written in Task 1 — the exact contract)
    - frontend/src/components/person/PersonCard.jsx (import target, unmodified)
    - frontend/src/theme.js (colors.line token)
  </read_first>
  <action>
    Create `frontend/src/components/person/GenerationGrid.jsx` importing `{ Box, Grid,
    CircularProgress } from '@mui/material'`, `{ colors } from '../../theme.js'`, and
    `PersonCard from './PersonCard.jsx'`. Define a local `ApexCue()` function
    component: an `aria-hidden="true"` `Box` with `data-testid="generation-apex"`
    containing two short rotated line segments (two child `Box`es, each
    `bgcolor: colors.line`, one rotated ~20deg and one ~-20deg from a shared
    center-top point) forming a calm inverted-V pointing up — a small, fixed-size
    (~14px tall, ~40px wide) restrained cue, matching the dashed-connector's
    plain-Box-no-SVG technique. Export default `GenerationGrid({ people, role,
    expandedId, onExpand, onEdit, loadingId })`: render a column `Box` (flex,
    centered, small gap) containing one `<ApexCue />` followed by a `<Grid container
    spacing={2}>`; for each `person` in `people`, render a `<Grid key={person.id}
    size={{ xs: 12, sm: 6, md: 4 }}>` wrapping a `position: 'relative'` `Box`
    containing the `<PersonCard member={person} role={role}
    spouse={person.spouses?.[0]} expanded={person.id === expandedId}
    onExpand={onExpand} onEdit={onEdit} />` and, only when `loadingId ===
    person.id`, a small absolutely-positioned `<CircularProgress size={16}
    aria-hidden="true" data-testid={`generation-loading-${person.id}`} sx={{
    position: 'absolute', top: 8, right: 8 }} />` badge. Do not modify
    `PersonCard.jsx` at all — this file only composes it.
  </action>
  <verify>
    <automated>npm test --workspace frontend -- GenerationGrid</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- GenerationGrid` exits 0, all tests from
      Task 1 green.
    - `grep -c "data-testid=\"generation-apex\"" frontend/src/components/person/GenerationGrid.jsx`
      equals `1` (single apex element defined once, reused per generation instance,
      never per-card).
    - `grep -c "size={{ xs: 12, sm: 6, md: 4 }}" frontend/src/components/person/GenerationGrid.jsx`
      is `>= 1` (confirms the MUI v6 `size` prop API is used, not the legacy
      `item xs={}` API).
    - `git diff --stat frontend/src/components/person/PersonCard.jsx` shows no
      changes (PersonCard remains untouched by this plan).
  </acceptance_criteria>
  <done>GenerationGrid renders any generation's people in a responsive grid with exactly one group-level apex cue, full spouse/expanded/loading passthrough, and PersonCard is unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure presentational component; receives already-fetched, already-authorized data as props (no fetch, no auth check, no user input) from its caller (Plan 27-04's DetailPage). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02 | N/A | `GenerationGrid.jsx` | accept | No genuine threat surface: renders props it is given, performs no network/DB access, no new trust boundary crossed. Honest assessment — this is a layout component, not a data boundary. |
</threat_model>

<verification>
- `npm test --workspace frontend -- GenerationGrid` passes.
- `npm test --workspace frontend` passes (no regressions — new files only).
</verification>

<success_criteria>
NAV-01: expanding a person shows a responsive grid of direct children (≤3/row desktop) grouped visually under a single connector cue.
D-05/D-06/D-07 are each concretely implemented: size-prop grid, one apex per generation, spouse-then-children-beneath-the-couple composition (via PersonCard's existing pairing, reused unchanged).
</success_criteria>

<output>
Create `.planning/phases/27-descendant-navigation-performance/27-02-SUMMARY.md` when done.
</output>
