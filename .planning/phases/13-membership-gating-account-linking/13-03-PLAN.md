---
phase: 13-membership-gating-account-linking
plan: 03
type: tdd
wave: 3
depends_on: ["13-02"]
files_modified:
  - frontend/src/context/AuthContext.jsx
  - frontend/src/components/ProtectedRoute.jsx
  - frontend/src/components/ProtectedRoute.test.jsx
  - frontend/src/pages/Pending.jsx
  - frontend/src/pages/Pending.test.jsx
  - frontend/src/App.jsx
autonomous: true
requirements: [ACC-01, ACC-03]

must_haves:
  truths:
    - "An authenticated, unlinked, non-admin user is redirected to /pending and sees the static awaiting-link message; an admin or any linked user is never shown /pending (D-01/D-02/ACC-01/ACC-03)"
  artifacts:
    - path: "frontend/src/pages/Pending.jsx"
      provides: "the /pending static gate screen"
      contains: "awaiting an admin to link you"
    - path: "frontend/src/components/ProtectedRoute.jsx"
      provides: "the pending-gate redirect branch"
      contains: "/pending"
  key_links:
    - from: "frontend/src/components/ProtectedRoute.jsx"
      to: "frontend/src/context/AuthContext.jsx (me query familyMemberId)"
      via: "user.familyMemberId read from useAuth()"
      pattern: "user\\.familyMemberId"
---

<objective>
Enforce the membership gate in the SPA: any authenticated, non-admin user with no linked member is routed to a static `/pending` screen (D-01/D-02), while a linked user or any ADMIN reaches the app normally (ACC-03 carve-out reflected in the frontend).

Purpose: this is the frontend half of ACC-01 — the backend `requireFamilyAccess` guard from Plan 13-02 is the authoritative enforcement, but the SPA must not dead-end or error-loop an unlinked user; it must route them somewhere coherent.
Output: `frontend/src/pages/Pending.jsx` (new); extended `ProtectedRoute.jsx` (pending-gate branch); extended `AuthContext.jsx` (`familyMemberId` in the `me` query); `/pending` route in `App.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-membership-gating-account-linking/13-CONTEXT.md
@.planning/phases/13-membership-gating-account-linking/13-PATTERNS.md
</context>

<interfaces>
<!-- ProtectedRoute — the component this plan extends -->
From frontend/src/components/ProtectedRoute.jsx:5-20:
```javascript
export default function ProtectedRoute({ allowedRoles }) {
  const { loading, user } = useAuth();
  if (loading) { return <Box>...<CircularProgress /></Box>; }
  if (!user) return <Navigate to="/login" replace />;
  if (allowedRoles && !allowedRoles.includes(user.role)) return <Navigate to="/dashboard" replace />;
  return <Outlet />;
}
```

<!-- AuthContext — me query this plan extends -->
From frontend/src/context/AuthContext.jsx:6-10:
```javascript
const ME_QUERY = `
  query Me {
    me { id name email role }
  }
`;
```

<!-- AuthShell — reused wrapper for the static Pending screen -->
From frontend/src/components/AuthShell.jsx:9 — `AuthShell({ eyebrow, title, subtitle, children, footer, maxWidth = 460 })`

<!-- backend GraphQL contract this plan's ME_QUERY change depends on (produced by Plan 13-02) -->
`type User { id: ID! name: String! email: String! role: Role! familyMemberId: ID createdAt: String! updatedAt: String! }`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD pending-gate redirect in ProtectedRoute + AuthContext me query</name>
  <files>frontend/src/components/ProtectedRoute.jsx, frontend/src/components/ProtectedRoute.test.jsx, frontend/src/context/AuthContext.jsx</files>
  <read_first>frontend/src/context/AuthContext.jsx, frontend/src/components/ProtectedRoute.jsx, frontend/src/components/ProtectedRoute.test.jsx</read_first>
  <behavior>
    - a linked, non-admin user ({ role: 'USER', familyMemberId: 5 }) renders the protected child route (no redirect) — regression
    - an ADMIN user with familyMemberId null renders the protected child route (D-06/ACC-03 carve-out)
    - an unlinked, non-admin user ({ role: 'USER', familyMemberId: null }) is redirected to /pending
    - the existing allowedRoles-mismatch redirect-to-/dashboard behavior is unaffected — regression
  </behavior>
  <action>
    In frontend/src/components/ProtectedRoute.test.jsx, add `<Route path="/pending" element={<div>Pending Sentinel</div>} />` to the `renderProtectedRoute` helper's `<Routes>` block (alongside the existing `/login` and `/dashboard` sentinel routes, after line 17). Add 3 new `it()` cases using `useAuthMock.mockReturnValue({ loading: false, user: {...} })`: (1) `{ id: 1, role: 'USER', familyMemberId: null }` asserts `screen.getByText('Pending Sentinel')`; (2) `{ id: 1, role: 'ADMIN', familyMemberId: null }` asserts `screen.getByText('Admin Sentinel')` (carve-out); (3) `{ id: 1, role: 'USER', familyMemberId: 5 }` asserts `screen.getByText('Admin Sentinel')` (linked user unaffected). Run `npx vitest run src/components/ProtectedRoute.test.jsx` and confirm the new `/pending` case fails (RED) — `ProtectedRoute` does not redirect there yet.

    In frontend/src/components/ProtectedRoute.jsx, insert a new guard clause between the existing `if (!user) return <Navigate to="/login" replace />;` line (line 16) and the `allowedRoles` check (line 17): `if (!user.familyMemberId && user.role !== 'ADMIN') return <Navigate to="/pending" replace />;`. Run the test file again and confirm all cases pass (GREEN), including the 2 pre-existing tests (regression).

    In frontend/src/context/AuthContext.jsx, extend `ME_QUERY` (lines 6-10) to select `familyMemberId` alongside the existing `id name email role` fields, so `user.familyMemberId` is populated after `loadUser`/`login`/`verifyEmail`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ProtectedRoute.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/components/ProtectedRoute.test.jsx` passes — 3 new cases plus all pre-existing cases (regression)
    - `grep -n "familyMemberId" frontend/src/components/ProtectedRoute.jsx` shows the new guard clause
    - `grep -n "familyMemberId" frontend/src/context/AuthContext.jsx` shows it added to `ME_QUERY`'s selection set
  </acceptance_criteria>
  <done>ProtectedRoute redirects unlinked, non-admin users to /pending while leaving admins and linked users unaffected; AuthContext's me query returns familyMemberId.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pending.jsx static gate screen + /pending route</name>
  <files>frontend/src/pages/Pending.jsx, frontend/src/pages/Pending.test.jsx, frontend/src/App.jsx</files>
  <read_first>frontend/src/pages/VerifyEmail.jsx, frontend/src/pages/VerifyEmail.test.jsx, frontend/src/components/AuthShell.jsx, frontend/src/App.jsx</read_first>
  <behavior>
    - renders the static awaiting-link message for an unlinked, non-admin user
    - redirects to /dashboard for a linked user (symmetric bounce — no reason to sit on /pending once linked)
    - redirects to /dashboard for an ADMIN with no linked member (carve-out)
    - redirects to /login when user is null (not authenticated)
  </behavior>
  <action>
    Create frontend/src/pages/Pending.test.jsx first (RED), mocking `useAuth` from `'../context/AuthContext.jsx'` the same way `ProtectedRoute.test.jsx` mocks it (`vi.mock('../context/AuthContext.jsx', () => ({ useAuth: () => useAuthMock() }))`), wrapping renders in a `MemoryRouter` with sentinel routes for `/dashboard` and `/login`. Write the 4 behavior cases as `it()` blocks. Run `npx vitest run src/pages/Pending.test.jsx` and confirm it fails (RED) — `Pending.jsx` does not exist yet.

    Create frontend/src/pages/Pending.jsx: default-exported function component. Imports `Navigate` from `react-router-dom`, `Box, Typography` from `@mui/material`, `useAuth` from `'../context/AuthContext.jsx'`, `AuthShell` from `'../components/AuthShell.jsx'`. Reads `const { user } = useAuth();`. If `!user`, return `<Navigate to="/login" replace />`. If `user.familyMemberId || user.role === 'ADMIN'`, return `<Navigate to="/dashboard" replace />` (the symmetric bounce — a linked or admin user hitting `/pending` directly is sent onward). Otherwise render `<AuthShell eyebrow="Almost there" title="Waiting for admin approval">` wrapping a single `<Typography>` whose exact text is "Your account is awaiting an admin to link you to your family member; you'll get access once linked." (D-02's wording). No `useEffect`, no polling, no mutation calls, no admin-contact link — deliberately simpler than `VerifyEmail.jsx`, per D-02.

    In frontend/src/App.jsx, import `Pending` from `'./pages/Pending.jsx'` and add `<Route path="pending" element={<Pending />} />` as a top-level sibling route (after the existing `verify-email` route, line 20) — NOT nested inside `<ProtectedRoute />`, since `/pending` must be reachable by an authenticated-but-unlinked user whom `ProtectedRoute` would otherwise redirect away from any route nested under it.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/Pending.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/pages/Pending.test.jsx` passes, all 4 cases green
    - `grep -n "useEffect" frontend/src/pages/Pending.jsx` returns zero matches (confirms static, non-polling per D-02)
    - `grep -n "awaiting an admin to link you" frontend/src/pages/Pending.jsx` returns one match
    - `grep -n "path=\"pending\"" frontend/src/App.jsx` returns one match, positioned outside the `<Route element={<ProtectedRoute />}>` block (source inspection)
    - `cd frontend && npx vitest run` (full suite) is green
  </acceptance_criteria>
  <done>An authenticated-but-unlinked, non-admin user sees the static /pending message; a linked user or admin visiting /pending directly is bounced to /dashboard; an unauthenticated visitor is bounced to /login.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (client-side routing) → backend GraphQL API | The frontend gate is a UX layer; the authoritative access-control boundary is the backend `requireFamilyAccess` guard (Plan 13-01/13-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-08 | Spoofing / Elevation of Privilege | Frontend routing (`ProtectedRoute`, `Pending.jsx`) | accept | Frontend-only enforcement is trivially bypassed via devtools/direct API calls. This is a UX convenience layer only — ASVS L1's actual access-control boundary is Plan 13-02's server-side `requireFamilyAccess` guard, proven by the SC5 adversarial test. This plan's frontend gate is not treated as a security control. |
| T-13-09 | Denial of Service (availability, self) | `ProtectedRoute` ↔ `Pending.jsx` redirect pair | mitigate | Both components use the identical carve-out condition shape (`familyMemberId` truthy OR `role === 'ADMIN'`); this plan's acceptance criteria grep-assert the condition in both files so a future edit to one without the other is caught in review/tests rather than producing an infinite-redirect UX bug. |

</threat_model>

<verification>
- `cd frontend && npx vitest run src/components/ProtectedRoute.test.jsx src/pages/Pending.test.jsx` — both pass
- `cd frontend && npx vitest run` — full frontend suite green
</verification>

<success_criteria>
- An unlinked, non-admin authenticated user is redirected to `/pending` and sees the exact D-02 static message.
- A linked user or any ADMIN is never shown `/pending` (both entry paths: via `ProtectedRoute` and via direct navigation to `/pending`).
- `AuthContext`'s `me` query returns `familyMemberId`.
- Full frontend suite stays green.
</success_criteria>

<output>
Create `.planning/phases/13-membership-gating-account-linking/13-03-SUMMARY.md` when done
</output>
