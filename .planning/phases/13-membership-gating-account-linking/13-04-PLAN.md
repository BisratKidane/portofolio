---
phase: 13-membership-gating-account-linking
plan: 04
type: execute
wave: 4
depends_on: ["13-02", "13-03"]
files_modified:
  - frontend/src/pages/AdminLinkMembers.jsx
  - frontend/src/pages/AdminLinkMembers.test.jsx
  - frontend/src/App.jsx
autonomous: true
requirements: [ACC-02, ACC-03]

must_haves:
  truths:
    - "An admin can reach /admin/link-members, see unlinked accounts, and link each one to an existing OR newly-created member through the UI (D-03/D-04)"
  artifacts:
    - path: "frontend/src/pages/AdminLinkMembers.jsx"
      provides: "the admin-only linking screen (unlinked-user list + member picker + create-and-link)"
      contains: "LINK_USER_TO_MEMBER_MUTATION"
  key_links:
    - from: "frontend/src/pages/AdminLinkMembers.jsx"
      to: "backend linkUserToMember mutation (Plan 13-02)"
      via: "graphqlRequest(LINK_USER_TO_MEMBER_MUTATION, ...)"
      pattern: "LINK_USER_TO_MEMBER_MUTATION"
---

<objective>
Ship the minimal, admin-only linking screen ACC-02 requires as a usable capability this phase (D-03): list unlinked accounts, let the admin pick an existing family member (search/select widget) OR create a bare member and link in one step (D-04), calling the `linkUserToMember` mutation from Plan 13-02.

Purpose: without this UI, `linkUserToMember` is a backend-only capability an admin could only exercise via raw GraphQL requests — D-03 explicitly requires a usable screen this phase, not a deferred backend-only mutation.
Output: `frontend/src/pages/AdminLinkMembers.jsx` (new); `/admin/link-members` route in `App.jsx`, role-gated to ADMIN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/13-membership-gating-account-linking/13-CONTEXT.md
@.planning/phases/13-membership-gating-account-linking/13-PATTERNS.md
</context>

<interfaces>
<!-- GraphQL contract this page consumes, produced by Plan 13-02 -->
```graphql
type Query {
  unlinkedUsers: [User!]!          # requireAdmin-gated
  familyMembers: [FamilyMember!]!  # requireAdmin-gated
}
type Mutation {
  linkUserToMember(userId: ID!, memberId: ID, newMember: NewFamilyMemberInput): User!
}
input NewFamilyMemberInput {
  firstname: String!
  lastname: String!
  gender: Gender!               # enum Gender { Male Female Other }
  mothersname: String
  email: String
  birthdate: String
  deathdate: String
  phone: String
  address: String
}
```

<!-- Admin list-row rendering pattern to copy (no role Chip needed for this page) -->
From frontend/src/pages/Dashboard.jsx:196-259 — `Paper`/`Stack`/`Divider`/`Avatar` list-row shape over `users.map(...)`.

<!-- try/catch/finally + Alert error pattern to copy -->
From frontend/src/pages/Login.jsx:14-26:
```javascript
const handleSubmit = async (event) => {
  event.preventDefault();
  setError('');
  setLoading(true);
  try {
    await login(form.email, form.password);
    navigate('/dashboard');
  } catch (err) {
    setError(err.message);
  } finally {
    setLoading(false);
  }
};
```

<!-- graphqlRequest signature -->
From frontend/src/api/graphqlClient.js: `export async function graphqlRequest(query, variables = {})` — throws `Error` with joined GraphQL error messages on failure.

<!-- ProtectedRoute's existing role-gating, already tested, reused unchanged -->
From frontend/src/components/ProtectedRoute.jsx:17 — `if (allowedRoles && !allowedRoles.includes(user.role)) return <Navigate to="/dashboard" replace />;`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdminLinkMembers page — unlinked-user list + member picker + create-and-link</name>
  <files>frontend/src/pages/AdminLinkMembers.jsx</files>
  <read_first>frontend/src/pages/Dashboard.jsx, frontend/src/pages/Login.jsx, frontend/src/api/graphqlClient.js</read_first>
  <behavior>
    - on mount, fetches unlinkedUsers and familyMembers via graphqlRequest and renders one row per unlinked user
    - each row defaults to "pick" mode: an Autocomplete of familyMembers plus a "Link" button and a "Create new member instead" toggle
    - toggling a row to "create" mode shows an inline mini-form (firstname*, lastname*, gender* + optional fields) with "Create & link"/"Back" buttons
    - submitting either mode calls linkUserToMember with the correct variables (memberId for pick mode, newMember for create mode) and removes that row from the list on success
    - a rejected mutation shows a per-row error Alert without removing the row or hiding other rows
    - an empty unlinkedUsers response renders a plain "No accounts are waiting to be linked." message
  </behavior>
  <action>
    Create frontend/src/pages/AdminLinkMembers.jsx as a default-exported function component. Import `useEffect, useState` from `react`; `Alert, Autocomplete, Avatar, Box, Button, MenuItem, Paper, Stack, TextField, Typography, CircularProgress` from `@mui/material`; `graphqlRequest` from `'../api/graphqlClient.js'`. Define constants: `UNLINKED_USERS_QUERY` (`query UnlinkedUsers { unlinkedUsers { id name email createdAt } }`), `FAMILY_MEMBERS_QUERY` (`query FamilyMembers { familyMembers { id firstname lastname fullname } }`), `LINK_USER_TO_MEMBER_MUTATION` (`mutation LinkUserToMember($userId: ID!, $memberId: ID, $newMember: NewFamilyMemberInput) { linkUserToMember(userId: $userId, memberId: $memberId, newMember: $newMember) { id familyMemberId } }`). On mount, a `useEffect` fetches both queries via `graphqlRequest`, storing results in `unlinkedUsers`/`familyMembers` state, with page-level `pageLoading`/`pageError` state mirroring `Dashboard.jsx`'s fetch-effect shape (lines 78-82). Render a `Paper` containing a `Stack` of rows (Dashboard.jsx's `Avatar` + name/email + timestamp row shape, without the role `Chip`), each row keeping local state `{ mode: 'pick' | 'create', selectedMember, form, submitting, error }` (default `mode: 'pick'`). Pick mode: an `Autocomplete` (`options={familyMembers}`, `getOptionLabel={(m) => m.fullname}`, `renderInput={(params) => <TextField {...params} label="Family member" />}`), a "Link" `Button` (disabled until a member is selected), and a text `Button` "Create new member instead" that sets `mode: 'create'`. Create mode: `TextField`s for `firstname`, `lastname` (both required), a `TextField select` with `MenuItem`s `Male`/`Female`/`Other` for `gender` (required), optional `TextField`s for `email`, `phone`, `address`, `birthdate`, `deathdate`, `mothersname`; a "Create & link" `Button` and a "Back" `Button` that resets `mode: 'pick'`. Both submit paths call an async handler using the Login.jsx `try { await graphqlRequest(LINK_USER_TO_MEMBER_MUTATION, { userId: row.id, memberId: mode === 'pick' ? selectedMember.id : undefined, newMember: mode === 'create' ? form : undefined }); setUnlinkedUsers((prev) => prev.filter((u) => u.id !== row.id)); } catch (err) { setRowError(err.message); } finally { setRowSubmitting(false); }` shape — error state and loading state are per-row, not page-level, so one row's failure never hides or blocks the rest of the list. When `unlinkedUsers.length === 0` (and not `pageLoading`), render `<Typography>No accounts are waiting to be linked.</Typography>` instead of an empty `Paper`.
  </action>
  <verify>
    <automated>cd frontend && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - source assertion: `grep -n "graphqlRequest(LINK_USER_TO_MEMBER_MUTATION" frontend/src/pages/AdminLinkMembers.jsx` present
    - source assertion: `grep -n "Autocomplete" frontend/src/pages/AdminLinkMembers.jsx` present (member-picker widget)
    - source assertion: `grep -n "'create new member instead'\|Create new member instead" frontend/src/pages/AdminLinkMembers.jsx` present (D-04's dual pick/create paths)
    - source assertion: `grep -n "No accounts are waiting to be linked" frontend/src/pages/AdminLinkMembers.jsx` present
    - `cd frontend && npx vite build` completes without a syntax/import error referencing this file
  </acceptance_criteria>
  <done>AdminLinkMembers.jsx renders the unlinked-user list, supports both pick-existing and create-and-link flows against linkUserToMember, and handles per-row success/error/empty states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route wiring (admin-only) + component tests</name>
  <files>frontend/src/App.jsx, frontend/src/pages/AdminLinkMembers.test.jsx</files>
  <read_first>frontend/src/App.jsx, frontend/src/components/ProtectedRoute.test.jsx, frontend/src/pages/Register.test.jsx</read_first>
  <behavior>
    - renders the fetched unlinked-users list
    - selecting an existing member via the Autocomplete and clicking "Link" calls graphqlRequest with LINK_USER_TO_MEMBER_MUTATION and the expected userId/memberId variables, then removes that row
    - switching a row to create-mode, filling firstname/lastname/gender, and clicking "Create & link" calls graphqlRequest with a newMember variable containing the typed fields and no memberId
    - a rejected mutation renders a per-row Alert with the thrown error message and does NOT remove the row
    - an empty unlinkedUsers response renders the "No accounts are waiting to be linked." message
  </behavior>
  <action>
    In frontend/src/App.jsx, import `AdminLinkMembers` from `'./pages/AdminLinkMembers.jsx'` and add a new admin-only guarded route after the existing `<Route element={<ProtectedRoute />}>...dashboard...</Route>` block: `<Route element={<ProtectedRoute allowedRoles={['ADMIN']} />}><Route path="admin/link-members" element={<AdminLinkMembers />} /></Route>`.

    Create frontend/src/pages/AdminLinkMembers.test.jsx mocking `'../api/graphqlClient.js'`'s `graphqlRequest` (per `Register.test.jsx`'s `vi.mock` pattern — `vi.mock('../api/graphqlClient.js', () => ({ graphqlRequest: vi.fn() }))`), rendering `<AdminLinkMembers />` inside a `MemoryRouter`. Sequence `graphqlRequest.mockResolvedValueOnce` calls to satisfy the initial `unlinkedUsers`/`familyMembers` fetches, then write the 5 behavior cases above as `it()` blocks using `userEvent` for Autocomplete selection and form-filling (per `Register.test.jsx`'s `userEvent.type`/`userEvent.click` pattern), asserting on the exact `graphqlRequest` call arguments via `expect(graphqlRequest).toHaveBeenCalledWith(LINK_USER_TO_MEMBER_MUTATION_STRING_OR_MATCHER, { userId, memberId, newMember })` (import or replicate the same mutation string constant used in the page, or assert on a loose object-containing matcher for variables if the exact query string isn't exported).
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/AdminLinkMembers.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/pages/AdminLinkMembers.test.jsx` passes, all 5 cases green
    - `grep -n "allowedRoles={\['ADMIN'\]}" frontend/src/App.jsx` present, confirming the admin-link-members route is role-gated
    - `grep -n "path=\"admin/link-members\"" frontend/src/App.jsx` returns one match, nested inside the ADMIN-gated `ProtectedRoute`
    - `cd frontend && npx vitest run` (full suite) is green
  </acceptance_criteria>
  <done>/admin/link-members is reachable only by ADMIN role (enforced via ProtectedRoute's existing allowedRoles), and AdminLinkMembers.jsx's list/pick/create/error/empty states are all covered by passing component tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser route `/admin/link-members` → backend `unlinkedUsers`/`familyMembers`/`linkUserToMember` | Frontend role-gate is UX only; backend `requireAdmin` (Plan 13-02) is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-10 | Elevation of Privilege | `/admin/link-members` route | mitigate | `ProtectedRoute`'s existing, already-tested `allowedRoles` check redirects a non-admin to `/dashboard`; the authoritative boundary remains the backend `requireAdmin` guard on `linkUserToMember`/`unlinkedUsers`/`familyMembers` (Plan 13-02) — this frontend route gate is UX-only, not relied upon for security. |
| T-13-11 | Information Disclosure | `FAMILY_MEMBERS_QUERY` (picker data source) | accept | The query selects only `id firstname lastname fullname` — no `email`/`phone`/`address`/`birthdate` fields are requested by this page, minimizing exposure to what the picker widget needs; the endpoint is also `requireAdmin`-gated server-side regardless of what fields a future caller might request. |

</threat_model>

<verification>
- `cd frontend && npx vitest run src/pages/AdminLinkMembers.test.jsx` — passes
- `cd frontend && npx vitest run` — full frontend suite green
- `cd frontend && npx vite build` — production build succeeds
</verification>

<success_criteria>
- An admin can navigate to `/admin/link-members`, see all unlinked accounts, and link each to an existing OR newly-created bare member.
- A non-admin cannot reach `/admin/link-members` (redirected to `/dashboard`).
- Full frontend suite and production build stay green.
</success_criteria>

<output>
Create `.planning/phases/13-membership-gating-account-linking/13-04-SUMMARY.md` when done
</output>
