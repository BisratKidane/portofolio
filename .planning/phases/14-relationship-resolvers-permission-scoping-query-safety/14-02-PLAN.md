---
phase: 14-relationship-resolvers-permission-scoping-query-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/familyMember.service.js
  - backend/src/services/familyMember.scope.test.js
  - backend/src/resolvers/user.resolver.js
  - backend/src/resolvers/dashboard.test.js
autonomous: true
requirements: [PERM-05, REL-04, PERM-01]

must_haves:
  truths:
    - "computeEditableScope is the single, reused server-side utility that computes who a member-user may edit; no resolver duplicates this logic."
    - "The editable set correctly includes self, both parents, spouses (from either Spouse-row direction), children, and either-parent-derived siblings (including half-siblings)."
    - "The editable set correctly EXCLUDES a grandparent, a cousin, and a sibling-of-sibling (the tricky half-sibling-of-a-half-sibling case) — proven by dedicated fixtures, not asserted by absence of a positive test."
    - "Every family-domain-adjacent resolver is gated by requireFamilyAccess, not requireAuth — the dashboard resolver (WR-04) is brought in line."
  artifacts:
    - path: "backend/src/services/familyMember.service.js"
      provides: "computeEditableScope(memberId, { transaction }) added alongside the existing wouldCreateCycle/linkParent/addChild/setSpouse/deleteMember"
    - path: "backend/src/services/familyMember.scope.test.js"
      provides: "SC-3 inclusion + exclusion fixtures (grandparent, cousin, sibling-of-sibling)"
  key_links:
    - from: "backend/src/resolvers/user.resolver.js"
      to: "backend/src/utils/auth.js"
      via: "dashboard resolver calls requireFamilyAccess(user) instead of requireAuth(user)"
      pattern: "dashboard:.*requireFamilyAccess"
    - from: "backend/src/services/familyMember.service.js"
      to: "backend/src/models/index.js"
      via: "computeEditableScope queries FamilyMember/Spouse via Op.or, never recursing beyond one hop"
      pattern: "computeEditableScope"
---

<objective>
Ship the single, reused editable-scope computation (PERM-05) that every Phase 14 mutation
resolver will depend on, proven correct against the exact SC-3 exclusion cases named in the
roadmap (grandparent, cousin, sibling-of-sibling) — and close the WR-04 defect (dashboard
resolver using `requireAuth` instead of `requireFamilyAccess`) carried forward from Phase 13's
code review, since this phase is the first to build real member-facing relationship logic on top
of the membership gate.

Purpose: PERM-05 requires the editable set be computed once, server-side, and never trusted from
the client — every later mutation plan (14-04, 14-05, 14-06) imports `computeEditableScope` rather
than re-deriving scope inline.
Output: `computeEditableScope` exported from `familyMember.service.js`; `dashboard` resolver gated
by `requireFamilyAccess`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-relationship-resolvers-permission-scoping-query-safety/14-CONTEXT.md
@.planning/phases/14-relationship-resolvers-permission-scoping-query-safety/14-RESEARCH.md
@.planning/phases/13-membership-gating-account-linking/deferred-items.md
</context>

<interfaces>
Existing exports from `backend/src/services/familyMember.service.js` to extend (do not reimplement):
```
export async function wouldCreateCycle(childId, candidateParentId)
export async function linkParent(childId, { motherId, fatherId } = {})
export async function addChild(attrs)
export async function setSpouse(memberAId, memberBId)
export async function getSpouseRows(memberId)
export async function deleteMember(memberId)
```
`Op`, `UniqueConstraintError` from `'sequelize'` and `models`, `sequelize` from `'../models/index.js'`
are already imported at the top of this file — reuse these imports, do not re-import.

`requireFamilyAccess` (already exported from `backend/src/utils/auth.js`, already imported and used
by `backend/src/resolvers/familyMember.resolver.js`):
```
export function requireFamilyAccess(user) {
  requireAuth(user);
  if (user.role === 'ADMIN') return;
  if (!user.familyMemberId) throw new Error('Your account is not yet linked to a family member.');
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: computeEditableScope service utility (PERM-05, REL-04)</name>
  <files>backend/src/services/familyMember.service.js, backend/src/services/familyMember.scope.test.js</files>
  <read_first>
    backend/src/services/familyMember.service.js
    backend/src/models/FamilyMember.js
    backend/src/models/Spouse.js
    backend/src/models/FamilyMember.cycle.test.js (test-shape analog: describe/it structure, resetTables, direct service-function import)
    .planning/phases/14-relationship-resolvers-permission-scoping-query-safety/14-CONTEXT.md (D-02, D-03, D-04)
    .planning/phases/14-relationship-resolvers-permission-scoping-query-safety/14-RESEARCH.md (Pattern 3, Pitfall 4, Assumption A3)
  </read_first>
  <behavior>
    - Test: `self` is always included in their own `ids` set (D-06 self-editable corollary), even
      with no parents/spouse/children recorded.
    - Test: mother and father, when set, are both included.
    - Test: a spouse is included regardless of which side of the canonical `Spouse` row (`memberAId`
      vs `memberBId`) the actor is on.
    - Test: children (rows where the actor is `motherId` or `fatherId`) are included.
    - Test: a full sibling (shares both `motherId` and `fatherId`) is included.
    - Test: a half-sibling (shares only `fatherId`, differing `motherId`) is included (D-03
      either-parent rule).
    - Test: a member with no parents recorded returns an empty `siblings` array without throwing
      (supports the D-04 guidance check other mutations will build on).
    - Test — SC-3 exclusion, grandparent: `self.mother.motherId` (a grandparent) is NOT in
      `self`'s `ids` set.
    - Test — SC-3 exclusion, cousin: a parent's sibling's child (a cousin) is NOT in `self`'s `ids`
      set.
    - Test — SC-3 exclusion, sibling-of-sibling (the dedicated tricky fixture, per Assumption A3):
      construct `self` and `S` sharing only `fatherId`; construct `S` and `T` sharing only
      `motherId` (a `motherId` DIFFERENT from `self`'s own). Assert `T` is NOT in `computeEditableScope(self.id)`'s `ids` set, even though `T` IS a sibling of `self`'s sibling `S`.
  </behavior>
  <action>
    Add `export async function computeEditableScope(memberId, { transaction } = {})` to
    `backend/src/services/familyMember.service.js`. If `memberId == null`, or the row is not
    found, return `{ ids: new Set(), self: null, parents: [], spouses: [], children: [], siblings: [] }`.
    Otherwise: load `self` via `findByPk`; compute `parentIds = [self.motherId, self.fatherId].filter((id) => id != null)`;
    run `parents` (find by `id: parentIds`, skip the query entirely if `parentIds.length === 0`),
    `spouses` (via the existing `Op.or` idiom on `memberAId`/`memberBId` with `memberA`/`memberB`
    includes, mapped to "the other side" of each row), `children` (via `Op.or` on `motherId`/
    `fatherId` equal to `memberId`), and `siblings` (via `Op.and[{ id: { [Op.ne]: memberId } },
    { [Op.or]: parentIds.flatMap((pid) => [{ motherId: pid }, { fatherId: pid }]) }]`, skipping the
    query entirely — returning `[]` — if `parentIds.length === 0`) as `Promise.all`-parallel
    queries, all accepting the optional `transaction`. Build `ids = new Set([memberId, ...parentIds,
    ...spouses.map(s => s.id), ...children.map(c => c.id), ...siblings.map(s => s.id)])`. Return
    `{ ids, self, parents, spouses, children, siblings }`.

    Critical NULL-semantics constraint (Pitfall 4): the sibling `Op.or` array must only ever
    contain clauses built from a NON-NULL `parentIds` entry — never add a `{ motherId: null }`-style
    clause (that would compile to `motherId IS NULL`, matching every other parentless member, which
    is wrong). This is exactly why the sibling query is skipped entirely, not defaulted to an
    always-false-but-present clause, when `parentIds.length === 0`.

    Write `familyMember.scope.test.js` FIRST covering every case in `<behavior>` above — including
    the sibling-of-sibling fixture verbatim as described. Run it, confirm every case fails RED
    against the not-yet-existing `computeEditableScope` (import error), then implement, then GREEN.
  </action>
  <acceptance_criteria>
    - `backend/src/services/familyMember.service.js` exports `computeEditableScope`
      (`grep "export async function computeEditableScope" backend/src/services/familyMember.service.js` matches).
    - No `Op.eq: null` or bare `{ motherId: null }` / `{ fatherId: null }` clause appears anywhere
      in the sibling-query construction (`grep -n "motherId: null\|fatherId: null" backend/src/services/familyMember.service.js`
      returns no matches inside the new function).
    - `npm test --workspace backend -- src/services/familyMember.scope.test.js` passes, including
      the grandparent-exclusion, cousin-exclusion, and sibling-of-sibling-exclusion assertions.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- src/services/familyMember.scope.test.js</automated>
  </verify>
  <done>computeEditableScope exists, is bounded (never recurses beyond one hop of self's own parentIds), and every SC-3 inclusion/exclusion case is proven by a dedicated test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-04 — gate the dashboard resolver with requireFamilyAccess</name>
  <files>backend/src/resolvers/user.resolver.js, backend/src/resolvers/dashboard.test.js</files>
  <read_first>
    backend/src/resolvers/user.resolver.js
    backend/src/resolvers/dashboard.test.js
    backend/src/resolvers/familyMember.resolver.test.js (the exact rejection-assertion shape to mirror: `errors[0].message` equals `requireFamilyAccess`'s exact string, `data.<field>` is `null`)
    backend/src/utils/auth.js
    .planning/phases/13-membership-gating-account-linking/deferred-items.md (WR-04 row)
  </read_first>
  <behavior>
    - Test (new, added to `dashboard.test.js`): a verified-but-unlinked `USER`
      (`createTestUser({ role: 'USER', familyMemberId: null })`) calling `dashboard` is rejected
      with `errors[0].message === 'Your account is not yet linked to a family member.'` and
      `data.dashboard` is `null`.
  </behavior>
  <action>
    Add the new test to `backend/src/resolvers/dashboard.test.js` FIRST. Run it — confirm RED
    (today's `dashboard` resolver uses `requireAuth` only, so a verified-but-unlinked USER
    currently succeeds and gets `data.dashboard` populated, not rejected — the new assertion
    fails). Then in `backend/src/resolvers/user.resolver.js`: add `requireFamilyAccess` to the
    existing named-import destructure from `'../utils/auth.js'` (currently imports
    `createResetToken, createVerificationToken, hashResetToken, hashVerificationToken,
    requireAdmin, requireAuth, resetTokenExpiry, signToken, verificationTokenExpiry`), and change
    the `dashboard` resolver's first line from `requireAuth(user);` to `requireFamilyAccess(user);`.
    Do not change anything else in the resolver body (the ADMIN-vs-USER `users` list branch stays
    exactly as-is). Run the test again, confirm GREEN, then run the full existing
    `dashboard.test.js` file to confirm the ADMIN and already-passing USER cases are unaffected.
  </action>
  <acceptance_criteria>
    - `backend/src/resolvers/user.resolver.js`'s `dashboard` resolver body starts with
      `requireFamilyAccess(user);`, not `requireAuth(user);`
      (`grep -A1 "dashboard: async" backend/src/resolvers/user.resolver.js | grep "requireFamilyAccess"` matches).
    - `requireFamilyAccess` appears in the top-of-file import destructure from `'../utils/auth.js'`.
    - `npm test --workspace backend -- src/resolvers/dashboard.test.js` passes, including the new
      unlinked-user rejection case and the pre-existing ADMIN/USER/unauthenticated cases.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- src/resolvers/dashboard.test.js</automated>
  </verify>
  <done>dashboard resolver rejects a verified-but-unlinked non-admin user exactly like every other family-domain resolver; WR-04 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Authenticated-but-unauthorized member vs. family-domain data | A logged-in, verified USER whose editable relative set does not include a given member (BOLA-shaped risk) |
| Authenticated-but-unlinked member vs. any member-scoped resolver | A verified USER with no `familyMemberId` calling a member-gated query/mutation directly, bypassing the SPA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Elevation of Privilege | `computeEditableScope` (familyMember.service.js) | mitigate | Single reused utility, unit-tested against all three SC-3 exclusion fixtures (grandparent, cousin, sibling-of-sibling) plus every inclusion case — no per-resolver duplicate scope derivation exists to drift out of sync |
| T-14-05 | Elevation of Privilege (missing gate) | `dashboard` resolver (user.resolver.js) | mitigate | `requireAuth` swapped for `requireFamilyAccess`, regression-tested against a verified-but-unlinked USER (WR-04 closed) |

</threat_model>

<verification>
- `npm test --workspace backend -- src/services/familyMember.scope.test.js` — passes
- `npm test --workspace backend -- src/resolvers/dashboard.test.js` — passes
- `npm test --workspace backend` — full suite green
</verification>

<success_criteria>
- `computeEditableScope(memberId)` is exported and correctly includes self/parents/spouses/children/either-parent-siblings while excluding grandparent/cousin/sibling-of-sibling.
- `dashboard` resolver is gated by `requireFamilyAccess`.
</success_criteria>

<output>
Create `.planning/phases/14-relationship-resolvers-permission-scoping-query-safety/14-02-SUMMARY.md` when done
</output>
