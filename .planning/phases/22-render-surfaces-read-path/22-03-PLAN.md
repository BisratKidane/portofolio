---
phase: 22-render-surfaces-read-path
plan: 03
type: execute
wave: 3
depends_on: ["22-02"]
files_modified: []
autonomous: false
requirements: [VIEW-01]

must_haves:
  truths:
    - "A human has visually confirmed, against the LONGEST real Ge'ez name in the actual production dataset (not a synthetic test fixture), that the /family tree card's Ge'ez line renders correctly, truncates via ellipsis when needed, and never causes the fixed 252x120px card to grow or overflow — including the worst-case scenario of Head tag + Latin name + Ge'ez name + birthday + mother + address all present at once (STATE.md blocker, ROADMAP SC1, UI-SPEC Component Contract #5)."
    - "A human has visually confirmed a member WITHOUT a Ge'ez name shows the /family card unchanged from its pre-Phase-22 appearance — no extra row, no dash, no blank gap."
    - "A human has visually confirmed the Ge'ez name renders correctly on both /manage surfaces (admin table + relationship-panel card) for at least one real member with a Ge'ez name."
    - "A human has visually confirmed the admin table search box matches a real Ge'ez substring."
  artifacts: []
  key_links: []
---

<objective>
Close the mandatory manual visual sign-off gate for Phase 22 (VIEW-01's fixed-card constraint, carried forward as a blocker in STATE.md since the milestone's font phase). jsdom cannot rasterize glyphs or measure real visual overflow, so this must be a human verification step against real production data — not an automated assertion.

Purpose: Prove, with the actual longest Ge'ez name in the real dataset (wider glyphs than the synthetic 'ጃነ ዶ' test fixture used in Plan 22-02's component tests), that the `/family` tree card never overflows its fixed height and that all three render surfaces look correct together in the running app.
Output: A recorded human sign-off (approved, or a list of issues to fix before Phase 22 can close).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/22-render-surfaces-read-path/22-CONTEXT.md
@.planning/phases/22-render-surfaces-read-path/22-UI-SPEC.md

<locked_decisions>
- STATE.md Blockers/Concerns: "the fixed 252×120px /family tree card is already tight for Latin noWrap text; Ge'ez glyphs are visually wider at the same character count. Phase 22 needs a mandatory manual visual pass against the longest real Ge'ez name in the actual dataset."
- UI-SPEC Component Contract #5: "jsdom cannot rasterize glyphs. Before phase close, a human must visually verify, on the actual /family tree, the longest real Ge'ez name in the production dataset rendered on a card that also has birthday + mother + address populated — confirming truncation/ellipsis behaves correctly and the card never overflows its fixed 120px height."
- This check is against REAL production data, not the synthetic Ethiopic test fixtures ('ጃነ ዶ') used in Plan 22-02's automated component tests — those already prove the WIRING is correct; this plan proves the VISUAL result with real, potentially-wider glyphs is correct.
</locked_decisions>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual visual sign-off — longest real Ge'ez name on a constrained /family card</name>
  <files>None — verification-only checkpoint, no files modified</files>
  <action>
    No code changes in this task — Plan 22-02 already shipped the rendering/search implementation. The executor's procedural action is: start the app (`npm run dev` from the repo root), sign in, open `/manage`'s admin table to identify the member with the longest `geezFullname` in the real dataset, then present the checkpoint below to the user for manual visual verification against that real member on `/family` and `/manage`. Do not proceed past this task until the user responds via `<resume-signal>`.
  </action>
  <what-built>
    Ge'ez name rendering wired into the `/family` tree card (`MemberNode.jsx`), the `/manage` admin member table (`AdminMemberTable.jsx`), and the `/manage` relationship-panel card (`MemberCard.jsx`), all consuming the shared `getGeezDisplay` helper with an identical visual treatment, plus a Ge'ez-aware admin-table search filter. All of this is covered by automated component tests using synthetic Ethiopic fixtures (Plan 22-02) — this checkpoint verifies the REAL visual result against actual production data, which jsdom cannot assert.
  </what-built>
  <how-to-verify>
    1. Start the app locally (`npm run dev` from the repo root) and sign in as an admin (or a linked member).
    2. Go to `/manage` (admin table) and identify the member with the LONGEST `geezFullname` in the actual production dataset — sort/scan the table if needed. Note their name.
    3. Go to `/family`. Locate that member's card. If possible, prefer a member whose card ALSO has a birthday, mother name, and living address populated (the worst-case row count). If no single real member has all of those plus the longest Ge'ez name, check the longest-name member and a separately-crowded member as two passes.
    4. On that card, confirm:
       - The Ge'ez name renders as a distinct line directly below the Latin name.
       - The card's outer box is the same fixed size as every other card — it does not grow taller or wider.
       - Every populated row (Latin name, Ge'ez name, birthday, mother, address) truncates with an ellipsis rather than wrapping onto a second line or overflowing the card's edges.
       - No row is missing/dropped to make room for another.
    5. Find a member WITHOUT a Ge'ez name on `/family` and confirm their card looks identical to how it did before this phase — no extra row, no dash, no blank gap where the Ge'ez line would be.
    6. Back on `/manage`, confirm the same longest-Ge'ez-name member's Ge'ez name appears as a second line under the Latin name in BOTH the relationship-panel card (Manage self-service view, or the focused-member panel as admin) and the admin member table row.
    7. In the `/manage` admin table search box, type a substring of that member's Ge'ez name and confirm their row stays visible; then type a substring that only matches a Latin name and confirm Latin-only members still surface correctly (both search paths still work together).
  </how-to-verify>
  <resume-signal>Type "approved" if every check in steps 4-7 passes, or describe exactly what's wrong (e.g. "card overflowed for member X", "Ge'ez line missing on the admin table for member Y", "search substring Z did not match").</resume-signal>
  <verify>
    <human-check>User responds "approved" to the resume-signal after completing steps 1-7 of how-to-verify.</human-check>
  </verify>
  <done>User has typed "approved", confirming the longest real Ge'ez name renders correctly and without overflow on /family, an absent-Ge'ez card is unchanged, both /manage surfaces show the Ge'ez name correctly, and the admin-table Ge'ez search works against real data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan makes zero code changes — it is a human visual review of Plan 22-02's already-shipped, already-tested rendering code against real data already fetched by already-authorized queries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | accept | No applicable threats — this plan is a manual verification step with zero file changes, zero new code, zero new input/network/persistence/dependency surface. |
</threat_model>

<verification>
Human sign-off recorded via the `checkpoint:human-verify` task's resume-signal ("approved", or a list of concrete issues to route back into a gap-closure plan before Phase 22 closes).
</verification>

<success_criteria>
- The longest real Ge'ez name in the production dataset renders correctly on a `/family` card without overflowing the fixed 252x120px size, with correct truncation on every row.
- A member without a Ge'ez name shows an unchanged card.
- The same member's Ge'ez name is visually confirmed on both `/manage` surfaces.
- The admin-table Ge'ez search is visually confirmed working against real data.
</success_criteria>

<output>
Create `.planning/phases/22-render-surfaces-read-path/22-03-SUMMARY.md` when done, recording the sign-off result (approved / issues found + routed to gap closure).
</output>
