---
phase: 17-family-deep-tree-visualization
plan: 04
type: execute
wave: 3
depends_on: ["17-01", "17-03"]
files_modified:
  - frontend/src/pages/FamilyTreePage.jsx
  - frontend/src/pages/FamilyTreePage.test.jsx
  - frontend/src/components/family/MemberDetailPanel.jsx
  - frontend/src/components/family/MemberDetailPanel.test.jsx
  - frontend/src/App.jsx
  - frontend/src/components/AppLayout.jsx
autonomous: true
requirements: [TREE-02, TREE-04, QUAL-02, QUAL-03]

must_haves:
  truths:
    - "A linked member visiting /family sees the tree populated by a single flat familyMembers query (no per-node fetch)"
    - "Clicking a node opens a read-only detail panel with Parents/Spouse/Children/Siblings groups, reading from already-fetched in-memory data (no new network call)"
    - "/family is reachable only by linked members and admins — an unlinked non-admin is redirected to /pending, reusing <ProtectedRoute> verbatim (D-15)"
    - "The full backend + frontend test suite is green (QUAL-03) — no new CI configuration is needed"
  artifacts:
    - path: "frontend/src/pages/FamilyTreePage.jsx"
      provides: "route component: single flat fetch, ReactFlowProvider mount, orchestrates FamilyTreeCanvas + MemberDetailPanel"
    - path: "frontend/src/components/family/MemberDetailPanel.jsx"
      provides: "read-only popover/drawer: photo, name, gender, dates, phone, address, relationship groups"
    - path: "frontend/src/App.jsx"
      provides: "/family route registered under the existing <ProtectedRoute /> block, no allowedRoles"
      contains: "path=\"family\""
    - path: "frontend/src/components/AppLayout.jsx"
      provides: "a 'Family tree' nav button alongside the existing Dashboard button"
  key_links:
    - from: "frontend/src/pages/FamilyTreePage.jsx"
      to: "frontend/src/api/graphqlClient.js"
      via: "graphqlRequest(FAMILY_TREE_QUERY) on mount, no linkedUser field selected"
      pattern: "graphqlRequest\\(FAMILY_TREE_QUERY\\)"
    - from: "frontend/src/App.jsx"
      to: "frontend/src/components/ProtectedRoute.jsx"
      via: "route registered inside the same <ProtectedRoute /> block as dashboard/manage, no allowedRoles prop"
      pattern: "path=\"family\""
---

<objective>
Wire the flat GraphQL fetch, the read-only detail panel, route registration, and nav placement that turn Plan 17-03's canvas into the actual `/family` page — closing out TREE-02 (initial paint = collapsed-by-default via the already-built canvas), TREE-04 (route gating), QUAL-02 (component-test coverage for the last new page), and QUAL-03 (confirm the whole milestone's test suite stays green with zero new CI config).

Purpose: This is the final integration plan of the phase — everything built in 17-01/17-02/17-03 gets connected into a reachable, tested, navigable page.
Output: `/family` live behind `<ProtectedRoute>`, a nav entry point, a read-only detail panel, and a full green `npm test --workspaces` run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-family-deep-tree-visualization/17-CONTEXT.md
@.planning/phases/17-family-deep-tree-visualization/17-RESEARCH.md
@.planning/phases/17-family-deep-tree-visualization/17-UI-SPEC.md
@.planning/phases/17-family-deep-tree-visualization/17-PATTERNS.md
</context>

<interfaces>
<!-- FamilyTreeCanvas's prop contract (built in 17-03, do not modify) -->
```javascript
// frontend/src/components/family/FamilyTreeCanvas.jsx
export default function FamilyTreeCanvas({ nodes, edges, initialExpandedIds, viewerId, onMemberClick })
```

`familyTree.assembly.js` contract (built in 17-02, do not modify):
```javascript
export function buildForest(flatMembers, viewerId) // -> { nodes, edges, generations, apexIds, initialExpandedIds }
export function deriveSiblings(memberId, membersById) // -> flatMember[]
```

ManagePage.jsx's fetch-on-mount / loading / error state shape (frontend/src/pages/ManagePage.jsx lines 124-176 `MemberBranch`, the exact pattern this page's data-fetch mirrors):
```javascript
const [pageLoading, setPageLoading] = useState(true);
const [pageError, setPageError] = useState('');

const refetch = useCallback(() => {
  setPageLoading(true);
  return graphqlRequest(MY_EDITABLE_MEMBERS_QUERY)
    .then((data) => { /* set state */ })
    .catch((err) => setPageError(err.message))
    .finally(() => setPageLoading(false));
}, [user.familyMemberId]);

useEffect(() => { refetch(); }, [refetch]);

if (pageLoading) {
  return (
    <Box sx={{ display: 'flex', justifyContent: 'center', py: 10 }}>
      <CircularProgress />
    </Box>
  );
}
if (pageError) return <Alert severity="error">{pageError}</Alert>;
```

AuthContext's `user` shape (frontend/src/context/AuthContext.jsx `ME_QUERY`, line 8): `{ id, name, email, role, familyMemberId }` — `useAuth()` exposes `{ user, loading, ... }`.

RESEARCH Pattern 1 — the exact flat query this plan's `FAMILY_TREE_QUERY` constant must match (no `linkedUser`, per D-14/Pitfall 6):
```graphql
query FamilyTree {
  familyMembers {
    id firstname lastname fullname gender birthdate deathdate photoUrl
    mother { id } father { id } spouses { id } children { id }
  }
}
```

RelationshipGroupedPanel's grouped-section rendering shape (frontend/src/components/manage/RelationshipGroupedPanel.jsx lines 65-118 — reuse the Parents/Spouse/Children/Siblings section/heading/divider structure, minus every edit affordance):
```jsx
<Paper elevation={0} sx={{ borderRadius: 5, border: `1px solid ${colors.line}`, overflow: 'hidden' }}>
  <Stack divider={<Box sx={{ borderBottom: `1px solid ${colors.line}` }} />}>
    <Box sx={{ px: { xs: 3, md: 4 }, py: 3 }}>
      <Typography variant="h6">Parents</Typography>
      {/* ...MemberRows... */}
    </Box>
  </Stack>
</Paper>
```

Route registration insertion point (frontend/src/App.jsx, current state after Plan 17-03 removed the spike route):
```jsx
<Route element={<ProtectedRoute />}>
  <Route path="dashboard" element={<Dashboard />} />
  <Route path="manage" element={<ManagePage />} />
</Route>
```

AppLayout's existing Dashboard nav button (frontend/src/components/AppLayout.jsx lines 36-43 — add a sibling button in this same `<Stack direction="row">`):
```jsx
<Button
  component={RouterLink}
  to="/dashboard"
  sx={{ color: colors.slate, fontWeight: 600, display: { xs: 'none', sm: 'inline-flex' } }}
>
  Dashboard
</Button>
```

UI-SPEC copy contract (exact strings, load-bearing):
- Loading: **"Building your family tree…"**
- Empty state heading: **"Your family tree is empty"**, body: **"Family members added on the Manage page will appear here as a tree."** (links to `/manage`)
- Error: **"We couldn't load your family tree."** + **"Check your connection and try again."** + a **"Retry"** button (re-issues the fetch)
- Detail panel missing dates: **"Dates unknown"**; empty relationship group: **"No recorded {parents/spouse/children/siblings}"**
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build MemberDetailPanel (read-only, in-memory data)</name>
  <files>frontend/src/components/family/MemberDetailPanel.jsx, frontend/src/components/family/MemberDetailPanel.test.jsx</files>
  <read_first>
    frontend/src/components/manage/RelationshipGroupedPanel.jsx
    frontend/src/components/family/familyTree.assembly.js
    .planning/phases/17-family-deep-tree-visualization/17-UI-SPEC.md
  </read_first>
  <behavior>
    - Renders nothing (`null`) when `open` is false or `member` is null.
    - When open, renders a MUI `Drawer` (anchor="right") containing: `MemberAvatarImage`, the member's `fullname` (20px/700, Sora — inherits theme `h5`/`h6`), a gender line, a dates line built the same way as `MemberNode` (`1932–2001` / `1932–` / `–2001` / **"Dates unknown"** if both null), `phone`/`address` if present, and four relationship sections (Parents, Spouse, Children, Siblings) resolved from `membersById` (a `Map` the parent passes in) using `member.mother`/`member.father`/`member.spouses`/`member.children` (thin `{id}` refs resolved to full rows via `membersById.get(id)`) and `deriveSiblings(member.id, membersById)` for the Siblings section. Each empty relationship group renders **"No recorded {parents/spouse/children/siblings}"** (lowercase group name substituted) instead of an empty list.
    - A close `IconButton` (`CloseRounded` from `@mui/icons-material`) calls `onClose`; dismissible via escape key and outside-click (MUI `Drawer`'s native `onClose` behavior, wired to the same `onClose` prop).
    - No `onEdit`/`onDelete`/`onPickPhoto`/`onRemovePhoto` props exist on this component at all (D-08 — read-only, structurally, not just by omission of buttons).
  </behavior>
  <action>
    Default-export `MemberDetailPanel({ open, member, membersById, onClose })`. Follow `RelationshipGroupedPanel.jsx`'s section/heading/`Stack divider` composition shape (import `colors` from `'../../theme.js'`) but strip every edit affordance. Write `MemberDetailPanel.test.jsx` per this project's RTL conventions (no `mockReactFlow()` needed here — this component has no xyflow dependency, plain MUI `Drawer`): test the closed state renders nothing, the open state shows the member's name/dates, an empty Spouse section shows the "No recorded spouse" copy, and clicking the close button calls `onClose`.
  </action>
  <acceptance_criteria>
    - `npm test --workspace frontend -- MemberDetailPanel` passes.
    - `grep -c "onEdit\|onDelete\|onPickPhoto\|onRemovePhoto" frontend/src/components/family/MemberDetailPanel.jsx` returns 0 (structural read-only guarantee, D-08).
    - `grep -c "Dates unknown\|No recorded" frontend/src/components/family/MemberDetailPanel.jsx` returns 2 or more (both copy strings present, UI-SPEC contract).
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace frontend -- MemberDetailPanel 2>&1 | tail -60</automated>
  </verify>
  <done>MemberDetailPanel renders member details and relationship groups from already-in-memory data, with zero edit affordances, verified by a passing component test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build FamilyTreePage — the single flat fetch + orchestration</name>
  <files>frontend/src/pages/FamilyTreePage.jsx, frontend/src/pages/FamilyTreePage.test.jsx</files>
  <read_first>
    frontend/src/pages/ManagePage.jsx
    frontend/src/components/family/FamilyTreeCanvas.jsx
    frontend/src/components/family/MemberDetailPanel.jsx
    frontend/src/context/AuthContext.jsx
    frontend/src/api/graphqlClient.js
  </read_first>
  <behavior>
    - On mount, calls `graphqlRequest(FAMILY_TREE_QUERY)` (the exact query from `<interfaces>`, no `linkedUser` field). While pending: `CircularProgress` + **"Building your family tree…"** text (UI-SPEC copy). On error: `Alert severity="error"` with **"We couldn't load your family tree."** / **"Check your connection and try again."** plus a **"Retry"** `Button` that re-invokes the fetch. On success with an empty `familyMembers` array: **"Your family tree is empty"** heading + **"Family members added on the Manage page will appear here as a tree."** body with a `RouterLink` to `/manage`.
    - On success with data: builds `membersById` (`Map` keyed by `String(member.id)`), calls `buildForest(data.familyMembers, user.familyMemberId)`, stores `{ nodes, edges, initialExpandedIds }` in state, wraps `<FamilyTreeCanvas>` in `<ReactFlowProvider>`, passes `viewerId={user.familyMemberId}` and `onMemberClick={(id) => setSelectedMemberId(id)}`.
    - Manages `selectedMemberId` state; renders `<MemberDetailPanel open={Boolean(selectedMemberId)} member={membersById.get(selectedMemberId)} membersById={membersById} onClose={() => setSelectedMemberId(null)} />` alongside the canvas.
  </behavior>
  <action>
    Default-export `FamilyTreePage()`. Declare `const FAMILY_TREE_QUERY` as a top-level `SCREAMING_SNAKE_CASE`-with-`_QUERY`-suffix template-literal constant (matching `ManagePage.jsx`'s `FAMILY_MEMBERS_QUERY` convention), selecting exactly the fields listed in `<interfaces>` — do not add `linkedUser`. Use `useAuth()` for `user`. Follow `ManagePage.jsx`'s `MemberBranch` fetch/loading/error state shape verbatim (`useCallback` refetch + `useEffect` on mount), adapted for this page's copy and empty-state branch. Write `FamilyTreePage.test.jsx` mocking `graphqlRequest` and `useAuth` per `ManagePage.test.jsx`'s exact `vi.mock` shape (lines 8-25), and call `mockReactFlow()` at module scope in this test file too (this page mounts `FamilyTreeCanvas`, which needs the same jsdom polyfills as Plan 17-03's canvas test — colocated here, not global, per RESEARCH Pitfall 2).
  </action>
  <acceptance_criteria>
    - `grep -c "linkedUser" frontend/src/pages/FamilyTreePage.jsx` returns 0 (D-14/Pitfall 6 — the tree query must never request this field).
    - `grep -c "ReactFlowProvider" frontend/src/pages/FamilyTreePage.jsx` returns 2 or more (import + JSX usage).
    - `npm test --workspace frontend -- FamilyTreePage` passes, covering: loading state, error state with working Retry, empty state with a `/manage` link, and a populated render showing at least one member's name plus a working node-click that opens the detail panel.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace frontend -- FamilyTreePage 2>&1 | tail -80</automated>
  </verify>
  <done>FamilyTreePage fetches the flat query once, assembles the forest, and orchestrates the canvas + detail panel across all four UI-SPEC states (loading/error/empty/populated).</done>
</task>

<task type="auto">
  <name>Task 3: Register the /family route, add nav entry, and gate the full workspace suite (QUAL-03)</name>
  <files>frontend/src/App.jsx, frontend/src/components/AppLayout.jsx</files>
  <read_first>
    frontend/src/App.jsx
    frontend/src/components/AppLayout.jsx
    frontend/src/components/ProtectedRoute.jsx
  </read_first>
  <action>
    In `App.jsx`, add `import FamilyTreePage from './pages/FamilyTreePage.jsx';` alongside the other page imports, and add `<Route path="family" element={<FamilyTreePage />} />` inside the SAME `<Route element={<ProtectedRoute />}>` block that already wraps `dashboard`/`manage` (no `allowedRoles` prop — D-15, reused verbatim). In `AppLayout.jsx`, add a sibling `Button` next to the existing `to="/dashboard"` Dashboard button (same `<Stack direction="row">`, lines 36-59), `component={RouterLink}` `to="/family"`, label **"Family tree"**, identical `sx` styling to the Dashboard button. Then run the full workspace suite to confirm QUAL-03 (no CI config changes needed — `.github/workflows/ci.yml` already runs `npm ci && npm test`, and root `npm test` already runs `npm test --workspaces`).
  </action>
  <acceptance_criteria>
    - `grep -c 'path="family"' frontend/src/App.jsx` returns 1, and it appears inside the unguarded (`allowedRoles`-free) `<ProtectedRoute />` block, not the `allowedRoles={['ADMIN']}` block.
    - `grep -c "Family tree" frontend/src/components/AppLayout.jsx` returns 1.
    - `npm test --workspaces` (root) passes with zero failing tests across both workspaces.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspaces 2>&1 | tail -60</automated>
  </verify>
  <done>/family is live behind the standard linked-member-or-admin gate, reachable via a "Family tree" nav button, and the full backend+frontend suite is green with no new CI configuration required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> `/family` route | Client-side gate via `<ProtectedRoute>` (UX redirect only); real enforcement is Plan 17-01's server-side `requireFamilyAccess` on `familyMembers`. |
| `FamilyTreePage` -> `familyMembers` query payload | The single flat query this page issues must not request any field with admin-only sensitivity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10 | Spoofing / Elevation of Privilege | `/family` route access | mitigate | Reuses `<ProtectedRoute>` verbatim (no `allowedRoles`) — identical redirect logic already covered by the existing, passing `ProtectedRoute.test.jsx` suite (unlinked non-admin -> `/pending`, unauthenticated -> `/login`, linked/admin -> allowed). Real enforcement is server-side (Plan 17-01); this is defense-in-depth UX only. |
| T-17-11 | Information Disclosure | `FAMILY_TREE_QUERY` field selection | mitigate | The query constant is grep-verified (acceptance criterion) to never include `linkedUser` — closing the one field on `FamilyMember` with prior admin-only sensitivity (Phase 14 CR-01), per D-14/RESEARCH Pitfall 6. |
| T-17-12 | Repudiation / Availability | Full-suite regression across the whole v2.0 milestone | mitigate | Task 3 runs `npm test --workspaces` (the exact command CI already runs on every push/PR) as an explicit phase-gate verification step, not left to CI to discover after merge — QUAL-03 requires the milestone to close with CI green, confirmed locally first. |

</threat_model>

<verification>
Run `npm test --workspace frontend -- MemberDetailPanel` (Task 1), `npm test --workspace frontend -- FamilyTreePage` (Task 2), then `npm test --workspaces` (Task 3, full milestone gate) — the last command must show zero failures across backend and frontend before this phase is considered complete.
</verification>

<success_criteria>
- `/family` is live, reachable only by linked members and admins, with a discoverable nav entry.
- The page fetches exactly one flat query, assembles the forest client-side, and renders the canvas + read-only detail panel.
- QUAL-02: MemberDetailPanel and FamilyTreePage both have passing component-test coverage.
- QUAL-03: `npm test --workspaces` is fully green — the milestone's CI-enforced safety net holds through the last phase.
</success_criteria>

<output>
Create `.planning/phases/17-family-deep-tree-visualization/17-04-SUMMARY.md` when done
</output>
</content>
