---
phase: 07-foundation-hardening-cors-jwt-fail-fast-password-strength
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/src/server.js
  - backend/test/helpers.js
  - backend/src/config/corsOptions.js
  - backend/src/config/corsOptions.test.js
  - backend/src/server.cors.test.js
autonomous: false
requirements: [CORS-01]

must_haves:
  truths:
    - "A rejected CORS origin never appears in the client-facing HTTP response body or headers (only in server-side console.warn logs)"
    - "An allowlisted origin's request still succeeds normally through CORS after the refactor"
    - "The full v1.0 backend test suite (51 tests) stays green after the app-export refactor and CORS wiring change"
    - "An HTTP-level (supertest) test harness exists that can exercise Express-layer code the in-process executeOperation() helper cannot reach"
  artifacts:
    - path: "backend/src/config/corsOptions.js"
      provides: "Pure corsOriginValidator/buildCorsOptions functions, unit-testable without HTTP"
      exports: ["corsOriginValidator", "buildCorsOptions"]
    - path: "backend/src/config/corsOptions.test.js"
      provides: "Pure-function unit tests for the CORS validator"
      min_lines: 15
    - path: "backend/test/helpers.js"
      provides: "httpClient() supertest wrapper, additive to existing graphql()/resetTables()/createTestUser()"
      contains: "httpClient"
    - path: "backend/src/server.cors.test.js"
      provides: "HTTP-level integration test proving the rejected origin is never echoed"
      contains: "supertest"
    - path: "backend/src/server.js"
      provides: "Importable app export, decoupled from app.listen()"
      contains: "export { app }"
  key_links:
    - from: "backend/src/server.js"
      to: "backend/src/config/corsOptions.js"
      via: "buildCorsOptions(env) import wired into cors() middleware"
      pattern: "buildCorsOptions"
    - from: "backend/test/helpers.js"
      to: "backend/src/server.js"
      via: "app import for supertest-based httpClient()"
      pattern: "from '\\.\\./src/server\\.js'"
---

<objective>
Stand up the first HTTP-level test harness in this codebase (importable Express `app` + `supertest`) and use it to fix the CORS-01 origin-echo vulnerability: a rejected CORS origin must never be reflected back to the client, only logged server-side.

Purpose: `backend/test/helpers.js`'s existing `graphql()` harness calls `ApolloServer#executeOperation()` in-process and never touches Express middleware — it structurally cannot reach `cors()`. Per PITFALLS.md Pitfall 1, the importable-`app` + `supertest` harness is a prerequisite that must land BEFORE any red CORS test, and is reused by Phase 10's coarse rate-limit layer.
Output: `backend/src/config/corsOptions.js` (pure, unit-tested validator), an importable `app` export from `backend/src/server.js`, a new `httpClient()` helper in `backend/test/helpers.js`, and a passing HTTP-level CORS regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation-hardening-cors-jwt-fail-fast-password-strength/07-CONTEXT.md
@.planning/phases/07-foundation-hardening-cors-jwt-fail-fast-password-strength/07-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md

<interfaces>
<!-- Existing code the executor consumes directly — no exploration needed. -->

From backend/src/server.js (current, full file — this is what gets refactored):
```javascript
import express from 'express';
import cors from 'cors';
import { ApolloServer } from '@apollo/server';
import { expressMiddleware } from '@apollo/server/express4';
import { env } from './config/env.js';
import { initializeDatabase, models } from './models/index.js';
import { typeDefs } from './schemas/index.js';
import { resolvers } from './resolvers/index.js';
import { getUserFromRequest } from './utils/auth.js';

const app = express();

app.get('/health', (_req, res) => {
  res.json({ status: 'ok' });
});

app.use(cors({
  origin(origin, callback) {
    if (!origin || env.clientOrigins.includes(origin)) return callback(null, true);
    return callback(new Error(`Origin ${origin} is not allowed by CORS.`));
  },
  credentials: true
}));

const apollo = new ApolloServer({ typeDefs, resolvers });

await apollo.start();
await initializeDatabase();

app.use(
  '/graphql',
  express.json(),
  expressMiddleware(apollo, {
    context: async ({ req }) => ({
      models,
      user: await getUserFromRequest(req, models)
    })
  })
);

app.listen(env.port, () => {
  console.log(`Backend ready at http://localhost:${env.port}/graphql`);
});
```

From backend/test/helpers.js (current, full file — additive only, do not modify these exports):
```javascript
import { ApolloServer } from '@apollo/server';
import { models } from '../src/models/index.js';
import { typeDefs } from '../src/schemas/index.js';
import { resolvers } from '../src/resolvers/index.js';

const server = new ApolloServer({ typeDefs, resolvers });

export async function graphql(query, variables, user = null) {
  const response = await server.executeOperation(
    { query, variables },
    { contextValue: { models, user } }
  );
  return response.body.singleResult;
}

export async function resetTables() { /* truncates models.User */ }
export async function createTestUser(overrides = {}) { /* creates a User row */ }
```

From backend/package.json (current devDependencies — supertest is not yet installed):
```json
"devDependencies": {
  "nodemon": "^3.1.9",
  "vitest": "^4.1.10"
}
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify supertest package legitimacy before install</name>
  <files>backend/package.json (verification only — no changes made in this task)</files>
  <read_first>backend/package.json (current devDependencies — confirm supertest is absent today); .planning/research/STACK.md lines 23-27, 55-63 (supertest@^7.2.2 recommendation and install command)</read_first>
  <what-built>Nothing yet — this is a pre-install verification gate. RESEARCH.md has no `## Package Legitimacy Audit` table for this milestone, so per the package legitimacy gate protocol `supertest` is treated as `[ASSUMED]` and requires explicit human sign-off before any `npm install` runs.</what-built>
  <action>Present the supertest package (registry URL, recommended version `^7.2.2`, and its role as the HTTP-test-harness devDependency) to the human for legitimacy review before any install command executes. Do not run `npm install` in this task — that happens in Task 2, gated on the "approved" resume-signal below.</action>
  <how-to-verify>
    1. Visit https://www.npmjs.com/package/supertest and confirm: publisher/maintainers look legitimate (ladjs/visionmedia-affiliated), weekly download count is in the millions, and the package has no unresolved critical security advisories.
    2. Run `npm view supertest versions --json | tail -5` locally and confirm a `7.x` version exists on the registry, consistent with the `^7.2.2` version STACK.md recommends.
    3. Confirm `supertest`'s `peerDependencies`/engine requirements are compatible with this repo's Express 4.21 / Node 24.x stack (no conflicting peer deps).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `npm install --workspace backend -D supertest@^7.2.2` in Task 2, or describe concerns to halt.</resume-signal>
  <acceptance_criteria>Human has typed an affirmative response before Task 2's install command runs. No file changes occur as part of this task.</acceptance_criteria>
  <verify>
    <manual>Human types "approved" (or equivalent affirmative) in response to the how-to-verify checklist above.</manual>
  </verify>
  <done>Human has explicitly approved the supertest package as legitimate; Task 2 is unblocked to run the install.</done>
</task>

<task type="auto">
  <name>Task 2: HTTP test harness — importable app export + httpClient()</name>
  <files>backend/package.json, backend/src/server.js, backend/test/helpers.js</files>
  <read_first>backend/src/server.js (full file, reproduced above); backend/test/helpers.js (full file, reproduced above); backend/src/config/env.js (env.nodeEnv, env.port); backend/vitest.config.js (confirms NODE_ENV=test is forced for every backend Vitest run); .planning/phases/07-foundation-hardening-cors-jwt-fail-fast-password-strength/07-PATTERNS.md lines 49-76 and 210-252 (app.listen() decoupling guidance, httpClient() illustrative shape, "no in-repo analog" note); .planning/research/STACK.md lines 27-28 (supertest requires an importable app, no real port binding)</read_first>
  <action>
    Only after Task 1's "approved" signal: run `npm install --workspace backend -D supertest@^7.2.2`.
    In `backend/src/server.js`, keep the existing top-to-bottom boot sequence unchanged in order (`cors()` mount, `apollo.start()`, `initializeDatabase()`, `expressMiddleware` mount on `/graphql`) — do not reorder any of the existing top-level `await` calls. Add `export { app };` immediately after `const app = express();` (or at the bottom — executor's discretion on placement, as long as it's a named export). Guard the final `app.listen(env.port, ...)` call behind `if (env.nodeEnv !== 'test')` so importing this module under Vitest (which force-sets `NODE_ENV=test` in `vitest.config.js`) never binds a real port or prints the "Backend ready at..." boot line.
    In `backend/test/helpers.js`, add a new named export `httpClient()` that imports `request` from `supertest` and `app` from `../src/server.js`, returning `request(app)`. This is purely additive — do not rename, remove, or change the signature of `graphql()`, `resetTables()`, or `createTestUser()`.
  </action>
  <acceptance_criteria>
    `npm test --workspace backend` (the full pre-existing suite) passes with zero new failures and the same 51-test count as before this task (this task adds no new test files itself).
    `grep -n "export function httpClient" backend/test/helpers.js` returns exactly one match.
    `grep -n "export { app }" backend/src/server.js` (or equivalent named export) returns exactly one match.
    `grep -n "env.nodeEnv !== 'test'" backend/src/server.js` returns exactly one match guarding the `app.listen(...)` call.
    `backend/package.json`'s `devDependencies` now lists `supertest` at `^7.2.2`.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend</automated>
  </verify>
  <done>`app` is exported from `server.js`, `app.listen()` is gated on `env.nodeEnv !== 'test'`, `httpClient()` exists in `test/helpers.js`, `supertest` is an installed devDependency, and the full pre-existing 51-test backend suite is unchanged and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: TDD corsOriginValidator + wire into server.js CORS middleware</name>
  <files>backend/src/config/corsOptions.js, backend/src/config/corsOptions.test.js, backend/src/server.js, backend/src/server.cors.test.js</files>
  <read_first>backend/src/server.js (current CORS block, lines 17-23, reproduced above); .planning/research/ARCHITECTURE.md lines 159-176 (corsOriginValidator/buildCorsOptions illustrative shape); .planning/phases/07-foundation-hardening-cors-jwt-fail-fast-password-strength/07-PATTERNS.md lines 20-77 and 296-311 (exact current CORS block, D-02 exact client string, supertest CORS test illustrative shape); .planning/phases/07-foundation-hardening-cors-jwt-fail-fast-password-strength/07-CONTEXT.md D-02 (locked exact client message); backend/src/utils/auth.js (throw-plain-Error convention to mirror); backend/test/helpers.js (httpClient() from Task 2)</read_first>
  <behavior>
    - Test 1 (corsOptions.test.js): `corsOriginValidator(undefined, callback, { clientOrigins: ['http://localhost:5173'] })` calls `callback(null, true)` — no-Origin requests (health checks, curl) are allowed.
    - Test 2 (corsOptions.test.js): `corsOriginValidator('http://localhost:5173', callback, { clientOrigins: ['http://localhost:5173'] })` calls `callback(null, true)` — an allowlisted origin passes.
    - Test 3 (corsOptions.test.js): `corsOriginValidator('https://evil.example', callback, { clientOrigins: ['http://localhost:5173'] })` calls `callback` with an `Error` whose `.message` is exactly `'Not allowed by CORS.'` (D-02) — the callback's `Error` message must never contain the substring `evil.example`.
    - Test 4 (server.cors.test.js, HTTP-level via `httpClient()`): a `POST /graphql` request with header `Origin: https://evil.example` is rejected by CORS, and neither the response body nor headers contain the substring `evil.example`; a request with an allowlisted `Origin` header still succeeds.
  </behavior>
  <action>
    Write `backend/src/config/corsOptions.test.js` first (RED) covering Tests 1-3 above, confirm it fails (module doesn't exist yet). Then create `backend/src/config/corsOptions.js` exporting `corsOriginValidator(origin, callback, { clientOrigins })` — no-origin or allowlisted origin calls `callback(null, true)`; otherwise `console.warn` the rejected origin server-side (e.g. `` `CORS rejected origin: ${origin}` ``) and call `callback(new Error('Not allowed by CORS.'))`, using D-02's exact locked client string (note: ARCHITECTURE.md's illustrative text used the slightly different wording `'Origin not allowed by CORS.'` — the CONTEXT.md D-02 decision governs and takes precedence). Also export `buildCorsOptions(env)` returning `{ origin: (o, cb) => corsOriginValidator(o, cb, env), credentials: true }`. Confirm GREEN.
    Replace the inline `cors({...})` block in `backend/src/server.js` (lines 17-23) with `cors(buildCorsOptions(env))`, importing `buildCorsOptions` from `./config/corsOptions.js` alongside the existing import block.
    Write `backend/src/server.cors.test.js` (co-located per the repo's test-file convention, using `httpClient()` from Task 2, not `graphql()`) covering Test 4 above. Run it to confirm GREEN against the now-fixed wiring, then run the full backend suite to confirm zero regressions.
  </action>
  <acceptance_criteria>
    `corsOptions.test.js` passes with `expect(err.message).toBe('Not allowed by CORS.')` (exact match) and `expect(err.message).not.toContain('evil.example')` for the rejected case, and `expect(spy).toHaveBeenCalledWith(null, true)` for the allowed/no-origin cases.
    `server.cors.test.js` passes with an assertion equivalent to `expect(JSON.stringify(res.body) + JSON.stringify(res.headers)).not.toContain('evil.example')` for the rejected-origin request, and a successful (non-CORS-error) response for the allowlisted-origin request.
    `npm test --workspace backend` (full suite, now 51 + the new CORS tests) is green.
    `grep -rn "Origin \${origin} is not allowed" backend/src/server.js` returns zero matches (the old origin-echoing string is fully removed).
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- corsOptions server.cors</automated>
  </verify>
  <done>CORS rejection returns the fixed D-02 client string, the real rejected origin only reaches `console.warn`, both the pure-function and HTTP-level tests pass, and the full backend suite (including the pre-existing 51 tests) stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (attacker-controlled `Origin` header) → Express `cors()` middleware | Untrusted request header crosses into server-side origin-allowlist logic |
| `npm install` → `backend/package.json` devDependencies | Third-party supply-chain code entering the build/test toolchain |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | `corsOriginValidator` / `server.js` CORS callback | mitigate | Return the fixed constant `'Not allowed by CORS.'` to the client (D-02); log the real rejected origin via `console.warn` server-side only; verified by an HTTP-level test asserting the response body/headers never contain the rejected origin string (CORS-01). |
| T-07-02 | Tampering | `server.js` `app` export / `app.listen()` guard | mitigate | Guard `app.listen()` behind `env.nodeEnv !== 'test'` so importing `app` for tests never binds a real port or double-starts Apollo; the pre-existing 51-test suite plus the new harness test both pass post-refactor, proving the CORS middleware still runs identically on the exported `app`. |
| T-07-SC | Tampering (supply chain) | `backend/package.json` — `supertest` devDependency install | mitigate | No `## Package Legitimacy Audit` table exists in RESEARCH.md for this milestone, so `supertest` is treated as `[ASSUMED]`; a blocking `checkpoint:human-verify` task (Task 1) requires confirming `npmjs.com/package/supertest` legitimacy before `npm install` runs. |
</threat_model>

<verification>
Run `npm test --workspace backend` after each task to confirm the pre-existing 51-test suite stays green throughout, and that new test counts increase only by intentional additions. Manually confirm `grep -rn "Origin \${origin}"` returns zero matches in `backend/src/server.js` after Task 3.
</verification>

<success_criteria>
- `backend/src/config/corsOptions.js` exports `corsOriginValidator`/`buildCorsOptions`, unit-tested with zero HTTP dependency.
- `backend/src/server.js` exports `app`, decoupled from a test-gated `app.listen()`.
- `backend/test/helpers.js` exposes `httpClient()`, additive to the existing `graphql()`/`resetTables()`/`createTestUser()`.
- A rejected CORS origin never appears in any client-visible response (body or headers), proven by `backend/src/server.cors.test.js`.
- Full backend suite (51 pre-existing + new CORS tests) is green.
</success_criteria>

<output>
Create `.planning/phases/07-foundation-hardening-cors-jwt-fail-fast-password-strength/07-01-SUMMARY.md` when done
</output>
