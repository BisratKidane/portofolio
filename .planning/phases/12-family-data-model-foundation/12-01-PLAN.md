---
phase: 12-family-data-model-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/src/models/FamilyMember.js
  - backend/src/models/FamilyMember.test.js
  - backend/src/models/index.js
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03, MEM-05]

must_haves:
  truths:
    - "A FamilyMember can be created with firstname, lastname, and gender; creating one missing any of these three is rejected."
    - "gender only accepts the exact values Male, Female, or Other; anything else is rejected."
    - "fullname always equals firstname + ' ' + lastname, derived at read time, never a separate input or stored column."
    - "birthdate/deathdate reject future dates and reject deathdate earlier than birthdate, but accept arbitrarily old dates (no lower bound)."
    - "mothersname, email, birthdate, deathdate, phone, address are all optional and accept null/undefined without error."
  artifacts:
    - path: "backend/src/models/FamilyMember.js"
      provides: "FamilyMember Sequelize model: fields, ENUM, VIRTUAL fullname, cross-field date validation"
      exports: ["FamilyMember", "initFamilyMember"]
    - path: "backend/src/models/FamilyMember.test.js"
      provides: "MEM-01/MEM-02/MEM-03/D-07/D-08/D-09/D-10 unit test coverage"
    - path: "backend/src/models/index.js"
      provides: "FamilyMember registered in the models barrel alongside User"
      contains: "initFamilyMember"
  key_links:
    - from: "backend/src/models/index.js"
      to: "backend/src/models/FamilyMember.js"
      via: "import { initFamilyMember } from './FamilyMember.js'; const FamilyMember = initFamilyMember(sequelize);"
      pattern: "initFamilyMember\\(sequelize\\)"
---

<objective>
Build the `FamilyMember` model's own fields — required identity fields, optional fields, the
derived `fullname` VIRTUAL, and date-validation rules — following `User.js`'s exact init-function
shape, and register it in the models barrel. This plan does NOT touch parent/spouse associations
(motherId/fatherId/memberAId/memberBId do not exist yet — those are added by association wiring in
Plan 12-02). Built test-first: RED (failing test file) before GREEN (model implementation).

Purpose: Establishes the foundational `FamilyMember` schema that every later plan in this phase
(associations, cycle-prevention, married-in delete) builds on top of.
Output: `backend/src/models/FamilyMember.js`, `backend/src/models/FamilyMember.test.js`, extended
`backend/src/models/index.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-family-data-model-foundation/12-CONTEXT.md
@.planning/phases/12-family-data-model-foundation/12-RESEARCH.md
@.planning/phases/12-family-data-model-foundation/12-PATTERNS.md
</context>

<interfaces>
<!-- Existing barrel state the executor extends. Executor should use this directly. -->

From backend/src/models/index.js (current, full file):
```javascript
import { sequelize } from '../config/database.js';
import { initUser } from './User.js';

const User = initUser(sequelize);

export const models = {
  User
};

export async function initializeDatabase() {
  await sequelize.authenticate();
  await sequelize.sync();
}

export { sequelize };
```

From backend/src/models/User.js (init-function shape to mirror exactly — imports, class shape,
`init(...)` fields object, options object with `sequelize`/`modelName`/`tableName`/`hooks`):
```javascript
import { DataTypes, Model } from 'sequelize';

export class User extends Model {}

export function initUser(sequelize) {
  User.init(
    {
      id: { type: DataTypes.INTEGER.UNSIGNED, autoIncrement: true, primaryKey: true },
      email: { type: DataTypes.STRING, allowNull: false, unique: true, validate: { isEmail: true } },
      role: { type: DataTypes.ENUM('ADMIN', 'USER'), allowNull: false, defaultValue: 'USER' },
      resetPasswordToken: { type: DataTypes.STRING, allowNull: true }
      /* ... */
    },
    { sequelize, modelName: 'User', tableName: 'users', hooks: { beforeValidate(user) { /* ... */ } } }
  );
  return User;
}
```

From backend/src/models/User.test.js (Vitest test-file shape — `.build()` for synchronous
field checks, `describe`/`it` naming, `rawAttributes` introspection for schema-shape assertions):
```javascript
import { describe, it, expect } from 'vitest';
import { models } from './index.js';

const { User } = models;

describe('emailVerified / emailVerificationToken / ... columns (VERIFY-01)', () => {
  it('declares emailVerificationToken and emailVerificationExpiresAt as nullable', () => {
    expect(User.rawAttributes.emailVerificationToken.allowNull).toBe(true);
  });
});
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Write FamilyMember.test.js against the not-yet-existing model</name>
  <files>backend/src/models/FamilyMember.test.js</files>
  <read_first>
    backend/src/models/User.test.js, backend/src/models/User.js, backend/src/models/index.js,
    .planning/phases/12-family-data-model-foundation/12-RESEARCH.md (Pattern 1, "Code Examples"
    section), .planning/phases/12-family-data-model-foundation/12-CONTEXT.md (D-07/D-08/D-09/D-10)
  </read_first>
  <behavior>
    - Required-field rejection (MEM-01): `FamilyMember.build({})` then `await instance.validate()`
      rejects (use `.rejects.toThrow()` — no DB round trip needed, `validate()` runs Sequelize
      validators without persisting). Also assert a build with only `firstname`+`lastname` (no
      `gender`) still rejects, and only all three present (`firstname`, `lastname`,
      `gender: 'Male'`) resolves.
    - `gender` ENUM (MEM-01, D-07): `build({ firstname, lastname, gender: 'Alien' })` then
      `validate()` rejects. Each of `'Male'`, `'Female'`, `'Other'` resolves without error.
    - Optional fields nullable (MEM-02, D-08): `build({ firstname, lastname, gender: 'Other' })`
      with `mothersname`/`email`/`birthdate`/`deathdate`/`phone`/`address` all omitted —
      `validate()` resolves. Assert `FamilyMember.rawAttributes.mothersname.allowNull`,
      `.email.allowNull`, `.birthdate.allowNull`, `.deathdate.allowNull`, `.phone.allowNull`,
      `.address.allowNull` are all `true` (rawAttributes introspection, mirrors
      `User.test.js`'s VERIFY-01 pattern).
    - `email` conditional `isEmail` (D-10): build with required fields + `email: 'not-an-email'`
      rejects; build with `email: 'a@b.com'` resolves; build with `email` omitted resolves (Sequelize
      skips validators on `null`/`undefined`).
    - `fullname` VIRTUAL (MEM-03, D-09): `build({ firstname: 'Jane', lastname: 'Doe', gender: 'Female' })`
      then `instance.fullname === 'Jane Doe'` (no `.save()` needed — VIRTUAL getters compute
      synchronously on the built instance). Assert `FamilyMember.rawAttributes.fullname.type`
      is an instance of `DataTypes.VIRTUAL` (import `DataTypes` from `'sequelize'` in the test file
      to compare: `FamilyMember.rawAttributes.fullname.type instanceof DataTypes.VIRTUAL`).
    - Date cross-validation (D-10): required fields + `birthdate: '2000-01-01', deathdate: '1999-01-01'`
      (deathdate before birthdate) rejects. `birthdate: '2000-01-01', deathdate: '2000-01-01'`
      (equal) resolves. `deathdate` one day after `birthdate` resolves. `birthdate` set to
      tomorrow's ISO date (computed at test-run time, e.g. `new Date(Date.now() + 86400000)`
      sliced to `'YYYY-MM-DD'`) rejects. Same for `deathdate` in the future. `birthdate: '1650-01-01'`
      with no `deathdate` resolves (no artificial lower bound — do not add a floor check).
    - `mothersname` is a plain optional string field at this stage (no interaction with `motherId`
      yet — that column does not exist until Plan 12-02; do not reference `motherId` in this file).
  </behavior>
  <action>
    Create backend/src/models/FamilyMember.test.js importing `{ describe, it, expect } from 'vitest'`,
    `{ DataTypes } from 'sequelize'`, and `{ models } from './index.js'`, destructuring
    `const { FamilyMember } = models;`. Write the test cases exactly as itemized above using
    `describe`/`it` blocks tagged with the relevant requirement/decision id in the block name
    (e.g. `describe('required fields (MEM-01)', ...)`, `describe('gender ENUM (MEM-01, D-07)', ...)`,
    `describe('fullname VIRTUAL getter (MEM-03, D-09)', ...)`, `describe('date validation (D-10)', ...)`),
    matching `User.test.js`'s naming convention. Use `instance.validate()` (not `.create()`) for every
    validation-only assertion so this file needs no DB writes and no `resetTables()` call. Run the
    test file and confirm it fails because `models.FamilyMember` is `undefined` (module does not
    exist yet) — this is the expected RED state.
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/models/FamilyMember.test.js 2>&1 | grep -qi "fail\|error" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - backend/src/models/FamilyMember.test.js exists and contains at least one `describe` block for
      each of: required fields, gender ENUM, optional-field nullability, email validation, fullname
      VIRTUAL, date validation.
    - Running `npx vitest run src/models/FamilyMember.test.js` from `backend/` fails (RED) because
      `FamilyMember` is not yet defined on `models`.
  </acceptance_criteria>
  <done>Failing test file committed to source; failure reason is "model does not exist yet", not a typo/syntax error in the test file itself.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement FamilyMember.js and register it in the models barrel</name>
  <files>backend/src/models/FamilyMember.js, backend/src/models/index.js</files>
  <read_first>
    backend/src/models/User.js, backend/src/models/index.js, backend/src/models/FamilyMember.test.js
    (written in Task 1), .planning/phases/12-family-data-model-foundation/12-RESEARCH.md (Pattern 1)
  </read_first>
  <action>
    Create backend/src/models/FamilyMember.js exporting `class FamilyMember extends Model {}` (empty
    body — no instance methods needed) and `function initFamilyMember(sequelize)` that calls
    `FamilyMember.init({...}, {...})` and returns `FamilyMember`. Fields object: `id` —
    `DataTypes.INTEGER.UNSIGNED, autoIncrement: true, primaryKey: true`; `firstname` —
    `DataTypes.STRING, allowNull: false`; `lastname` — `DataTypes.STRING, allowNull: false`;
    `gender` — `DataTypes.ENUM('Male', 'Female', 'Other'), allowNull: false` (no `defaultValue`);
    `mothersname` — `DataTypes.STRING, allowNull: true`; `email` — `DataTypes.STRING, allowNull: true,
    validate: { isEmail: true }`; `birthdate` — `DataTypes.DATEONLY, allowNull: true`; `deathdate` —
    `DataTypes.DATEONLY, allowNull: true`; `phone` — `DataTypes.STRING, allowNull: true`; `address` —
    `DataTypes.STRING, allowNull: true`; `fullname` — `DataTypes.VIRTUAL` with a `get()` returning
    `` `${this.firstname} ${this.lastname}` ``. Do NOT declare `motherId`/`fatherId` fields here —
    those are added by association definitions in Plan 12-02. Options object: `sequelize`,
    `modelName: 'FamilyMember'`, `tableName: 'family_members'`, and a model-level `validate` object
    with two named functions — `deathAfterBirth()` throwing `new Error('deathdate must not be before birthdate.')`
    when `this.birthdate && this.deathdate && this.deathdate < this.birthdate`, and `noFutureDates()`
    throwing `new Error('birthdate must not be in the future.')` / `'deathdate must not be in the future.'`
    when either date string exceeds today's ISO date (`new Date().toISOString().slice(0, 10)`).
    Then edit backend/src/models/index.js: add `import { initFamilyMember } from './FamilyMember.js';`
    below the existing `initUser` import, add `const FamilyMember = initFamilyMember(sequelize);` below
    the `User` init line, and change `export const models = { User };` to
    `export const models = { User, FamilyMember };`. Do NOT modify `initializeDatabase()`'s body — it
    must remain plain `sequelize.sync()` with no arguments (D-13).
  </action>
  <verify>
    <automated>cd backend && npx vitest run src/models/FamilyMember.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/models/FamilyMember.test.js` exits 0, all test cases from Task 1 pass.
    - `cd backend && npm test` (full backend suite) exits 0 — no regression against `User` model or
      existing resolver/util tests.
    - `backend/src/models/FamilyMember.js` contains `gender: { type: DataTypes.ENUM('Male', 'Female', 'Other')`.
    - `backend/src/models/index.js` contains `initFamilyMember(sequelize)` and exports `FamilyMember`
      inside the `models` object.
    - `backend/src/models/index.js`'s `initializeDatabase` function body is unchanged (`sequelize.sync()`
      with zero arguments — `grep -c 'force' backend/src/models/index.js` returns `0`).
  </acceptance_criteria>
  <done>FamilyMember model implemented, registered in the barrel, FamilyMember.test.js green, full backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Node.js callers → Sequelize validation pipeline | Any code (tests today, future resolvers in Phase 14) constructing a `FamilyMember` crosses into the model's validation rules here — this is the only input boundary that exists at the model layer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-12-01 | Tampering | `FamilyMember.init()` field validators | mitigate | Required fields (`firstname`/`lastname`/`gender`) enforced via `allowNull: false`; `gender` constrained to `ENUM('Male','Female','Other')` — no code path in this phase calls `create()`/`update()` with `validate: false`, which would bypass this. |
| T-12-02 | Tampering | Model-level `validate` block (`deathAfterBirth`, `noFutureDates`) | mitigate | Cross-field date validation runs on every `create()`/`update()`/`validate()` call by default in Sequelize; proven by the date-validation tests in Task 1. |
| T-12-03 | Information Disclosure | `email` field | accept | No auth/PII exposure risk at this layer — no resolver or HTTP surface exists yet in this phase; `isEmail` format validation mirrors the existing `User` model's posture. |
</threat_model>

<verification>
- `cd backend && npx vitest run src/models/FamilyMember.test.js` exits 0.
- `cd backend && npm test` (full backend suite, includes existing `globalSetup.js` fresh-DB sync) exits 0.
</verification>

<success_criteria>
- `FamilyMember` model exists with all required/optional fields, derived `fullname`, and full date
  validation, matching D-07/D-08/D-09/D-10 exactly.
- Registered in `backend/src/models/index.js`'s `models` object following the existing `User`
  barrel convention.
- Built test-first: FamilyMember.test.js existed and failed before FamilyMember.js was implemented.
</success_criteria>

<output>
Create `.planning/phases/12-family-data-model-foundation/12-01-SUMMARY.md` when done
</output>
