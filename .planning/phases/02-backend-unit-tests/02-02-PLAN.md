---
phase: 02-backend-unit-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/User.test.js
autonomous: true
requirements: [BE-02]

must_haves:
  truths:
    - "validatePassword accepts the correct password and rejects an incorrect one against a known bcrypt hash (D-04)"
    - "The beforeCreate hook hashes passwordHash so the stored value is never the plaintext, proven by invoking the real hook via User.runHooks without touching the DB (D-05)"
  artifacts:
    - path: "backend/src/models/User.test.js"
      provides: "Unit coverage of validatePassword and the beforeCreate password-hashing hook"
      min_lines: 25
      contains: "runHooks"
  key_links:
    - from: "backend/src/models/User.test.js"
      to: "backend/src/models/index.js"
      via: "import { models } from './index.js' (co-located, same directory)"
      pattern: "from '\\./index\\.js'"
---

<objective>
Write `backend/src/models/User.test.js`: unit tests for `validatePassword` (BE-02) and the `beforeCreate` password-hashing hook (BE-02, D-05), both exercised without opening a database connection.

Purpose: Lock in the existing, security-critical guarantee that passwords are never stored in plaintext and that `validatePassword` correctly accepts/rejects credentials, so a future change to `User.js` that weakens hashing or validation fails a fast unit test rather than surfacing as a live vulnerability.
Output: `backend/src/models/User.test.js` — a single new spec file, no application source modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-unit-tests/02-CONTEXT.md
@.planning/phases/02-backend-unit-tests/02-RESEARCH.md
@.planning/phases/02-backend-unit-tests/02-PATTERNS.md
@.planning/phases/02-backend-unit-tests/02-VALIDATION.md

<interfaces>
<!-- backend/src/models/User.js — code under test, unmodified. Relevant excerpt. -->
```javascript
export class User extends Model {
  async validatePassword(password) {
    return bcrypt.compare(password, this.passwordHash);
  }
}
// User.init(..., { hooks: {
//   async beforeCreate(user) { if (user.passwordHash) user.passwordHash = await bcrypt.hash(user.passwordHash, 12); },
//   async beforeUpdate(user) { if (user.changed('passwordHash')) user.passwordHash = await bcrypt.hash(user.passwordHash, 12); }
// }})
```

<!-- backend/src/models/index.js — full file, the import source for `models`. -->
```javascript
import { sequelize } from '../config/database.js';
import { initUser } from './User.js';

const User = initUser(sequelize);

export const models = { User };

export async function initializeDatabase() {
  await sequelize.authenticate();
  await sequelize.sync();
}

export { sequelize };
```

<!-- backend/src/models/database.test.js — co-location/import-style analog only. This is a DB-touching integration spec; copy ONLY its import/co-location convention, NOT its sequelize.authenticate()/count() pattern (D-08 requires this new file stay DB-free). -->
```javascript
import { describe, it, expect } from 'vitest';
import { sequelize, models } from './index.js';
```

**Critical fact (verified empirically in 02-RESEARCH.md Pattern 2 / Pitfall 1):** `User.build({...})` does NOT fire `beforeCreate` — only `save()`, `create()`, `bulkCreate()`, `upsert()` do. To exercise the real hashing hook without a DB connection, call `await User.runHooks('beforeCreate', builtInstance)` directly — this is the exact same internal call `save()`/`create()` make (`node_modules/sequelize/lib/model.js:2436`). Do NOT reach into `User.options.hooks.beforeCreate[0](...)` (works but relies on undocumented internal array shape).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: validatePassword unit tests (BE-02, D-04)</name>
  <files>backend/src/models/User.test.js</files>
  <read_first>
    backend/src/models/User.js
    backend/src/models/index.js
    backend/src/models/database.test.js
    .planning/phases/02-backend-unit-tests/02-RESEARCH.md (Don't Hand-Roll table, lines 181-189)
    .planning/phases/02-backend-unit-tests/02-CONTEXT.md (D-04, D-08)
  </read_first>
  <behavior>
    - Test: a `User.build({ passwordHash: knownBcryptHash })` instance (no save/create) resolves `validatePassword('correctPlaintext')` to `true` when `knownBcryptHash` is a real `bcrypt.hash` of that plaintext.
    - Test: the same instance resolves `validatePassword('wrongPlaintext')` to `false`.
  </behavior>
  <action>
    Create backend/src/models/User.test.js. Import describe/it/expect from vitest, bcrypt from 'bcryptjs', and { models } from './index.js'; destructure const { User } = models. Add a describe('validatePassword') block: compute `const known = await bcrypt.hash('Password123!', 12)`, build `const user = User.build({ passwordHash: known })` (do not call save()/create() — no DB touch per D-08), then assert `await user.validatePassword('Password123!')` is `true` and `await user.validatePassword('WrongPassword')` is `false`. Use a real bcrypt hash as the fixture, not a fake string (per Don't Hand-Roll table — validatePassword calls real bcrypt.compare internally).
  </action>
  <acceptance_criteria>
    - `cd backend && npx vitest run src/models/User.test.js -t validatePassword` exits 0 with 2 passing assertions.
    - `grep -c 'sequelize' backend/src/models/User.test.js` returns 0 (no direct sequelize import/usage — proves no DB connection is opened by this file).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx vitest run src/models/User.test.js -t validatePassword</automated>
  </verify>
  <done>backend/src/models/User.test.js exists with a passing validatePassword describe block covering correct and incorrect password cases; no DB connection is opened by this file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: beforeCreate password-hashing hook unit test (BE-02, D-05)</name>
  <files>backend/src/models/User.test.js</files>
  <read_first>
    backend/src/models/User.js
    .planning/phases/02-backend-unit-tests/02-RESEARCH.md (Pattern 2, lines 134-159; Pitfall 1, lines 193-197; Anti-Patterns, lines 176-179)
    .planning/phases/02-backend-unit-tests/02-CONTEXT.md (D-05, D-08)
  </read_first>
  <behavior>
    - Test: immediately after `User.build({ passwordHash: 'Password123!', ... })`, `user.passwordHash` is still exactly `'Password123!'` (proves `build()` alone does not hash).
    - Test: after `await User.runHooks('beforeCreate', user)`, `user.passwordHash` is no longer `'Password123!'`, and `await bcrypt.compare('Password123!', user.passwordHash)` is `true`.
    - Test: `user.isNewRecord` is still `true` after the hook runs, proving nothing was persisted to a database.
  </behavior>
  <action>
    Append to backend/src/models/User.test.js a describe('beforeCreate hashing hook') block. Build an unsaved instance: `const user = User.build({ name: 'Test', email: 'test@example.com', passwordHash: 'Password123!', role: 'USER' })`. Assert `user.passwordHash === 'Password123!'` before invoking the hook (Pitfall 1 proof). Call `await User.runHooks('beforeCreate', user)` — the sanctioned internal entry point `save()`/`create()` use; do not use `User.options.hooks.beforeCreate[0](...)`. Assert `user.passwordHash !== 'Password123!'`, assert `await bcrypt.compare('Password123!', user.passwordHash) === true`, and assert `user.isNewRecord === true`.
  </action>
  <acceptance_criteria>
    - `cd backend && npx vitest run src/models/User.test.js -t beforeCreate` exits 0 with all 4 assertions passing (pre-hook plaintext check, post-hook non-plaintext, bcrypt.compare true, isNewRecord true).
    - `grep -c 'runHooks' backend/src/models/User.test.js` returns >= 1.
    - `grep -c 'User.options.hooks' backend/src/models/User.test.js` returns 0.
    - `cd backend && npx vitest run src/models/User.test.js` exits 0 (full file green, both tasks combined).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && npx vitest run src/models/User.test.js</automated>
  </verify>
  <done>backend/src/models/User.test.js is complete: validatePassword and the beforeCreate hashing hook are both unit-tested via User.runHooks, with no DB connection opened by this file; the full file runs green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Registration input -> `beforeCreate` hashing hook | Plaintext password crosses this boundary once, at user creation; this phase adds regression tests over the existing hashing behavior at that boundary — it does not move or widen the boundary, and no live registration path is touched (this is an in-memory `runHooks` invocation, not a resolver call). |
| Test process (in-process function calls) | The new spec file introduces no trust boundary — it calls `User` model methods/hooks directly in-process on an unsaved (`isNewRecord: true`) instance. No network, no persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | `beforeCreate` hook (`backend/src/models/User.js`) | mitigate | Regression test asserts `passwordHash !== plaintext` after `User.runHooks('beforeCreate', ...)` and that the result verifies via `bcrypt.compare` (Task 2) — locks in existing hashing so a future edit that accidentally stores plaintext (or removes the hook) fails a fast unit test instead of shipping a live vulnerability. |
| T-02-06 | Spoofing | `validatePassword` (`backend/src/models/User.js`) | mitigate | Regression test asserts the correct password is accepted and an incorrect password is rejected against a known bcrypt hash (Task 1) — locks in existing credential-verification behavior. |
| T-02-SC | Tampering (supply chain) | npm installs | accept | No new packages are installed this phase (RESEARCH.md Package Legitimacy Audit: not applicable) — `bcryptjs` and `sequelize` (via `models/index.js` import only, no connection opened) are pre-existing Phase 1 dependencies. |

No application runtime code is modified in this phase; the security value delivered is regression coverage over the already-implemented password-hashing control.
</threat_model>

<verification>
Run `cd backend && npx vitest run src/models/User.test.js` and confirm all tests pass (exit code 0). Confirm the file does not import or call `sequelize` directly (`grep -c 'sequelize'` returns 0). Confirm `backend/src/models/User.js` is unmodified (git diff on that file is empty).
</verification>

<success_criteria>
- `backend/src/models/User.test.js` exists and is committed.
- BE-02 satisfied: passwords are hashed on create (never plaintext) and `validatePassword` correctly accepts/rejects.
- D-05 satisfied: the hook is exercised via `User.runHooks`, without a live DB connection.
- `backend/src/models/User.js` is unmodified.
</success_criteria>

<output>
Create `.planning/phases/02-backend-unit-tests/02-02-SUMMARY.md` when done
</output>
