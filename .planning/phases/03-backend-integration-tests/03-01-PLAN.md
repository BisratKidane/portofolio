---
phase: 03-backend-integration-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/test/helpers.js
  - backend/src/resolvers/register.test.js
autonomous: true
requirements: [BE-04]

must_haves:
  truths:
    - "The graphql() test helper drives the GraphQL layer in-process without importing server.js, injecting an authed/anon context and reading singleResult (D-01, D-02, D-03)"
    - "Registering into an empty users table (reset via beforeEach) assigns the ADMIN role (D-04, D-05)"
    - "Registering after a seeded user assigns the USER role (D-05)"
    - "Registering a duplicate email is rejected with the exact API-contract message (D-11)"
    - "Registering a malformed email is rejected via Sequelize's isEmail validation, with a DB spot-check that the password is stored hashed not plaintext (D-10, D-11)"
  artifacts:
    - path: "backend/test/helpers.js"
      provides: "graphql() in-process Apollo executeOperation wrapper, plus existing resetTables()/createTestUser()"
      contains: "export async function graphql"
    - path: "backend/src/resolvers/register.test.js"
      provides: "register mutation integration tests (ADMIN/USER branches, duplicate email, invalid email)"
      min_lines: 40
  key_links:
    - from: "backend/src/resolvers/register.test.js"
      to: "backend/test/helpers.js"
      via: "import { graphql, resetTables, createTestUser } from '../../test/helpers.js'"
      pattern: "from '\\.\\./\\.\\./test/helpers\\.js'"
    - from: "backend/test/helpers.js"
      to: "backend/src/resolvers/index.js"
      via: "new ApolloServer({ typeDefs, resolvers })"
      pattern: "new ApolloServer"
---

<objective>
Add a shared, side-effect-free Apollo in-process test server helper (`graphql()`) to `backend/test/helpers.js`, then write the first backend integration spec — `register` mutation coverage (BE-04) — proving the ADMIN/USER first-user role matrix, duplicate-email rejection, and invalid-email rejection, per CONTEXT.md D-01/D-02/D-03/D-04/D-05/D-10/D-11.

Purpose: Establish the reusable test-entry-point contract (`graphql(query, variables, user)`) that every subsequent integration spec in this phase (login, dashboard/me, requestPasswordReset) depends on, and deliver the first of four required GraphQL flow specs.
Output: Extended `backend/test/helpers.js` (new `graphql()` export) and new `backend/src/resolvers/register.test.js` with 4 passing tests.
</objective>

<decision_traceability>
Locked CONTEXT.md decisions implemented by this plan:
- D-01: In-process `new ApolloServer({ typeDefs, resolvers })` + `executeOperation` seam in the `graphql()` helper; never imports `server.js`.
- D-02: Authed context injects a `createTestUser()` instance as `context.user`; unauth passes `user: null`.
- D-03: Result read from `response.body.singleResult.{data,errors}`; errors asserted there, not via try/catch.
- D-04: `beforeEach(resetTables)` resets the users table before every test.
- D-05: Register test covers both role branches — ADMIN (empty table) and USER (seeded first).
- D-10: Assertions go through the GraphQL response AND spot-check DB side-effects (`passwordHash !== plaintext`, verifiable JWT claims).
- D-11: Negative case asserts the exact error string `'A user with this email already exists.'` on `.message` only.
</decision_traceability>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backend-integration-tests/03-CONTEXT.md
@.planning/phases/03-backend-integration-tests/03-RESEARCH.md
@.planning/phases/03-backend-integration-tests/03-PATTERNS.md

<interfaces>
<!-- Current full content of backend/test/helpers.js — extend in place, do not replace. -->
```javascript
import { models } from '../src/models/index.js';

export async function resetTables() {
  await models.User.destroy({ where: {}, truncate: true });
}

export async function createTestUser(overrides = {}) {
  return models.User.create({
    name: 'Test User',
    email: `test-${Date.now()}@example.com`,
    passwordHash: 'Password123!',
    role: 'USER',
    ...overrides
  });
}
```

<!-- Barrels the new graphql() helper must import from (NOT backend/src/server.js). -->
```javascript
// backend/src/schemas/index.js
export const typeDefs = [userTypeDefs];

// backend/src/resolvers/index.js
export const resolvers = [userResolvers];
```

<!-- backend/src/resolvers/user.resolver.js — register mutation (lines 25-38), exact strings and role logic. -->
```javascript
register: async (_parent, { name, email, password }, { models }) => {
  const existingUser = await models.User.findOne({ where: { email: email.toLowerCase().trim() } });
  if (existingUser) throw new Error('A user with this email already exists.');

  const userCount = await models.User.count();
  const user = await models.User.create({
    name,
    email,
    passwordHash: password,
    role: userCount === 0 ? 'ADMIN' : 'USER'
  });

  return { token: signToken(user), user };
}
```

<!-- backend/src/utils/auth.js — signToken claim shape used for JWT assertions. -->
```javascript
export function signToken(user) {
  return jwt.sign({ sub: user.id, role: user.role }, env.jwtSecret, { expiresIn: env.jwtExpiresIn });
}
```

<!-- backend/src/models/User.js — isEmail validator (the "invalid input" rejection path, Pitfall 4) and passwordHash field. -->
```javascript
email: {
  type: DataTypes.STRING,
  allowNull: false,
  unique: true,
  validate: { isEmail: true }
},
passwordHash: {
  type: DataTypes.STRING,
  allowNull: false
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add graphql() in-process Apollo test-server helper to backend/test/helpers.js</name>
  <files>backend/test/helpers.js</files>
  <read_first>
    backend/test/helpers.js (full current content, shown in interfaces above)
    backend/src/schemas/index.js
    backend/src/resolvers/index.js
    backend/src/models/index.js
    .planning/phases/03-backend-integration-tests/03-RESEARCH.md (Pattern 1, "Shared in-process Apollo test server + thin graphql() wrapper")
    .planning/phases/03-backend-integration-tests/03-PATTERNS.md ("In-process Apollo test server construction (D-01)" under Shared Patterns)
  </read_first>
  <action>
    Extend backend/test/helpers.js (do not create a new file) by adding: `import { ApolloServer } from '@apollo/server';`, `import { typeDefs } from '../src/schemas/index.js';`, `import { resolvers } from '../src/resolvers/index.js';`. Construct `const server = new ApolloServer({ typeDefs, resolvers });` once at module scope — NO `.start()` call (D-01). Add and export `export async function graphql(query, variables, user = null)` that calls `await server.executeOperation({ query, variables }, { contextValue: { models, user } })` and returns `response.body.singleResult` (an object shaped `{ data, errors }`, per D-03 — Apollo's `body.kind` is always `'single'` for this schema). Keep the existing `resetTables()` and `createTestUser(overrides)` exports byte-for-byte unchanged; only add the new import lines, the module-scope `server` construction, and the new `graphql` export. Do NOT import `backend/src/server.js` anywhere (it has top-level `apollo.start()`/`initializeDatabase()`/`app.listen` side effects — D-01 anti-pattern).
  </action>
  <acceptance_criteria>
    - `grep -c '^export' backend/test/helpers.js | grep -v '^#'` reports 3 (graphql, resetTables, createTestUser)
    - `grep -c "server\.js" backend/test/helpers.js` reports 0 (no import of backend/src/server.js)
    - `grep -c "new ApolloServer" backend/test/helpers.js` reports 1
    - `node --check backend/test/helpers.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --check backend/test/helpers.js</automated>
  </verify>
  <done>backend/test/helpers.js exports graphql, resetTables, and createTestUser; constructs one module-scope ApolloServer from the typeDefs/resolvers barrels; does not import server.js.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write register mutation integration tests (BE-04)</name>
  <files>backend/src/resolvers/register.test.js</files>
  <read_first>
    backend/src/resolvers/user.resolver.js (register, lines 25-38)
    backend/src/utils/auth.js (signToken)
    backend/src/models/User.js (isEmail validator, passwordHash field)
    backend/test/helpers.js (after Task 1 — graphql/resetTables/createTestUser signatures)
    backend/src/utils/auth.test.js (describe/it nesting convention, jwt.verify pattern)
    .planning/phases/03-backend-integration-tests/03-RESEARCH.md (Code Examples: register block)
    .planning/phases/03-backend-integration-tests/03-CONTEXT.md (D-04, D-05, D-10, D-11)
  </read_first>
  <behavior>
    - Test 1: register into an empty (post-beforeEach) users table assigns role ADMIN; returned token decodes via jwt.verify with payload.role === 'ADMIN' and payload.sub === Number(user.id); DB row's passwordHash !== plaintext and validatePassword(plaintext) resolves true.
    - Test 2: register after seeding one prior user via createTestUser() assigns role USER.
    - Test 3: register with an email already used by an existing user returns errors[0].message === 'A user with this email already exists.' and data is null.
    - Test 4: register with a malformed email ('not-an-email') returns errors[0].message === 'Validation error: Validation isEmail on email failed' (Sequelize model-level rejection, not a GraphQL schema-level one — per RESEARCH.md Pitfall 4).
  </behavior>
  <action>
    Create backend/src/resolvers/register.test.js. Import `describe, it, expect, beforeEach` from 'vitest'; `jwt` from 'jsonwebtoken'; `env` from '../config/env.js'; `models` from '../models/index.js'; `graphql, resetTables, createTestUser` from '../../test/helpers.js'. Call `beforeEach(resetTables)` at the top level (D-04). Define `REGISTER_MUTATION` requesting `token` and `user { id name email role }`. Nest all four cases (see behavior block) inside `describe('register', () => { ... })`, one `it` per case, matching the auth.test.js describe/it nesting style. Add a one-line comment above the invalid-email case noting it exercises Sequelize's isEmail validator (resolver/model-level), not a GraphQL schema-level rejection, since CONTEXT.md left the exact "invalid input" case to planner discretion.
  </action>
  <acceptance_criteria>
    <automated>npm test --workspace backend -- --run src/resolvers/register.test.js</automated>
    - Command exits 0 with all 4 tests passing
    - Spec asserts `data.register.user.role === 'ADMIN'` for the empty-table case and `'USER'` for the seeded case
    - Spec asserts `errors[0].message === 'A user with this email already exists.'` for the duplicate-email case
    - Spec asserts `errors[0].message === 'Validation error: Validation isEmail on email failed'` for the malformed-email case
    - Spec asserts `row.passwordHash !== 'Password123!'` after a successful register (plaintext never persisted, D-10)
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- --run src/resolvers/register.test.js</automated>
  </verify>
  <done>register.test.js exists with 4 passing tests covering the ADMIN branch, the USER branch, duplicate-email rejection, and invalid-email rejection; full backend suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assertions ↔ resolver business logic | The integration suite is the mechanism that catches regressions in security-relevant resolver behavior (role assignment, input validation) — it does not add new runtime attack surface itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | `register` resolver's `userCount === 0` first-user-ADMIN role assignment (`user.resolver.js:29-34`) | mitigate | Test explicitly pins the ADMIN branch (empty table) and the USER branch (seeded table) with `beforeEach(resetTables)` isolation (D-04/D-05), so a future regression in the role-matrix logic fails the suite loudly |
| T-03-02 | Tampering | `register` resolver's email input handling (Sequelize `isEmail` model validator) | mitigate | Test asserts a malformed email is rejected via the model-level `isEmail` validation, guarding against a future change that silently accepts unvalidated email input |
| T-03-SC | Tampering | npm installs | accept | No new packages installed this phase — RESEARCH.md's Package Legitimacy Audit is not applicable (zero new dependencies; `@apollo/server`, `jsonwebtoken`, `vitest` are pre-existing, already-installed dependencies) |
</threat_model>

<verification>
Run `npm test --workspace backend -- --run` after this plan completes — the full backend suite (existing Phase 1/2 specs + the new register.test.js) must be green with no failures.
</verification>

<success_criteria>
- `backend/test/helpers.js` exports a working `graphql()` in-process Apollo wrapper alongside the existing `resetTables`/`createTestUser`
- `backend/src/resolvers/register.test.js` exists with 4 passing tests satisfying BE-04
- No application runtime source file was modified (only test files)
</success_criteria>

<output>
Create `.planning/phases/03-backend-integration-tests/03-01-SUMMARY.md` when done
</output>
