---
phase: 15-sibling-dedup-guard-manage-self-service-ui
plan: 06
type: execute
wave: 3
depends_on: ["15-04", "15-05"]
files_modified:
  - frontend/src/pages/ManagePage.jsx
  - frontend/src/pages/ManagePage.test.jsx
autonomous: true
requirements: [MNG-03]

must_haves:
  truths:
    - "An admin visiting /manage sees a searchable table of all members and can select one to focus into the same grouped panel the member view uses, with admin powers (Edit bypassing D-06, Remove) active."
    - "An admin can remove a member via a two-step confirm dialog (UI-SPEC copy), which calls deleteMember."
    - "An admin can link an unlinked user account to a family member node from the same /manage page (re-homed from /admin/link-members)."
  artifacts:
    - path: "frontend/src/pages/ManagePage.jsx"
      provides: "The admin branch: AdminMemberTable wiring, table-select-to-focus, delete confirm, re-homed account-linking"
  key_links:
    - from: "frontend/src/pages/ManagePage.jsx admin branch"
      to: "backend familyMembers/familyMember(id)/deleteMember/linkUserToMember"
      via: "graphqlRequest"
      pattern: "DELETE_MEMBER_MUTATION|LINK_USER_TO_MEMBER_MUTATION"
---

<objective>
Complete `/manage` for admins: wire `AdminMemberTable` (15-04) into `ManagePage`'s admin branch so
selecting a row focuses that member into the SAME `RelationshipGroupedPanel` the member view uses
(D-03 — one grouped component, two entry points) with admin powers active, add the UI-SPEC's
destructive confirm dialog for `deleteMember`, and re-home `AdminLinkMembers.jsx`'s account-linking
UI (unlinked-users list + `linkUserToMember`) verbatim into this same page (MNG-03).

Purpose: this is the final wiring plan for the phase — it depends on both `AdminMemberTable`
(15-04) and `ManagePage`'s member branch + routing (15-05, same file, must land first). No new
backend capability is introduced; every mutation this plan calls (`familyMembers`, `familyMember`,
`deleteMember`, `linkUserToMember`) already exists and is already `requireAdmin`/
`requireFamilyAccess`-gated server-side.
Output: `ManagePage.jsx`'s admin branch, completing MNG-03; full frontend + backend suites green as
the phase-level gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-sibling-dedup-guard-manage-self-service-ui/15-CONTEXT.md
@.planning/phases/15-sibling-dedup-guard-manage-self-service-ui/15-RESEARCH.md
@.planning/phases/15-sibling-dedup-guard-manage-self-service-ui/15-UI-SPEC.md
</context>

<interfaces>
From 15-04 (already merged):
```
AdminMemberTable({ members, onSelect })
```
From 15-05 (already merged, same file this plan extends):
```
ManagePage.jsx — member branch, groupByRelation helper, MY_EDITABLE_MEMBERS_QUERY, RelationshipGroupedPanel/AddRelativeDialog/EditMemberDialog wiring, /manage route
```
`familyMembers` (admin table query, extend the field selection):
```graphql
query FamilyMembersTable {
  familyMembers { id firstname lastname fullname gender linkedUser { id name email } }
}
```
`familyMember(id)` (row-select focus query — same nested shape as `myEditableMembers`, reusing the
SAME `groupByRelation` helper 15-05 already wrote):
```graphql
query FamilyMemberFocus($id: ID!) {
  familyMember(id: $id) {
    id firstname lastname fullname gender birthdate deathdate phone email address
    mother { id } father { id }
    spouses { id fullname } children { id fullname } siblings { id fullname }
    linkedUser { id name email }
  }
}
```
`deleteMember` / `linkUserToMember` (both already shipped, unchanged):
```graphql
mutation DeleteMember($id: ID!) { deleteMember(id: $id) }
mutation LinkUserToMember($userId: ID!, $memberId: ID, $newMember: NewFamilyMemberInput) {
  linkUserToMember(userId: $userId, memberId: $memberId, newMember: $newMember) { id familyMemberId }
}
```
`UNLINKED_USERS_QUERY` (lifted verbatim from `AdminLinkMembers.jsx:18-22`):
```graphql
query UnlinkedUsers { unlinkedUsers { id name email createdAt } }
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Admin branch — table, focus-into-panel, delete confirm</name>
  <files>frontend/src/pages/ManagePage.jsx, frontend/src/pages/ManagePage.test.jsx</files>
  <read_first>
    frontend/src/pages/ManagePage.jsx (15-05's final state, to extend with an admin branch)
    frontend/src/components/manage/AdminMemberTable.jsx (15-04, final prop contract)
    frontend/src/components/manage/RelationshipGroupedPanel.jsx (15-02, final prop contract —
    `isAdmin`/`onDelete` props this task now actually wires)
    .planning/phases/15-sibling-dedup-guard-manage-self-service-ui/15-RESEARCH.md ("Admin table +
    focus query shape", Open Question 1 — recommends the same deep-relation shape as
    myEditableMembers for the focus query)
    .planning/phases/15-sibling-dedup-guard-manage-self-service-ui/15-UI-SPEC.md (admin subtitle
    copy; destructive confirm dialog exact copy: title, body, Confirm/Cancel labels)
  </read_first>
  <behavior>
    - An admin-role user (`user.role === 'ADMIN'`) visiting the page fetches `FAMILY_MEMBERS_QUERY`
      on mount and renders `<AdminMemberTable members={...} onSelect={handleFocus} />` under the
      admin subtitle "Search the whole tree and manage any member." (exact UI-SPEC copy).
    - Selecting a row fetches `FAMILY_MEMBER_FOCUS_QUERY(id)`, groups it via the SAME
      `groupByRelation` helper 15-05 wrote (the shape matches `myEditableMembers`'s), and renders
      `<RelationshipGroupedPanel scope={focusedScope} isAdmin={true} actingUserId={user.id}
      onAddRelative={...} onEdit={...} onDelete={handleDeleteRequest} />` — every card's Edit
      button is now active (D-06 lock bypassed for admins, already implemented by `MemberCard` in
      15-02) and a Remove button is present.
    - Clicking "Remove" opens a confirm `Dialog` with EXACT UI-SPEC copy — title "Remove member?",
      body "Remove {fullname} from the family tree? Blood relatives are preserved. This can't be
      undone.", Confirm button "Remove" (`color="error"`), Cancel neutral.
    - Confirming calls `graphqlRequest(DELETE_MEMBER_MUTATION, { id })`; on success, re-fetches
      `FAMILY_MEMBERS_QUERY` and clears the focused member.
    - The add-relative flow (`AddRelativeDialog`) works identically for the admin-focused panel —
      same component, `targetId` = the focused member's id.
  </behavior>
  <action>
    In `ManagePage.jsx`, add the admin branch: when `user.role === 'ADMIN'`, fetch
    `FAMILY_MEMBERS_QUERY` (exact field list from the interfaces block) on mount into a
    `members`/`setMembers` state; render `<AdminMemberTable members={members}
    onSelect={handleFocus} />`. `handleFocus(member)` fetches `FAMILY_MEMBER_FOCUS_QUERY({ id:
    member.id })`, applies `groupByRelation(rows=[focusedRow, ...focusedRow's nested relation
    objects flattened], focusedRow)` — reuse the EXACT same helper 15-05 defined, do not write a
    second grouping function — and sets `focusedScope`. Render
    `<RelationshipGroupedPanel scope={focusedScope} isAdmin={true} actingUserId={user.id}
    onAddRelative={...} onEdit={(member) => setEditTarget(member)}
    onDelete={(member) => setDeleteTarget(member)} />` only when `focusedScope` is set. Add a
    `Dialog` (inline in this file — no separate component needed for a single confirm/cancel) for
    delete confirmation using the EXACT UI-SPEC copy above; on confirm, call
    `graphqlRequest(DELETE_MEMBER_MUTATION, { id: deleteTarget.id })`, then refetch
    `FAMILY_MEMBERS_QUERY` and clear both `deleteTarget` and `focusedScope`. Reuse the SAME
    `AddRelativeDialog`/`EditMemberDialog` instances already rendered for the member branch,
    parameterized by whichever `targetId`/`member` is currently active (member's own `self.id` in
    the member branch, `focusedScope.self.id` in the admin branch).
  </action>
  <acceptance_criteria>
    - An admin-role render (mocked `graphqlRequest` + `useAuth`) shows `AdminMemberTable`; selecting
      a row renders `RelationshipGroupedPanel` populated from the `familyMember(id)` mock response.
    - A component test asserts every card in the admin-focused panel shows an active "Edit" button
      (D-06 bypass) regardless of `linkedUser`.
    - Clicking "Remove" on a card opens a dialog containing the exact text "Remove member?" and
      "Blood relatives are preserved. This can't be undone."; confirming calls `graphqlRequest` with
      the `deleteMember` mutation and the correct `id`.
    - `npm test --workspace frontend -- src/pages/ManagePage.test.jsx` (admin-branch cases) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace frontend -- src/pages/ManagePage.test.jsx</automated>
  </verify>
  <done>Admin table→focus→panel flow and destructive delete confirmation are wired and tested green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-home account-linking (MNG-03) + phase-level full-suite gate</name>
  <files>frontend/src/pages/ManagePage.jsx, frontend/src/pages/ManagePage.test.jsx</files>
  <read_first>
    frontend/src/pages/AdminLinkMembers.jsx (git history / prior state before 15-05 replaced it
    with a redirect — the `UnlinkedUserRow` component, `UNLINKED_USERS_QUERY`,
    `LINK_USER_TO_MEMBER_MUTATION`, `EMPTY_FORM`, mode-toggle/submit-handler code to port verbatim;
    read this plan's own 15-04/15-05 SUMMARYs if the original file's pre-redirect content is no
    longer directly readable, or use `git show` against the commit that introduced it)
    .planning/phases/15-sibling-dedup-guard-manage-self-service-ui/15-UI-SPEC.md ("Create & link"
    copy — reserved for THIS path specifically, distinct from AddRelativeDialog's "Add member")
    .planning/phases/15-sibling-dedup-guard-manage-self-service-ui/15-CONTEXT.md (D-03's "Link
    account" affordance description — re-homed, not redesigned)
  </read_first>
  <behavior>
    - The admin branch additionally fetches `UNLINKED_USERS_QUERY` (alongside `FAMILY_MEMBERS_QUERY`
      in the same `Promise.all` on mount) and renders a "Link accounts" section listing unlinked
      users, empty state "No accounts are waiting to be linked." (verbatim).
    - Each unlinked-user row offers the SAME pick-existing-member Autocomplete / create-new-member
      TextField toggle as the original `AdminLinkMembers.jsx`, submitting `linkUserToMember`, with
      the button label "Create & link" preserved on THIS path specifically (distinct from
      `AddRelativeDialog`'s "Add member").
    - A successfully linked user disappears from the unlinked-users list, matching the original
      page's UX exactly.
  </behavior>
  <action>
    Port `AdminLinkMembers.jsx`'s (pre-redirect) `UnlinkedUserRow` component and its
    `UNLINKED_USERS_QUERY`/`LINK_USER_TO_MEMBER_MUTATION`/`EMPTY_FORM` constants into
    `ManagePage.jsx` verbatim (same file, admin-branch-only — this UI is never rendered for a
    non-admin). Fetch `UNLINKED_USERS_QUERY` in the same `Promise.all` as `FAMILY_MEMBERS_QUERY` on
    mount for the admin branch. Render the "Link accounts" section (heading + the ported row
    component list, or the empty-state text) alongside `AdminMemberTable`. Re-add the meaningful
    assertions originally in `AdminLinkMembers.test.jsx` (link-existing flow, create-and-link flow,
    per-row error, empty state) to `ManagePage.test.jsx`, adapted to render inside the admin
    branch. Run both full suites as the final phase-level gate.
  </action>
  <acceptance_criteria>
    - `ManagePage.jsx` defines a `LINK_USER_TO_MEMBER_MUTATION` constant and renders the exact text
      "Create & link" (not "Add member") on this specific path.
    - A component test proves linking an existing member removes that user's row from the
      unlinked-users list, mirroring the original `AdminLinkMembers.test.jsx` assertions.
    - `npm test --workspace frontend` (full frontend suite) exits 0.
    - `npm test --workspace backend` (full backend suite, phase-level sanity re-check) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace frontend && npm test --workspace backend</automated>
  </verify>
  <done>Account-linking is fully re-homed into /manage (MNG-03 complete); both full suites green — this is the phase's final gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `user.role === 'ADMIN'` client-side branch → rendering `AdminMemberTable`/unlinked-users/delete affordances | This branch decision is UX-only; every query/mutation it drives (`familyMembers`, `unlinkedUsers`, `deleteMember`, `linkUserToMember`) is independently `requireAdmin`-gated server-side, unchanged this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-12 | Elevation of Privilege | Admin branch rendered for a non-admin due to a client bug | mitigate | `isAdmin` branch strictly gated on `user.role === 'ADMIN'` from `useAuth()`; a client bug here would only affect what renders, not what's permitted — `requireAdmin` on `familyMembers`/`unlinkedUsers`/`deleteMember`/`linkUserToMember` (unchanged) is the actual enforcement (defense in depth, Architectural Responsibility Map) |
| T-15-13 | Tampering (accidental destructive action) | `deleteMember` confirmation flow | mitigate | Two-step confirm `Dialog` (UI-SPEC-mandated exact copy) required before `deleteMember` fires — no single-click delete path exists |
| T-15-14 | Information Disclosure | Unlinked-users list exposes `name`/`email`/`createdAt` for every unlinked account | accept | Identical to the already-shipped `AdminLinkMembers.jsx` behavior (requireAdmin-gated); this plan relocates it, introducing no new exposure |

</threat_model>

<verification>
- `npm test --workspace frontend -- src/pages/ManagePage.test.jsx` — green, admin-branch cases (table, focus, delete, account-linking)
- `npm test --workspace frontend` — full frontend suite green
- `npm test --workspace backend` — full backend suite green (phase-level sanity re-check)
</verification>

<success_criteria>
- An admin can search/select any member into the shared grouped panel with Edit (D-06 bypass) and Remove active.
- deleteMember is gated behind a two-step, UI-SPEC-exact confirm dialog.
- Account-linking is fully re-homed into /manage; /admin/link-members no longer hosts it.
- Both full suites (frontend + backend) are green — the phase's final gate before /gsd:verify-work.
</success_criteria>

<output>
Create `.planning/phases/15-sibling-dedup-guard-manage-self-service-ui/15-06-SUMMARY.md` when done
</output>
