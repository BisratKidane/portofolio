---
phase: 11-email-verification-admin-race-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/mailer.js
  - backend/src/schemas/user.schema.js
  - backend/src/config/rateLimits.js
autonomous: true
requirements: [VERIFY-02, VERIFY-03, VERIFY-07]

must_haves:
  truths:
    - "sendVerificationEmail({ to, token }) exists as a mailer wrapper, copy-shaped like sendPasswordResetEmail, ready for register/resendVerificationEmail to call (D-09)"
    - "The GraphQL schema declares RegisterPayload, verifyEmail, and resendVerificationEmail as additive contracts that do not change any existing field's shape yet — register's return type stays AuthPayload! until Plan 11-04 changes it alongside the resolver, so this plan cannot break any currently-passing test"
    - "resendVerificationEmail has a RATE_LIMITS entry (5/hour) before its resolver exists, so the rate-limit plugin enforces it automatically the moment the resolver lands (D-13)"
  artifacts:
    - path: "backend/src/services/mailer.js"
      provides: "sendVerificationEmail wrapper"
      contains: "export async function sendVerificationEmail"
    - path: "backend/src/schemas/user.schema.js"
      provides: "RegisterPayload type + verifyEmail/resendVerificationEmail mutation fields (additive)"
      contains: "type RegisterPayload"
    - path: "backend/src/config/rateLimits.js"
      provides: "resendVerificationEmail rate-limit entry"
      contains: "resendVerificationEmail:"
  key_links:
    - from: "backend/src/schemas/user.schema.js (Mutation.resendVerificationEmail)"
      to: "backend/src/config/rateLimits.js (RATE_LIMITS.resendVerificationEmail)"
      via: "the rate-limit plugin keys on the GraphQL operation's root field name, which now matches an entry in the map"
      pattern: "resendVerificationEmail: \\{ max: 5, windowMs: 60 \\* 60 \\* 1000 \\}"
---

<objective>
Stand up the additive, non-breaking contracts this phase's resolvers will implement against: the mailer wrapper for verification emails (D-09), the GraphQL schema types/mutations for the message-only register payload and the two new mutations (D-10/D-11/D-12 shapes), and the rate-limit budget for `resendVerificationEmail` (D-13). Interface-first: none of these changes touch `register`'s existing return type or any resolver logic, so the full backend suite stays green immediately after this plan lands, with zero coordination required against Plan 11-01's schedule.

Purpose: Plans 11-04 and 11-05 implement the resolver logic against these exact contracts — no exploration needed, no schema drift risk.
Output: `sendVerificationEmail` in the mailer, `RegisterPayload`/`verifyEmail`/`resendVerificationEmail` in the SDL, and a `resendVerificationEmail` entry in `RATE_LIMITS`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-email-verification-admin-race-fix/11-CONTEXT.md
@.planning/phases/11-email-verification-admin-race-fix/11-PATTERNS.md

<interfaces>
<!-- Current backend/src/services/mailer.js sendPasswordResetEmail (the template): -->
<!--
export async function sendPasswordResetEmail({ to, token }) {
  const link = `${env.clientUrl}/reset-password?token=${token}`;
  const subject = 'Reset your password';
  const text = `We received a request to reset your password...`;
  return sendMail({ to, subject, text });
}
sendMail({to, subject, text, html}) already handles jsonTransport in dev/test — no changes needed there.
-->

<!-- Current backend/src/schemas/user.schema.js (full file, 44 lines): -->
<!--
type AuthPayload { token: String!, user: User! }
type PasswordResetPayload { message: String! }
type Mutation {
  register(name: String!, email: String!, password: String!): AuthPayload!   <- UNCHANGED this plan
  login(email: String!, password: String!): AuthPayload!
  logout: Boolean!
  requestPasswordReset(email: String!): PasswordResetPayload!
  resetPassword(token: String!, password: String!): Boolean!
}
-->

<!-- Current backend/src/config/rateLimits.js RATE_LIMITS map (full): -->
<!--
export const RATE_LIMITS = {
  login: { max: 5, windowMs: 15 * 60 * 1000 },
  register: { max: 5, windowMs: 60 * 60 * 1000 },
  requestPasswordReset: { max: 5, windowMs: 60 * 60 * 1000 }
};
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sendVerificationEmail mailer wrapper (D-09)</name>
  <files>backend/src/services/mailer.js</files>
  <read_first>backend/src/services/mailer.js (full file, current 37 lines); .planning/phases/11-email-verification-admin-race-fix/11-CONTEXT.md D-09; .planning/phases/11-email-verification-admin-race-fix/11-PATTERNS.md "backend/src/services/mailer.js" section</read_first>
  <action>
    Add `export async function sendVerificationEmail({ to, token })` directly after `sendPasswordResetEmail` (end of file), copy-shaped identically: build `const link = \`${env.clientUrl}/verify-email?token=${token}\`;`, `const subject = 'Verify your email';`, `const text` referencing the 24-hour expiry window in prose (e.g. "This link expires in 24 hours. If you did not create this account, you can ignore this email."), then `return sendMail({ to, subject, text });`. No changes to `sendMail`, `buildTransportOptions`, or the module-level `transporter` — `jsonTransport` in dev/test already applies with zero code changes.
  </action>
  <acceptance_criteria>
    `grep -n "export async function sendVerificationEmail" backend/src/services/mailer.js` matches exactly once.
    `grep -n "verify-email?token=" backend/src/services/mailer.js` matches exactly once.
    `grep -n "return sendMail({ to, subject, text });" backend/src/services/mailer.js` count is 2 (one for `sendPasswordResetEmail`, one for the new `sendVerificationEmail`).
    `npm test --workspace backend -- src/services/mailer.test.js` still passes unchanged (this task adds no new tests there, matching the existing convention that `sendPasswordResetEmail` itself has no dedicated unit test — call-args are asserted at the resolver-test boundary instead, per Phase 8 D-05).
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- src/services/mailer.test.js</automated>
  </verify>
  <done>sendVerificationEmail exists, mirrors sendPasswordResetEmail's shape, and is ready to be mocked at the resolver-test boundary in Plans 11-04/11-05.</done>
</task>

<task type="auto">
  <name>Task 2: GraphQL schema contracts — RegisterPayload, verifyEmail, resendVerificationEmail (additive)</name>
  <files>backend/src/schemas/user.schema.js</files>
  <read_first>backend/src/schemas/user.schema.js (full file, current 44 lines); .planning/phases/11-email-verification-admin-race-fix/11-CONTEXT.md D-10, D-11, D-12; .planning/phases/11-email-verification-admin-race-fix/11-PATTERNS.md "backend/src/schemas/user.schema.js" section</read_first>
  <action>
    Add a new type `RegisterPayload { message: String! }` directly after the existing `type PasswordResetPayload { message: String! }` block — kept as a distinct type from `PasswordResetPayload` for domain clarity even though structurally identical (D-10 discretion). Add two new fields to the `Mutation` block, after `resetPassword(token: String!, password: String!): Boolean!`: `verifyEmail(token: String!): AuthPayload!` and `resendVerificationEmail(email: String!): PasswordResetPayload!` (reuses the existing message-only type per D-12, matching `requestPasswordReset`'s exact return shape). Do NOT change `register`'s existing `(...): AuthPayload!` return type in this task — that change is deliberately deferred to Plan 11-04, where it lands atomically together with the resolver rewrite and the register.test.js rewrite, so this plan introduces zero risk of breaking the current `register.test.js`.
  </action>
  <acceptance_criteria>
    `grep -n "type RegisterPayload { message: String! }" backend/src/schemas/user.schema.js` matches exactly once.
    `grep -n "verifyEmail(token: String!): AuthPayload!" backend/src/schemas/user.schema.js` matches exactly once.
    `grep -n "resendVerificationEmail(email: String!): PasswordResetPayload!" backend/src/schemas/user.schema.js` matches exactly once.
    `grep -n "register(name: String!, email: String!, password: String!): AuthPayload!" backend/src/schemas/user.schema.js` STILL matches exactly once (confirms register's type was deliberately left untouched this plan).
    `npm test --workspace backend` (full suite) passes — these are pure additions with no existing resolver wired to the two new mutation fields yet, so nothing currently queries them.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend</automated>
  </verify>
  <done>RegisterPayload, verifyEmail, and resendVerificationEmail are declared in the SDL; register's type is untouched; full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: resendVerificationEmail rate-limit entry (D-13)</name>
  <files>backend/src/config/rateLimits.js</files>
  <read_first>backend/src/config/rateLimits.js (full file, current 10 lines — its own comment already anticipates this exact addition)</read_first>
  <action>
    Add `resendVerificationEmail: { max: 5, windowMs: 60 * 60 * 1000 }` as a new entry in the `RATE_LIMITS` object, directly after `requestPasswordReset` (same shape, 5 attempts/hour, matching `register`/`requestPasswordReset`). Remove or rewrite the now-stale top-of-file comment lines that say "Phase 11 will add `resendVerificationEmail` as one more entry to this same object — out of scope for Phase 10, do not add it now" since Phase 11 is doing it now — replace with a one-line note that the entry was added in Phase 11 (VERIFY-07), keeping the first comment line about the map being the single edit point.
  </action>
  <acceptance_criteria>
    `grep -n "resendVerificationEmail: { max: 5, windowMs: 60 \* 60 \* 1000 }" backend/src/config/rateLimits.js` matches exactly once.
    `grep -c "Phase 11 will add" backend/src/config/rateLimits.js` returns `0` (stale anticipatory comment removed/rewritten).
    `npm test --workspace backend` (full suite) passes — `resendVerificationEmail` has no resolver yet, so `rateLimitPlugin.test.js`/`rateLimit.test.js` are unaffected (this key is simply unused by any current test).
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend</automated>
  </verify>
  <done>RATE_LIMITS carries a resendVerificationEmail entry (5/hour); the moment Plan 11-05 wires the resolver, the existing rate-limit plugin enforces it with zero further plugin changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GraphQL SDL vs. resolver map | Adding a mutation field to the SDL without a resolver is safe (default resolver returns undefined for unqueried fields) — this plan deliberately exploits that safety to sequence schema-first without breaking tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02a | Denial of Service | `resendVerificationEmail` (future resolver) | mitigate | Rate-limit budget (5/hour) is defined in the single edit-point map BEFORE the resolver exists, so there is no window where the mutation is reachable without a throttle once Plan 11-05 wires it (D-13). |
| T-11-02b | Tampering (schema drift) | `register`'s return type | accept (temporary, scoped) | Deliberately left as `AuthPayload!` in this plan so the schema and the resolver never disagree mid-phase; the type change and resolver rewrite land atomically together in Plan 11-04. |
</threat_model>

<verification>
Run `npm test --workspace backend` after each task. All three must leave the full suite green with zero new failures, since every change in this plan is additive.
</verification>

<success_criteria>
- `sendVerificationEmail({ to, token })` exists, mirroring `sendPasswordResetEmail`'s shape exactly (D-09).
- `RegisterPayload`, `verifyEmail(token: String!): AuthPayload!`, and `resendVerificationEmail(email: String!): PasswordResetPayload!` are declared in the SDL, with `register`'s existing type left untouched.
- `RATE_LIMITS.resendVerificationEmail` exists at 5/hour.
- Full backend suite green throughout (every change here is additive).
</success_criteria>

<output>
Create `.planning/phases/11-email-verification-admin-race-fix/11-02-SUMMARY.md` when done
</output>
</content>
