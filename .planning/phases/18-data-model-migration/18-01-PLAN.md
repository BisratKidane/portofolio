---
phase: 18-data-model-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/models/FamilyMember.js
  - backend/src/models/FamilyMember.test.js
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "FamilyMember.build() with geezFirstname and/or geezLastname set derives geezFullname as the space-joined parts, excluding geezMothersname (D-01)"
    - "FamilyMember.build() with no Ge'ez fields set, or only geezMothersname set, has geezFullname === null (never '', 'null', or 'undefined') — defensive getter per D-02"
    - "The three Ge'ez columns are nullable STRING with no validate key or length constraint beyond the default, matching mothersname/address/phone (D-05)"
    - "Existing FamilyMember validation (required fields, gender ENUM, email format, date rules) is unaffected by the new fields"
    - "The geezFullname fill-matrix is covered by unit tests authored test-first (RED failing tests before the GREEN getter implementation) per D-06, and the full backend suite introduces no new failures"
  artifacts:
    - path: "backend/src/models/FamilyMember.js"
      provides: "geezFirstname/geezLastname/geezMothersname nullable STRING attrs + geezFullname VIRTUAL getter"
      contains: "geezFullname: {"
    - path: "backend/src/models/FamilyMember.test.js"
      provides: "fill-matrix unit tests for geezFullname (none/first-only/last-only/mothersname-only/all-filled)"
      contains: "geezFullname VIRTUAL getter"
  key_links:
    - from: "backend/src/models/FamilyMember.js geezFullname getter"
      to: "this.geezFirstname / this.geezLastname instance properties"
      via: "synchronous property read inside VIRTUAL get()"
      pattern: "this\\.geezFirstname.*this\\.geezLastname"
---

<objective>
Add three nullable Ge'ez-script name attributes (`geezFirstname`, `geezLastname`, `geezMothersname`) plus a defensively-written `geezFullname` VIRTUAL getter to the `FamilyMember` Sequelize model, test-first per CONTEXT.md D-06: write the failing fill-matrix unit tests first (RED), then implement the getter to make them pass (GREEN).

Purpose: Gives the data layer a correctly-derived combined Ge'ez name before any GraphQL or frontend code depends on it (DATA-01, DATA-02). The getter must NOT be a naive port of the existing `fullname` template-literal join — `geezFirstname`/`geezLastname` are both optional (unlike required `firstname`/`lastname`), so a defensive `.filter(Boolean).join(' ') || null` is required (D-02) to avoid stray spaces or literal `"null"`/`"undefined"` strings.
Output: `FamilyMember.js` with 3 new nullable attrs + `geezFullname` VIRTUAL; `FamilyMember.test.js` with 8 new passing tests (2 nullable-declaration + 6 getter fill-matrix/structural); no new failures introduced into the backend suite relative to the pre-task baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-data-model-migration/18-CONTEXT.md
@.planning/phases/18-data-model-migration/18-PATTERNS.md
@.planning/research/SUMMARY.md
</context>

<interfaces>
<!-- Current state of the file this plan modifies. Executor edits this exact file, not a rewrite. -->

From `backend/src/models/FamilyMember.js` (lines 1, 26-29, 59-68) — the exact attrs being extended:
```javascript
import { DataTypes, Model } from 'sequelize';
// ...
mothersname: {
  type: DataTypes.STRING,
  allowNull: true
},
// ...
profilePicture: {
  type: DataTypes.STRING,
  allowNull: true
},
fullname: {
  type: DataTypes.VIRTUAL,
  get() {
    return `${this.firstname} ${this.lastname}`;
  }
}
```

From `backend/src/models/FamilyMember.test.js` (lines 1-5, 88-98) — import shape and the analog getter-test block to mirror:
```javascript
import { describe, it, expect } from 'vitest';
import { DataTypes } from 'sequelize';
import { models } from './index.js';

const { FamilyMember } = models;
// ...
describe('fullname VIRTUAL getter (MEM-03, D-09)', () => {
  it('derives fullname as firstname + lastname on a built (unsaved) instance', () => {
    const instance = FamilyMember.build({ firstname: 'Jane', lastname: 'Doe', gender: 'Female' });
    expect(instance.fullname).toBe('Jane Doe');
  });

  it('declares fullname as a Sequelize VIRTUAL field', () => {
    expect(FamilyMember.rawAttributes.fullname.type).toBeInstanceOf(DataTypes.VIRTUAL);
  });
});
```

**Pre-task baseline (measured, not hypothetical):** the full backend suite (`npm test --workspace backend`) currently reports `Test Files 2 failed | 55 passed (57)`, `Tests 2 failed | 380 passed (382)`. The 2 pre-existing, unrelated failures are:
1. `src/resolvers/verifyEmail.test.js > verifyEmail > lets two users racing to verify simultaneously each keep a usable session, with exactly one becoming ADMIN (VERIFY-04)` — a concurrency/race-timing test, unrelated to `FamilyMember`.
2. `src/services/familyMember.dedup.test.js > addChild REL-06 dedup guard (D-08/D-09/D-10/D-11) > (D-10 resolver-path TOCTOU, CR-01) detects a duplicate even when a plain read earlier in the SAME transaction froze the REPEATABLE READ snapshot — mirrors addChild/addSibling resolvers findByPk-ing the target before the guard runs` — a transaction-isolation timing test in a different file (`familyMember.dedup.test.js`, not `FamilyMember.test.js`); this plan does not touch dedup logic.
Neither failure is in `FamilyMember.js` or `FamilyMember.test.js`, and neither is caused by or related to Ge'ez fields. Task 2's acceptance criteria compares against this measured baseline rather than asserting a blanket exit code 0, so this plan is not blocked by pre-existing, unrelated timing flakiness. If the executor's own pre-Task-1 run shows a different baseline (e.g. these are fixed, or a different transient failure appears), use that freshly-observed baseline instead — the requirement is always "zero NEW failures caused by this plan's changes," not "zero failures total."
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): Add failing fill-matrix tests for the three Ge'ez fields and geezFullname getter</name>
  <files>backend/src/models/FamilyMember.test.js</files>
  <read_first>
    backend/src/models/FamilyMember.test.js
    backend/src/models/FamilyMember.js
    .planning/phases/18-data-model-migration/18-PATTERNS.md
  </read_first>
  <behavior>
    - Test group A — `describe("Ge'ez name fields nullable (DATA-01)", ...)`:
      - `resolves when geezFirstname/geezLastname/geezMothersname are all omitted` — `FamilyMember.build({ firstname: 'Jane', lastname: 'Doe', gender: 'Other' })` then `await expect(instance.validate()).resolves.not.toThrow()`.
      - `declares geezFirstname, geezLastname, geezMothersname as nullable` — asserts `FamilyMember.rawAttributes.geezFirstname.allowNull`, `.geezLastname.allowNull`, `.geezMothersname.allowNull` are all `true`.
    - Test group B — `describe("geezFullname VIRTUAL getter (DATA-02, D-01, D-02)", ...)`:
      - `declares geezFullname as a Sequelize VIRTUAL field` — `expect(FamilyMember.rawAttributes.geezFullname.type).toBeInstanceOf(DataTypes.VIRTUAL)`.
      - `returns null when no Ge'ez name parts are set (none)` — build with only `firstname`/`lastname`/`gender`; `expect(instance.geezFullname).toBeNull()`.
      - `returns just geezFirstname when only it is set (first-only)` — add `geezFirstname: 'ጃነ'`; expect `'ጃነ'`.
      - `returns just geezLastname when only it is set (last-only)` — add `geezLastname: 'ዶ'`; expect `'ዶ'`.
      - `returns null when only geezMothersname is set (mothersname excluded per D-01)` — add `geezMothersname: 'ኣለም'` only; `expect(instance.geezFullname).toBeNull()`.
      - `joins geezFirstname and geezLastname with a single space, excluding geezMothersname, when all three are set (all-filled)` — set all three (`geezFirstname: 'ጃነ'`, `geezLastname: 'ዶ'`, `geezMothersname: 'ኣለም'`); expect `'ጃነ ዶ'` (mothersname must NOT appear in the result).
  </behavior>
  <action>
    Append the two new `describe` blocks to the end of `backend/src/models/FamilyMember.test.js`, after the existing `describe('date validation (D-10)', ...)` block. Use `FamilyMember.build({...})` with real Ge'ez/Ethiopic UTF-8 literals exactly as specified in `<behavior>` (do not use Latin placeholder text for the Ge'ez fields — the point is to exercise real Ethiopic-script strings). Do NOT modify `backend/src/models/FamilyMember.js` in this task — `geezFirstname`/`geezLastname`/`geezMothersname`/`geezFullname` do not exist on the model yet, so every new test in this task MUST fail (RED). No other existing `describe` block in the file is touched.
  </action>
  <acceptance_criteria>
    - `npm test --workspace backend -- FamilyMember 2>&1 | tail -60` shows all 8 new tests (2 in group A, 6 in group B) FAILING — the nullable-declaration tests fail because `FamilyMember.rawAttributes.geezFirstname` is `undefined`, and the getter tests fail because `instance.geezFullname` is `undefined`, not the asserted value.
    - Every pre-existing `describe` block in the file (`required fields`, `gender ENUM`, `optional fields nullable`, `email conditional isEmail validation`, `fullname VIRTUAL getter`, `date validation`) still passes unmodified.
    - `backend/src/models/FamilyMember.js` has zero diff after this task (`git diff --stat backend/src/models/FamilyMember.js` is empty).
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- FamilyMember 2>&1 | tail -60</automated>
  </verify>
  <done>The 8 new tests exist, are syntactically correct, use real Ge'ez UTF-8 literals, and fail for the expected reason (model attrs not yet implemented) — confirmed by reading the failure output, not just the exit code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement the three Ge'ez STRING attrs and the defensive geezFullname VIRTUAL getter</name>
  <files>backend/src/models/FamilyMember.js</files>
  <read_first>
    backend/src/models/FamilyMember.js
    backend/src/models/FamilyMember.test.js
  </read_first>
  <action>
    In `backend/src/models/FamilyMember.js`, insert three new attributes immediately after the existing `profilePicture: { type: DataTypes.STRING, allowNull: true }` block (currently lines 59-62) and before the existing `fullname` VIRTUAL (currently lines 63-68):
    `geezFirstname: { type: DataTypes.STRING, allowNull: true },`
    `geezLastname: { type: DataTypes.STRING, allowNull: true },`
    `geezMothersname: { type: DataTypes.STRING, allowNull: true },`
    Each mirrors the exact `mothersname`/`profilePicture` shape — no `validate` key (D-05: no format/length validation beyond the default `VARCHAR` limit; do NOT copy the `email` field's `validate: { isEmail: true }` pattern).
    Immediately after those three attrs (still before the existing `fullname` VIRTUAL), add:
    `geezFullname: { type: DataTypes.VIRTUAL, get() { return [this.geezFirstname, this.geezLastname].filter(Boolean).join(' ') || null; } },`
    This is the exact defensive expression from CONTEXT.md D-02 — do not use a template-literal join (`` `${this.geezFirstname} ${this.geezLastname}` ``), which is unsafe here because both parts are nullable (unlike `firstname`/`lastname`). Do not add `geezMothersname` to this getter — D-01 excludes it from the combined name. Leave the existing `fullname` VIRTUAL and the model-level `validate` block (`deathAfterBirth`, `noFutureDates`) completely untouched.
  </action>
  <acceptance_criteria>
    - `grep -c "geezFirstname: {" backend/src/models/FamilyMember.js` equals `1`; same for `geezLastname: {` and `geezMothersname: {`.
    - `grep -A2 "geezFullname: {" backend/src/models/FamilyMember.js` shows `type: DataTypes.VIRTUAL` and a `get()` body containing the exact substring `filter(Boolean).join(' ') || null`.
    - `grep -A2 "geezFullname: {" backend/src/models/FamilyMember.js | grep -c geezMothersname` equals `0` (confirms `geezMothersname` is never referenced inside the `geezFullname` getter body, per D-01's exclusion rule).
    - All 8 tests added in Task 1 now PASS (GREEN): `npm test --workspace backend -- FamilyMember 2>&1 | tail -30` shows 0 failing tests in `FamilyMember.test.js`.
    - Full backend suite introduces no NEW failures relative to the measured pre-task baseline in `<interfaces>` (380 passed / 2 pre-existing, named failures / 382 total): after this task, passed count is baseline-passed + 8, and the only failures present (if any) are the same 2 named pre-existing ones (`verifyEmail.test.js` VERIFY-04 race test, `familyMember.dedup.test.js` TOCTOU test) — no failure appears in `FamilyMember.test.js` or in any other file this plan did not touch.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend 2>&1 | tail -30</automated>
  </verify>
  <done>`FamilyMember.js` exposes `geezFirstname`, `geezLastname`, `geezMothersname` (all nullable, unvalidated `STRING`) and a defensive `geezFullname` VIRTUAL getter that excludes `geezMothersname`; all 8 new tests pass; the full backend suite introduces zero new failures relative to the measured pre-task baseline (the same 2 named, unrelated timing-flake failures are the only ones allowed to persist).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new in this plan | Model/test-only change; no new external input path — these fields are not yet reachable via the GraphQL API (schema/resolver exposure is Phase 19). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `geezFirstname`/`geezLastname`/`geezMothersname` STRING attrs | accept | No format/length validation beyond the default `VARCHAR(255)` truncation (D-05), matching the existing unvalidated `mothersname`/`address`/`phone` precedent on the same model; no new attack surface since these fields are not yet writable through any API in this phase. |
| T-18-02 | Information Disclosure | `geezFullname` VIRTUAL getter | accept | The getter only reads and joins fields already present on the same in-memory model instance — it introduces no new data-access path and mirrors the existing `fullname` getter's trust level exactly. |
| T-18-SC | Tampering | npm/pip/cargo installs | not applicable | This plan installs zero new packages (model attribute + test additions only, using `sequelize`'s already-imported `DataTypes`). |

</threat_model>

<verification>
Run `npm test --workspace backend -- FamilyMember` after Task 1 (expect 8 new failing tests, RED, all pre-existing tests still passing) and again after Task 2 (expect all passing, GREEN). Run `npm test --workspace backend` after Task 2 and compare against the measured pre-task baseline (380 passed / 2 named pre-existing failures / 382 total) to confirm zero new failures.
</verification>

<success_criteria>
- `FamilyMember.js` exposes `geezFirstname`, `geezLastname`, `geezMothersname` as nullable, unvalidated `STRING` attrs and a `geezFullname` VIRTUAL getter that joins only `geezFirstname`+`geezLastname` (excluding `geezMothersname`), returning `null` (never `""`/`"null"`/`"undefined"`) when both are absent.
- The fill-matrix (none/first-only/last-only/mothersname-only/all-filled) is unit-tested in `FamilyMember.test.js` and passes.
- Full backend suite introduces no new failures relative to the measured pre-task baseline — existing member CRUD/validation behavior is unaffected (Success Criteria #4, backward compatibility).
</success_criteria>

<output>
Create `.planning/phases/18-data-model-migration/18-01-SUMMARY.md` when done
</output>
</content>
