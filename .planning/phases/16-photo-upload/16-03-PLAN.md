---
phase: 16-photo-upload
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - docker-compose.yml
  - backend/scripts/verify-photo-persistence.sh
autonomous: true
requirements: [PHOTO-02]

must_haves:
  truths:
    - "Uploaded photos persist across a container rebuild because they live on a named Docker volume, not the container's writable layer"
  artifacts:
    - path: "docker-compose.yml"
      provides: "photo_uploads named volume mounted into the backend service"
      contains: "photo_uploads"
    - path: "backend/scripts/verify-photo-persistence.sh"
      provides: "A repeatable up -> write -> down -> up -> verify script proving persistence"
  key_links:
    - from: "docker-compose.yml (backend service volumes)"
      to: "backend/src/config/photoStorage.js (photosDir)"
      via: "matching container path /app/backend/storage/photos"
      pattern: "photo_uploads:/app/backend/storage/photos"
---

<objective>
Give uploaded photos a durable home: a named Docker volume mounted at the exact path `photoStorageConfig.photosDir` resolves to inside the container, mirroring the existing `mysql_data` precedent — plus a repeatable, real (not hand-waved) verification script that rebuilds the stack and proves a file survives.

Purpose: PHOTO-02's success criterion is specific — "verified against a named Docker volume (not the container's writable layer)" — this must be a checkable deliverable, not an assumption.
Output: `docker-compose.yml`'s `photo_uploads` volume + `backend/scripts/verify-photo-persistence.sh`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-photo-upload/16-RESEARCH.md
</context>

<interfaces>
From docker-compose.yml (current, full file) — the `mysql_data` precedent this plan's `photo_uploads` volume mirrors:
```
services:
  mysql:
    volumes:
      - mysql_data:/var/lib/mysql
      - ./backend/test/init/01-create-test-db.sh:/docker-entrypoint-initdb.d/01-create-test-db.sh:ro
  backend:
    build: { context: ., dockerfile: backend/Dockerfile }
    ports: ["${PORT:-4000}:4000"]
    depends_on: { mysql: { condition: service_healthy } }
volumes:
  mysql_data:
```
`backend/Dockerfile`'s `WORKDIR /app/backend` confirms the container-side absolute path `/app/backend/storage/photos` matches what `backend/src/config/photoStorage.js` (16-01) resolves to via `__dirname`.

Root `package.json`'s `docker:local` script: `docker compose --env-file env/local.env up --build`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the photo_uploads named volume</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml, backend/Dockerfile, backend/src/config/photoStorage.js</read_first>
  <action>
    Add `- photo_uploads:/app/backend/storage/photos` to the `backend` service's `volumes:` list in `docker-compose.yml` (the `backend` service currently has no `volumes:` key at all — add one). Add `photo_uploads:` as a new top-level entry under the existing `volumes:` block, alongside `mysql_data:`.
  </action>
  <acceptance_criteria>
    - `docker-compose.yml` contains a top-level `volumes:` entry named `photo_uploads`
    - the `backend` service block contains `photo_uploads:/app/backend/storage/photos`
    - `docker compose config` (from the repo root) exits 0 and its output contains `photo_uploads`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio && docker compose config 2>/dev/null | grep -c photo_uploads</automated>
  </verify>
  <done>docker-compose.yml declares a named photo_uploads volume mounted into the backend service at the path photoStorageConfig resolves to.</done>
</task>

<task type="auto">
  <name>Task 2: Automated rebuild-persistence verification script</name>
  <files>backend/scripts/verify-photo-persistence.sh</files>
  <read_first>docker-compose.yml (post-Task-1), package.json (docker:local script), env/local.env (existence only, do not print contents)</read_first>
  <action>
    Create an executable `backend/scripts/verify-photo-persistence.sh` that: (1) `docker compose --env-file env/local.env up -d --build mysql backend`; (2) polls `http://localhost:${PORT:-4000}/health` with retries until it returns 200; (3) writes a marker file directly under the mounted volume via `docker compose exec -T backend node -e "require('fs').writeFileSync('/app/backend/storage/photos/.persistence-check', 'ok')"`; (4) `docker compose down` (explicitly NOT `docker compose down -v` — `-v` would delete the volume and invalidate the entire test); (5) `docker compose --env-file env/local.env up -d --build mysql backend` again; (6) `docker compose exec -T backend node -e "if (!require('fs').existsSync('/app/backend/storage/photos/.persistence-check')) process.exit(1)"`; (7) prints `PASS: photo_uploads persisted across a rebuild` or `FAIL: ...` and exits non-zero on any failure; make the file executable (`chmod +x`).
    Attempt to run this script once via the Bash tool now. If a Docker daemon is unavailable in this execution environment, do NOT fabricate a pass — record in the plan SUMMARY that the script exists and ran manually is still required before `/gsd:verify-work`, per 16-VALIDATION.md's "Manual-Only Verifications" table, and give the exact command (`bash backend/scripts/verify-photo-persistence.sh`) for a human to run later.
  </action>
  <acceptance_criteria>
    - `backend/scripts/verify-photo-persistence.sh` exists and is executable (`test -x`)
    - the script contains `docker compose down` and does NOT contain `docker compose down -v` (grep confirms zero matches for `down -v`)
    - the script contains exactly two `docker compose ... up` invocations (the rebuild-and-verify shape phase_specific_constraints #2 requires)
  </acceptance_criteria>
  <verify>
    <automated>bash -n /Users/bisrat/Projects/portofolio/backend/scripts/verify-photo-persistence.sh && test -x /Users/bisrat/Projects/portofolio/backend/scripts/verify-photo-persistence.sh && echo ok</automated>
  </verify>
  <done>A repeatable, syntactically valid, executable script exists proving (or ready to prove, if Docker is unavailable in this sandbox) that a file survives a full container rebuild.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Container rebuild | The writable container layer is ephemeral; only the named volume is durable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Denial of Service (data loss) | Photos stored on the container's writable layer instead of a volume | mitigate | Named `photo_uploads` volume, mirroring the `mysql_data` precedent; verified via a real rebuild script, not assumed |
| T-16-07 | Tampering | Verification script accidentally destroying the volume it's supposed to prove persists | mitigate | Script explicitly uses `docker compose down` (no `-v`), never `down -v` or `docker volume rm` |
</threat_model>

<verification>
`docker compose config | grep photo_uploads` succeeds; `bash -n backend/scripts/verify-photo-persistence.sh` exits 0; full script run (when Docker is available) prints `PASS`.
</verification>

<success_criteria>
- photo_uploads named volume declared and mounted at the correct container path
- A repeatable rebuild-and-verify script exists and is runnable
</success_criteria>

<output>
Create `.planning/phases/16-photo-upload/16-03-SUMMARY.md` when done
</output>
