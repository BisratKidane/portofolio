---
phase: 16-photo-upload
plan: 07
type: execute
wave: 6
depends_on: ["16-06"]
files_modified:
  - frontend/src/components/manage/MemberCard.jsx
  - frontend/src/components/manage/MemberCard.test.jsx
  - frontend/src/pages/ManagePage.jsx
  - frontend/src/pages/ManagePage.test.jsx
  - frontend/src/components/manage/AdminMemberTable.jsx
  - frontend/src/components/manage/AdminMemberTable.test.jsx
autonomous: true
requirements: [PHOTO-01]

must_haves:
  truths:
    - "Clicking an in-scope member's avatar on MemberCard opens the crop dialog (D-01); an out-of-scope avatar is not interactive"
    - "A member's photo displays wherever MemberCard is used (relationship panels) and in the admin table, with zero code changes needed in RelationshipGroupedPanel itself"
    - "A member within editable scope can remove their photo via a confirm dialog distinct from the existing 'Remove member?' admin dialog (D-11)"
  artifacts:
    - path: "frontend/src/components/manage/MemberCard.jsx"
      provides: "Avatar click trigger, camera overlay, remove-photo action"
      contains: "MemberAvatarImage"
    - path: "frontend/src/pages/ManagePage.jsx"
      provides: "PhotoCropDialog wiring + 'Remove photo?' confirm dialog in both branches"
      contains: "Remove photo?"
  key_links:
    - from: "frontend/src/components/manage/MemberCard.jsx"
      to: "frontend/src/components/manage/MemberAvatarImage.jsx"
      via: "avatar rendering"
      pattern: "MemberAvatarImage"
    - from: "frontend/src/pages/ManagePage.jsx"
      to: "frontend/src/components/manage/PhotoCropDialog.jsx"
      via: "avatar-click -> crop dialog"
      pattern: "PhotoCropDialog"
    - from: "frontend/src/pages/ManagePage.jsx (GraphQL queries)"
      to: "backend/src/schemas/familyMember.schema.js (photoUrl)"
      via: "photoUrl field requested in every member query"
      pattern: "photoUrl"
---

<objective>
Wire the tested primitives from 16-06 into the live `/manage` surfaces: `MemberCard`'s avatar becomes the D-01 upload trigger (with a D-10 icon placeholder replacing initials everywhere), `ManagePage` gains the crop dialog and a "Remove photo?" confirmation distinct from the existing "Remove member?" dialog, and the admin table gains a photo thumbnail column — completing every "modified" surface 16-UI-SPEC.md declares in scope.

Purpose: this is the last-mile UI wiring that makes PHOTO-01's "the resulting photo displays on that member" observably true for a real user, across every surface that renders a member.
Output: `MemberCard.jsx`, `ManagePage.jsx`, `AdminMemberTable.jsx` updated; `photoUrl` requested by every member-fetching GraphQL query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-photo-upload/16-CONTEXT.md
@.planning/phases/16-photo-upload/16-UI-SPEC.md
@.planning/phases/16-photo-upload/16-PATTERNS.md
</context>

<interfaces>
From frontend/src/components/manage/MemberCard.jsx (full current file — the `locked` boolean this plan reuses verbatim for avatar-interactivity, per PATTERNS.md's "reuse the same boolean-composition style, do not invent a second lock-condition shape"):
```
const locked = !isAdmin && !isSelf && member.linkedUser && member.linkedUser.id !== actingUserId;
<Avatar sx={{ width: 42, height: 42, bgcolor: '#eef1f8', color: colors.slate }}>{getInitials(member.fullname)}</Avatar>
{!locked && <Button variant="text" onClick={() => onEdit(member)}>Edit</Button>}
{isAdmin && <Button variant="text" color="error" onClick={() => onDelete(member)}>Remove</Button>}
```

From frontend/src/pages/ManagePage.jsx:495-513 (the exact confirm-dialog shape "Remove photo?" mirrors, non-dismissible-while-in-flight guard, Cancel-left/destructive-confirm-right):
```
<Dialog open={Boolean(deleteTarget)} onClose={() => (deleting ? null : setDeleteTarget(null))}>
  <DialogTitle>Remove member?</DialogTitle>
  ...
  <Button variant="text" disabled={deleting} onClick={() => setDeleteTarget(null)}>Cancel</Button>
  <Button variant="contained" color="error" disabled={deleting} onClick={handleDeleteConfirm}>{deleting ? 'Removing…' : 'Remove'}</Button>
```

From frontend/src/pages/ManagePage.jsx:26-58 (the 3 GraphQL query constants gaining `photoUrl`): `MY_EDITABLE_MEMBERS_QUERY`, `FAMILY_MEMBERS_QUERY`, `FAMILY_MEMBER_FOCUS_QUERY`.

From 16-UI-SPEC.md's Copywriting Contract: Tooltip/`aria-label` "Add a photo for {fullname}" (no photo) / "Change photo for {fullname}" (has photo); "Remove photo" (neutral text button, distinct from the error-colored "Remove" member-delete action); destructive confirm title "Remove photo?", body `"Remove {fullname}'s photo? Their avatar goes back to the default icon. This can't be undone."`, confirm label "Remove photo" / in-flight "Removing…".
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemberCard.jsx — avatar click trigger, camera overlay, remove-photo action</name>
  <files>frontend/src/components/manage/MemberCard.jsx, frontend/src/components/manage/MemberCard.test.jsx</files>
  <read_first>frontend/src/components/manage/MemberCard.jsx, frontend/src/components/manage/MemberCard.test.jsx, frontend/src/components/manage/MemberAvatarImage.jsx, 16-UI-SPEC.md</read_first>
  <behavior>
    - the placeholder `<Avatar>{getInitials(...)}</Avatar>` is fully replaced by `<MemberAvatarImage member={member} />` — `getInitials` is no longer imported/used in this file (D-10 regression guard: `screen.queryByText('AL')` must NOT be in the document)
    - when `!locked`, the avatar is wrapped in a `ButtonBase` (`minWidth: 44, minHeight: 44`, `borderRadius: '50%'`), tabbable, with `aria-label` `"Add a photo for {fullname}"` (no `photoUrl`) or `"Change photo for {fullname}"` (`photoUrl` set), and a hidden `<input type="file" accept="image/jpeg,image/png,image/webp" />` whose `onChange` calls a new `onPickPhoto(member, file)` prop then resets `value = ''`
    - when `locked`, the avatar renders as a plain, non-interactive `MemberAvatarImage` — no `ButtonBase`, no `aria-label` beyond the `Avatar`'s own `alt`, not tabbable
    - a neutral `variant="text"` "Remove photo" button renders only when `!locked && member.photoUrl`, calling a new `onRemovePhoto(member)` prop — visually and textually distinct from the existing admin-only error-colored "Remove" (member-delete) button in the same row
  </behavior>
  <action>
    Replace `MemberCard.jsx` lines 1-18's `Avatar`/`getInitials` usage per the behavior above; add `onPickPhoto` and `onRemovePhoto` to the component's prop signature (both optional — safe no-ops when `locked`). Update `MemberCard.test.jsx`'s first test ("renders the member name and avatar initials") to assert the placeholder icon renders and `screen.queryByText('AL')` is absent. Add new tests: `aria-label` text for no-photo vs has-photo; no interactive avatar trigger when `locked`; picking a file calls `onPickPhoto(member, file)`; "Remove photo" appears only when `!locked && photoUrl` and calls `onRemovePhoto(member)`.
  </action>
  <acceptance_criteria>
    - `MemberCard.jsx` imports `MemberAvatarImage` from `./MemberAvatarImage.jsx`
    - `grep -c "getInitials" frontend/src/components/manage/MemberCard.jsx` returns `0`
    - `MemberCard.test.jsx` no longer contains `getByText('AL')`
    - `npx vitest run src/components/manage/MemberCard.test.jsx` exits 0 (all existing + new tests green)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio/frontend && npx vitest run src/components/manage/MemberCard.test.jsx</automated>
  </verify>
  <done>Avatar is the D-01 upload trigger for in-scope members only; D-10 icon placeholder fully replaces initials; Remove-photo action is scoped and visually distinct from member-delete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ManagePage.jsx — PhotoCropDialog + Remove-photo confirm + photoUrl queries</name>
  <files>frontend/src/pages/ManagePage.jsx, frontend/src/pages/ManagePage.test.jsx</files>
  <read_first>frontend/src/pages/ManagePage.jsx, frontend/src/pages/ManagePage.test.jsx, frontend/src/components/manage/PhotoCropDialog.jsx, frontend/src/api/photoClient.js</read_first>
  <behavior>
    - all 3 member-fetching GraphQL query constants (`MY_EDITABLE_MEMBERS_QUERY`, `FAMILY_MEMBERS_QUERY`, `FAMILY_MEMBER_FOCUS_QUERY`) request `photoUrl`
    - in BOTH `MemberBranch` and `AdminBranch`: `onPickPhoto(member, file)` opens `PhotoCropDialog` with `{ open: true, file, member }` local state; `onRemovePhoto(member)` opens a NEW "Remove photo?" confirm `Dialog` (own local state: target/removing/error), mirroring the existing "Remove member?" dialog's non-dismissible-while-in-flight/Cancel-left/destructive-confirm-right shape, with the exact D-11 copy from 16-UI-SPEC.md
    - confirming "Remove photo?" calls `removeMemberPhoto(member.id)` then re-runs that branch's existing refetch function (`refetch` in `MemberBranch`; `refetchMembers()` + `refetchFocused()` in `AdminBranch`)
    - `PhotoCropDialog`'s `onUploaded` closes the dialog and re-runs the same branch's refetch function — no full-page reload, no toast
  </behavior>
  <action>
    Add `photoUrl` to each of the 3 query template strings (alongside existing scalar fields). In `MemberBranch`, add local state for the crop dialog and the remove-photo confirm dialog, thread `onPickPhoto`/`onRemovePhoto` through to `RelationshipGroupedPanel` (which already spreads extra props to `MemberCard` via `rowProps`), and render `<PhotoCropDialog ... onUploaded={refetch} />` plus the new "Remove photo?" `<Dialog>` (copying the exact JSX shape from `AdminBranch`'s existing "Remove member?" dialog at lines 495-513, retitled and re-worded per 16-UI-SPEC.md) at the bottom of the returned `<Stack>`. Repeat identically in `AdminBranch`, using `() => { refetchMembers(); refetchFocused(); }` as its refetch callback (matching its existing `EditMemberDialog onSaved` shape). Update `ManagePage.test.jsx`: mock `../api/photoClient.js` (`uploadMemberPhoto`/`removeMemberPhoto`/`fetchMemberPhotoBlob`) the same way `graphqlClient.js` is already mocked; add tests confirming the "Remove photo?" dialog opens and calls `removeMemberPhoto`.
  </action>
  <acceptance_criteria>
    - all 3 query constants in `ManagePage.jsx` contain `photoUrl`
    - `ManagePage.jsx` contains a `DialogTitle` reading `Remove photo?` AND still contains the original `Remove member?` dialog (both present — `grep -c` for each returns `>= 1`)
    - `ManagePage.jsx` imports `PhotoCropDialog` and `removeMemberPhoto`
    - `npx vitest run src/pages/ManagePage.test.jsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio/frontend && npx vitest run src/pages/ManagePage.test.jsx</automated>
  </verify>
  <done>Both branches wire avatar-click to the crop dialog and photo-removal to its own confirm dialog, distinct from member-removal; every member query requests photoUrl.</done>
</task>

<task type="auto">
  <name>Task 3: AdminMemberTable.jsx — photo thumbnail column</name>
  <files>frontend/src/components/manage/AdminMemberTable.jsx, frontend/src/components/manage/AdminMemberTable.test.jsx</files>
  <read_first>frontend/src/components/manage/AdminMemberTable.jsx, frontend/src/components/manage/AdminMemberTable.test.jsx, frontend/src/components/manage/MemberAvatarImage.jsx</read_first>
  <action>
    Add a leading `<TableCell sx={{ width: 56 }}>` (empty header label) to the `<TableHead>`'s `<TableRow>`, and a matching leading `<TableCell>` rendering `<MemberAvatarImage member={member} />` in each data row — placed before the existing "Name" cell. Existing search/filter/pagination logic is unchanged. Update `AdminMemberTable.test.jsx` to assert an avatar renders per row (query via `container.querySelectorAll('.MuiAvatar-root')` or an equivalent DOM check) without breaking any existing text-based assertions.
  </action>
  <acceptance_criteria>
    - `AdminMemberTable.jsx` imports `MemberAvatarImage` from `./MemberAvatarImage.jsx`
    - existing search/filter/pagination tests in `AdminMemberTable.test.jsx` still pass unmodified
    - `npx vitest run src/components/manage/AdminMemberTable.test.jsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio/frontend && npx vitest run src/components/manage/AdminMemberTable.test.jsx</automated>
  </verify>
  <done>Admin table renders a photo thumbnail per row, completing every "modified" surface 16-UI-SPEC.md declares in scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-declared vs. server-enforced authorization | This plan's `locked`/scope gating is a UX affordance only — the actual write authorization is enforced server-side in 16-04/16-05, never trusted from the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-17 | Spoofing (of authorization) | A hidden/disabled avatar trigger being bypassed client-side (e.g. dev tools) to POST an upload for an out-of-scope member | accept | Purely cosmetic — the server-side `computeEditableScope` check in 16-04's upload route is the actual enforcement; this UI gating only prevents accidental/confused actions, matching every other action button in this file (`Edit`, `Remove`) which rely on the same server-side backstop |
| T-16-18 | Repudiation | Naming collision between "Remove photo" and "Remove" (member) confusing a user into deleting the wrong thing | mitigate | Distinct dialog titles ("Remove photo?" vs "Remove member?"), distinct button colors (neutral vs error), never abbreviated, per 16-UI-SPEC.md's explicit naming-collision guard |
</threat_model>

<verification>
`npm test --workspace frontend` exits 0 (full frontend suite, phase-gate).
</verification>

<success_criteria>
- Avatar is the working D-01 upload trigger on every in-scope MemberCard
- Photo displays on MemberCard (relationship panels, for free) and on the admin table
- Remove-photo has its own distinct, correctly-labeled confirm flow
- Full frontend suite green
</success_criteria>

<output>
Create `.planning/phases/16-photo-upload/16-07-SUMMARY.md` when done
</output>
