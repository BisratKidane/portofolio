---
phase: 16-photo-upload
plan: 02
type: tdd
wave: 2
depends_on: ["16-01"]
files_modified:
  - backend/src/services/photoStorage.service.js
  - backend/src/services/photoStorage.service.test.js
autonomous: true
requirements: [PHOTO-03, QUAL-01]

must_haves:
  truths:
    - "Every stored photo filename is server-generated (crypto.randomUUID + a file-type-verified extension) — never derived from client input"
    - "Replacing a photo leaves exactly one file on disk for that member; a failed DB commit leaves zero orphaned new files"
  artifacts:
    - path: "backend/src/services/photoStorage.service.js"
      provides: "generatePhotoFilename, resolvePhotoPath, writePhotoFile, deletePhotoFile, finalizePhotoReplacement"
      exports: ["generatePhotoFilename", "resolvePhotoPath", "writePhotoFile", "deletePhotoFile", "finalizePhotoReplacement"]
  key_links:
    - from: "backend/src/services/photoStorage.service.js"
      to: "backend/src/config/photoStorage.js"
      via: "photoStorageConfig.photosDir"
      pattern: "photoStorageConfig"
---

<objective>
Build and test-first prove the one piece of this phase with zero codebase precedent: the file-system service that generates server-side-only filenames, writes/deletes photo bytes, and safely sequences a replace (write-new → caller's DB commit → delete-old, or discard-new on commit failure) so replacing/removing a photo never orphans a blob (D-11) and never derives a stored name from anything client-supplied (PHOTO-03).

Purpose: This is pure, testable business logic with a clear input/output contract (Pitfall 3's exact ordering) — the ideal TDD candidate — and it is the dependency every later route plan (16-04, 16-05) imports rather than re-deriving file-I/O logic inline.
Output: `backend/src/services/photoStorage.service.js` with full unit test coverage, red before green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-photo-upload/16-RESEARCH.md
@.planning/phases/16-photo-upload/16-PATTERNS.md
</context>

<interfaces>
From backend/src/services/familyMember.service.js:143-152 (`setSpouse` — the exact "caller-supplied transaction vs open-one-here" convention; photoStorage's `finalizePhotoReplacement` mirrors this shape but with a `commit` callback instead of a Sequelize transaction, since this service has zero Sequelize/DB awareness by design):
```
export async function setSpouse(memberAId, memberBId, { transaction } = {}) {
  if (transaction) {
    return createOrFindSpouseRow(memberAId, memberBId, transaction);
  }
  return sequelize.transaction((t) => createOrFindSpouseRow(memberAId, memberBId, t));
}
```

From backend/src/utils/auth.js:1-2,49-51 (the established `node:crypto` convention this service reuses for filename generation — no new dependency):
```
import crypto from 'node:crypto';
export function createResetToken() {
  return crypto.randomBytes(32).toString('hex');
}
```

From backend/src/config/photoStorage.js (created in 16-01): exports `photoStorageConfig.photosDir` — the single source of truth for where photos live on disk.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: generatePhotoFilename / resolvePhotoPath / writePhotoFile / deletePhotoFile</name>
  <files>backend/src/services/photoStorage.service.js, backend/src/services/photoStorage.service.test.js</files>
  <read_first>backend/src/services/familyMember.service.js, backend/src/services/familyMember.scope.test.js, backend/src/config/photoStorage.js, backend/src/utils/auth.js</read_first>
  <behavior>
    - `generatePhotoFilename('jpg')` returns a string matching `/^[0-9a-f-]{36}\.jpg$/` (UUIDv4 + the given extension, no dot-stripping bugs); two consecutive calls never return equal strings
    - `resolvePhotoPath('abc.jpg')` returns `photoStorageConfig.photosDir` joined with `'abc.jpg'` via `path.join` (not string concatenation)
    - `writePhotoFile(buffer, filename)` creates `photoStorageConfig.photosDir` if missing (`fs.mkdir({ recursive: true })`) and writes `buffer`'s exact bytes to `resolvePhotoPath(filename)`; reading the file back afterward returns identical bytes
    - `deletePhotoFile(filename)` removes an existing file; called again on the same (now-missing) filename resolves without throwing (ENOENT swallowed); called with `null`/`undefined` resolves as a pure no-op with zero filesystem calls
  </behavior>
  <action>
    Write `backend/src/services/photoStorage.service.test.js` FIRST with the behaviors above as failing tests (the service module does not exist yet, so every test fails on import — this is the RED state; record that the run fails before continuing).
    Then create `backend/src/services/photoStorage.service.js` exporting named functions `generatePhotoFilename(ext)`, `resolvePhotoPath(filename)`, `writePhotoFile(buffer, filename)`, `deletePhotoFile(filename)`, using `node:crypto`'s `randomUUID()` (same import convention as `auth.js`), `node:fs/promises`, and `photoStorageConfig.photosDir` imported from `../config/photoStorage.js`. `deletePhotoFile` must return immediately (no `fs` call) when `filename` is nullish, and must catch and swallow ONLY `err.code === 'ENOENT'`, rethrowing every other error. Run the suite again to confirm GREEN.
    Follow `familyMember.scope.test.js`'s test-file shape: `beforeEach`/`afterEach` cleanup of any files this test creates under `photoStorageConfig.photosDir` (real filesystem I/O against isolated state, no mocking — matching this codebase's existing "real I/O" test philosophy).
  </action>
  <acceptance_criteria>
    - `photoStorage.service.test.js` exists with at least 5 `it(` blocks covering the 4 behaviors above
    - `photoStorage.service.js` imports `randomUUID` from `node:crypto` (grep confirms)
    - `photoStorage.service.js`'s `deletePhotoFile` source contains the literal substring `'ENOENT'`
    - `npx vitest run src/services/photoStorage.service.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio/backend && npx vitest run src/services/photoStorage.service.test.js</automated>
  </verify>
  <done>All 4 primitive functions implemented and green; deletePhotoFile is ENOENT-safe and null-safe; no client input reachable in either function's signature.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: finalizePhotoReplacement — Pitfall 3's write-then-commit-then-cleanup ordering</name>
  <files>backend/src/services/photoStorage.service.js, backend/src/services/photoStorage.service.test.js</files>
  <read_first>backend/src/services/photoStorage.service.js (Task 1 output, same file)</read_first>
  <behavior>
    - `finalizePhotoReplacement({ buffer, ext, previousFilename, commit })`: writes the new file to disk under a freshly generated filename BEFORE calling `commit(newFilename)`
    - If `commit(newFilename)` resolves: deletes `previousFilename` (best-effort, ENOENT-safe, via `deletePhotoFile`) and returns `newFilename`
    - If `commit(newFilename)` rejects: deletes the just-written NEW file (best-effort) and rethrows the original error unchanged; `previousFilename` is left completely untouched
    - When `previousFilename` is `null`/`undefined` (first-ever upload — no prior photo, D-11's baseline case): the delete-old step is skipped entirely, no `deletePhotoFile(null)` side effects
    - After a full successful replace-cycle (old file present, then replaced), exactly one file remains on disk under `photoStorageConfig.photosDir` for that logical "slot" — never two
  </behavior>
  <action>
    Add failing tests to `photoStorage.service.test.js` FIRST for the 5 behaviors above, using a `vi.fn()` for `commit` that resolves or rejects on demand (RED — `finalizePhotoReplacement` does not exist yet).
    Then implement `finalizePhotoReplacement` in `photoStorage.service.js`, composing `generatePhotoFilename`, `writePhotoFile`, and `deletePhotoFile` from Task 1 exactly per the ordering above (Pitfall 3 in 16-RESEARCH.md). Run to GREEN.
  </action>
  <acceptance_criteria>
    - `photoStorage.service.js` exports `finalizePhotoReplacement`
    - A test asserts exactly one file remains on disk after a successful replace (old file gone via `fs.access` rejecting, new file present via `fs.access` resolving)
    - A test asserts the new file is deleted and the old file is UNTOUCHED (`fs.access` still resolves for it) when `commit` rejects
    - A test asserts no `deletePhotoFile` filesystem call occurs when `previousFilename` is `null`
    - `npx vitest run src/services/photoStorage.service.test.js` exits 0 (all Task 1 + Task 2 tests green together)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio/backend && npx vitest run src/services/photoStorage.service.test.js</automated>
  </verify>
  <done>finalizePhotoReplacement correctly sequences write-new → commit → cleanup-old-or-discard-new-on-failure; no orphaned blobs in either the success or failure path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem write/delete surface | This service is the ONLY code in the backend that touches `photoStorageConfig.photosDir` directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | `generatePhotoFilename`/`resolvePhotoPath` | mitigate | Filenames are exclusively `crypto.randomUUID()` + a caller-supplied extension (never client input at this layer — the caller, Plan 16-04, is responsible for sourcing `ext` only from `file-type`'s detected value, not `req.file.originalname`) |
| T-16-04 | Denial of Service | Orphaned files accumulating on the volume | mitigate | `finalizePhotoReplacement`'s ordering guarantees at most one live file per replace cycle; failed commits discard the new file rather than leaving it stranded |
| T-16-05 | Repudiation | Silent data loss if `deletePhotoFile` swallows a non-ENOENT error | mitigate | Only `err.code === 'ENOENT'` is caught; every other filesystem error propagates to the caller |
</threat_model>

<verification>
`cd backend && npx vitest run src/services/photoStorage.service.test.js` exits 0.
</verification>

<success_criteria>
- All 4 primitive functions + finalizePhotoReplacement implemented, fully unit-tested, red before green
- No orphaned files possible in either the replace-success or replace-failure path
</success_criteria>

<output>
Create `.planning/phases/16-photo-upload/16-02-SUMMARY.md` when done
</output>
