---
phase: 16-photo-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/package.json
  - backend/package-lock.json
  - backend/src/config/photoStorage.js
  - backend/migrations/manual/013-add-family-members-profile-picture.sql
  - backend/src/models/FamilyMember.js
  - backend/src/models/database.test.js
  - backend/test/fixtures/images.js
  - backend/test/fixtures/images.test.js
  - .gitignore
autonomous: true
requirements: [QUAL-01]

must_haves:
  truths:
    - "backend/src/config/photoStorage.js resolves a storage directory path the same way whether launched by npm start, nodemon, or the Docker CMD (via __dirname, never process.cwd())"
    - "A real, already-provisioned MySQL database can have profilePicture added to family_members via a tracked manual ALTER, not assumed to apply via sequelize.sync()"
    - "Real (non-synthetic) JPEG/PNG/WebP byte fixtures exist for every later adversarial/happy-path upload test to reuse"
  artifacts:
    - path: "backend/migrations/manual/013-add-family-members-profile-picture.sql"
      provides: "Manual ALTER TABLE adding profilePicture to family_members"
      contains: "ALTER TABLE family_members ADD COLUMN profilePicture"
    - path: "backend/src/models/FamilyMember.js"
      provides: "profilePicture nullable STRING column on the Sequelize model"
      contains: "profilePicture"
    - path: "backend/src/config/photoStorage.js"
      provides: "photoStorageConfig.photosDir resolved via __dirname"
      exports: ["photoStorageConfig"]
    - path: "backend/test/fixtures/images.js"
      provides: "Real JPEG/PNG/WebP/non-image byte buffers for upload tests"
  key_links:
    - from: "backend/src/models/database.test.js"
      to: "backend/src/models/FamilyMember.js"
      via: "profilePicture round-trip test"
      pattern: "profilePicture"
    - from: "backend/test/fixtures/images.js"
      to: "file-type (fileTypeFromBuffer)"
      via: "self-verifying fixture test"
      pattern: "fileTypeFromBuffer"
---

<objective>
Lay the backend foundation every later photo-upload plan builds on: install the two new backend dependencies (multer, file-type), define where uploaded bytes live on disk (mirroring env.js's `__dirname`-relative resolution), add the `profilePicture` column to the pre-existing `family_members` table via the same tracked manual-ALTER + boot-verify pattern already established for `users.familyMemberId` (Phase 13), and produce real (not synthetic) image byte fixtures the rest of the phase's adversarial and happy-path tests will import.

Purpose: `sequelize.sync()` never alters an already-provisioned table (STATE.md's standing blocker) — this plan closes that gap for `profilePicture` before any route logic is built on top of a column that might not exist in a real deployed database. It also removes the biggest hidden risk in later TDD plans: untested/synthetic image fixtures whose "valid JPEG" is a lie.
Output: `photoStorageConfig`, the `013` migration, the model column + boot-verify test, and `backend/test/fixtures/images.js`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-photo-upload/16-CONTEXT.md
@.planning/phases/16-photo-upload/16-RESEARCH.md
@.planning/phases/16-photo-upload/16-PATTERNS.md
</context>

<interfaces>
From backend/src/config/env.js:1-10 (the `__dirname`-relative resolution pattern this plan's config module must mirror exactly):
```
import path from 'node:path';
import { fileURLToPath } from 'node:url';
const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
const defaultEnvPath = path.resolve(__dirname, '../../../env/local.env');
```

From backend/migrations/manual/012-add-users-family-member-id.sql (full file, the exact precedent to mirror — header-comment convention + plain ALTER, no transaction wrapper, no rollback script):
```
ALTER TABLE users ADD COLUMN familyMemberId INT UNSIGNED NULL DEFAULT NULL;
ALTER TABLE users ADD CONSTRAINT users_familyMemberId_unique UNIQUE (familyMemberId);
ALTER TABLE users ADD CONSTRAINT users_familyMemberId_fk FOREIGN KEY (familyMemberId) REFERENCES family_members(id) ON DELETE SET NULL ON UPDATE CASCADE;
```

From backend/src/models/FamilyMember.js:43-50 (the nearest sibling nullable-string field shape to copy for `profilePicture`):
```
phone: { type: DataTypes.STRING, allowNull: true },
address: { type: DataTypes.STRING, allowNull: true },
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install multer + file-type, add photoStorage config module</name>
  <files>backend/package.json, backend/package-lock.json, backend/src/config/photoStorage.js</files>
  <read_first>backend/src/config/env.js, backend/package.json</read_first>
  <action>
    Run `npm install multer@^2.2.0 file-type@^22.0.1 --workspace backend` — scoped to the backend workspace only (RESEARCH.md's explicit process note: `slopcheck install` previously installed these at the repo root by accident during research and had to be reverted; do NOT repeat that mistake — confirm the ROOT package.json/package-lock.json remain untouched after install).
    Create `backend/src/config/photoStorage.js` exporting a single `photoStorageConfig` object with one field, `photosDir`, resolved via `path.resolve(__dirname, '../../storage/photos')` using the exact `fileURLToPath(import.meta.url)` + `path.dirname` pattern from `env.js:1-10` — never `process.cwd()` (the same relative path must resolve correctly whether launched by `npm start`, `nodemon`, or the Docker `CMD` per `backend/Dockerfile`'s `WORKDIR /app/backend`).
  </action>
  <acceptance_criteria>
    - `backend/package.json` `dependencies` contains `"multer"` and `"file-type"`
    - Root `/Users/bisrat/Projects/portofolio/package.json` has NOT gained `multer`/`file-type` as dependencies
    - `backend/src/config/photoStorage.js` exports `photoStorageConfig` with a `photosDir` property ending in `storage/photos`
    - `node --input-type=module -e "import('./backend/src/config/photoStorage.js').then(m => { if (!m.photoStorageConfig.photosDir.endsWith('storage/photos')) throw new Error('bad path'); console.log('ok'); })"` prints `ok`
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module -e "import('/Users/bisrat/Projects/portofolio/backend/src/config/photoStorage.js').then(m => { if (!m.photoStorageConfig.photosDir.endsWith('storage/photos')) process.exit(1); console.log('ok'); })"</automated>
  </verify>
  <done>multer and file-type installed scoped to backend; photoStorageConfig.photosDir resolves via __dirname to backend/storage/photos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [BLOCKING] Manual ALTER migration + profilePicture model column + boot-verify test</name>
  <files>backend/migrations/manual/013-add-family-members-profile-picture.sql, backend/src/models/FamilyMember.js, backend/src/models/database.test.js</files>
  <read_first>backend/migrations/manual/012-add-users-family-member-id.sql, backend/src/models/FamilyMember.js, backend/src/models/database.test.js</read_first>
  <behavior>
    - `models.FamilyMember.create({..., profilePicture: 'abc.jpg'})` then `.reload()` returns `profilePicture === 'abc.jpg'`
    - `models.FamilyMember.create({...})` with no `profilePicture` supplied defaults to `null` (no backfill, matches D-10's placeholder-by-default state)
    - This is the load-bearing proof that a real, already-provisioned database (not just the CI force-recreated test DB) needs the manual `013` ALTER — `sequelize.sync()` alone would silently do nothing there (Pitfall 4)
  </behavior>
  <action>
    Create `backend/migrations/manual/013-add-family-members-profile-picture.sql` mirroring `012`'s header-comment convention (explain: sync() never alters an existing table; why nullable with no backfill — every existing member legitimately has no photo until someone uploads one) followed by exactly `ALTER TABLE family_members ADD COLUMN profilePicture VARCHAR(255) NULL DEFAULT NULL;` (simpler than 012 — no UNIQUE/FK, per RESEARCH.md's Pattern Map).
    Add `profilePicture: { type: DataTypes.STRING, allowNull: true }` to `FamilyMember.init(...)`'s field object in `backend/src/models/FamilyMember.js`, in the same style as the existing `phone`/`address` fields (lines 43-50) — place it directly after `address`, before the `fullname` VIRTUAL getter.
    Extend `backend/src/models/database.test.js` with a new `describe('profilePicture column (PHOTO-02)', ...)` block (mirroring the existing `familyMemberId link column (ACC-05)` block's shape) containing the two behaviors above.
  </action>
  <acceptance_criteria>
    - `backend/migrations/manual/013-add-family-members-profile-picture.sql` contains the exact string `ALTER TABLE family_members ADD COLUMN profilePicture VARCHAR(255) NULL DEFAULT NULL;`
    - `backend/src/models/FamilyMember.js` contains a `profilePicture:` field with `allowNull: true`
    - `backend/src/models/database.test.js` contains a `describe('profilePicture column (PHOTO-02)'` block with 2 `it(` tests
    - `npx vitest run src/models/database.test.js -t "profilePicture"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio/backend && npx vitest run src/models/database.test.js -t "profilePicture"</automated>
  </verify>
  <done>013 migration file exists mirroring 012's precedent; FamilyMember model has profilePicture; boot-verify integration test proves round-trip + null-default, green.</done>
</task>

<task type="auto">
  <name>Task 3: Real image byte fixtures for upload tests</name>
  <files>backend/test/fixtures/images.js, backend/test/fixtures/images.test.js</files>
  <read_first>backend/test/familyTreeFactory.js</read_first>
  <action>
    Create `backend/test/fixtures/images.js` exporting named `Buffer` constants: `validJpegBuffer`, `validPngBuffer`, `validWebpBuffer` — each a small, REAL (not hand-rolled synthetic) minimal valid image of its format, each with a doc-comment above it stating what it represents (mirroring `familyTreeFactory.js`'s doc-comment convention) — plus `nonImageBuffer` (real non-image bytes, e.g. a shell-script-like text buffer, for the mislabeled-content-type adversarial vector) and a function `oversizedImageBuffer()` returning `Buffer.alloc(5 * 1024 * 1024 + 1, 0xff)` (one byte over the 5 MB cap, D-04).
    Create `backend/test/fixtures/images.test.js` that imports `fileTypeFromBuffer` from `file-type` (installed in Task 1) and asserts `validJpegBuffer`/`validPngBuffer`/`validWebpBuffer` are each detected as `image/jpeg`/`image/png`/`image/webp` respectively, and that `nonImageBuffer` is detected as `undefined` or a non-image mime — this is the fixture self-check that proves the bytes are real, not synthetic garbage (Wave 0 gap requirement).
  </action>
  <acceptance_criteria>
    - `backend/test/fixtures/images.js` exports `validJpegBuffer`, `validPngBuffer`, `validWebpBuffer`, `nonImageBuffer`, `oversizedImageBuffer` — all `Buffer` instances (or a function returning one)
    - `npx vitest run test/fixtures/images.test.js` exits 0, proving each fixture's magic-byte-detected mime matches its filename
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio/backend && npx vitest run test/fixtures/images.test.js</automated>
  </verify>
  <done>Real JPEG/PNG/WebP/non-image fixtures exist and are self-verified via file-type's actual magic-byte detection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy-time schema change | The `013` manual ALTER runs against a real, already-provisioned database outside CI's force-recreate path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | `family_members.profilePicture` schema drift between test DB and real deployed DB | mitigate | Manual `013` migration + integration boot-verify test proving round-trip, mirroring the exact `012`/ACC-05 precedent (Pitfall 4) |
| T-16-02 | Tampering | Test fixtures silently not representing real image bytes | mitigate | `images.test.js` self-verifies every fixture's real magic-byte type via `file-type` before any later plan trusts them |
| T-16-SC | Tampering | npm supply-chain risk from `multer`/`file-type` installs | accept | Both packages already vetted `[OK]` by `slopcheck` in 16-RESEARCH.md's Package Legitimacy Audit (11+/10+ year old, `expressjs`/`sindresorhus` orgs, no `postinstall` scripts) — no blocking checkpoint needed, install scoped to `backend` workspace only per the research process note |
</threat_model>

<verification>
`npx vitest run src/models/database.test.js test/fixtures/images.test.js` (from `backend/`) exits 0.
</verification>

<success_criteria>
- multer + file-type installed scoped to the backend workspace only
- photoStorageConfig.photosDir resolves via __dirname, matching the Docker WORKDIR
- 013 migration + profilePicture column + boot-verify test all green
- Real (file-type-verified) JPEG/PNG/WebP/non-image fixtures exist for every later plan to import
</success_criteria>

<output>
Create `.planning/phases/16-photo-upload/16-01-SUMMARY.md` when done
</output>
