---
phase: 16-photo-upload
plan: 06
type: execute
wave: 5
depends_on: ["16-05"]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vite.config.js
  - frontend/src/api/photoClient.js
  - frontend/src/api/photoClient.test.js
  - frontend/src/components/manage/MemberAvatarImage.jsx
  - frontend/src/components/manage/MemberAvatarImage.test.jsx
  - frontend/src/components/manage/PhotoCropDialog.jsx
  - frontend/src/components/manage/PhotoCropDialog.test.jsx
autonomous: true
requirements: [PHOTO-01]

must_haves:
  truths:
    - "The frontend can authenticate a photo GET/POST/DELETE request using the same Bearer-token mechanism graphqlClient already uses, without a new backend auth mechanism"
    - "A member with no photo renders a generic person icon placeholder, never initials (D-10)"
    - "A picked file is cropped client-side into a square JPEG under 5 MB before it is ever sent to the server (D-02/D-05)"
  artifacts:
    - path: "frontend/src/api/photoClient.js"
      provides: "uploadMemberPhoto, removeMemberPhoto, fetchMemberPhotoBlob, attachAuthHeader"
      exports: ["uploadMemberPhoto", "removeMemberPhoto", "fetchMemberPhotoBlob", "attachAuthHeader"]
    - path: "frontend/src/components/manage/MemberAvatarImage.jsx"
      provides: "Authenticated blob-fetch avatar renderer (D-09)"
    - path: "frontend/src/components/manage/PhotoCropDialog.jsx"
      provides: "Crop/preview dialog (D-02) mirroring EditMemberDialog's shell"
  key_links:
    - from: "frontend/src/components/manage/MemberAvatarImage.jsx"
      to: "frontend/src/api/photoClient.js (fetchMemberPhotoBlob)"
      via: "authenticated blob fetch"
      pattern: "fetchMemberPhotoBlob"
    - from: "frontend/src/components/manage/PhotoCropDialog.jsx"
      to: "frontend/src/api/photoClient.js (uploadMemberPhoto)"
      via: "cropped-blob multipart POST"
      pattern: "uploadMemberPhoto"
---

<objective>
Build the three new, reusable frontend primitives this phase introduces — a photo-specific axios client reusing the existing Bearer-token pattern, the authenticated blob-fetch avatar renderer (D-09), and the crop/preview dialog (D-02) — before wiring them into the existing `MemberCard`/`ManagePage` surfaces (16-07). Building these first (interface-first ordering) means the wiring plan consumes finished, tested components rather than developing them inline.

Purpose: this app has no authenticated-`<img>` pattern and no multipart-upload client today — both are genuinely new capabilities that need their own dedicated build-and-test pass per 16-PATTERNS.md's "No Analog Found" section.
Output: `photoClient.js`, `MemberAvatarImage.jsx`, `PhotoCropDialog.jsx`, each with component test coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-photo-upload/16-CONTEXT.md
@.planning/phases/16-photo-upload/16-UI-SPEC.md
@.planning/phases/16-photo-upload/16-RESEARCH.md
</context>

<interfaces>
From frontend/src/api/graphqlClient.js (full file — the exact axios-instance + Bearer-interceptor pattern `photoClient.js` mirrors):
```
const graphqlClient = axios.create({ baseURL: normalizeGraphqlUrl(import.meta.env.VITE_API_URL), headers: { 'Content-Type': 'application/json' } });
graphqlClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('authToken');
  if (token) config.headers.Authorization = `Bearer ${token}`;
  return config;
});
```

From frontend/vite.config.js (full file, current):
```
const proxyTarget = process.env.VITE_PROXY_TARGET || process.env.BACKEND_URL || 'http://localhost:4000';
export default defineConfig({
  plugins: [react()],
  server: { proxy: { '/graphql': { target: proxyTarget, changeOrigin: true, secure: false } } }
});
```

From frontend/src/components/manage/EditMemberDialog.test.jsx:1-11 (the `vi.mock` convention this plan's component tests must follow):
```
vi.mock('../../api/graphqlClient.js', () => ({ graphqlRequest: vi.fn() }));
import { graphqlRequest } from '../../api/graphqlClient.js';
```

From frontend/src/components/manage/MemberCard.jsx:16 (current placeholder avatar styling — reuse these exact values, not new tokens): `sx={{ width: 42, height: 42, bgcolor: '#eef1f8', color: colors.slate }}`.

From frontend/src/pages/ManagePage.jsx:495 (the non-dismissible-while-submitting `onClose` guard `PhotoCropDialog` must mirror): `onClose={() => (deleting ? null : setDeleteTarget(null))}`.

Backend route contract this plan's client targets (established in 16-04/16-05): `POST /api/family-members/:id/photo` (multipart field name `photo`) returns `{ photoUrl }`; `DELETE /api/family-members/:id/photo` returns `{ photoUrl: null }`; `GET /api/family-members/:id/photo` returns the raw image bytes.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: react-easy-crop install, vite /api proxy, photoClient.js</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/vite.config.js, frontend/src/api/photoClient.js, frontend/src/api/photoClient.test.js</files>
  <read_first>frontend/src/api/graphqlClient.js, frontend/vite.config.js, frontend/package.json</read_first>
  <action>
    Run `npm install react-easy-crop@^6.2.2 --workspace frontend`.
    Add a second proxy entry to `frontend/vite.config.js`'s `server.proxy` object: `'/api': { target: proxyTarget, changeOrigin: true, secure: false }` (reusing the existing `proxyTarget` constant, alongside the current `/graphql` entry).
    Create `frontend/src/api/photoClient.js`: export a named function `attachAuthHeader(config)` that reads `localStorage.getItem('authToken')` and sets `config.headers.Authorization = \`Bearer ${token}\`` when present (returning `config` unchanged otherwise) — this is the SAME logic as `graphqlClient.js`'s interceptor, extracted into a standalone, directly-unit-testable function. Create a second axios instance, `const photoClient = axios.create();` (no `baseURL` override — resolves relative `/api/...` paths against the page's own origin, unlike `graphqlClient`'s GraphQL-specific `baseURL`), and register `photoClient.interceptors.request.use(attachAuthHeader)`. Export named async functions: `uploadMemberPhoto(memberId, blob)` — builds a `FormData` with field name `photo` (matching the backend's `upload.single('photo')`) and `POST`s to `` `/api/family-members/${memberId}/photo` ``, returning `response.data`; `removeMemberPhoto(memberId)` — `DELETE`s the same path, returning `response.data`; `fetchMemberPhotoBlob(photoUrl)` — `GET`s `photoUrl` with `{ responseType: 'blob' }`, returning `response.data`. Mirror `graphqlRequest`'s "Network Error" special-case (graphqlClient.js lines 30-34) in all three: catch and re-throw a more actionable message when `error.message === 'Network Error'`.
    Create `frontend/src/api/photoClient.test.js` with tests: `attachAuthHeader` adds `Authorization: Bearer <token>` when `localStorage.getItem('authToken')` returns a token; `attachAuthHeader` leaves `headers.Authorization` unset when no token is stored.
  </action>
  <acceptance_criteria>
    - `frontend/package.json` `dependencies` contains `"react-easy-crop"`
    - `frontend/vite.config.js`'s `server.proxy` object contains an `'/api'` key
    - `photoClient.js` exports `uploadMemberPhoto`, `removeMemberPhoto`, `fetchMemberPhotoBlob`, `attachAuthHeader`
    - `npx vitest run src/api/photoClient.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio/frontend && npx vitest run src/api/photoClient.test.js</automated>
  </verify>
  <done>react-easy-crop installed, /api proxied in dev, photoClient.js exports a tested Bearer-auth interceptor and 3 named request functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MemberAvatarImage.jsx — authenticated blob-fetch avatar (D-09/D-10)</name>
  <files>frontend/src/components/manage/MemberAvatarImage.jsx, frontend/src/components/manage/MemberAvatarImage.test.jsx</files>
  <read_first>frontend/src/components/manage/MemberCard.jsx, frontend/src/components/manage/EditMemberDialog.test.jsx, frontend/src/theme.js</read_first>
  <behavior>
    - no `member.photoUrl` → renders a placeholder `Avatar` (42x42, `bgcolor: '#eef1f8'`) containing `PersonRoundedIcon` (`aria-hidden`, color `colors.slate`); the `Avatar` itself carries `alt={member.fullname}`; NO skeleton is ever shown for a photo-less member
    - `member.photoUrl` present, fetch pending → renders `<Skeleton variant="circular" width={42} height={42} />`
    - `member.photoUrl` present, fetch resolves → renders `<Avatar src={objectUrl} alt={member.fullname} sx={{ width: 42, height: 42 }} />`; `URL.revokeObjectURL` is called on unmount and whenever `photoUrl` changes
    - fetch rejects → silently falls back to the placeholder state (no `Alert`, no console error visible to the user)
  </behavior>
  <action>
    Create `frontend/src/components/manage/MemberAvatarImage.jsx`: props `{ member }`; on mount and whenever `member.photoUrl` changes, if `photoUrl` is falsy, skip fetching and render the placeholder immediately; otherwise set a `fetching` state true, call `fetchMemberPhotoBlob(member.photoUrl)` (imported from `../../api/photoClient.js`, NOT `graphqlClient.js`), on success `URL.createObjectURL(blob)` into local state and clear `fetching`; on failure clear `fetching` and leave `objectUrl` `null` (silent fallback); track the current object URL in a ref/cleanup function that calls `URL.revokeObjectURL` on unmount/change; import `PersonRoundedIcon` from `@mui/icons-material/PersonRounded` (NOT the generic `Person` icon).
    Create `frontend/src/components/manage/MemberAvatarImage.test.jsx` mocking `../../api/photoClient.js` (`vi.mock` pattern from `EditMemberDialog.test.jsx`), covering the 4 behaviors above.
  </action>
  <acceptance_criteria>
    - `MemberAvatarImage.jsx` imports `PersonRoundedIcon` from `@mui/icons-material/PersonRounded`
    - `MemberAvatarImage.jsx` imports `fetchMemberPhotoBlob` from `../../api/photoClient.js`
    - `MemberAvatarImage.test.jsx` contains at least 4 `it(` blocks matching the 4 behaviors
    - `npx vitest run src/components/manage/MemberAvatarImage.test.jsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio/frontend && npx vitest run src/components/manage/MemberAvatarImage.test.jsx</automated>
  </verify>
  <done>Placeholder is an icon (never initials); skeleton only appears for members with a photoUrl; object URLs are revoked on cleanup; fetch failure degrades silently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PhotoCropDialog.jsx — crop/preview dialog (D-02)</name>
  <files>frontend/src/components/manage/PhotoCropDialog.jsx, frontend/src/components/manage/PhotoCropDialog.test.jsx</files>
  <read_first>frontend/src/components/manage/EditMemberDialog.jsx, frontend/src/pages/ManagePage.jsx, frontend/src/api/photoClient.js</read_first>
  <behavior>
    - renders a `react-easy-crop` `Cropper` with `aspect={1}`, `cropShape="round"`, `showGrid={false}` inside a `Box` sized `sx={{ position: 'relative', height: { xs: 320, sm: 360 }, bgcolor: '#0f172a' }}`
    - renders a `Slider` with `aria-label="Zoom"`, `min={1}`, `max={3}`, `step={0.1}`
    - renders helper text `"Drag to reposition, zoom to frame. JPEG, PNG or WebP · max 5 MB."` (`body2`, `text.secondary`)
    - renders `Save photo` / `Cancel` buttons; submitting sets `Save photo` → `Uploading…`, disables both buttons, and the dialog's `onClose` no-ops while submitting (mirrors `ManagePage.jsx`'s delete-confirm guard)
    - Cancel discards the selection and calls `onClose` WITHOUT ever calling `uploadMemberPhoto`
    - on submit: draws the cropped region onto an off-screen 512×512 `<canvas>` using the `onCropComplete` pixel bounds, calls `canvas.toBlob(fn, 'image/jpeg', 0.9)`, then calls `uploadMemberPhoto(member.id, blob)`; success calls `onUploaded()` and closes; failure renders `<Alert severity="error">` with the thrown message and re-enables both buttons
  </behavior>
  <action>
    Create `frontend/src/components/manage/PhotoCropDialog.jsx` mirroring `EditMemberDialog.jsx`'s `<Dialog fullWidth maxWidth="sm">` → `<DialogTitle>Crop photo</DialogTitle>` → `<DialogContent><Stack spacing={2} sx={{ pt: 1 }}>` shell (error `Alert` first in the stack, action row last: `Save photo` primary contained, `Cancel` text). Props: `{ open, file, member, onClose, onUploaded }` where `file` is the raw picked `File`. Manage local `crop`/`zoom`/`submitting`/`error` state; `onClose={() => (submitting ? null : handleCancel())}`. Import `uploadMemberPhoto` from `../../api/photoClient.js` and `Cropper` from `'react-easy-crop'`.
    Create `frontend/src/components/manage/PhotoCropDialog.test.jsx` mocking `../../api/photoClient.js`, covering: renders `Save photo`/`Cancel` buttons; `Slider` has `aria-label="Zoom"`; submitting shows `Uploading…` and disables both buttons; `onClose` is not invoked while submitting; a rejected `uploadMemberPhoto` renders an error `Alert` and re-enables buttons; clicking `Cancel` never calls `uploadMemberPhoto`.
  </action>
  <acceptance_criteria>
    - `PhotoCropDialog.jsx` imports `Cropper` from `'react-easy-crop'`
    - `PhotoCropDialog.jsx` imports `uploadMemberPhoto` from `../../api/photoClient.js`
    - `PhotoCropDialog.test.jsx` contains at least 5 `it(` blocks matching the behaviors above
    - `npx vitest run src/components/manage/PhotoCropDialog.test.jsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio/frontend && npx vitest run src/components/manage/PhotoCropDialog.test.jsx</automated>
  </verify>
  <done>Crop dialog mirrors EditMemberDialog's shell exactly, is non-dismissible while submitting, and only ever uploads a client-cropped JPEG blob.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage → Authorization header | The same trust model graphqlClient.js already uses; no new mechanism introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-16 | Information Disclosure | JWT leakage via URL if a token-query-param approach were used instead | accept (avoided) | `fetchMemberPhotoBlob` uses the header-based Bearer pattern (D-09's recommendation), never puts the JWT in a URL |
| T-16-SC | Tampering | npm supply-chain risk from `react-easy-crop` | accept | Already vetted `[OK]` by `slopcheck` in 16-RESEARCH.md's Package Legitimacy Audit and re-confirmed in 16-UI-SPEC.md's Registry Safety section; no `postinstall` script |
</threat_model>

<verification>
`cd frontend && npx vitest run src/api/photoClient.test.js src/components/manage/MemberAvatarImage.test.jsx src/components/manage/PhotoCropDialog.test.jsx` exits 0.
</verification>

<success_criteria>
- photoClient.js reuses the existing Bearer-auth pattern with zero new backend mechanism
- MemberAvatarImage never shows initials, only an icon placeholder or the real photo
- PhotoCropDialog mirrors EditMemberDialog's shell and only ever uploads a cropped, size-bounded JPEG
</success_criteria>

<output>
Create `.planning/phases/16-photo-upload/16-06-SUMMARY.md` when done
</output>
