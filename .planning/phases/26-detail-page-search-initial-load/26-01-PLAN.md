---
phase: 26-detail-page-search-initial-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/DetailPage.jsx
  - frontend/src/pages/DetailPage.test.jsx
  - frontend/src/App.jsx
  - frontend/src/components/AppLayout.jsx
autonomous: true
requirements: [DETAIL-01, DETAIL-02, DETAIL-03]

must_haves:
  truths:
    - "An authenticated user can navigate to /detail; an unauthenticated user is redirected to login like every protected route (DETAIL-01, D-06)"
    - "On first load /detail shows only the family head's PersonCard via the head-id then person-by-id flow, with no descendants expanded (DETAIL-02, D-04)"
    - "loading, failed-request, missing-family-head, and missing-person-info states each render via an existing MUI component — never an empty or broken card (DETAIL-03, D-08)"
    - "A /detail entry point appears in the AppLayout top nav alongside Dashboard/Family/Manage (D-07)"
  artifacts:
    - path: "frontend/src/pages/DetailPage.jsx"
      provides: "Protected /detail page: familyHead -> familyMember(id) initial load, mainPerson state, the five page-level D-08 states, PersonCard composition"
      min_lines: 70
    - path: "frontend/src/pages/DetailPage.test.jsx"
      provides: "Vitest+RTL coverage of loading, error+retry, missing-head, and the two-round-trip happy path"
    - path: "frontend/src/App.jsx"
      provides: "detail Route mounted inside the <ProtectedRoute /> group"
      contains: "path=\"detail\""
    - path: "frontend/src/components/AppLayout.jsx"
      provides: "Detail nav Button to /detail"
      contains: "to=\"/detail\""
  key_links:
    - from: "frontend/src/pages/DetailPage.jsx"
      to: "familyHead / familyMember(id)"
      via: "graphqlRequest"
      pattern: "graphqlRequest\\("
    - from: "frontend/src/App.jsx"
      to: "DetailPage"
      via: "Route inside ProtectedRoute group"
      pattern: "path=\"detail\""
---

<objective>
Build the `/detail` page shell: a protected route that opens on the family head
in a single Phase-25 `PersonCard`, using the uniform head-id → person-by-id load
path, with every page-level edge/empty state rendered through the app's existing
MUI components. Also wire the route into `App.jsx` and add the top-nav entry point.

Purpose: Deliver DETAIL-01 (protected reachability), DETAIL-02 (initial-load-on-head),
and DETAIL-03 (edge/empty states) so Phase 26-02 can drop the inline search into a
working page and reuse its `loadPersonById` path.

Output: `frontend/src/pages/DetailPage.jsx` (+ test), route in `App.jsx`, nav link in `AppLayout.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-detail-page-search-initial-load/26-CONTEXT.md
@.planning/phases/26-detail-page-search-initial-load/26-PATTERNS.md

<interfaces>
<!-- Backend query contract (backend/src/schemas/familyMember.schema.js lines 80-85) -->
familyHead: FamilyMember            # D-01: returns head id (bounded)
familyMember(id: ID!): FamilyMember # returns every card field (SC-2); null when absent
searchFamilyMembers(term: String!, limit: Int): [FamilyMember!]!  # used in plan 26-02

<!-- PersonCard contract (frontend/src/components/person/PersonCard.jsx line 46) -->
PersonCard({ member, role, spouse, isSpouse, expanded, onExpand, onEdit })
# Card gates the expand control on member.children?.length (line 80-81) and the
# edit button on member.canEdit === true (line 104). onExpand/onEdit are invoked
# UNGUARDED (lines 107, 153) — this page MUST pass no-op functions, not undefined.

<!-- graphqlRequest (frontend/src/api/graphqlClient.js lines 23-36) -->
graphqlRequest(query, variables) -> Promise<data>  # unwrapped response.data.data; throws Error on failure; auto-attaches Bearer token
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DetailPage page shell with initial-load-on-head and edge states</name>
  <files>frontend/src/pages/DetailPage.jsx, frontend/src/pages/DetailPage.test.jsx</files>
  <read_first>
    - frontend/src/pages/FamilyTreePage.jsx (page fetch/loading/error idiom: useCallback refetch, .then/.catch/.finally, CircularProgress, Alert + Retry Button — lines 46-119)
    - frontend/src/pages/FamilyTreePage.test.jsx (page mock+state harness, loading/error/data branch tests)
    - frontend/src/components/person/PersonCard.jsx (props contract; unguarded onEdit/onExpand at lines 107, 153)
    - frontend/src/components/person/PersonCard.test.jsx (card assertions: getByTestId person-card-<id>, Living/Deceased chip)
    - frontend/src/api/graphqlClient.js (graphqlRequest return/throw shape, lines 23-36)
    - backend/src/schemas/familyMember.schema.js (query names/args, lines 80-85)
  </read_first>
  <behavior>
    - Loading: while the initial fetch is in flight, renders CircularProgress (D-08 loading).
    - Happy path (D-04): calls familyHead first, reads head id, then calls familyMember(id) with { id }, then renders exactly one PersonCard for the head with role="Head" and no descendants. Assert graphqlRequest.mock.calls[0][0] contains "familyHead" and calls[1][1] equals { id: '<headId>' }.
    - failed-request (D-08): when a fetch rejects, renders Alert severity="error" plus a Retry Button that re-runs the load.
    - missing-family-head (D-08): when familyHead resolves null, renders Alert severity="info" with a "No family head found" message and does NOT call familyMember.
    - missing-person-info (D-08): when familyMember resolves null, renders a graceful Typography message, never an empty PersonCard.
  </behavior>
  <action>
    Create module-scope SCREAMING_SNAKE_CASE query constants `FAMILY_HEAD_QUERY`
    (`query FamilyHead { familyHead { id } }`) and `FAMILY_MEMBER_QUERY`
    (`query FamilyMember($id: ID!) { familyMember(id: $id) { id fullname geezFullname gender isAlive photoUrl canEdit spouses { id fullname geezFullname gender isAlive photoUrl } children { id } } }`)
    per D-04 and the PersonCard contract (children{id} gates expand, canEdit gates edit).
    Implement one uniform `loadPersonById(id)` in a `useCallback` that sets a loading
    flag, `graphqlRequest(FAMILY_MEMBER_QUERY, { id })`, sets `mainPerson` state on
    success (null → missing-person-info branch), sets an error string on catch, clears
    loading in finally. Implement the initial load in a `useEffect` that calls
    `graphqlRequest(FAMILY_HEAD_QUERY)`; if `familyHead` is null → set a
    missing-family-head flag (Alert severity="info", D-08); otherwise chain into
    `loadPersonById(familyHead.id)` (D-04 two-round-trip, single person-by-id code path
    reused later by 26-02's suggestion-select). Hold `mainPerson` in page state only —
    no URL param (D-05). Render branches in order: loading → CircularProgress in a
    centered Box; failed-request → Alert severity="error" + a Retry Button calling the
    loader again; missing-family-head → Alert severity="info" "No family head found";
    missing-person-info → graceful Typography; otherwise a top region placeholder Box
    (where 26-02 mounts the search) plus a centered `PersonCard` with `member={mainPerson}`,
    `role="Head"`, `spouse={mainPerson.spouses?.[0]}`, `expanded={false}`, and no-op
    `onExpand={() => {}}` / `onEdit={() => {}}` (expand is Phase 27, edit is Phase 28 —
    pass no-ops NOT undefined per the Phase 25 advisory). Match FamilyTreePage's
    centered-Box loading and Alert+Retry layout. Write DetailPage.test.jsx mirroring
    FamilyTreePage.test.jsx's harness (MemoryRouter initialEntries ['/detail'], mock
    ../api/graphqlClient.js and ../api/photoClient.js, mock ../context/AuthContext.jsx
    if DetailPage reads useAuth) covering all five behaviors above.
  </action>
  <verify>
    <automated>npm test --workspace frontend -- DetailPage</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- DetailPage` exits 0.
    - DetailPage.jsx declares FAMILY_HEAD_QUERY and FAMILY_MEMBER_QUERY as module-scope const template literals.
    - On the happy path the test asserts familyHead is called before familyMember and familyMember receives { id: '<headId>' }.
    - Loading renders CircularProgress; failed-request renders Alert severity="error" + Retry; missing-family-head renders Alert severity="info"; missing-person-info renders a Typography message with no person-card testid present.
    - The head PersonCard is passed no-op onExpand/onEdit (grep confirms `onExpand={() => {}}` and `onEdit={() => {}}` in DetailPage.jsx).
  </acceptance_criteria>
  <done>DetailPage renders the family head via the head-id→person-by-id path and all five page-level D-08 states, fully covered by passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the /detail route and add the top-nav entry point</name>
  <files>frontend/src/App.jsx, frontend/src/components/AppLayout.jsx</files>
  <read_first>
    - frontend/src/App.jsx (the <ProtectedRoute /> group + the /family sibling route, lines 27-35; page imports lines 4-14)
    - frontend/src/components/AppLayout.jsx (the Family tree nav Button, lines 52-58; the authenticated Stack lines 44-97; the default lg Container branch lines 109-116)
    - frontend/src/components/ProtectedRoute.jsx (no-args protected group vs allowedRoles gate)
  </read_first>
  <action>
    In `App.jsx` add `import DetailPage from './pages/DetailPage.jsx';` beside the other
    page imports, then add `<Route path="detail" element={<DetailPage />} />` INSIDE the
    existing no-args `<ProtectedRoute />` group (alongside dashboard/manage/family) so it
    is auth-gated for any authenticated user — do NOT use `allowedRoles={['ADMIN']}`
    (D-06, not admin-only). In `AppLayout.jsx` add a nav `Button component={RouterLink}
    to="/detail"` inside the authenticated `Stack` next to the Family tree button, with
    the same `sx` (color colors.slate, fontWeight 600, `display: { xs: 'none', sm: 'inline-flex' }`)
    and no role guard (D-07). Do not change the layout container: `/detail` uses the
    default `Container maxWidth="lg"` centered branch (head card centered, D-07) — do NOT
    add a `pathname === '/detail'` full-bleed/wide flag.
  </action>
  <verify>
    <automated>grep -q 'path="detail"' frontend/src/App.jsx && grep -q 'to="/detail"' frontend/src/components/AppLayout.jsx && npm test --workspace frontend</automated>
  </verify>
  <acceptance_criteria>
    - App.jsx imports DetailPage and contains a `<Route path="detail" ...>` nested inside the no-args `<ProtectedRoute />` element block (NOT inside the `allowedRoles={['ADMIN']}` group).
    - AppLayout.jsx renders a nav Button linking to `/detail` with no `user.role === 'ADMIN'` guard.
    - AppLayout.jsx has NO new `pathname === '/detail'` full-bleed/wide branch (default lg container unchanged).
    - `npm test --workspace frontend` exits 0 (no regressions).
  </acceptance_criteria>
  <done>/detail is reachable behind ProtectedRoute for any authenticated user, unauthenticated users redirect to login, and a Detail nav link appears in the top nav.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /detail route | Route access must be gated by auth, matching every other protected page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Elevation of Privilege | /detail route mount in App.jsx | mitigate | Mount inside the existing no-args `<ProtectedRoute />` group so unauthenticated users redirect to login exactly like `/family` (DETAIL-01, D-06). Primary control for this phase. |
| T-26-02 | Information Disclosure | familyHead / familyMember(id) reads | accept | Reuses existing Phase-24 authenticated resolvers behind the same family-access guards; no new field, no new read surface introduced here. |
</threat_model>

<verification>
- `npm test --workspace frontend -- DetailPage` passes.
- `npm test --workspace frontend` passes (no regressions).
- Manual grep: `path="detail"` in App.jsx (inside ProtectedRoute group) and `to="/detail"` in AppLayout.jsx.
</verification>

<success_criteria>
DETAIL-01: /detail is protected and reachable via a nav link; unauthenticated → login.
DETAIL-02: first load shows only the head's PersonCard, no descendants.
DETAIL-03: loading, failed-request, missing-family-head, missing-person-info all render via existing components.
</success_criteria>

<output>
Create `.planning/phases/26-detail-page-search-initial-load/26-01-SUMMARY.md` when done.
</output>
