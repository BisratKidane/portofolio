---
phase: 26-detail-page-search-initial-load
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - frontend/src/components/person/PersonSearch.jsx
  - frontend/src/components/person/PersonSearch.test.jsx
  - frontend/src/pages/DetailPage.jsx
  - frontend/src/pages/DetailPage.test.jsx
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03]

must_haves:
  truths:
    - "Typing in the search bar drives debounced live suggestions via searchFamilyMembers, matching partial/full Latin (case-insensitive) or Ge'ez first/last names with the raw term passed straight through (SEARCH-01, D-01/D-02)"
    - "Each suggestion row shows avatar, full Latin name, full Ge'ez name (when present via getGeezDisplay), birth year, and family context (SEARCH-02, D-03)"
    - "Selecting a suggestion runs the same familyMember(id) load, clears the current view, and shows only that person's PersonCard with descendants collapsed (SEARCH-03, D-05)"
    - "When a search returns no matches, an inline 'No matches' message renders under the bar (D-08 no-search-results)"
  artifacts:
    - path: "frontend/src/components/person/PersonSearch.jsx"
      provides: "Debounced async MUI Autocomplete: searchFamilyMembers-driven options, rich renderOption rows, onSelect(id) callback"
      min_lines: 50
    - path: "frontend/src/components/person/PersonSearch.test.jsx"
      provides: "Vitest+RTL coverage: debounced fetch, Latin + Ge'ez term pass-through, rich row content, no-matches, select→onSelect"
    - path: "frontend/src/pages/DetailPage.jsx"
      provides: "PersonSearch mounted in the top region, onSelect wired to loadPersonById (view clears, new main person, descendants collapsed)"
      contains: "PersonSearch"
  key_links:
    - from: "frontend/src/components/person/PersonSearch.jsx"
      to: "searchFamilyMembers(term)"
      via: "debounced graphqlRequest"
      pattern: "searchFamilyMembers|SEARCH_MEMBERS_QUERY"
    - from: "frontend/src/pages/DetailPage.jsx"
      to: "PersonSearch onSelect"
      via: "loadPersonById"
      pattern: "onSelect=\\{"
---

<objective>
Add the inline live-search bar to `/detail`: a debounced MUI `Autocomplete` whose
options come from an async `searchFamilyMembers(term)` fetch (Latin + Ge'ez, raw
term straight through), with rich disambiguating suggestion rows, and selection
that resets the main person via the uniform `loadPersonById` path built in 26-01.

Purpose: Deliver SEARCH-01 (Latin+Ge'ez match), SEARCH-02 (inline rich suggestions),
and SEARCH-03 (select → new main person, descendants collapsed).

Output: `frontend/src/components/person/PersonSearch.jsx` (+ test) and the wiring in `DetailPage.jsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-detail-page-search-initial-load/26-CONTEXT.md
@.planning/phases/26-detail-page-search-initial-load/26-PATTERNS.md
@.planning/phases/26-detail-page-search-initial-load/26-01-PLAN.md

<interfaces>
<!-- Backend search query (backend/src/schemas/familyMember.schema.js line 85) -->
searchFamilyMembers(term: String!, limit: Int): [FamilyMember!]!
# Backend already does partial, case-insensitive Latin AND Ge'ez first/last matching
# server-side (mother's-name excluded), capped ~20, name-sorted. Pass the raw term —
# NO client-side language detection/transliteration (D-02). Client must NOT re-filter.

<!-- getGeezDisplay (frontend/src/utils/displayName.js lines 10-14) -->
getGeezDisplay(member) -> { text, lang: 'ti' } | null   # null when geezFullname absent → omit the line

<!-- MemberAvatarImage (frontend/src/components/manage/MemberAvatarImage.jsx line 12) -->
<MemberAvatarImage member={member} size={32} />  # needs photoUrl+gender+fullname; falls back to gender illustration silently

<!-- DetailPage (from 26-01) exposes loadPersonById(id): sets mainPerson, descendants collapsed -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create PersonSearch — debounced async Autocomplete with rich suggestion rows</name>
  <files>frontend/src/components/person/PersonSearch.jsx, frontend/src/components/person/PersonSearch.test.jsx</files>
  <read_first>
    - frontend/src/pages/LinkAccountsPage.jsx (async-list Autocomplete with avatar renderOption rows, lines 183-207; birth-year slice idiom ~line 63)
    - frontend/src/components/manage/AddRelativeDialog.jsx (createFilterOptions Latin-OR-Ge'ez precedent lines 58-60; Autocomplete controlled value/onChange lines 271-278)
    - frontend/src/components/manage/AddRelativeDialog.test.jsx (Autocomplete typing + Ge'ez-term test pattern, lines 449-511)
    - frontend/src/utils/displayName.js (getGeezDisplay return shape, lines 10-14)
    - frontend/src/components/manage/MemberAvatarImage.jsx (avatar props)
    - frontend/src/api/graphqlClient.js (graphqlRequest, lines 23-36)
  </read_first>
  <behavior>
    - Below min-char threshold (2 chars): typing 1 char does NOT call searchFamilyMembers.
    - Debounce: rapid typing of a multi-char term results in a single searchFamilyMembers fetch after the ~250ms debounce settles (advance timers / waitFor), NOT one request per keystroke.
    - Latin term: typing "Ab" and resolving matches renders a suggestion row containing the member's fullname, its Ge'ez text (when getGeezDisplay is non-null), a birth year "b. <YYYY>", and family context.
    - Ge'ez term (D-02): typing a Ge'ez string passes that exact raw term to searchFamilyMembers (assert the mock received the raw string, no transformation).
    - No matches: when searchFamilyMembers resolves [], the Autocomplete shows the "No matches" noOptionsText (D-08 no-search-results).
    - Select: clicking a suggestion calls the onSelect prop with that member's id (D-05).
  </behavior>
  <action>
    Create `PersonSearch({ onSelect })`. Declare a module-scope `SEARCH_MEMBERS_QUERY`
    const (`query SearchFamilyMembers($term: String!) { searchFamilyMembers(term: $term) { id fullname geezFullname gender birthdate photoUrl mothersname } }`)
    — includes `birthdate` + `mothersname` as the D-03 disambiguators, no new backend
    field. Hold `inputValue`, `options`, and a `loading` flag in state. On
    `onInputChange`, debounce (~250 ms, Claude's discretion per D-01 — use a
    setTimeout ref cleared on each change/unmount) and only fire when the trimmed term
    length ≥ 2 (min-char threshold, D-01); the debounced callback runs
    `graphqlRequest(SEARCH_MEMBERS_QUERY, { term })` with the raw term (D-02, no
    language detection) and sets `options` to the result. Configure the MUI
    `Autocomplete` with `filterOptions={(x) => x}` (identity — the server already did
    Latin+Ge'ez matching, do NOT re-filter, per PATTERNS.md), `getOptionLabel={(m) => m.fullname}`,
    `isOptionEqualToValue={(o, v) => o.id === v.id}`, `noOptionsText="No matches"`
    (D-08), `onChange={(_e, value) => value && onSelect(value.id)}` (D-05), and a
    `renderInput` `TextField` labeled "Search by name". Implement a rich `renderOption`
    (adapt LinkAccountsPage lines 189-205): destructure `{ key, ...optionProps }`,
    render `<MemberAvatarImage member size={32} />`, the `fullname` Typography, a Ge'ez
    line from `getGeezDisplay(member)` inside `<Typography lang={geez.lang}>` (omit when
    null), and a context line joining `b. <birthdate.slice(0,4)>` and `member.mothersname`
    with ' · ', filtering falsy parts (graceful when absent, D-03). Write
    PersonSearch.test.jsx (Vitest fake timers or waitFor for the debounce) covering all
    six behaviors above, including the Ge'ez raw-term pass-through assertion.
  </action>
  <verify>
    <automated>npm test --workspace frontend -- PersonSearch</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- PersonSearch` exits 0.
    - PersonSearch.jsx uses `filterOptions={(x) => x}` (grep confirms — no client-side re-filter).
    - Test proves typing 1 char does not fetch and a debounced multi-char term fires exactly one searchFamilyMembers call.
    - Test proves a Ge'ez-typed term is passed to searchFamilyMembers unmodified.
    - A suggestion row asserts fullname + birth year ("b. " + 4-digit year) are present, and empty searchFamilyMembers renders "No matches".
    - Clicking a suggestion calls onSelect with the member id.
  </acceptance_criteria>
  <done>PersonSearch renders debounced Latin+Ge'ez suggestions as rich rows and reports selection via onSelect(id), fully tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PersonSearch into DetailPage and reset the main person on select</name>
  <files>frontend/src/pages/DetailPage.jsx, frontend/src/pages/DetailPage.test.jsx</files>
  <read_first>
    - frontend/src/pages/DetailPage.jsx (the top-region placeholder Box and the loadPersonById callback from 26-01)
    - frontend/src/components/person/PersonSearch.jsx (onSelect prop contract, from Task 1)
    - frontend/src/pages/DetailPage.test.jsx (existing harness from 26-01)
    - frontend/src/components/manage/AddRelativeDialog.test.jsx (Autocomplete typing/select test pattern, lines 449-511)
  </read_first>
  <action>
    Import `PersonSearch from '../components/person/PersonSearch.jsx'` in DetailPage.jsx
    and mount `<PersonSearch onSelect={(id) => loadPersonById(id)} />` in the persistent
    top region (D-07: search stays visible above the centered card) — render it in the
    happy-path branch AND, so search stays reachable, keep it above the card region.
    Because `loadPersonById` sets loading true and replaces `mainPerson`, selecting a
    suggestion clears the current view and shows only the new person's card with
    descendants collapsed (SEARCH-03/D-05 — the card's `expanded={false}` and inert
    onExpand from 26-01 already collapse descendants). Search input clear-vs-retain after
    select is Claude's discretion (view clears regardless, D-05). Extend
    DetailPage.test.jsx: after the head loads, type a term into the search box, resolve
    the mocked searchFamilyMembers, `findByText`/click the suggestion, and assert
    graphqlRequest is called again with `{ id: '<selectedId>' }` (familyMember) and the
    new person's card testid renders while the previous head card is gone. Include a
    Ge'ez-term case asserting the raw term reaches searchFamilyMembers.
  </action>
  <verify>
    <automated>npm test --workspace frontend -- DetailPage</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- DetailPage` exits 0.
    - DetailPage.jsx imports and renders `<PersonSearch onSelect={...}>` wired to loadPersonById.
    - Test proves selecting a suggestion re-calls familyMember with the selected id and swaps the rendered person-card testid (old head card no longer present).
    - Test proves a Ge'ez-typed term reaches searchFamilyMembers unmodified.
    - `npm test --workspace frontend` exits 0 (no regressions).
  </acceptance_criteria>
  <done>The /detail search bar is live: selecting a suggestion clears the view and makes that person the new main person with descendants collapsed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| search input → searchFamilyMembers(term) | User-typed term crosses to an existing backend GraphQL query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering / Injection | search term → searchFamilyMembers | accept | Term flows through the existing parameterized GraphQL variable ($term) to the Phase-24 resolver, which uses parameterized Sequelize queries — no new injection surface, no string interpolation into SQL, no new backend field. |
| T-26-04 | Denial of Service | per-keystroke search fetch | mitigate | Debounce (~250 ms) + a 2-char min threshold prevent a request-per-keystroke storm (D-01); server already caps results (~20). |
| T-26-05 | Information Disclosure | searchFamilyMembers results | accept | Query is served by the existing authenticated, family-access-guarded resolver reused unchanged from Phase 24. |
</threat_model>

<verification>
- `npm test --workspace frontend -- PersonSearch` passes.
- `npm test --workspace frontend -- DetailPage` passes.
- `npm test --workspace frontend` passes (no regressions).
</verification>

<success_criteria>
SEARCH-01: search finds people by partial/full Latin (case-insensitive) or Ge'ez first/last name.
SEARCH-02: matches appear as inline suggestion rows with avatar, Latin name, Ge'ez name (when present), birth year, and family context.
SEARCH-03: selecting a suggestion clears the view and shows only that person's card, descendants collapsed.
</success_criteria>

<output>
Create `.planning/phases/26-detail-page-search-initial-load/26-02-SUMMARY.md` when done.
</output>
