---
phase: 23-write-path-quality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/manage/MemberFields.jsx
  - frontend/src/components/manage/MemberFields.test.jsx
  - frontend/src/components/manage/EditMemberDialog.jsx
  - frontend/src/components/manage/EditMemberDialog.test.jsx
  - frontend/src/pages/ManagePage.jsx
  - frontend/src/pages/ManagePage.test.jsx
  - frontend/src/components/manage/AddRelativeDialog.jsx
  - frontend/src/components/manage/AddRelativeDialog.test.jsx
autonomous: true
requirements: [EDIT-01]

must_haves:
  truths:
    - "A user can type into three new fields — Ge'ez first name (ስም), Ge'ez last name (ስም ኣቦ), Ge'ez mother's name (ስም ኣደ) — in both the Add relative dialog and the Edit member dialog, using their own device keyboard/IME (D-01/D-02/D-04)."
    - "Values entered in the add-relative dialog are sent as geezFirstname/geezLastname/geezMothersname inside the create mutation's newMember variable."
    - "Opening the edit-member dialog for a member with existing Ge'ez values pre-fills all three fields from the fetched member data, not just the derived geezFullname (SC1 round-trip)."
    - "Clearing a previously-filled Ge'ez field and saving sends an empty string for that key in the mutation payload (the backend's existing OPTIONAL_FAMILY_MEMBER_FIELDS passthrough, unchanged this phase, maps '' to null)."
    - "The Gender | Mother's name row is unchanged in position; Ge'ez mother's name sits directly under Mother's name with an empty left slot under Gender (D-02); none of the three Ge'ez fields has a required flag (D-03)."
  artifacts:
    - path: "frontend/src/components/manage/MemberFields.jsx"
      provides: "Three new optional Ge'ez TextFields, column-aligned directly below their Latin twins"
      contains: "Ge'ez first name (ስም)"
    - path: "frontend/src/components/manage/EditMemberDialog.jsx"
      provides: "EMPTY_FORM/formFromMember carrying geezFirstname/geezLastname/geezMothersname"
      contains: "geezMothersname"
    - path: "frontend/src/pages/ManagePage.jsx"
      provides: "EDITABLE_MEMBER_FIELDS widened to fetch the raw Ge'ez parts needed for edit-dialog hydration"
      contains: "geezFirstname geezLastname geezMothersname"
  key_links:
    - from: "frontend/src/components/manage/MemberFields.jsx"
      to: "onChange('geezFirstname', value)"
      via: "handleTextChange('geezFirstname')"
      pattern: "handleTextChange\\('geezFirstname'\\)"
    - from: "frontend/src/pages/ManagePage.jsx"
      to: "frontend/src/components/manage/EditMemberDialog.jsx"
      via: "member prop sourced from the EDITABLE_MEMBER_FIELDS-built queries"
      pattern: "geezFirstname geezLastname geezMothersname geezFullname"
    - from: "frontend/src/components/manage/AddRelativeDialog.jsx"
      to: "NewFamilyMemberInput (backend, Phase 19, unchanged)"
      via: "newMember: form spread"
      pattern: "newMember: form"
---

<objective>
Add the three Ge'ez name inputs (geezFirstname, geezLastname, geezMothersname) to the shared `MemberFields.jsx` form in the exact locked layout (D-01/D-02), with the exact bilingual labels (D-04), and wire them through both the Add relative dialog (`AddRelativeDialog.jsx`) and the Edit member dialog (`EditMemberDialog.jsx`) so a user can enter, save, and reopen/reload the values correctly (EDIT-01, SC1).

Purpose: `MemberFields.jsx` is the single shared form used by both dialogs — adding the three inputs here wires entry into both create and edit paths at once. The backend already accepts and persists these fields via Phase 19's spread-passthrough resolvers and `OPTIONAL_FAMILY_MEMBER_FIELDS` (clear → null); this phase is purely frontend wiring.
Output: MemberFields.jsx rendering the 3 new optional fields in the locked position; AddRelativeDialog.jsx and EditMemberDialog.jsx carrying the 3 keys through their form state and mutation variables; ManagePage.jsx's EDITABLE_MEMBER_FIELDS widened so the edit dialog actually receives the raw parts to hydrate from; component tests proving render, entry, round-trip, and clear-to-empty-string behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-write-path-quality-gate/23-CONTEXT.md
@.planning/phases/23-write-path-quality-gate/23-PATTERNS.md

<locked_decisions>
- D-01: Each Ge'ez input sits in a row directly below its Latin twin, column-aligned, preserving the existing `Stack direction={{ xs: 'column', sm: 'row' }}` 2-column structure. NOT beside the Latin field, NOT a separate grouped section.
- D-02: The existing `Gender | Mother's name` row is preserved exactly where it is. Ge'ez mother's name goes in the right column directly under Mother's name; the left slot under Gender stays empty (Gender has no Ge'ez twin). Do not move Gender.
- D-03: The three Ge'ez fields are optional (nullable) — no `required` flag, matching the existing optional `Mother's name` field.
- D-04: Labels are bilingual, user-supplied, exact, verbatim — do not translate/transliterate/"correct":
  - `Ge'ez first name (ስም)`
  - `Ge'ez last name (ስም ኣቦ)`
  - `Ge'ez mother's name (ስም ኣደ)`
- D-05: SC1 round-trip must hold — values entered persist and read back correctly on dialog reopen (both add and edit paths). Clearing a Ge'ez field must send it such that the backend stores null (the existing `OPTIONAL_FAMILY_MEMBER_FIELDS` `'' → null` passthrough already handles this — no backend change).
- No input validation that Ge'ez fields contain Ge'ez script — free text via device IME, trust the user (Claude's Discretion, consistent with the no-transliteration decision).
- Do NOT modify any backend file — the create/update resolvers, `OPTIONAL_FAMILY_MEMBER_FIELDS`, and the GraphQL schema already accept these three fields (Phase 19). This plan is frontend-only.
</locked_decisions>

<interfaces>
`frontend/src/components/manage/MemberFields.jsx` — current relevant excerpts (BEFORE this plan):
- `handleTextChange` (line 33, already generalized, reuse verbatim): `const handleTextChange = (field) => (event) => onChange(field, event.target.value);`
- Name row (lines 93-108):
```jsx
<Stack direction={{ xs: 'column', sm: 'row' }} spacing={2}>
  <TextField label="First name" required value={form.firstname} onChange={handleTextChange('firstname')} fullWidth />
  <TextField label="Last name" required value={form.lastname} onChange={handleTextChange('lastname')} fullWidth />
</Stack>
```
- Gender/Mother's-name row (lines 110-129):
```jsx
<Stack direction={{ xs: 'column', sm: 'row' }} spacing={2}>
  <TextField select label="Gender" required value={form.gender} onChange={handleTextChange('gender')} fullWidth>
    <MenuItem value="Male">Male</MenuItem>
    <MenuItem value="Female">Female</MenuItem>
    <MenuItem value="Other">Other</MenuItem>
  </TextField>
  <TextField label="Mother's name" value={form.mothersname} onChange={handleTextChange('mothersname')} fullWidth />
</Stack>
```
- Standalone single-slot `Box` precedent (Address, lines 155-157): `<Box><TextField label="Address" value={form.address} onChange={handleTextChange('address')} fullWidth /></Box>`

`frontend/src/components/manage/EditMemberDialog.jsx` — current relevant excerpts (BEFORE this plan):
- `EDIT_MEMBER_MUTATION` (lines 6-12): selection set is `id firstname lastname fullname gender mothersname email birthdate isAlive phone address` (no geez fields yet — cosmetic only, see Task 2 action).
- `EMPTY_FORM` (lines 14-24) and `formFromMember` (lines 26-39) both enumerate every field with `member.<field> ?? ''` (e.g. `mothersname: member.mothersname ?? ''`).
- `handleSubmit` (line 64): `await graphqlRequest(EDIT_MEMBER_MUTATION, { id: member.id, fields: form });` — `form` is spread wholesale, no per-key enumeration, so no change needed here once `form` carries the 3 new keys.

`frontend/src/pages/ManagePage.jsx` — current relevant excerpts (BEFORE this plan):
- `EDITABLE_MEMBER_FIELDS` (lines 28-29): `'id firstname lastname fullname geezFullname gender mothersname email birthdate isAlive phone address photoUrl'` — feeds BOTH `MY_EDITABLE_MEMBERS_QUERY` (lines 31-41) and `FAMILY_MEMBER_FOCUS_QUERY` (lines 63-75) via string interpolation. One edit here propagates to both queries.
- `FAMILY_MEMBERS_QUERY` (lines 43-51, admin table's own separate query) already includes `geezFullname` — do NOT touch this query, it only needs the derived field for VIEW-02/FIND-01 (Phase 22), not the raw parts.

`frontend/src/components/manage/AddRelativeDialog.jsx` — current relevant excerpts (BEFORE this plan):
- `EMPTY_FORM` (lines 41-51): flat object, no nested structure, spread wholesale into `newMember: form` at submit time (e.g. line 149) — order of keys is cosmetic.

Existing test fixture conventions to extend, not replace:
- `MemberFields.test.jsx`: `EMPTY_FORM` literal (lines 8-18) + `renderFields(props)` helper — the fixture must also carry the 3 new keys as `''` or MUI's controlled `TextField` will warn about switching from uncontrolled to controlled once the component reads `form.geezFirstname`.
- `EditMemberDialog.test.jsx`: `MEMBER` fixture (lines 22-33) + exact-object assertion at lines 80-93 (`fields: {...}` must gain the 3 new keys once `EMPTY_FORM`/`formFromMember` include them).
- `AddRelativeDialog.test.jsx`: exact-object `newMember` assertions exist for parent-Male (lines ~117-132), spouse (lines ~190-204), child-FATHER (lines ~336-352), and sibling (lines ~477-491) — each must gain the 3 new blank keys once `EMPTY_FORM` includes them, or these 4 tests will fail on the next full-object comparison.
- `ManagePage.test.jsx`: existing WR-01-style test (lines 149-160) already asserts a regex against the interpolated query string for nested `mother.siblings`/`father.siblings` `geezFullname` — mirror this pattern for a new assertion on the raw Ge'ez parts.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemberFields.jsx — add the 3 Ge'ez inputs in the locked layout (D-01/D-02/D-03/D-04)</name>
  <files>frontend/src/components/manage/MemberFields.jsx, frontend/src/components/manage/MemberFields.test.jsx</files>
  <read_first>
    frontend/src/components/manage/MemberFields.jsx (full file — handleTextChange line 33, name row lines 93-108, gender/mothersname row lines 110-129, address Box lines 155-157)
    frontend/src/components/manage/MemberFields.test.jsx (full file — EMPTY_FORM fixture lines 8-18, renderFields helper, "renders every member field once" test)
    .planning/phases/23-write-path-quality-gate/23-CONTEXT.md (D-01, D-02, D-03, D-04)
    .planning/phases/23-write-path-quality-gate/23-PATTERNS.md (MemberFields.jsx section — row/column structure to slot the new rows into)
  </read_first>
  <behavior>
    - Test: a new row renders directly below the First/Last name row containing "Ge'ez first name (ስም)" and "Ge'ez last name (ስም ኣቦ)" TextFields
    - Test: a new row renders directly below the Gender/Mother's-name row containing an empty left slot and a "Ge'ez mother's name (ስም ኣደ)" TextField in the right slot (no input under Gender)
    - Test: typing into each of the 3 new fields calls onChange('geezFirstname', value) / onChange('geezLastname', value) / onChange('geezMothersname', value) respectively
    - Test: none of the 3 new fields renders with a `required` attribute (D-03)
    - Test: all pre-existing fields (First/Last name, Gender, Mother's name, Birthdate, Living, Email, Phone, Address) still render exactly once — regression guard extending the existing "renders every member field once" test
  </behavior>
  <action>
    Update the `EMPTY_FORM` fixture in `MemberFields.test.jsx` (lines 8-18) to add `geezFirstname: '', geezLastname: '', geezMothersname: ''` so the default `form` prop carries the 3 keys before the component reads them. In `MemberFields.jsx`, insert a new `Stack direction={{ xs: 'column', sm: 'row' }} spacing={2}` row immediately after the existing First/Last name row (after line 108, before line 110) containing two non-required `TextField`s: label exactly `Ge'ez first name (ስም)` (value={form.geezFirstname}, onChange={handleTextChange('geezFirstname')}, fullWidth) and label exactly `Ge'ez last name (ስም ኣቦ)` (value={form.geezLastname}, onChange={handleTextChange('geezLastname')}, fullWidth) — per D-01. Insert a second new `Stack` row immediately after the existing Gender/Mother's-name row (after line 129, before line 131) containing an empty `<Box sx={{ flex: 1 }} />` as the left slot (D-02 — Gender has no Ge'ez twin, do not put any input there) and a non-required `TextField` labeled exactly `Ge'ez mother's name (ስም ኣደ)` (value={form.geezMothersname}, onChange={handleTextChange('geezMothersname')}, fullWidth) as the right slot. Do not add `required` to any of the three. Do not move, rename, or restructure the existing Gender field or row. Reuse `handleTextChange` verbatim — do not write new handlers.
  </action>
  <acceptance_criteria>
    - `grep -c "Ge'ez first name (ስም)" frontend/src/components/manage/MemberFields.jsx` returns `1`.
    - `grep -c "Ge'ez last name (ስም ኣቦ)" frontend/src/components/manage/MemberFields.jsx` returns `1`.
    - `grep -c "Ge'ez mother's name (ስም ኣደ)" frontend/src/components/manage/MemberFields.jsx` returns `1`.
    - `grep -c "handleTextChange('geezFirstname')" frontend/src/components/manage/MemberFields.jsx` returns `1` (and likewise `1` each for `handleTextChange('geezLastname')` and `handleTextChange('geezMothersname')`).
    - `grep -c "required" frontend/src/components/manage/MemberFields.jsx` returns `3` exactly — the pre-existing First name/Last name/Gender required flags, and no new ones (D-03).
    - `cd frontend && npm test -- MemberFields` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- MemberFields</automated>
  </verify>
  <done>MemberFields renders the 3 Ge'ez fields in the locked D-01/D-02 layout with the exact D-04 bilingual labels, none carrying a required flag (D-03), each wired via handleTextChange to onChange; MemberFields.test.jsx passes (existing + new cases).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EditMemberDialog.jsx + ManagePage.jsx EDITABLE_MEMBER_FIELDS — edit-path hydration and round-trip (SC1, D-05)</name>
  <files>frontend/src/components/manage/EditMemberDialog.jsx, frontend/src/components/manage/EditMemberDialog.test.jsx, frontend/src/pages/ManagePage.jsx, frontend/src/pages/ManagePage.test.jsx</files>
  <read_first>
    frontend/src/components/manage/EditMemberDialog.jsx (full file — EDIT_MEMBER_MUTATION lines 6-12, EMPTY_FORM lines 14-24, formFromMember lines 26-39, handleSubmit line 64)
    frontend/src/components/manage/EditMemberDialog.test.jsx (full file — MEMBER fixture, "pre-fills the form fields" test, exact-object mutation assertion lines 80-93)
    frontend/src/pages/ManagePage.jsx (EDITABLE_MEMBER_FIELDS lines 28-29, MY_EDITABLE_MEMBERS_QUERY lines 31-41, FAMILY_MEMBER_FOCUS_QUERY lines 63-75)
    frontend/src/pages/ManagePage.test.jsx (WR-01-style query-regex test, lines 149-160, pattern to mirror)
    .planning/phases/23-write-path-quality-gate/23-CONTEXT.md (D-05, code_context integration point #4)
    .planning/phases/23-write-path-quality-gate/23-PATTERNS.md (EditMemberDialog.jsx section — EMPTY_FORM/formFromMember diff, EDITABLE_MEMBER_FIELDS gap)
  </read_first>
  <behavior>
    - Test: ManagePage's EDITABLE_MEMBER_FIELDS-built query string includes geezFirstname, geezLastname, and geezMothersname (the raw parts, not just geezFullname) — assert via a regex against the interpolated query string, mirroring the existing WR-01-style test
    - Test: EditMemberDialog pre-fills geezFirstname/geezLastname/geezMothersname TextFields from a member object that carries real Ge'ez values
    - Test: EditMemberDialog pre-fills empty string for a member whose Ge'ez fields are null/absent (existing members with no Ge'ez data yet — safe default)
    - Test: submitting after editing a Ge'ez field sends geezFirstname/geezLastname/geezMothersname alongside every pre-existing key in the fields: form mutation variable — extend the existing exact-object assertion (lines 80-93)
    - Test: clearing a previously-filled Ge'ez field before submit sends '' (empty string) for that key, not omitted/undefined (D-05 — the backend, unchanged, converts '' to null; this proves the frontend sends '')
  </behavior>
  <action>
    In `frontend/src/pages/ManagePage.jsx`, widen the `EDITABLE_MEMBER_FIELDS` string constant (lines 28-29) by inserting `geezFirstname geezLastname geezMothersname` immediately before the existing `geezFullname` token — a single edit to this one fan-out constant propagates to both `MY_EDITABLE_MEMBERS_QUERY` and `FAMILY_MEMBER_FOCUS_QUERY`. Do NOT touch `FAMILY_MEMBERS_QUERY` (admin table's own separate query — already has `geezFullname`, doesn't need the raw parts). In `frontend/src/components/manage/EditMemberDialog.jsx`, add `geezFirstname: '', geezLastname: '', geezMothersname: ''` to `EMPTY_FORM` (lines 14-24), and add `geezFirstname: member.geezFirstname ?? '', geezLastname: member.geezLastname ?? '', geezMothersname: member.geezMothersname ?? ''` to `formFromMember` (lines 26-39), using the exact same `?? ''` null-coalescing convention as the existing `mothersname` line. No change needed to `handleSubmit` (line 64) — `fields: form` already spreads whichever keys `form` carries. Update the existing exact-object `fields: {...}` assertion in `EditMemberDialog.test.jsx` (lines 80-93) to include the 3 new keys with their expected values.
  </action>
  <acceptance_criteria>
    - `grep -c "geezFirstname geezLastname geezMothersname geezFullname" frontend/src/pages/ManagePage.jsx` returns `1` (single fan-out constant updated once).
    - `grep -c "geezFirstname: '', geezLastname: '', geezMothersname: ''" frontend/src/components/manage/EditMemberDialog.jsx` returns `1`.
    - `grep -c "geezFirstname: member.geezFirstname ?? ''" frontend/src/components/manage/EditMemberDialog.jsx` returns `1` (and likewise `1` each for geezLastname/geezMothersname).
    - `cd frontend && npm test -- EditMemberDialog` exits `0`.
    - `cd frontend && npm test -- ManagePage` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- EditMemberDialog && cd frontend && npm test -- ManagePage</automated>
  </verify>
  <done>EditMemberDialog's EMPTY_FORM/formFromMember carry the 3 Ge'ez keys with the `?? ''` convention; ManagePage's single EDITABLE_MEMBER_FIELDS constant fetches the raw Ge'ez parts feeding both queries that hydrate the edit dialog; EditMemberDialog.test.jsx and ManagePage.test.jsx pass (existing + new cases), proving SC1's edit-path round-trip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AddRelativeDialog.jsx — add-path entry and round-trip (SC1, D-05)</name>
  <files>frontend/src/components/manage/AddRelativeDialog.jsx, frontend/src/components/manage/AddRelativeDialog.test.jsx</files>
  <read_first>
    frontend/src/components/manage/AddRelativeDialog.jsx (full file — EMPTY_FORM lines 41-51, handleFieldChange lines 101-103, handleSubmit newMember: form assembly lines 140-185)
    frontend/src/components/manage/AddRelativeDialog.test.jsx (full file — existing exact-object newMember assertions for parent-Male, spouse, child-FATHER, sibling)
    .planning/phases/23-write-path-quality-gate/23-CONTEXT.md (D-05)
    .planning/phases/23-write-path-quality-gate/23-PATTERNS.md (AddRelativeDialog.jsx section — EMPTY_FORM extension)
  </read_first>
  <behavior>
    - Test: typing a Ge'ez value into the "Ge'ez first name" field (rendered by MemberFields, shipped in Task 1) before submitting a spouse creation sends geezFirstname with that value inside the addSpouse mutation's newMember variable — extend the existing "submits addSpouse with form fields only" test
    - Test: submitting without touching any Ge'ez field still sends geezFirstname: '', geezLastname: '', geezMothersname: '' in newMember (safe default, consistent with every other optional field) — update the existing exact-object newMember assertions for parent-Male, spouse, child-FATHER, and sibling to include the 3 new blank keys, since EMPTY_FORM now always carries them
  </behavior>
  <action>
    Add `geezFirstname: '', geezLastname: '', geezMothersname: ''` to `EMPTY_FORM` (lines 41-51) in `AddRelativeDialog.jsx`, placed after `lastname`/`gender` respectively to mirror the D-01 layout order (cosmetic — `form` is spread wholesale into `newMember` regardless of key order, no other code change needed in this file for this task). In `AddRelativeDialog.test.jsx`, update the 4 existing exact-object `newMember` assertions (parent-Male, spouse, child-FATHER, sibling) to include the 3 new blank keys. Add one new test: type a Ge'ez value (e.g. 'ጃነ') into `screen.getByLabelText("Ge'ez first name", { exact: false })` before submitting the spouse-creation flow, and assert the resulting `graphqlRequest` call's `newMember` variable includes `geezFirstname: 'ጃነ'` — proving the entry path is wired end-to-end from `MemberFields` through this dialog's `newMember: form` spread to the create mutation, with zero backend change (Phase 19 spread-passthrough, unchanged).
  </action>
  <acceptance_criteria>
    - `grep -c "geezFirstname: '', geezLastname: '', geezMothersname: ''" frontend/src/components/manage/AddRelativeDialog.jsx` returns `1`.
    - `cd frontend && npm test -- AddRelativeDialog` exits `0`, including the 4 updated exact-object assertions and the new Ge'ez-entry test.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && npm test -- AddRelativeDialog</automated>
  </verify>
  <done>AddRelativeDialog's EMPTY_FORM carries the 3 Ge'ez keys; every relation type's create mutation call includes them (blank by default, filled when entered); AddRelativeDialog.test.jsx passes with all exact-object assertions updated and a new test proving Ge'ez text entered via MemberFields reaches the create mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Device IME → Ge'ez TextField → form state | Free-text Ge'ez input entered via the user's own device keyboard/IME, same trust level as every other existing free-text field (firstname, lastname, mothersname) already accepted by these dialogs — no new input class. |
| Frontend form state → GraphQL create/update mutation | The 3 new keys ride the existing `newMember: form` / `fields: form` wholesale-spread pattern into `NewFamilyMemberInput`/`EditFamilyMemberInput`, both already declaring these fields (Phase 19) and already passed through the existing `OPTIONAL_FAMILY_MEMBER_FIELDS` sanitizer (backend, unchanged) — no new resolver logic, no new endpoint, no new mutation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering / Information Disclosure (stored-XSS-via-entered-name) | `MemberFields.jsx` new TextFields, later rendered elsewhere as `member.geezFullname`/`getGeezDisplay` output | accept | React auto-escapes all text-node content by default (existing pattern for every other name field in this app, e.g. `firstname`/`mothersname`); these 3 fields are plain controlled `TextField`s with no `dangerouslySetInnerHTML` anywhere in the render path. No script-content validation is added by design (D-04/CONTEXT — trust the device IME, matching the no-transliteration decision) since React's default escaping already neutralizes the only realistic script-injection vector. |
| T-23-02 | Tampering (malformed/oversized free text) | `MemberFields.jsx` new TextFields → GraphQL mutation variables | accept | Same un-length-limited free-text trust level as the existing `firstname`/`lastname`/`mothersname` fields already accepted by these dialogs — no new validation gap introduced; any future length/format constraint would apply equally to all name fields, not a Ge'ez-specific concern. |
| T-23-03 | Elevation of Privilege (bypassing membership/permission scoping) | `AddRelativeDialog.jsx` / `EditMemberDialog.jsx` submit paths | accept | This plan adds 3 new form keys spread into already-existing, already-permission-scoped mutations (`addParent`/`addSpouse`/`addChild`/`addSibling`/`editMember`, Phase 14/19) — no new mutation, no new resolver, no change to the existing membership/permission guards. |

No new endpoints, no new mutations, no new auth logic. This is a frontend-only extension of an already-shipped, already-scoped write path.
</threat_model>

<verification>
Run `cd frontend && npm test -- MemberFields`, `cd frontend && npm test -- EditMemberDialog`, `cd frontend && npm test -- ManagePage`, `cd frontend && npm test -- AddRelativeDialog` — all pass.
Run `cd frontend && npm test` (full frontend suite) — stays green, confirming no regression to any pre-existing MemberFields/EditMemberDialog/AddRelativeDialog/ManagePage behavior.
`grep -c "geezFirstname" frontend/src/components/manage/MemberFields.jsx frontend/src/components/manage/EditMemberDialog.jsx frontend/src/components/manage/AddRelativeDialog.jsx frontend/src/pages/ManagePage.jsx` shows the 3 Ge'ez keys wired through every file in the write path.
</verification>

<success_criteria>
- MemberFields.jsx renders the 3 Ge'ez fields in the locked D-01/D-02 layout with exact D-04 labels, none required (D-03).
- Values entered in either dialog reach the corresponding create/update GraphQL mutation's variables as geezFirstname/geezLastname/geezMothersname.
- The edit dialog hydrates from the real raw Ge'ez parts (not just geezFullname), proven by the widened EDITABLE_MEMBER_FIELDS constant and the updated formFromMember.
- Clearing a Ge'ez field and saving sends '' for that key (D-05); no backend change was made or needed.
- `cd frontend && npm test` exits 0.
</success_criteria>

<output>
Create `.planning/phases/23-write-path-quality-gate/23-01-SUMMARY.md` when done
</output>
