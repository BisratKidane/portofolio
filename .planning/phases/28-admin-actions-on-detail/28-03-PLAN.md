---
phase: 28-admin-actions-on-detail
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/hooks/descendantNav.reducer.js
  - frontend/src/hooks/descendantNav.reducer.test.js
  - frontend/src/hooks/useDescendantNav.js
  - frontend/src/hooks/useDescendantNav.test.js
autonomous: true
requirements: [PERM-02]

must_haves:
  truths:
    - "A single per-id cache entry can be evicted and refetched on demand, without discarding the rest of the session cache (resolves the Phase-27-deferred cache-invalidation gap, D-04)"
    - "The refetch is one bounded request combining the target's own display fields and its full children set — no new N+1 shape"
    - "A cache write via the refresh path triggers a real re-render (the ref-cache alone never does, per PERF-03)"
  artifacts:
    - path: "frontend/src/hooks/useDescendantNav.js"
      provides: "refreshEntry(id) -> Promise<familyMember|null>, added to the hook's returned object"
      exports: ["useDescendantNav"]
    - path: "frontend/src/hooks/descendantNav.reducer.js"
      provides: "REFRESH action case — a pure state-reference bump with no field change"
      exports: ["navReducer", "initial"]
  key_links:
    - from: "frontend/src/hooks/useDescendantNav.js"
      to: "frontend/src/hooks/descendantNav.reducer.js"
      via: "dispatch({ type: 'REFRESH' }) after a cache.current write, forcing React to re-derive topPerson/gen1/gen2 from the ref"
      pattern: "dispatch\\(\\{ type: 'REFRESH' \\}\\)"
    - from: "frontend/src/pages/DetailPage.jsx (Plans 28-04/28-05)"
      to: "frontend/src/hooks/useDescendantNav.js"
      via: "nav.refreshEntry(member.id) called from refreshAfterMutation after an edit/add/add-spouse succeeds"
      pattern: "refreshEntry"
---

<objective>
Close the cache-invalidation gap Phase 27 deliberately deferred: add a
`refreshEntry(id)` method to `useDescendantNav` that evicts and refetches a
single cached descendant (or the current top) with one bounded GraphQL
request, and a matching `REFRESH` action to the pure `navReducer` so the ref
write actually triggers a re-render. This is the foundation Plans 28-04
(edit-in-place) and 28-05 (add-in-place + auto-expand) both consume.

Purpose: `useDescendantNav`'s cache is a ref (PERF-03 requirement — a write
alone never re-renders). Every existing fetch path (`ensureEntry`) is a
cache-hit-or-fresh-fetch; none of them can force a refetch of an id already
in cache. D-04 requires exactly that capability.

Output: `useDescendantNav.js` (+ test), `descendantNav.reducer.js` (+ test)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-admin-actions-on-detail/28-CONTEXT.md
@.planning/phases/28-admin-actions-on-detail/28-PATTERNS.md

<interfaces>
<!-- Current useDescendantNav contract — do not rename existing exports/fields. -->

useDescendantNav(mainPerson) returns:
  { topPerson, topExpanded, gen1, expandedChildId, gen2, loadingId,
    onExpandTop, onExpandChild, onExpandGrandchild }

cache: useRef(new Map()) — entry shape { self, children }, `children` stays
  `undefined` until a fetch resolves (cache miss signal).
ensureEntry(person): cache-hit -> Promise.resolve(); cache-miss -> fetches
  EXPAND_CHILDREN_QUERY(id: person.id) which returns ONLY `children` (not the
  target's own fields) -- this is why the new REFRESH_PERSON_QUERY below must
  fetch the target's OWN fields too (an edit/add-spouse changes those).

New contract this plan adds (do not deviate — Plans 28-04/28-05 depend on
these exact names):
  refreshEntry(id) -> Promise<familyMember-shaped object | null>
    - Always issues a fresh request (never a cache-hit short-circuit, unlike ensureEntry)
    - On resolve: cache.current.set(id, { self: <own fields minus children>, children: <fresh children array> })
    - Dispatches { type: 'REFRESH' } to force a re-render
    - Sets/clears loadingId the same way ensureEntry does
  navReducer(state, { type: 'REFRESH' }) -> { ...state } (new reference, same
    field values — the minimal "bump" needed since state didn't logically
    change but the ref cache backing it did)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for refreshEntry + REFRESH (RED)</name>
  <files>frontend/src/hooks/descendantNav.reducer.test.js, frontend/src/hooks/useDescendantNav.test.js</files>
  <read_first>
    - frontend/src/hooks/descendantNav.reducer.js (full — current 4 cases + default)
    - frontend/src/hooks/descendantNav.reducer.test.js (full — style/idiom to extend)
    - frontend/src/hooks/useDescendantNav.js (full — esp. lines 31-67 cache/ensureEntry, 69-98 exported handlers/derived values)
    - frontend/src/hooks/useDescendantNav.test.js (full — fixture chain MAIN_PERSON/CHILD/GRANDCHILD/GREAT_GRANDCHILD at lines 21-79, the `graphqlRequest` mock idiom, and the rest of the file beyond line 80 for the exact `renderHook`/`act`/`waitFor` sequencing already used by existing `onExpandChild`/`onExpandGrandchild` tests)
  </read_first>
  <behavior>
    Reducer:
    - `navReducer(someState, { type: 'REFRESH' })` returns a value that is `.not.toBe(someState)` (new reference) but `.toEqual(someState)` (identical field values) for any prior state, including a state with non-empty `history`.

    Hook:
    - The object returned by `useDescendantNav` includes a `refreshEntry` function.
    - Calling `refreshEntry(CHILD.id)` after `CHILD` is already the expanded gen1 entry issues exactly ONE new `graphqlRequest` call (in addition to whatever prior calls got it into cache), whose query string references both `familyMember` and `children` (a single combined request, not two separate ones).
    - Resolving that call with a mocked response containing updated `fullname` and a new `children` array causes a subsequent render to show the NEW `fullname` (via `nav.gen1` — since `CHILD` is a gen1 entry, `topPerson`/`gen1` must reflect the refreshed `self`) and the NEW children under it once expanded.
    - `refreshEntry` returns a Promise that resolves with the mocked `familyMember` payload.
    - `refreshEntry` resolves with `null` (and makes no cache write) when the mocked response's `familyMember` field is `null`.
    - While the `refreshEntry` promise is pending, `nav.loadingId` equals the refreshed id; after it resolves/rejects, `loadingId` is cleared back to `null`.
    - Calling `refreshEntry` for an id NOT previously in cache still performs the fetch and populates the cache from scratch (no crash on a cache-miss target).
  </behavior>
  <action>
    Append a new `describe('REFRESH', ...)` block to `descendantNav.reducer.test.js`
    (do not restructure the existing 4 describe blocks). Append a new
    `describe('refreshEntry', ...)` block to `useDescendantNav.test.js`, reusing
    the existing fixtures and the file's established `graphqlRequest.mockResolvedValueOnce`
    sequencing + `renderHook`/`act`/`waitFor` idiom (read the full file before
    writing — each `it` renders its own fresh hook instance per the file's header
    comment). `refreshEntry` does not exist yet on the hook's return object and
    the reducer has no `REFRESH` case — these assertions must fail (RED).
  </action>
  <verify>
    <automated>npm test --workspace frontend -- descendantNav.reducer</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- descendantNav.reducer` FAILS.
    - `npm test --workspace frontend -- useDescendantNav` FAILS.
    - `descendantNav.reducer.test.js` has a new `REFRESH` describe block with at least 2 `it`s (new-reference, field-preservation-with-history).
    - `useDescendantNav.test.js` has a new `refreshEntry` describe block with at least 5 `it`s covering the bullets above.
  </acceptance_criteria>
  <done>A complete, currently-failing test suite specifies refreshEntry's + REFRESH's full contract before any implementation exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement refreshEntry + REFRESH (GREEN)</name>
  <files>frontend/src/hooks/useDescendantNav.js, frontend/src/hooks/descendantNav.reducer.js</files>
  <read_first>
    - frontend/src/hooks/descendantNav.reducer.test.js and useDescendantNav.test.js (Task 1's tests — the exact contract to satisfy)
    - frontend/src/hooks/useDescendantNav.js (current, full)
    - frontend/src/hooks/descendantNav.reducer.js (current, full)
  </read_first>
  <action>
    In `descendantNav.reducer.js`, add `case 'REFRESH': return { ...state };`
    immediately before the `default` case.

    In `useDescendantNav.js`, add a new query constant `REFRESH_PERSON_QUERY`
    selecting, under `familyMember(id: $id)`: `id fullname geezFullname gender
    isAlive photoUrl canEdit`, `spouses { id fullname geezFullname gender
    isAlive photoUrl }`, and `children { id fullname geezFullname gender
    isAlive photoUrl canEdit spouses { id fullname geezFullname gender isAlive
    photoUrl } children { id } }` — this single query combines the target's
    own display fields (needed after an edit or add-spouse) with its full
    children set (needed after an add-child), reusing the exact field shapes
    already proven N+1-free by `FAMILY_MEMBER_QUERY` (DetailPage.jsx) and
    `EXPAND_CHILDREN_QUERY` (this file) — no new bounded-query shape is being
    invented. Add `refreshEntry` as a new `useCallback` (deps: `[]`, same as
    `ensureEntry`): `(id) => { setLoadingId(id); return
    graphqlRequest(REFRESH_PERSON_QUERY, { id }).then((data) => { const fresh
    = data.familyMember; if (!fresh) return null; const { children, ...self }
    = fresh; cache.current.set(id, { self, children: children ?? [] });
    dispatch({ type: 'REFRESH' }); return fresh; }).finally(() =>
    setLoadingId((current) => (current === id ? null : current))); }`. Add
    `refreshEntry` to the object returned at the bottom of the hook (alongside
    `onExpandTop`/`onExpandChild`/`onExpandGrandchild`).
  </action>
  <verify>
    <automated>npm test --workspace frontend -- descendantNav.reducer</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- descendantNav.reducer` exits 0, all Task 1 tests green.
    - `npm test --workspace frontend -- useDescendantNav` exits 0, all Task 1 tests green.
    - `grep -c "case 'REFRESH'" frontend/src/hooks/descendantNav.reducer.js` equals 1.
    - `grep -c "refreshEntry" frontend/src/hooks/useDescendantNav.js` is at least 3 (defined, returned, dispatched-from).
    - `npm test --workspace frontend` passes with no regressions.
  </acceptance_criteria>
  <done>useDescendantNav exposes refreshEntry(id), which evicts and refetches a single cached descendant (or the current top) with one bounded request and forces a re-render via the new reducer REFRESH action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | `refreshEntry` calls the same server-side `familyMember(id)` GraphQL field every other `/detail` read already calls (`requireFamilyAccess`-gated, unchanged resolver). No new trust boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Denial of Service (client-side) | `refreshEntry` cache-eviction | accept | `refreshEntry` is only ever invoked from Plan 28-04/28-05's mutation-success callbacks (one call per successful admin edit/add), not from user-triggered polling — no unbounded refetch loop is introduced. |
</threat_model>

<verification>
`npm test --workspace frontend -- descendantNav.reducer` and `npm test --workspace frontend -- useDescendantNav` pass. `npm test --workspace frontend` passes with no regressions.
</verification>

<success_criteria>
Foundational for SC-2 (ROADMAP Phase 28): "after a successful add, that person's children/spouses refresh in place" — this plan provides the cache-refresh primitive; Plans 28-04/28-05 wire it to the actual mutation success callbacks.
</success_criteria>

<output>
Create `.planning/phases/28-admin-actions-on-detail/28-03-SUMMARY.md` when done.
</output>
