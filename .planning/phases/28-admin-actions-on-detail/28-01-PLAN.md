---
phase: 28-admin-actions-on-detail
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/resolvers/familyMember.detailAdminActions.test.js
autonomous: true
requirements: [PERM-03]

must_haves:
  truths:
    - "A non-admin (member-user) attempting editMember/addChild/addSpouse on a member outside their editable scope is rejected server-side, regardless of any client UI (SC-3)"
    - "No FamilyMember/Spouse row is created or mutated as a side effect of a rejected mutation"
  artifacts:
    - path: "backend/src/resolvers/familyMember.detailAdminActions.test.js"
      provides: "Adversarial integration proof that the /detail admin-action mutations (editMember, addChild, addSpouse) are rejected server-side for a non-admin, independent of the new /detail UI"
  key_links:
    - from: "backend/src/resolvers/familyMember.detailAdminActions.test.js"
      to: "backend/src/resolvers/familyMember.resolver.js"
      via: "graphql(EDIT_MEMBER_MUTATION/ADD_CHILD_MUTATION/ADD_SPOUSE_MUTATION, vars, nonAdminActor) -> requireFamilyAccess + scope.ids.has(targetId) guard"
      pattern: "This member is outside your editable scope\\."
---

<objective>
Prove — via a new adversarial integration test, not new guard code — that the
three mutations Phase 28's `/detail` admin actions wire up (`editMember`,
`addChild`, `addSpouse`) are already rejected server-side for a non-admin
acting outside their editable scope. This is the ROADMAP SC-3 / PERM-03 proof
that authorization is enforced independent of the new UI's client-side
`canEdit` hiding.

Purpose: `/detail`'s Edit/Add controls are gated purely on `member.canEdit`
(a client-side hide). D-09 requires proving the hide is not the only defense
— the existing `requireFamilyAccess` + admin scope-bypass guards on these
exact three mutations must reject a direct, UI-bypassing call. No production
code changes in this plan.

Output: `backend/src/resolvers/familyMember.detailAdminActions.test.js`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-admin-actions-on-detail/28-CONTEXT.md
@.planning/phases/28-admin-actions-on-detail/28-PATTERNS.md

<interfaces>
<!-- Existing guard behavior this test proves, verbatim from
     backend/src/resolvers/familyMember.resolver.js. Not modified by this plan. -->

editMember (`familyMember.resolver.js:245-256` per PATTERNS.md):
  requireFamilyAccess(user); if not ADMIN, computeEditableScope(user.familyMemberId)
  and throw new Error('This member is outside your editable scope.') when the
  target id is not in scope.ids.

addChild / addSpouse: identical requireFamilyAccess + scope.ids.has(targetId)
check before their respective transactions (familyMember.resolver.js:~100-190).

Test harness (`backend/test/helpers.js`, already used by every resolver test
in this codebase):
  graphql(query, variables, actorUser) -> { data, errors }
  resetTables() -> truncates all tables between tests
  createTestUser({ role, familyMemberId }) -> persisted User row
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and verify the SC-3 adversarial rejection test</name>
  <files>backend/src/resolvers/familyMember.detailAdminActions.test.js</files>
  <read_first>
    - backend/src/resolvers/familyMember.editMember.test.js (lines 1-15 harness imports; lines 110-134 "rejects an id outside the actor editable scope" — the exact shape to mirror for editMember)
    - backend/src/resolvers/familyMember.addChild.test.js (lines 130-160 "rejects a memberId (primary target) outside the actor editable scope")
    - backend/src/resolvers/familyMember.addSpouse.test.js (lines 1-20 imports/mutation string; lines 65-85 "rejects a memberId outside the actor editable scope")
    - backend/src/utils/auth.js (lines 42-51, `requireAdmin`/`requireFamilyAccess` definitions — confirms only `role === 'ADMIN'` bypasses the scope check)
    - backend/src/resolvers/familyMember.resolver.js (lines 100-190, the editMember/addChild/addSpouse guard blocks — read-only, not modified)
  </read_first>
  <action>
    Create `backend/src/resolvers/familyMember.detailAdminActions.test.js` importing
    `{ describe, it, expect, beforeEach }` from vitest, `{ models }` from
    `../models/index.js`, and `{ graphql, resetTables, createTestUser }` from
    `../../test/helpers.js`. Add local copies of the three mutation strings
    (`EDIT_MEMBER_MUTATION`, `ADD_CHILD_MUTATION`, `ADD_SPOUSE_MUTATION`) matching
    the exact shapes used in the three analog test files. Call `beforeEach(resetTables)`.
    In a `describe('Phase 28 admin-actions-on-detail adversarial rejection (PERM-03, SC-3)', ...)`
    block, create two unrelated `models.FamilyMember` rows in each `it` — a
    `headCandidate` (the target, standing in for any person displayed on `/detail`,
    e.g. `/detail`'s family head) and an `actorSelf` (a separate, unrelated member,
    proving the rejection is scope-based, not merely "record doesn't exist") — then
    `createTestUser({ role: 'USER', familyMemberId: actorSelf.id })` as the
    non-admin actor. Write exactly 3 `it` blocks:
    (1) `editMember(id: headCandidate.id, fields: { phone: '...' })` as the actor
        -> assert `errors[0].message === 'This member is outside your editable scope.'`,
        `data === null`, and `headCandidate.reload()` shows the field unchanged.
    (2) `addChild(memberId: headCandidate.id, role: 'FATHER', newMember: {...valid...})`
        as the actor -> assert the same error message, `data === null`, and
        `models.FamilyMember.count()` is unchanged before/after.
    (3) `addSpouse(memberId: headCandidate.id, newMember: {...valid...})` as the
        actor -> assert the same error message, `data === null`, and
        `models.FamilyMember.count()`/`models.Spouse.count()` unchanged before/after.
    Do not add a 4th "ADMIN succeeds" case — that behavior is already covered by
    the three analog files; this plan's scope is strictly the rejection proof.
  </action>
  <verify>
    <automated>npm test --workspace backend -- familyMember.detailAdminActions</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace backend -- familyMember.detailAdminActions` exits 0.
    - The file contains exactly 3 `it(...)` blocks (editMember, addChild, addSpouse).
    - Each `it` asserts both `errors[0].message === 'This member is outside your editable scope.'` and `data === null`.
    - The addChild/addSpouse cases additionally assert row counts (`FamilyMember.count()` / `Spouse.count()`) are unchanged before vs. after the rejected call.
    - No file other than `backend/src/resolvers/familyMember.detailAdminActions.test.js` is modified — no guard/resolver code changes.
  </acceptance_criteria>
  <done>A new, passing adversarial integration test proves editMember/addChild/addSpouse are all rejected server-side for a non-admin acting outside their editable scope, independent of and unreachable by any client-side canEdit hiding on /detail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Non-admin client -> GraphQL mutation | A non-admin (or any actor bypassing the UI entirely, e.g. via curl/Postman with a valid JWT) can issue `editMember`/`addChild`/`addSpouse` directly against `/graphql`, regardless of what `/detail`'s React UI hides. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Elevation of Privilege | `editMember`/`addChild`/`addSpouse` resolvers (`familyMember.resolver.js`) | mitigate | Already enforced by existing `requireFamilyAccess` + `computeEditableScope` guards (unchanged, no new guard code per D-09); THIS plan adds the adversarial test proving the enforcement holds from the specific angle `/detail`'s new admin-action surface will use. |
| T-28-02 | Tampering | Test fixtures / DB state | accept | `resetTables()` runs in `beforeEach`, isolating every test's fixtures from any other — no cross-test data leakage risk. |
</threat_model>

<verification>
`npm test --workspace backend -- familyMember.detailAdminActions` passes. `npm test --workspace backend` passes with no regressions (new file only, no existing file touched).
</verification>

<success_criteria>
SC-3 (ROADMAP Phase 28): "Sending an edit/add mutation as a non-admin (bypassing the UI) is rejected server-side by the existing guards, proven by an adversarial test exercised from this new surface" — proven for all three mutations `/detail`'s admin actions will call.
</success_criteria>

<output>
Create `.planning/phases/28-admin-actions-on-detail/28-01-SUMMARY.md` when done.
</output>
