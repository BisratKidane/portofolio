---
phase: 28-admin-actions-on-detail
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/person/PersonCard.jsx
  - frontend/src/components/person/PersonCard.test.jsx
  - frontend/src/components/person/GenerationGrid.jsx
  - frontend/src/components/person/GenerationGrid.test.jsx
autonomous: true
requirements: [PERM-02]

must_haves:
  truths:
    - "An admin sees a single Add control beside the existing Edit button on every non-spouse person card, opening a menu with 'Add child' and 'Add spouse' (D-01/D-02)"
    - "A non-admin never sees the Add control (member.canEdit === true gate, mirrors the existing Edit-button gate)"
    - "A spouse leaf card never shows the Add control, even when canEdit is true (D-07 — add-a-child happens from the anchor, not the spouse)"
  artifacts:
    - path: "frontend/src/components/person/PersonCard.jsx"
      provides: "onAddRelative(relationType, member) callback prop + anchored MUI Menu ('Add child' / 'Add spouse'), threaded to the non-spouse PersonCardSingle instance only"
    - path: "frontend/src/components/person/GenerationGrid.jsx"
      provides: "onAddRelative prop forwarded to every PersonCard it renders (gen1/gen2 rows)"
  key_links:
    - from: "frontend/src/components/person/GenerationGrid.jsx"
      to: "frontend/src/components/person/PersonCard.jsx"
      via: "onAddRelative prop pass-through (mirrors existing onEdit pass-through)"
      pattern: "onAddRelative"
    - from: "frontend/src/pages/DetailPage.jsx (Plan 28-05)"
      to: "frontend/src/components/person/PersonCard.jsx"
      via: "onAddRelative callback consumed by Plan 28-05's handleAddRelative"
      pattern: "onAddRelative"
---

<objective>
Add the admin-only "Add" control (D-01: one icon button opening a 2-item menu
— "Add child" / "Add spouse") to `PersonCard`'s non-spouse anchor instance,
beside the existing Edit button, and thread the new `onAddRelative` callback
prop through `GenerationGrid` so DetailPage (Plan 28-05) can wire it at all
three render sites. `PersonCard` stays purely presentational — the callback
fires with `(relationType, member)`; opening dialogs and fetching stays owned
by the page layer (Phase 25/27 convention, unchanged).

Purpose: this is the visible half of PERM-02 (SC-2) — the control an admin
clicks, and the gate a non-admin never sees. Wiring it to a real dialog +
refresh-in-place is Plan 28-05's concern.

Output: `PersonCard.jsx` (+ test), `GenerationGrid.jsx` (+ test)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-admin-actions-on-detail/28-CONTEXT.md
@.planning/phases/28-admin-actions-on-detail/28-PATTERNS.md

<interfaces>
<!-- Current PersonCard contract (frontend/src/components/person/PersonCard.jsx) —
     do not rename existing props/exports. -->

export default function PersonCard({ member, role, spouse, isSpouse = false, expanded, onExpand, onEdit })
  - composes PersonCardSingle (anchor) + optional spouse leaf when `spouse` is
    supplied and `!isSpouse` (PersonCard.jsx:46-73)
  - PersonCardSingle({ member, role, isSpouse = false, expanded, onExpand, onEdit })
    - existing canEdit-gated Edit IconButton at PersonCard.jsx:104-112,
      `position: 'absolute', top: 8, right: 8`
  - GenerationGrid({ people, role, expandedId, onExpand, onEdit, loadingId })
    renders <PersonCard member={person} role={role} spouse={person.spouses?.[0]}
    expanded={...} onExpand={onExpand} onEdit={onEdit} /> per person
    (GenerationGrid.jsx:52-67)

New contract this plan adds (do not deviate — Plan 28-05 depends on these
exact names):
  PersonCard(..., onAddRelative)  — threaded ONLY to the non-spouse
    PersonCardSingle instance, never to the `isSpouse` leaf (D-07).
  PersonCardSingle(..., onAddRelative) — renders the Add IconButton + Menu
    only when `!isSpouse && member.canEdit === true && typeof onAddRelative === 'function'`.
    Menu items call `onAddRelative('child', member)` / `onAddRelative('spouse', member)`.
  GenerationGrid(..., onAddRelative) — forwarded straight through to PersonCard.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for the Add-menu control (RED)</name>
  <files>frontend/src/components/person/PersonCard.test.jsx, frontend/src/components/person/GenerationGrid.test.jsx</files>
  <read_first>
    - frontend/src/components/person/PersonCard.jsx (full — esp. lines 46-73 PersonCard wrapper, 75-82 PersonCardSingle signature, 104-112 existing Edit button)
    - frontend/src/components/person/PersonCard.test.jsx (full — `renderCard` helper at lines 24-35, existing Edit-button tests at lines 148-166, spouse-pairing tests at 167-210, to mirror style exactly)
    - frontend/src/components/person/GenerationGrid.jsx (full — 52-67 signature/PersonCard invocation)
    - frontend/src/components/person/GenerationGrid.test.jsx (full — existing prop-forwarding test style)
    - frontend/src/pages/Dashboard.jsx (lines 1-30 imports, 93-95 `rowMenu` state, 309-317 IconButton trigger, 324-349 Menu/MenuItem — the anchored-Menu mechanics to assert against, e.g. `screen.getByRole('menuitem', { name: ... })`)
  </read_first>
  <behavior>
    - `renderCard({ member: { ...BASE_MEMBER, canEdit: false } })` renders no Add control (`queryByLabelText(/add relative/i)` is null).
    - `renderCard({ member: { ...BASE_MEMBER, canEdit: true }, onAddRelative: vi.fn() })` renders an Add IconButton (`getByLabelText(/add relative to ada lovelace/i)` or equivalent aria-label).
    - `renderCard({ isSpouse: true, member: { ...BASE_MEMBER, canEdit: true }, onAddRelative: vi.fn() })` renders NO Add control, even though `canEdit` is true (D-07).
    - Rendering with both `spouse: SPOUSE` and `onAddRelative` supplied: the Add control appears on the anchor card but NOT on the composed spouse leaf card (assert exactly one Add-control element in the container).
    - Clicking the Add IconButton opens a menu with exactly two items whose accessible names are "Add child" and "Add spouse".
    - Clicking "Add child" calls `onAddRelative('child', member)` and closes the menu (menu items no longer in the document).
    - Clicking "Add spouse" calls `onAddRelative('spouse', member)`.
    - `renderCard({ member: { ...BASE_MEMBER, canEdit: true } })` (no `onAddRelative` prop at all) does not throw and renders no Add control (defensive default, mirrors how `onEdit` is always required today but this new prop must degrade gracefully since Plan 28-05 wires it later).
    - GenerationGrid: rendering with a supplied `onAddRelative` mock and one person in `people` forwards the callback such that clicking that person's Add control -> "Add child" invokes the SAME mock with `('child', person)`.
  </behavior>
  <action>
    Add the new `it` blocks above to `PersonCard.test.jsx` (extend the file's
    existing `describe('PersonCard', ...)` block; do not restructure existing
    tests) and to `GenerationGrid.test.jsx`. Use `userEvent` (already imported in
    both files) for click interactions and MUI's `role="menuitem"` semantics for
    menu assertions. `onAddRelative` does not exist as a prop yet — these
    assertions must fail (RED).
  </action>
  <verify>
    <automated>npm test --workspace frontend -- PersonCard</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- PersonCard` FAILS.
    - `npm test --workspace frontend -- GenerationGrid` FAILS.
    - `PersonCard.test.jsx` contains new tests for: hidden-when-non-admin, visible-when-admin-non-spouse, hidden-on-spouse-card (both direct isSpouse and composed-pair cases), menu-has-two-items, add-child-click, add-spouse-click, menu-closes-after-click, no-crash-without-onAddRelative.
    - `GenerationGrid.test.jsx` contains a new onAddRelative-forwarding test.
  </acceptance_criteria>
  <done>A complete, currently-failing test suite specifies the Add-menu control's full contract before any implementation exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Add-menu control (GREEN)</name>
  <files>frontend/src/components/person/PersonCard.jsx, frontend/src/components/person/GenerationGrid.jsx</files>
  <read_first>
    - frontend/src/components/person/PersonCard.test.jsx and GenerationGrid.test.jsx (Task 1's tests — the exact contract to satisfy)
    - frontend/src/components/person/PersonCard.jsx (current, full)
    - frontend/src/pages/Dashboard.jsx (lines 93-95, 309-317, 324-349 — the anchored `Menu`/`MenuItem` pattern to copy mechanically)
  </read_first>
  <action>
    In `PersonCard.jsx`: add `import { useState } from 'react';` and add `Menu,
    MenuItem` to the existing `@mui/material` import; add
    `import AddRoundedIcon from '@mui/icons-material/AddRounded';`. In
    `PersonCardSingle`, add `const [addMenuAnchor, setAddMenuAnchor] = useState(null);`.
    Immediately after the existing Edit `IconButton` block (after line 112),
    render — gated on `!isSpouse && member.canEdit === true && typeof onAddRelative === 'function'`
    — an `IconButton` with `aria-label={\`Add relative to ${member.fullname}\`}`,
    `onClick={(e) => setAddMenuAnchor(e.currentTarget)}`, and
    `sx={{ position: 'absolute', top: 8, right: 56, minWidth: 44, minHeight: 44 }}`
    (56px clears the existing edit button at `right: 8` plus its 44px width and
    an 8px gap) containing `<AddRoundedIcon />`, followed by
    `<Menu anchorEl={addMenuAnchor} open={Boolean(addMenuAnchor)} onClose={() => setAddMenuAnchor(null)}>`
    with two `MenuItem`s: `onClick={() => { setAddMenuAnchor(null); onAddRelative('child', member); }}`
    labeled "Add child", and the spouse equivalent with `'spouse'` labeled
    "Add spouse". Add `onAddRelative` to `PersonCardSingle`'s destructured props
    (default `undefined`) and to the outer `PersonCard` function's destructured
    props (line 46). Thread `onAddRelative={onAddRelative}` into the NON-SPOUSE
    `<PersonCardSingle>` invocation (the `card` const, lines 48-56) only — do
    NOT add it to the spouse-leaf `<PersonCardSingle member={spouse} isSpouse
    onEdit={onEdit} />` invocation at line 70 (D-07).

    In `GenerationGrid.jsx`: add `onAddRelative` to the function signature
    (line 52) and pass `onAddRelative={onAddRelative}` into the existing
    `<PersonCard>` invocation (~lines 60-67), alongside `onEdit`.
  </action>
  <verify>
    <automated>npm test --workspace frontend -- PersonCard</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend -- PersonCard` exits 0, all Task 1 tests green.
    - `npm test --workspace frontend -- GenerationGrid` exits 0, all Task 1 tests green.
    - `grep -c "onAddRelative" frontend/src/components/person/PersonCard.jsx` is at least 4 (destructured on both components, rendered in JSX gate, used in both MenuItem handlers).
    - `grep -A1 "PersonCardSingle member={spouse}" frontend/src/components/person/PersonCard.jsx | grep -c "onAddRelative"` equals 0 (spouse leaf never receives it).
    - `npm test --workspace frontend` passes with no regressions.
  </acceptance_criteria>
  <done>An admin-only Add control (icon button + 2-item menu) renders beside the Edit button on every non-spouse PersonCard instance, forwards cleanly through GenerationGrid, and never appears on a spouse leaf card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PersonCard render -> Add control visibility | `member.canEdit` is a server-derived boolean (Phase 24: `Boolean(user?.role === 'ADMIN')`) consumed as-is; PersonCard performs no independent authorization logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03 | Elevation of Privilege | `PersonCard.jsx` Add-menu gate | mitigate | Gated identically to the existing, already-shipped Edit button (`member.canEdit === true`, same field, same pattern) — no new client-side authorization logic introduced. Real enforcement is server-side; proven independently by Plan 28-01's adversarial test against the exact mutations this control will trigger (wired in Plan 28-05). |
| T-28-04 | Information Disclosure | Add-menu control on spouse leaf | accept | D-07 structurally omits `onAddRelative` from the spouse-leaf `PersonCardSingle` invocation at the JSX call-site level (not a runtime conditional that could be bypassed by bad data) — the spouse leaf component instance simply never receives the prop needed to render the control. |
</threat_model>

<verification>
`npm test --workspace frontend -- PersonCard` and `npm test --workspace frontend -- GenerationGrid` pass. `npm test --workspace frontend` passes with no regressions.
</verification>

<success_criteria>
SC-2 (partial, ROADMAP Phase 28): "An admin sees a control to add a child or spouse to a displayed person... A non-admin never sees it" — the visible control and its gating are fully implemented and tested here; dialog wiring + refresh-in-place lands in Plan 28-05.
</success_criteria>

<output>
Create `.planning/phases/28-admin-actions-on-detail/28-02-SUMMARY.md` when done.
</output>
