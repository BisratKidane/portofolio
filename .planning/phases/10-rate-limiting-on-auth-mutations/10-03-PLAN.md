---
phase: 10-rate-limiting-on-auth-mutations
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - backend/test/helpers.js
  - backend/test/setupRateLimit.js
  - backend/vitest.config.js
  - backend/src/resolvers/resetPassword.test.js
  - backend/src/resolvers/rateLimit.test.js
autonomous: true
requirements: [RATE-01, RATE-02, RATE-03, RATE-04, RATE-05]

must_haves:
  truths:
    - "login is rejected with TOO_MANY_REQUESTS after 5 attempts per client IP within 15 minutes, regardless of credential validity (ROADMAP SC-1)"
    - "register and requestPasswordReset are each rejected after 5 attempts per client IP within their respective windows (ROADMAP SC-2)"
    - "The full mechanism is exercised entirely through the in-process executeOperation() harness (graphql() in test/helpers.js) — no HTTP boot is required to prove any of RATE-01..05 (ROADMAP SC-3)"
    - "An interleaved me/dashboard query burst on the same IP as an exhausted login budget is never throttled (ROADMAP SC-4)"
    - "A breach returns the generic message, and the attempt-count that triggers TOO_MANY_REQUESTS is identical for a real account and a nonexistent one — proven by a dedicated parity test (ROADMAP SC-5)"
    - "Renaming or anonymizing the client-supplied GraphQL operation name does not let an attacker evade the limiter — a dedicated test proves the same field-level budget still applies"
    - "The full pre-existing backend test suite stays green after the plugin is registered on the SAME ApolloServer instance the test harness exercises — no pre-existing test is weakened, only fixture-isolated where the new cross-cutting plugin would otherwise cause a false-positive throttle"
  artifacts:
    - path: "backend/test/helpers.js"
      provides: "graphql() extended with an injectable clientIp; rateLimitPlugin registered on the SAME ApolloServer instance server.js uses; resetRateLimitStore re-exported alongside resetTables"
      contains: "plugins: [rateLimitPlugin]"
    - path: "backend/test/setupRateLimit.js"
      provides: "a global Vitest setupFiles module resetting the rate-limit store before every single test in every file, independent of any one file's own beforeEach"
      contains: "beforeEach(() => resetRateLimitStore())"
    - path: "backend/src/resolvers/rateLimit.test.js"
      provides: "the mandatory RATE-01..05 + bypass-resistance proof suite, driven entirely by graphql()"
      contains: "TOO_MANY_REQUESTS"
  key_links:
    - from: "backend/test/helpers.js (graphql())"
      to: "backend/src/plugins/rateLimitPlugin.js (rateLimitPlugin)"
      via: "the test-harness ApolloServer is constructed with the identical plugins array server.js uses — the critical integration point flagged in 10-CONTEXT.md"
      pattern: "plugins: \\[rateLimitPlugin\\]"
    - from: "backend/vitest.config.js (setupFiles)"
      to: "backend/test/setupRateLimit.js"
      via: "every test file loads this setup module before its own beforeEach hooks run, guaranteeing a clean rate-limit counter per test"
      pattern: "setupFiles"
---

<objective>
Turn the plugin built in Plan 10-02 on for the SAME ApolloServer instance the in-process test harness (`graphql()` in `backend/test/helpers.js`) exercises — the critical integration point CONTEXT.md flags: without this, `executeOperation()` tests never see the real plugin. Guarantee test-suite integrity against this newly-global, cross-cutting piece of state (a global per-test reset hook, plus a targeted fix to the one pre-existing test that would otherwise collide with it). Then write the mandatory, comprehensive RATE-01..05 proof suite — including the two hardest-to-fake guarantees: the real-vs-nonexistent enumeration parity test (RATE-05) and the operation-rename bypass-resistance test (closing T-10-02b from Plan 10-02).

Purpose: This is where the phase's goal becomes independently verifiable. Every prior plan built a piece of the mechanism; this plan proves the mechanism as the ROADMAP's 5 success criteria actually specify it, using only the harness RATE-04 requires (`executeOperation()`, no HTTP).
Output: `test/helpers.js` wired to the real plugin; a global per-test rate-limit reset; a fixed `resetPassword.test.js`; a new `rateLimit.test.js` covering every phase success criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-rate-limiting-on-auth-mutations/10-CONTEXT.md
@.planning/phases/10-rate-limiting-on-auth-mutations/10-PATTERNS.md
@.planning/phases/10-rate-limiting-on-auth-mutations/10-01-SUMMARY.md
@.planning/phases/10-rate-limiting-on-auth-mutations/10-02-SUMMARY.md

<interfaces>
<!-- Existing/prior-plan code the executor consumes directly — no exploration needed. -->

From backend/test/helpers.js (full file, current state before this plan):
```javascript
import request from 'supertest';
import { ApolloServer } from '@apollo/server';
import { models } from '../src/models/index.js';
import { typeDefs } from '../src/schemas/index.js';
import { resolvers } from '../src/resolvers/index.js';
import { app } from '../src/server.js';

const server = new ApolloServer({ typeDefs, resolvers });

export function httpClient() {
  return request(app);
}

export async function graphql(query, variables, user = null) {
  const response = await server.executeOperation(
    { query, variables },
    { contextValue: { models, user } }
  );
  return response.body.singleResult;
}

export async function resetTables() {
  await models.User.destroy({ where: {}, truncate: true });
}

export async function createTestUser(overrides = {}) {
  return models.User.create({
    name: 'Test User',
    email: `test-${Date.now()}@example.com`,
    passwordHash: 'Password123!',
    role: 'USER',
    ...overrides
  });
}
```

From backend/vitest.config.js (full file, current state before this plan):
```javascript
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { defineConfig } from 'vitest/config';

const __dirname = path.dirname(fileURLToPath(import.meta.url));
process.env.ENV_FILE = path.resolve(__dirname, '../env/test.env');
process.env.NODE_ENV = 'test';

export default defineConfig({
  test: {
    globalSetup: ['./test/globalSetup.js'],
    pool: 'forks',
    fileParallelism: false
  }
});
```
`fileParallelism: false` means test files run sequentially in a shared worker process — this is precisely why a module-level singleton store (Plan 10-01's `rateLimitStore.js`) can leak counters across `it()` blocks and across files if nothing resets it. The `setupFiles` config key (not currently present) runs its module once per test file, before that file's own `beforeEach`/`it` blocks — registering a `beforeEach` there applies globally without touching every existing test file.

From backend/src/resolvers/resetPassword.test.js (the ONLY pre-existing test at risk — the full test, this plan's Task 1 modifies only the `sample` helper and its two call sites, nothing else in the file):
```javascript
it('does not leak account existence through response latency', async () => {
  await createTestUser({ email: 'timing-existing@example.com' });

  const sample = async (email) => {
    const startedAt = performance.now();
    const response = await graphql(REQUEST_RESET_MUTATION, { email });
    const elapsed = performance.now() - startedAt;
    expect(response.errors).toBeUndefined();
    expect(response.data.requestPasswordReset.message).toBe(
      'If the account exists, a password reset link has been sent.'
    );
    return elapsed;
  };

  const existing = [];
  const missing = [];
  for (let i = 0; i < 5; i += 1) {
    existing.push(await sample('timing-existing@example.com'));
    missing.push(await sample('no-such-user@example.com'));
  }
  ...
}, 20000);
```
This test issues 10 sequential `requestPasswordReset` calls (5 + 5) inside ONE `it` block, all at whatever `clientIp` `graphql()` defaults to. `requestPasswordReset`'s configured limit is 5/hour (`backend/src/config/rateLimits.js`). Once this plan's Task 1 registers the plugin on `test/helpers.js`'s `ApolloServer`, calls 6-10 in this test would incorrectly receive `TOO_MANY_REQUESTS` instead of the expected generic reset message, breaking this test's own assertions (`response.errors` would no longer be `undefined`) — this is a genuine, structural conflict introduced by turning the plugin on here, not a pre-existing bug. Fix it by giving each of the 10 calls a distinct `clientIp` so none of them share a rate-limit budget; do not touch any `expect(...)` assertion.

From backend/src/resolvers/dashboard.test.js and login.test.js (query/mutation constants to copy for the new `rateLimit.test.js`'s `ME_QUERY`/`DASHBOARD_QUERY`/`LOGIN_MUTATION` — both files already fully read in prior context, reused verbatim).

From backend/src/resolvers/sessionRevocation.test.js (fake-timer convention to mirror for the window-expiry test — `vi.setSystemTime(...)`, `afterEach(() => vi.useRealTimers())`, already fully read in prior context).

From backend/src/plugins/rateLimitPlugin.js (built in Plan 10-02 — exact message/code this suite asserts against): throws `GraphQLError('Too many requests. Please try again later.', { extensions: { code: 'TOO_MANY_REQUESTS' } })` on breach; keys by `${clientIp}:${field}` where `field` comes from the parsed operation AST, never the client-supplied operation name.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the plugin into test/helpers.js's ApolloServer + global per-test reset + fix the one pre-existing collision</name>
  <files>backend/test/helpers.js, backend/test/setupRateLimit.js, backend/vitest.config.js, backend/src/resolvers/resetPassword.test.js</files>
  <read_first>backend/test/helpers.js, backend/vitest.config.js, backend/src/resolvers/resetPassword.test.js (all reproduced in full above); backend/src/plugins/rateLimitPlugin.js and backend/src/utils/rateLimitStore.js (built in prior plans — the exact exports this task imports); .planning/phases/10-rate-limiting-on-auth-mutations/10-CONTEXT.md D-03, D-05, and the "Integration Points" note in canonical_refs (test/helpers.js constructs its OWN ApolloServer — the plugin MUST be present there or executeOperation() tests never exercise it)</read_first>
  <action>
    In `backend/test/helpers.js`: add `import { rateLimitPlugin } from '../src/plugins/rateLimitPlugin.js';` and `import { resetRateLimitStore } from '../src/utils/rateLimitStore.js';` to the top import block. Change `const server = new ApolloServer({ typeDefs, resolvers });` to `const server = new ApolloServer({ typeDefs, resolvers, plugins: [rateLimitPlugin] });` — this is the SAME `rateLimitPlugin` module `server.js` imports, satisfying the shared-plugin requirement. Change `export async function graphql(query, variables, user = null)` to `export async function graphql(query, variables, user = null, clientIp = '127.0.0.1')`, and change the `contextValue` passed to `executeOperation` from `{ models, user }` to `{ models, user, clientIp }`. Add `export { resetRateLimitStore };` as a new named export, placed after `resetTables` and before `createTestUser`, matching this file's existing flat barrel-of-utilities style.

    Create `backend/test/setupRateLimit.js`: a small Vitest setup module. Import `beforeEach` from `'vitest'` and `resetRateLimitStore` from `'../src/utils/rateLimitStore.js'`. At the top level of the module (not inside any function), call `beforeEach(() => resetRateLimitStore());`. This is the global, belt-and-suspenders isolation guarantee: every single test in every file starts with a fully cleared rate-limit store, regardless of any one file's own `beforeEach` hooks and regardless of whether Vitest's file isolation happens to reset module state between files or not.

    In `backend/vitest.config.js`: add `setupFiles: ['./test/setupRateLimit.js']` as a new key inside the `test: { ... }` config object, alongside the existing `globalSetup` key (do not remove or reorder `globalSetup`, `pool`, or `fileParallelism`).

    In `backend/src/resolvers/resetPassword.test.js`: fix ONLY the "does not leak account existence through response latency" test. Change the `sample` helper's signature from `async (email) => { ... }` to `async (email, clientIp) => { ... await graphql(REQUEST_RESET_MUTATION, { email }, null, clientIp); ... }` (pass `clientIp` through to `graphql`'s new 4th parameter — everything else in the helper, including every `expect(...)`, stays byte-identical). Update the `for` loop's two call sites from `sample('timing-existing@example.com')` / `sample('no-such-user@example.com')` to `sample('timing-existing@example.com', \`10.0.0.${i}\`)` / `sample('no-such-user@example.com', \`10.0.1.${i}\`)` — giving each of the 10 calls across the loop's 5 iterations a distinct clientIp (10 total distinct IPs), so no single IP ever exceeds `requestPasswordReset`'s 5-per-hour budget within this one test. Do not modify any other test in this file.
  </action>
  <acceptance_criteria>
    `grep -n "plugins: \[rateLimitPlugin\]" backend/test/helpers.js` returns exactly 1 match.
    `grep -n "clientIp = '127.0.0.1'" backend/test/helpers.js` returns exactly 1 match.
    `grep -n "export { resetRateLimitStore }" backend/test/helpers.js` returns exactly 1 match.
    `backend/test/setupRateLimit.js` exists; `grep -n "beforeEach(() => resetRateLimitStore())" backend/test/setupRateLimit.js` returns exactly 1 match.
    `grep -n "setupFiles" backend/vitest.config.js` returns exactly 1 match; `grep -n "globalSetup" backend/vitest.config.js` still returns exactly 1 match (unchanged, not removed).
    `grep -n "sample(email, clientIp)" backend/src/resolvers/resetPassword.test.js` or equivalent signature returns exactly 1 match; `grep -c "10.0.0." backend/src/resolvers/resetPassword.test.js` and `grep -c "10.0.1." backend/src/resolvers/resetPassword.test.js` each return at least 1 (distinct-IP fixture present).
    `npm test --workspace backend -- resetPassword` exits 0 (all its tests pass, including the previously-collision-prone timing test).
    Full backend suite `npm test --workspace backend` is green.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend</automated>
  </verify>
  <done>The in-process `graphql()` test harness now exercises the exact same `rateLimitPlugin` instance production traffic does; every test in the suite starts from a guaranteed-clean rate-limit counter state via a global setup hook; the one pre-existing test that would otherwise collide with the newly-global plugin is fixed by fixture isolation only, with zero assertion changes; full backend suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Mandatory RATE-01..05 + bypass-resistance proof suite via executeOperation()</name>
  <files>backend/src/resolvers/rateLimit.test.js</files>
  <read_first>backend/src/resolvers/login.test.js, backend/src/resolvers/register.test.js, backend/src/resolvers/resetPassword.test.js (mutation-string + graphql() conventions, all reproduced/read in prior context); backend/src/resolvers/dashboard.test.js (ME_QUERY/DASHBOARD_QUERY constants, reproduced in prior context); backend/src/resolvers/sessionRevocation.test.js (fake-timer convention for the window-expiry case); backend/src/plugins/rateLimitPlugin.js and backend/src/config/rateLimits.js (exact message, code, and thresholds this suite asserts against); .planning/ROADMAP.md Phase 10 success criteria 1-5; .planning/phases/10-rate-limiting-on-auth-mutations/10-CONTEXT.md D-08 (the mandatory real-vs-nonexistent parity requirement)</read_first>
  <action>
    Create `backend/src/resolvers/rateLimit.test.js`. Import `graphql`, `resetTables`, `resetRateLimitStore`, `createTestUser` from `'../../test/helpers.js'`; import `describe`, `it`, `expect`, `beforeEach`, `afterEach`, `vi` from `'vitest'`. Define local GraphQL constants copied from the existing analog files: `LOGIN_MUTATION`, `REGISTER_MUTATION`, `REQUEST_RESET_MUTATION`, `ME_QUERY`, `DASHBOARD_QUERY` — plus one new constant `RENAMED_LOGIN_MUTATION` that invokes the SAME `login` field under a DIFFERENT declared operation name: `mutation NotLogin($email: String!, $password: String!) { login(email: $email, password: $password) { token } }`. Add `beforeEach(async () => { await resetTables(); resetRateLimitStore(); });` (the explicit `resetRateLimitStore()` call here is redundant with Task 1's global `setupFiles` hook but keeps this file self-documenting and independently correct, matching `sessionRevocation.test.js`'s explicit multi-statement `beforeEach` style).

    Every `graphql(...)` call in every test below MUST pass an explicit, scenario-unique `clientIp` as the 4th argument — never rely on the shared `'127.0.0.1'` default — so scenarios never share a budget with each other or with any other test file.

    Write these `describe` blocks:

    1. `describe('login rate limiting (RATE-01)')`: (a) `it('rejects the 6th login attempt from the same IP within the window, regardless of credential validity')` — call `graphql(LOGIN_MUTATION, { email: 'nobody@example.com', password: 'wrong' }, null, '10.10.10.1')` 5 times, asserting `errors[0].message === 'Invalid email or password.'` each time; call it a 6th time and assert `errors[0].message === 'Too many requests. Please try again later.'` and `errors[0].extensions.code === 'TOO_MANY_REQUESTS'`. (b) `it('is keyed by the invoked field, not the client-supplied operation name')` — repeat the identical 6-call pattern using `RENAMED_LOGIN_MUTATION` and a FRESH clientIp (e.g. `'10.10.11.1'`), asserting the 6th call is STILL throttled identically — proving field-AST extraction, not the operation name, drives the limit.

    2. `describe('register rate limiting (RATE-02)')`: `it('rejects the 6th register attempt from the same IP within the window')` — loop `i` from 0 to 5 (6 calls), calling `graphql(REGISTER_MUTATION, { name: 'X', email: \`rl-user-${i}@example.com\`, password: 'Password123!' }, null, '10.10.20.1')` with a distinct email each time; assert calls 1-5 have `errors` undefined (5 users created); assert call 6 has `errors[0].message === 'Too many requests. Please try again later.'`; then query `await models.User.count({ where: { email: { [Op.like]: 'rl-user-%@example.com' } } })` (import `models` from `'../models/index.js'` and `Op` from `'sequelize'`) and assert it equals exactly 5 — the 6th attempt never reached the resolver.

    3. `describe('requestPasswordReset rate limiting (RATE-03)')`: `it('rejects the 6th reset request from the same IP within the window')` — call `graphql(REQUEST_RESET_MUTATION, { email: 'whoever@example.com' }, null, '10.10.30.1')` 6 times; assert calls 1-5 return `data.requestPasswordReset.message === 'If the account exists, a password reset link has been sent.'` with `errors` undefined; assert call 6 returns `errors[0].message === 'Too many requests. Please try again later.'`.

    4. `describe('normal queries are unaffected (RATE-04)')`: `it('never throttles an interleaved me/dashboard burst sharing an IP with an exhausted login budget')` — `createTestUser` a user; on clientIp `'10.10.40.1'`, interleave: 5 `graphql(LOGIN_MUTATION, { email: 'nobody@example.com', password: 'wrong' }, null, '10.10.40.1')` calls (exhausting THAT IP's login budget) with 10 `graphql(ME_QUERY, {}, user, '10.10.40.1')` calls and 10 `graphql(DASHBOARD_QUERY, {}, user, '10.10.40.1')` calls, all on the identical clientIp; assert every single one of the 20 me/dashboard responses has `errors` undefined, regardless of how exhausted that IP's `login` budget is.

    5. `describe('no enumeration oracle (RATE-05)')`: `it('breaches on the identical attempt number with the identical message for a real account and a nonexistent one')` — create a real user via `createTestUser({ email: 'rl-real@example.com' })` with the default password `'Password123!'`; run the SAME 6-call sequence twice on two FRESH, distinct clientIps: once as `graphql(LOGIN_MUTATION, { email: 'rl-real@example.com', password: 'Password123!' }, null, '10.10.50.1')` (correct credentials — calls 1-5 succeed, `data.login.token` present, `errors` undefined) and once as `graphql(LOGIN_MUTATION, { email: 'rl-nonexistent@example.com', password: 'whatever' }, null, '10.10.50.2')` (calls 1-5 return `errors[0].message === 'Invalid email or password.'`); assert BOTH sequences' 6th call returns the IDENTICAL `errors[0].message === 'Too many requests. Please try again later.'` and the IDENTICAL `errors[0].extensions.code === 'TOO_MANY_REQUESTS'` — pinning that the breach happens on the same attempt number with the same message regardless of account existence or credential correctness.

    6. `describe('window expiry')`: `afterEach(() => vi.useRealTimers());` at the top of this block. `it('allows a new attempt once the window has fully elapsed')` — on a fresh clientIp `'10.10.60.1'`, exhaust the 5-call login budget with 5 wrong-password attempts (assert `errors[0].message === 'Invalid email or password.'` on each); call `vi.useFakeTimers({ shouldAdvanceTime: true }); vi.setSystemTime(Date.now() + 15 * 60 * 1000 + 1000);` (advance past the 15-minute window); call `graphql(LOGIN_MUTATION, { email: 'nobody@example.com', password: 'wrong' }, null, '10.10.60.1')` again and assert it returns `errors[0].message === 'Invalid email or password.'` (NOT the rate-limit message) — proving the window actually resets at the full-stack level, not just in the Plan 10-01 unit test.
  </action>
  <acceptance_criteria>
    `grep -c "clientIp" backend/src/resolvers/rateLimit.test.js` returns at least 15 (nearly every `graphql()` call passes an explicit, scenario-unique clientIp).
    `grep -n "RENAMED_LOGIN_MUTATION" backend/src/resolvers/rateLimit.test.js` returns at least 2 matches (defined once, used once).
    `grep -c "TOO_MANY_REQUESTS" backend/src/resolvers/rateLimit.test.js` returns at least 6 matches (one breach assertion per describe block: login, renamed-login, register, requestPasswordReset, parity x2).
    `grep -n "describe('no enumeration oracle" backend/src/resolvers/rateLimit.test.js` returns exactly 1 match, and within that block both the real-account and nonexistent-account 6th-call assertions use the literal string `'Too many requests. Please try again later.'`.
    `grep -n "vi.setSystemTime" backend/src/resolvers/rateLimit.test.js` returns at least 1 match, paired with an `afterEach(() => vi.useRealTimers())` in the same describe block.
    `npm test --workspace backend -- rateLimit.test` exits 0.
    Full backend suite `npm test --workspace backend` is green.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- rateLimit.test && npm test --workspace backend</automated>
  </verify>
  <done>RATE-01 through RATE-05 and ROADMAP Phase 10 success criteria 1-5 are each proven by a dedicated, in-process `executeOperation()`-driven test — including the mandatory real-vs-nonexistent enumeration parity test and a dedicated operation-rename bypass-resistance test — with zero HTTP boot required; full backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Shared in-process rate-limit store across the ENTIRE test run (all files, sequential, `fileParallelism: false`) | This plan is the first point where that shared state becomes globally live across every test file — it must not become a source of flaky, order-dependent test failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03a | Information Disclosure (enumeration oracle) | `login` resolver + `rateLimitPlugin` combined behavior | mitigate | Dedicated parity test (Task 2, `describe('no enumeration oracle')`) pins an identical breach-attempt-number and identical message for a real vs. nonexistent account — the mandatory ROADMAP SC-5 proof. |
| T-10-03b | Tampering (limiter bypass via operation renaming) | `rateLimitPlugin` field extraction, exercised end-to-end | mitigate | Dedicated bypass-resistance test (Task 2, `describe('login rate limiting')` part (b)) proves a renamed mutation invoking the same `login` field is throttled identically — closes T-10-02b from Plan 10-02 with a full-stack proof, not just the isolated unit test. |
| T-10-03c | Denial of Service (this change breaking the test suite / CI itself) | `test/helpers.js`, `vitest.config.js`, all pre-existing `graphql()`-driven test files | mitigate | Global `setupFiles`-based reset before every test (belt-and-suspenders, correct regardless of Vitest's file-isolation behavior) plus a targeted, assertion-preserving fixture fix to the one pre-existing test proven to collide with the newly-global plugin (Task 1). Proven by the full suite staying green. |
| T-10-03d | Denial of Service (normal-query starvation) | `me`/`dashboard` queries sharing an IP with an exhausted `login` budget | mitigate | Dedicated interleaved test (Task 2, `describe('normal queries are unaffected')`) proves 20 me/dashboard calls never throw regardless of a co-located exhausted login budget on the identical IP — the mandatory ROADMAP SC-4 proof. |
</threat_model>

<verification>
Run `npm test --workspace backend` after Task 1 (confirms the plugin going live on the test harness does not regress the pre-existing suite). Run `npm test --workspace backend -- rateLimit.test` after Task 2, then the full suite again. Confirm via each task's `<acceptance_criteria>` grep checks that every scenario uses a distinct clientIp and that the parity/bypass-resistance tests assert the exact locked message and code.
</verification>

<success_criteria>
- `login` is rejected after 5 attempts/15min per IP, before-or-regardless-of credential validity (SC-1).
- `register` and `requestPasswordReset` are each rejected after 5 attempts/hour per IP (SC-2).
- The entire mechanism is proven via `executeOperation()` (`graphql()`), no HTTP boot required (SC-3).
- An interleaved `me`/`dashboard` burst on an exhausted-login IP is never throttled (SC-4).
- A breach returns the generic message with an identical trigger-attempt-number for a real vs. nonexistent account (SC-5).
- Renaming/anonymizing the GraphQL operation name does not bypass the limiter.
- D-09 (frontend surfaces the throttle error through the existing error `<Alert>`, no special UI) requires zero frontend code changes: `frontend/src/api/graphqlClient.js`'s `graphqlRequest` already extracts and joins any `response.data.errors[].message` into a single thrown `Error`, and every auth page (`Login.jsx`, `Register.jsx`, `ForgotPassword.jsx`) already renders that message via MUI `<Alert severity="error">` — the new `'Too many requests. Please try again later.'` message surfaces correctly with no frontend files modified by this phase.
- Full backend suite is green.
</success_criteria>

<output>
Create `.planning/phases/10-rate-limiting-on-auth-mutations/10-03-SUMMARY.md` when done
</output>
</content>
