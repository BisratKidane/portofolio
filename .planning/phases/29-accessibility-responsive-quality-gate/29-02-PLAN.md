---
phase: 29-accessibility-responsive-quality-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/test/setup.js
  - frontend/src/theme.contrast.test.js
  - frontend/src/components/person/PersonCard.jsx
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "Text/background contrast on /detail's PersonCard surfaces (Ge'ez name, role label, Living/Deceased chip) meets WCAG AA (>=4.5:1 normal text) — code-enforced by a deterministic unit test, not eyeballed (SC-2)."
    - "jest-axe's toHaveNoViolations() matcher is available to every frontend test file without per-file boilerplate."
    - "The root Paper in PersonCard.jsx no longer declares a &:focus-visible CSS rule that can never fire (it has no tabIndex/interactive role) — dead code removed, not silently left."
    - "No shared/exported gender token (MALE_TINT, FEMALE_TINT, colors.slate) was changed — the contrast fix is local to PersonCard.jsx's own text-color usage, so /family's rendering is provably unaffected."
  artifacts:
    - path: "frontend/src/theme.contrast.test.js"
      provides: "Deterministic WCAG AA contrast assertions for /detail's text-on-background pairs"
      contains: "wcag-contrast"
    - path: "frontend/test/setup.js"
      provides: "Global jest-axe matcher registration"
      contains: "toHaveNoViolations"
    - path: "frontend/src/components/person/PersonCard.jsx"
      provides: "WCAG-AA-passing text colors for Ge'ez name / role label / Living chip; no dead focus-visible CSS"
  key_links:
    - from: "frontend/src/theme.contrast.test.js"
      to: "frontend/src/components/person/PersonCard.jsx"
      via: "identical hex color constants (test asserts the exact values PersonCard.jsx renders)"
      pattern: "wcag_contrast\\.hex|contrast\\.hex"
    - from: "frontend/test/setup.js"
      to: "jest-axe"
      via: "expect.extend(toHaveNoViolations)"
      pattern: "expect\\.extend\\(toHaveNoViolations\\)"
---

<objective>
Install and wire `jest-axe` (D-02 tooling) and land the code-enforced WCAG AA contrast gate
(D-03), fixing the three real contrast failures RESEARCH.md's audit confirmed on `/detail`'s
`PersonCard` (Ge'ez name text, role label text, "Living" chip text) — all against the SAME
composited card background PersonCard.jsx actually renders. Also resolves the dead
`&:focus-visible` CSS landmine on the card's root `Paper` (RESEARCH.md Pitfall 1) by removing it,
since the card's real interactive elements (Edit/Add/Expand buttons) already receive native
browser focus-visible styling with no custom CSS needed.

Purpose: SC-2 (WCAG AA contrast) must be a real, code-enforced fix — not a test-only exercise —
and the fix must not touch the shared `MALE_TINT`/`FEMALE_TINT`/`colors.slate` tokens `/family`
also consumes (D-06 audit-boundary discipline; CLAUDE.md's non-destructive-milestone constraint).
Output: `jest-axe`/`wcag-contrast` installed, `theme.contrast.test.js` (red-then-green TDD),
`PersonCard.jsx` fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-accessibility-responsive-quality-gate/29-CONTEXT.md
@.planning/phases/29-accessibility-responsive-quality-gate/29-RESEARCH.md
@.planning/phases/29-accessibility-responsive-quality-gate/29-PATTERNS.md
</context>

<interfaces>
<!-- frontend/src/theme.js -- colors this plan reads/composites against (must NOT be modified): -->
```javascript
export const colors = {
  ink: '#0f172a', slate: '#64748b', bg: '#f5f6fb', paper: '#ffffff', success: '#10b981', /* ... */
};
```

<!-- frontend/src/utils/genderTheme.js -- shared tokens this plan must NOT modify (whole file): -->
```javascript
export const MALE_TINT = '#3b82f6';
export const FEMALE_TINT = '#ec4899';
export function genderMeta(gender) {
  if (gender === 'Male') return { label: 'Male', tint: MALE_TINT };
  if (gender === 'Female') return { label: 'Female', tint: FEMALE_TINT };
  return { label: 'Other', tint: colors.slate };
}
```

<!-- PersonCard.jsx's card background composition (line 97) -- the exact "effective background"
     the contrast test and the fix must both reason about: -->
```javascript
sx={{ bgcolor: `${genderTint}14`, /* '14' = alpha hex byte, ~7.8%, over page colors.bg #f5f6fb */ }}
```
Composited (page colors.bg #f5f6fb under genderTint at alpha 0x14): male `#e6edfb`, female
`#f4e8f3`, other `#eaecf2` (all confirmed in RESEARCH.md's contrast table; luminances of the three
are within ~0.01 of each other).

<!-- frontend/test/setup.js -- whole file, to extend in place: -->
```javascript
import '@testing-library/jest-dom/vitest';
import { afterEach } from 'vitest';
import { cleanup } from '@testing-library/react';

window.matchMedia = window.matchMedia || function matchMedia(query) { /* ... */ };

afterEach(() => {
  localStorage.clear();
  cleanup();
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install jest-axe + wcag-contrast, register the global matcher</name>
  <files>frontend/package.json, frontend/test/setup.js</files>
  <read_first>
    frontend/package.json (current devDependencies block to extend)
    frontend/test/setup.js (whole file, shown above — extend in place)
    .planning/phases/29-accessibility-responsive-quality-gate/29-RESEARCH.md "Standard Stack" and
    "Package Legitimacy Audit" sections (jest-axe ^11.0.0 and wcag-contrast ^3.0.0, both
    slopcheck-`[OK]`-verified — this is a pre-cleared install, no legitimacy checkpoint needed)
  </read_first>
  <action>
    Run `npm install --workspace frontend jest-axe wcag-contrast` (adds both to
    `frontend/package.json` devDependencies and the root lockfile — per RESEARCH.md's version pins
    `jest-axe@^11.0.0`, `wcag-contrast@^3.0.0`; do NOT add to the root `package.json`). Then edit
    `frontend/test/setup.js`: add `import { toHaveNoViolations } from 'jest-axe';` and
    `expect.extend(toHaveNoViolations);` (top-level, alongside the existing imports) so
    `toHaveNoViolations()` is globally available to every frontend test file with zero per-file
    boilerplate.
  </action>
  <acceptance_criteria>
    - `frontend/package.json` devDependencies contains both `jest-axe` and `wcag-contrast`.
    - `frontend/test/setup.js` contains the literal string `expect.extend(toHaveNoViolations)`.
    - `npm ls --workspace frontend jest-axe wcag-contrast` reports both installed with no errors.
  </acceptance_criteria>
  <verify>
    <automated>npm ls --workspace frontend jest-axe wcag-contrast</automated>
  </verify>
  <done>Both packages installed under the frontend workspace; the axe matcher is registered globally in setup.js.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the deterministic WCAG contrast test (RED)</name>
  <files>frontend/src/theme.contrast.test.js</files>
  <behavior>
    - Fullname (`colors.ink #0f172a`) on the male/female/other composited card background
      (`#e6edfb` / `#f4e8f3` / `#eaecf2`) meets >=4.5:1 (PASS today, ~15:1 — write to confirm
      it stays that way, a regression guard).
    - Ge'ez name text color on its own gender's card background meets >=4.5:1 for all three
      gender variants (Male/Female/Other) — CURRENTLY FAILS with today's `PersonCard.jsx` (raw
      `genderTint` used directly as text color: male 3.13:1, female 2.97:1, other ~4.03:1). This
      test must import PersonCard.jsx's actual rendered text-color value (see Task 3's new
      `TEXT_TINT` export), not the raw `MALE_TINT`/`FEMALE_TINT` — the assertion validates the
      FIX, not the pre-fix token.
    - Role label text color (currently `colors.slate #64748b`) on the same three card backgrounds
      meets >=4.5:1 — CURRENTLY FAILS (4.01-4.05:1 today).
    - "Living" chip text color (currently `success.main #10b981` at full opacity per MUI's
      outlined-Chip behavior) on the same three card backgrounds meets >=4.5:1 — CURRENTLY FAILS
      as low as 2.16:1.
  </behavior>
  <read_first>
    frontend/src/theme.js (colors.ink, colors.bg — composited background source)
    frontend/src/utils/genderTheme.js (MALE_TINT, FEMALE_TINT, genderMeta — read-only, do not
    modify)
    .planning/phases/29-accessibility-responsive-quality-gate/29-RESEARCH.md section "D-03:
    Deterministic WCAG contrast test" (the full computed contrast table for all 8 text/background
    pairs, and the worked `compositeOverPage()` + test-file-shape example)
    .planning/phases/29-accessibility-responsive-quality-gate/29-PATTERNS.md section
    `frontend/src/theme.contrast.test.js` (the exact recommended import/test-shape, since this
    file has no in-repo analog)
  </read_first>
  <action>
    Create `frontend/src/theme.contrast.test.js` (pure-data test, no DOM/component rendering).
    Import `contrast` from `wcag-contrast`, `colors` from `./theme.js`. Write a local
    `compositeOverPage(hexFg, alphaByte, hexBg = colors.bg)` helper that alpha-composites a
    foreground hex color at the given alpha byte (0-255) over a background hex color, mirroring
    `PersonCard.jsx`'s `bgcolor: \`${genderTint}14\`` (alpha byte `0x14` = 20/255). Compute the
    three composited card backgrounds (male over `MALE_TINT`, female over `FEMALE_TINT`, other
    over `colors.slate`, each at alpha `0x14`) using that helper. Import the fix's new
    `TEXT_TINT` map and darkened chip-text color from `PersonCard.jsx` (Task 3 will export these
    as named exports alongside the default export) rather than re-deriving them, so the test is
    provably testing what the component actually renders. Write 4 `it()` blocks per the behavior
    list above, each asserting `contrast.hex(fg, bg) >= 4.5` for all three gender variants (loop or
    explicit per-variant assertions — either is fine). At this point `PersonCard.jsx` has not yet
    been fixed (Task 3 runs next), so `npm test --workspace frontend -- --run
    theme.contrast.test.js` is EXPECTED to fail on import (TEXT_TINT does not exist yet) or on the
    3 known-failing assertions — this is the RED state; do not attempt to make it pass in this
    task.
  </action>
  <acceptance_criteria>
    - `frontend/src/theme.contrast.test.js` exists and contains at least 4 `it(` blocks.
    - The file imports `contrast` from `'wcag-contrast'`.
    - Running `npm test --workspace frontend -- --run theme.contrast.test.js` FAILS at this point
      in the plan (RED) — either an import error (TEXT_TINT not yet exported) or one/more failing
      assertions. Confirm by running the command and reading the actual output; do not assume.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace frontend -- --run theme.contrast.test.js; test $? -ne 0 && echo "RED confirmed"</automated>
  </verify>
  <done>theme.contrast.test.js exists, asserts >=4.5:1 for all 4 pairs across all 3 gender variants, and is currently RED against the unfixed PersonCard.jsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix PersonCard.jsx contrast + remove dead focus-visible CSS (GREEN)</name>
  <files>frontend/src/components/person/PersonCard.jsx</files>
  <read_first>
    frontend/src/components/person/PersonCard.jsx (whole file, 197 lines — the current Ge'ez name
    Typography at line 171, role label Typography at line 177, Chip at line 182, and the dead
    `&:focus-visible` CSS block on the root Paper at line 106)
    frontend/src/theme.contrast.test.js (just written in Task 2 — the exact assertions this fix
    must satisfy)
    .planning/phases/29-accessibility-responsive-quality-gate/29-RESEARCH.md "Open Question 3"
    (explicit guidance: prefer a fix local to PersonCard.jsx's own text-color usage over changing
    the shared `MALE_TINT`/`FEMALE_TINT`/`colors.slate` tokens, to avoid an unplanned `/family`-wide
    visual change)
  </read_first>
  <action>
    In `PersonCard.jsx`, add a new local, NOT-exported-from-genderTheme.js constant map (named
    export from PersonCard.jsx itself, so Task 2's test can import it): `export const TEXT_TINT =
    { Male: '#1e40af', Female: '#9d174d', Other: '#334155' };` (computed to give >=4.5:1 against
    all three composited card backgrounds, per RESEARCH.md's contrast math — Male ~7.4:1, Female
    ~6.6:1, Other ~8.8:1). Also add `export const CHIP_TEXT_ALIVE = '#065f46';` (a darker green,
    ~6.5:1 against all three card backgrounds — used ONLY for the `isAlive === true` state; do not
    touch the `Deceased`/`default`-color branch, which RESEARCH.md did not flag as failing).
    Replace line 171's `color: genderTint` with `color: TEXT_TINT[genderLabel] ?? TEXT_TINT.Other`
    on the Ge'ez name Typography. Replace line 177's `color: colors.slate` with `color:
    TEXT_TINT[genderLabel] ?? TEXT_TINT.Other` on the role label Typography (reuse the same map --
    both are text-on-card-bg pairs with the same contrast requirement). On the Chip (line 182),
    when `isAlive` is true, add an `sx` prop setting `color: CHIP_TEXT_ALIVE` and `borderColor:
    CHIP_TEXT_ALIVE` (leave the Deceased/`color="default"` branch's styling untouched — only
    override for the `isAlive` branch). Finally, remove the dead `'&:focus-visible': { outline:
    ...}` block entirely from the root `Paper`'s `sx` prop (line 106) — the card's real
    interactive elements (Edit/Add/Expand `IconButton`s) are native `<button>`s and already
    receive the browser's native focus-visible outline with no custom CSS; the `Paper` itself has
    no `tabIndex`/interactive role and can never receive this outline via keyboard navigation
    (RESEARCH.md Pitfall 1, option (a) — the safest choice, no scope-creep into making the whole
    card focusable). Do not change `MALE_TINT`, `FEMALE_TINT`, `colors.slate`, or any other
    exported token in `theme.js`/`genderTheme.js` — those files are read-only for this task.
  </action>
  <acceptance_criteria>
    - `PersonCard.jsx` exports `TEXT_TINT` and `CHIP_TEXT_ALIVE` as named exports alongside its
      existing `export default function PersonCard`.
    - `grep -c "focus-visible" frontend/src/components/person/PersonCard.jsx` equals `0`.
    - `git diff frontend/src/utils/genderTheme.js frontend/src/theme.js` is empty (neither file
      touched).
    - `npm test --workspace frontend -- --run theme.contrast.test.js` exits 0 (GREEN).
    - `npm test --workspace frontend -- --run PersonCard.test.jsx` exits 0 (no existing behavior
      regressed — same text content, same role/aria-label assertions, same Living/Deceased chip
      text).
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace frontend -- --run theme.contrast.test.js PersonCard.test.jsx</automated>
  </verify>
  <done>
    theme.contrast.test.js is GREEN, PersonCard.test.jsx still passes unmodified, the dead
    focus-visible CSS is gone, and no shared gender/theme token was changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry → frontend workspace | New dev dependencies `jest-axe`/`wcag-contrast` installed from the public npm registry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-29-SC | Tampering | `npm install --workspace frontend jest-axe wcag-contrast` | mitigate | Both packages were pre-audited in RESEARCH.md's "Package Legitimacy Audit" table (slopcheck `[OK]`, long-established, high-download, source-repo-confirmed) — no `[ASSUMED]`/`[SUS]`/`[SLOP]` verdict, so no blocking human-verify checkpoint is required per the gate protocol; Task 1's acceptance criteria re-confirms the exact installed packages via `npm ls`. |
| T-29-03 | Tampering | Shared gender/theme tokens (`MALE_TINT`, `FEMALE_TINT`, `colors.slate`) | mitigate | The contrast fix is scoped to `PersonCard.jsx`'s own local `TEXT_TINT`/`CHIP_TEXT_ALIVE` constants — Task 3's acceptance criteria explicitly requires `git diff` on `theme.js`/`genderTheme.js` to be empty, preventing an unplanned `/family`-wide visual regression (non-destructive-milestone constraint, D-06 audit-boundary discipline). |

</threat_model>

<verification>
`npm test --workspace frontend -- --run theme.contrast.test.js PersonCard.test.jsx` exits 0.
`npm ls --workspace frontend jest-axe wcag-contrast` shows both installed. `git diff
frontend/src/theme.js frontend/src/utils/genderTheme.js` is empty.
</verification>

<success_criteria>
- jest-axe + wcag-contrast installed under the frontend workspace; `toHaveNoViolations` globally
  registered in `frontend/test/setup.js`.
- `theme.contrast.test.js` exists, went RED then GREEN, and asserts >=4.5:1 for all 4
  text/background pairs across all 3 gender variants.
- `PersonCard.jsx`'s Ge'ez name, role label, and Living-chip text all meet WCAG AA contrast; no
  shared token changed; the dead `&:focus-visible` CSS is removed.
- `PersonCard.test.jsx` (existing suite) still passes unmodified.
</success_criteria>

<output>
Create `.planning/phases/29-accessibility-responsive-quality-gate/29-02-SUMMARY.md` when done.
</output>
