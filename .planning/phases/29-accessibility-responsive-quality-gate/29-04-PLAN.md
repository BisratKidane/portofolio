---
phase: 29-accessibility-responsive-quality-gate
plan: 04
type: execute
wave: 3
depends_on: ["29-01", "29-02", "29-03"]
files_modified:
  - .planning/phases/29-accessibility-responsive-quality-gate/29-HUMAN-UAT.md
autonomous: false
requirements: [A11Y-01]

must_haves:
  truths:
    - "The full `npm test --workspaces` suite exits 0 on the developer's local engine, with the D-01 MariaDB-skip caveat explicitly surfaced in the report — never a false unqualified 'all green' (D-05)."
    - "The layout stays readable and usable at 360px and 768px viewport widths, confirmed by a human (jsdom cannot measure rendered layout) (SC-3)."
    - "A real, visually-painted focus ring is confirmed by a human Tab-walk at both target widths -- the one thing RTL's toHaveFocus() cannot prove (SC-1's visible-focus requirement)."
  artifacts:
    - path: ".planning/phases/29-accessibility-responsive-quality-gate/29-HUMAN-UAT.md"
      provides: "Mobile-viewport + keyboard-visible-focus human sign-off checklist, mirroring 26-HUMAN-UAT.md/27-HUMAN-UAT.md"
      contains: "360px"
  key_links:
    - from: ".planning/phases/29-accessibility-responsive-quality-gate/29-HUMAN-UAT.md"
      to: "frontend/src/components/person/GenerationGrid.jsx"
      via: "manually verifies the responsive grid Plan 29-03's breakpoint-CSS-presence test only proves exists in markup, not that it visually works"
      pattern: "768px"
---

<objective>
Close the milestone: create the mobile/keyboard HUMAN-UAT checklist (D-04's human leg, mirroring
the established `26-HUMAN-UAT.md`/`27-HUMAN-UAT.md` pattern), run the full `npm test --workspaces`
gate and report its result honestly (D-05 -- no false "100% green" without the D-01 MariaDB-skip
caveat surfaced), then have the human perform the one class of check jsdom structurally cannot do:
confirm the mobile layout is genuinely readable/usable and that keyboard focus is genuinely
*visible* (painted), not merely landed on the right element.

Purpose: this is the phase's final gate -- it depends on every other plan (01, 02, 03) being
complete, per ROADMAP.md's SC-4 ("full automated suite green ... consistent with this project's
TDD/CI convention") and CONTEXT.md D-05.
Output: `29-HUMAN-UAT.md`, a full-suite test run report, and human sign-off on mobile/keyboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/29-accessibility-responsive-quality-gate/29-CONTEXT.md
@.planning/phases/29-accessibility-responsive-quality-gate/29-RESEARCH.md
@.planning/phases/27-descendant-navigation-performance/27-HUMAN-UAT.md
</context>

<interfaces>
<!-- 27-HUMAN-UAT.md's exact frontmatter + section structure to mirror (whole file, 40 lines): -->
```markdown
---
status: partial
phase: 27-descendant-navigation-performance
source: [27-VERIFICATION.md, 27-REVIEW.md]
started: "2026-08-04T06:35:00Z"
updated: "2026-08-04T06:35:00Z"
---

## Current Test

[awaiting human testing]

## Tests

### 1. Responsive grid + apex connector visual check
expected: ...
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create 29-HUMAN-UAT.md (mobile + keyboard-visible-focus checklist)</name>
  <files>.planning/phases/29-accessibility-responsive-quality-gate/29-HUMAN-UAT.md</files>
  <read_first>
    .planning/phases/27-descendant-navigation-performance/27-HUMAN-UAT.md (whole file -- the exact
    frontmatter/section format to mirror)
    .planning/phases/29-accessibility-responsive-quality-gate/29-RESEARCH.md section "D-04: Mobile
    responsive verification" (the precise breakpoint arithmetic: at 360px only `xs:12` applies -- 1
    card/row; at 768px `sm:6` applies -- 2 cards/row, NOT 3, NOT 1 -- and the exact 3-item HUMAN-UAT
    checklist RESEARCH.md drafted)
  </read_first>
  <action>
    Create `.planning/phases/29-accessibility-responsive-quality-gate/29-HUMAN-UAT.md` with
    frontmatter `status: partial`, `phase: 29-accessibility-responsive-quality-gate`, `source:
    [29-VERIFICATION.md, 29-REVIEW.md]`, `started`/`updated` set to the current ISO timestamp.
    Include a `## Current Test` section with `[awaiting human testing]`, then a `## Tests` section
    with exactly 3 numbered items: (1) "360px mobile layout" -- expected: single-column card
    stack, no horizontal scroll, no text truncation/overlap on the head card or an expanded
    generation row, search bar and its suggestion dropdown remain usable/tappable (44px+ touch
    targets already coded on the IconButtons); (2) "768px tablet layout" -- expected: exactly 2
    generation-grid cards per row (not 3, not 1 -- per the confirmed `sm:600/md:900` breakpoint
    arithmetic), apex connector and spouse dashed-connector remain legible and don't wrap
    awkwardly; (3) "Keyboard-only visible-focus pass at both widths" -- expected: Tab through the
    head card's Edit/Add/Expand controls at 360px and 768px and confirm a real, visually-painted
    focus ring appears on each -- this is the one thing the automated `toHaveFocus()` assertions
    from Plan 29-03 cannot prove (jsdom has no paint engine). Each item's `result:` is `[pending]`.
    Add a `## Summary` section with `total: 3`, `passed: 0`, `issues: 0`, `pending: 3`, `skipped:
    0`, `blocked: 0`. Add an empty `## Gaps` section.
  </action>
  <acceptance_criteria>
    - `.planning/phases/29-accessibility-responsive-quality-gate/29-HUMAN-UAT.md` exists with
      valid YAML frontmatter (`status`, `phase`, `source`, `started`, `updated`).
    - Contains exactly 3 numbered items under `## Tests`.
    - Item 2's expected text contains the literal string "2" adjacent to "per row" (or equivalent
      unambiguous "2, not 3" phrasing) -- the precise, easy-to-get-wrong number for 768px.
    - `## Summary` shows `total: 3` and `pending: 3`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^### " ".planning/phases/29-accessibility-responsive-quality-gate/29-HUMAN-UAT.md"</automated>
  </verify>
  <done>29-HUMAN-UAT.md exists with the 3-item mobile/keyboard checklist, format-matching 26-HUMAN-UAT.md/27-HUMAN-UAT.md.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full-suite SC-4 gate and report honestly (D-05)</name>
  <files>n/a -- verification only, no files created or modified</files>
  <read_first>
    .planning/phases/29-accessibility-responsive-quality-gate/29-CONTEXT.md D-05 ("SC-4 is
    satisfied when frontend is fully green (local + CI) and backend is green in CI (MySQL 8.4)
    with the two tests engine-gated locally. Do not report 'all green' without the D-01 caveat
    surfaced.")
    .github/workflows/ci.yml (confirms CI still runs against a `mysql:8.4` service -- unaffected
    by this phase's changes)
    KNOWN-ISSUES.md (the MariaDB-skip entry Plan 29-01 added -- cite it in the report)
  </read_first>
  <action>
    Run `npm test --workspaces` from the repo root and capture the full output. Confirm the
    process exits 0. Inspect the output for the backend suite's reporter lines covering
    `verifyEmail.test.js` and `familyMember.dedup.test.js` -- on a local MariaDB engine, both the
    VERIFY-04 and REL-06 tests must show as skipped with their documented reason text visible; on
    a MySQL engine, both must show as passed. Confirm the frontend suite (including
    `theme.contrast.test.js` and all four axe-scanned files from Plans 29-02/29-03) is fully green
    with zero skips. Do not report a bare "all tests pass" -- the plan's SUMMARY.md must explicitly
    state the current local engine, whether the two named tests ran or were skipped, and that CI
    (MySQL 8.4, per `.github/workflows/ci.yml`) is the environment where both unconditionally run
    and pass. This task is a verification/reporting step -- it modifies no source files.
  </action>
  <acceptance_criteria>
    - `npm test --workspaces` exits 0 (verify via `echo $?` immediately after the run).
    - The command's captured output is inspected (not assumed) for the exact pass/skip status of
      VERIFY-04 and REL-06.
    - The plan's SUMMARY.md explicitly states the local engine detected, the two tests' status on
      that engine, and that CI runs both unconditionally on MySQL 8.4 -- satisfying D-05's
      no-false-100%-claim requirement.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspaces; echo "exit:$?"</automated>
  </verify>
  <done>Full suite confirmed green (exit 0) on the local engine, with an honest, D-01-caveat-inclusive status reported in this plan's SUMMARY.md -- never an unqualified "100% green" claim.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off — mobile layout + visible keyboard focus at 360px and 768px</name>
  <files>.planning/phases/29-accessibility-responsive-quality-gate/29-HUMAN-UAT.md</files>
  <what-built>
    Automated coverage now proves: (1) zero axe-core violations on all four `/detail` surfaces,
    (2) correct keyboard tab order landing on the right elements, (3) WCAG AA contrast on
    previously-failing text (Ge'ez name, role label, Living/Deceased chip), and (4) the existence
    of the 600px/900px responsive breakpoint CSS rules. What automation structurally cannot prove
    (jsdom has no layout/paint engine): that the mobile grid *visually* reads correctly at 360px
    and 768px, and that a Tab-focused control *visually* shows a focus ring.
  </what-built>
  <action>
    Pause for the human to perform the 5-step browser check described in how-to-verify below, then
    record each of the 3 `29-HUMAN-UAT.md` items' `result:` (pass/issue) and update its
    `## Summary` counts accordingly.
  </action>
  <how-to-verify>
    1. Start the app locally (`npm run dev`) and open `/detail` in a real browser.
    2. Resize the viewport (or use browser devtools' device toolbar) to 360px wide. Verify: the
       generation grid stacks to a single column, no horizontal scroll appears, no name/label text
       is truncated or overlapping on the head card or an expanded generation row, and the search
       bar plus its suggestion dropdown remain comfortably tappable.
    3. Resize to 768px wide. Verify: an expanded generation grid shows exactly 2 cards per row
       (not 3, not 1), the apex connector and any spouse dashed-connector remain legible and don't
       wrap awkwardly.
    4. At both 360px and 768px, click into the page and press Tab repeatedly through the head
       card's Edit, Add, and Expand controls (and the search input). Confirm a real, visible focus
       ring is painted on each focused control — not just that focus silently landed there.
    5. Record each of the 3 items' `result:` in
       `.planning/phases/29-accessibility-responsive-quality-gate/29-HUMAN-UAT.md` (pass/issue),
       and update its `## Summary` counts accordingly.
  </how-to-verify>
  <verify>
    <human-check>29-HUMAN-UAT.md's ## Summary shows passed: 3 (or documents specific issues/gaps if not all 3 pass).</human-check>
  </verify>
  <done>All 3 items in 29-HUMAN-UAT.md are resolved (pass or documented issue) and the human has replied "approved" or described issues.</done>
  <resume-signal>Type "approved" once all 3 items pass, or describe any issue found (which item, what viewport, what broke).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Reported gate status → project record | This plan's own SUMMARY.md and STATE.md updates are the record future phases/audits rely on for "is v4.0 green." |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-29-05 | Repudiation | Full-suite gate reporting (Task 2) | mitigate | D-05 requires the report to name the local engine and the two tests' actual status, never a bare "all green" -- Task 2's acceptance criteria makes this explicit and auditable in the plan's own SUMMARY.md, preventing a future milestone-close audit from being misled by an unqualified pass claim. |
| T-29-06 | Repudiation | Human-verify checkpoint sign-off (Task 3) | mitigate | The checkpoint requires updating `29-HUMAN-UAT.md`'s per-item `result:` and `## Summary` counts, not just a verbal "approved" — leaving a durable, itemized record of what was actually checked at each viewport. |

</threat_model>

<verification>
`npm test --workspaces` exits 0. `.planning/phases/29-accessibility-responsive-quality-gate/29-HUMAN-UAT.md`'s
`## Summary` shows `passed: 3` after the human checkpoint completes (or documents specific
issues/gaps if not). This plan's SUMMARY.md states the local test engine and the VERIFY-04/REL-06
status explicitly.
</verification>

<success_criteria>
- `29-HUMAN-UAT.md` exists, mirrors the 26/27 format, and its 3 items are resolved (not left
  `[pending]`) by the end of this plan.
- `npm test --workspaces` exits 0 locally, with the D-01 MariaDB-skip caveat explicitly reported.
- CI (`.github/workflows/ci.yml`, MySQL 8.4) is confirmed unaffected -- both VERIFY-04 and REL-06
  still run and pass there.
- All four A11Y-01 success criteria (SC-1 keyboard/focus, SC-2 contrast, SC-3 mobile, SC-4 green)
  are traceable to a completed plan/task by the end of this phase.
</success_criteria>

<output>
Create `.planning/phases/29-accessibility-responsive-quality-gate/29-04-SUMMARY.md` when done.
</output>
