---
phase: 29-accessibility-responsive-quality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/test/dbEngine.js
  - backend/src/resolvers/verifyEmail.test.js
  - backend/src/services/familyMember.dedup.test.js
  - KNOWN-ISSUES.md
autonomous: true
requirements: [A11Y-01]

must_haves:
  truths:
    - "On local MariaDB, `npm test --workspace backend` exits 0 with exactly two tests visibly skipped (VERIFY-04, REL-06), each showing a documented reason string in reporter output."
    - "On CI (MySQL 8.4), both VERIFY-04 and REL-06 still execute and pass — CI concurrency coverage is unchanged."
    - "The MariaDB-vs-MySQL caveat is discoverable in KNOWN-ISSUES.md, not silent."
  artifacts:
    - path: "backend/test/dbEngine.js"
      provides: "isMariaDB() async helper (SELECT VERSION() engine auto-detection)"
      exports: ["isMariaDB"]
    - path: "backend/src/resolvers/verifyEmail.test.js"
      provides: "VERIFY-04 test guarded by ctx.skip(await isMariaDB(), reason)"
      contains: "ctx.skip"
    - path: "backend/src/services/familyMember.dedup.test.js"
      provides: "REL-06 test guarded by ctx.skip(await isMariaDB(), reason)"
      contains: "ctx.skip"
    - path: "KNOWN-ISSUES.md"
      provides: "MariaDB-only skip entry documenting the caveat"
      contains: "MariaDB-only skip"
  key_links:
    - from: "backend/src/resolvers/verifyEmail.test.js"
      to: "backend/test/dbEngine.js"
      via: "import { isMariaDB }"
      pattern: "import \\{ isMariaDB \\} from '\\.\\./\\.\\./test/dbEngine\\.js'"
    - from: "backend/src/services/familyMember.dedup.test.js"
      to: "backend/test/dbEngine.js"
      via: "import { isMariaDB }"
      pattern: "import \\{ isMariaDB \\} from '\\.\\./\\.\\./test/dbEngine\\.js'"
---

<objective>
Make the v4.0 milestone-close gate (SC-4) honest on both engines: create a single shared
`isMariaDB()` engine-detection helper and apply a visible, reason-documented conditional skip to
exactly the two named pre-existing MariaDB-only concurrency tests (VERIFY-04, REL-06), per
CONTEXT.md D-01 (locked decision). This does NOT touch product/resolver code — only test
infrastructure and documentation.

Purpose: `npm test --workspaces` must exit 0 on the developer's local MariaDB dev DB AND in CI
(MySQL 8.4), without weakening CI's concurrency coverage or silently masking a real defect.
Output: `backend/test/dbEngine.js`, two modified test files (one guarded test each), and an
updated `KNOWN-ISSUES.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-accessibility-responsive-quality-gate/29-CONTEXT.md
@.planning/phases/29-accessibility-responsive-quality-gate/29-RESEARCH.md
</context>

<interfaces>
<!-- backend/src/resolvers/verifyEmail.test.js's existing rawConnection() pattern (lines 25-39),
     the exact connection-construction shape dbEngine.js's isMariaDB() must mirror: -->
```javascript
import mysql from 'mysql2/promise';
import { env } from '../config/env.js'; // note: relative path differs by file depth

async function rawConnection() {
  const conn = await mysql.createConnection({
    host: env.database.host,
    port: env.database.port,
    user: env.database.user,
    password: env.database.password,
    database: env.database.name
  });
  await conn.query('SET SESSION innodb_lock_wait_timeout = 10');
  return conn;
}
```

<!-- backend/test/guard.js -- the sibling-module shape/convention for a small, single-purpose,
     named-export helper living in backend/test/ (whole file): -->
```javascript
import { env } from '../src/config/env.js';

export function assertTestDatabase() {
  const isTestEnv = env.nodeEnv === 'test';
  const isTestDbName = /_test$/.test(env.database.name);
  if (!isTestEnv || !isTestDbName) {
    throw new Error(`Refusing to run tests: expected NODE_ENV=test and DB_NAME ending in "_test", ...`);
  }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared `isMariaDB()` engine-detection helper</name>
  <files>backend/test/dbEngine.js</files>
  <read_first>
    backend/test/guard.js (module shape/style convention to mirror — small, named export, relative
    import `../src/config/env.js`)
    backend/src/resolvers/verifyEmail.test.js lines 1-39 (rawConnection() — the exact
    mysql2/promise connection-construction pattern to reuse for the version query)
    .planning/phases/29-accessibility-responsive-quality-gate/29-RESEARCH.md section "D-01:
    MariaDB engine-guard skip" (confirms no existing engine-detection helper anywhere in the
    backend — this is genuinely new code, not an extraction)
  </read_first>
  <action>
    Create `backend/test/dbEngine.js` as a new ESM module (sibling to `backend/test/guard.js`,
    `backend/test/helpers.js`). Import `mysql` from `mysql2/promise` and `env` from
    `../src/config/env.js`. Export a single async function `isMariaDB()` that: opens a
    `mysql.createConnection({ host: env.database.host, port: env.database.port, user:
    env.database.user, password: env.database.password, database: env.database.name })`, runs
    `SELECT VERSION() AS version`, tests the returned version string against `/mariadb/i`, closes
    the connection in a `finally` block via `conn.end()`, and returns the boolean match result. No
    manual env toggle — detection is fully automatic per D-01. Add a one-line comment noting
    MariaDB's `SELECT VERSION()` reports a string containing "MariaDB" (e.g.
    `10.11.6-MariaDB`) while MySQL 8.4's does not.
  </action>
  <acceptance_criteria>
    - `backend/test/dbEngine.js` exports a named async function `isMariaDB` (no default export).
    - Function body uses `mysql2/promise`'s `createConnection` with the same 5 connection keys
      (`host`, `port`, `user`, `password`, `database`) as `rawConnection()` in
      `verifyEmail.test.js`.
    - `node --check backend/test/dbEngine.js` exits 0 (syntax-valid ESM).
    - Running `node -e "import('./backend/test/dbEngine.js').then(m => console.log(typeof m.isMariaDB))"`
      from repo root prints `function`.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && node --check test/dbEngine.js</automated>
  </verify>
  <done>`backend/test/dbEngine.js` exists, exports `isMariaDB`, and is syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the documented `ctx.skip` guard to VERIFY-04 and REL-06 only</name>
  <files>
    backend/src/resolvers/verifyEmail.test.js
    backend/src/services/familyMember.dedup.test.js
  </files>
  <read_first>
    backend/test/dbEngine.js (the isMariaDB() export just created in Task 1)
    backend/src/resolvers/verifyEmail.test.js lines 155-222 (the exact VERIFY-04 test — "lets two
    users racing to verify simultaneously each keep a usable session, with exactly one becoming
    ADMIN (VERIFY-04)" — the ONLY test in this file to guard; the other 5 tests, including the
    similarly-concurrency-flavored "serializes a concurrent verifier..." test at line 109, are
    engine-agnostic and must NOT be touched)
    backend/src/services/familyMember.dedup.test.js lines 113-147 (the exact REL-06 test —
    "(D-10 resolver-path TOCTOU, CR-01) detects a duplicate even when a plain read earlier in the
    SAME transaction froze the REPEATABLE READ snapshot..." — the ONLY test in this file to guard;
    the other 6 tests, including the similarly-named "(D-10 TOCTOU proof) two genuinely concurrent
    addChild calls..." at line 72, are engine-agnostic and must NOT be touched)
    .planning/phases/29-accessibility-responsive-quality-gate/29-RESEARCH.md "Pitfall 3" (why
    `context.skip(condition, reason)` — NOT `it.skipIf`/`describe.skipIf` — is required: the
    condition is async, and `skipIf` needs a synchronous boolean at collection time)
  </read_first>
  <action>
    In both files, add `import { isMariaDB } from '../../test/dbEngine.js';` near the top. In
    EACH file, modify ONLY the one named test (VERIFY-04 in verifyEmail.test.js; REL-06 in
    familyMember.dedup.test.js) to accept the Vitest test-context parameter — change `async () =>`
    to `async (ctx) =>` in that test's callback signature — and add as the FIRST line of the test
    body: `ctx.skip(await isMariaDB(), '...')` where the reason string states that MariaDB does
    not implement the MySQL 8.4 `SELECT ... FOR UPDATE` lock-wait semantics the test asserts
    (MariaDB raises a Sequelize optimistic-version error instead), that the test runs and must
    pass on CI (MySQL 8.4), and references `KNOWN-ISSUES.md`. Do not modify any other test in
    either file, do not touch `rawConnection()`, and do not modify any resolver/service/product
    code — this is a test-file-only, two-line-per-file diff (import + one `ctx.skip` call).
  </action>
  <acceptance_criteria>
    - `grep -c "ctx.skip" backend/src/resolvers/verifyEmail.test.js` equals `1`.
    - `grep -c "ctx.skip" backend/src/services/familyMember.dedup.test.js` equals `1`.
    - Both files import `isMariaDB` from `../../test/dbEngine.js`.
    - `grep -c "^  it(" backend/src/resolvers/verifyEmail.test.js` is unchanged from before this
      task (6 `it(` calls total — the guard only edits an existing test's callback, it does not
      add or remove a test).
    - On the developer's current local engine, `npm test --workspace backend -- --run
      verifyEmail.test.js familyMember.dedup.test.js` exits 0, and the Vitest reporter output
      shows both guarded tests as skipped with their reason text visible (or, if the local engine
      is MySQL, shows both tests passing) — confirm by inspecting the actual command output, not
      by assumption.
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- --run verifyEmail.test.js familyMember.dedup.test.js</automated>
  </verify>
  <done>
    Both target tests (and only those two) carry `ctx.skip(await isMariaDB(), reason)` as their
    first statement; the full command above exits 0 on the current local engine, and every other
    test in both files is unaffected (still runs, still passes).
  </done>
</task>

<task type="auto">
  <name>Task 3: Document the MariaDB-vs-MySQL caveat in KNOWN-ISSUES.md</name>
  <files>KNOWN-ISSUES.md</files>
  <read_first>
    KNOWN-ISSUES.md (whole file — the existing "Reset-token exposure" entry, lines 10-16, defines
    this doc's `## Title` / `- **Location:**` / `- **Expected:**` / `- **Actual:**` /
    `- **Severity:**` / `- **Documented by test:**` format to mirror exactly)
  </read_first>
  <action>
    Append a new `## MariaDB-only skip on two concurrency-locking tests` section to
    `KNOWN-ISSUES.md`, following the exact same 5-field format as the existing "Reset-token
    exposure" entry. Location: `backend/src/resolvers/verifyEmail.test.js` (VERIFY-04),
    `backend/src/services/familyMember.dedup.test.js` (REL-06). Expected: both tests pass on any
    supported MySQL-compatible engine. Actual: both assert `SELECT ... FOR UPDATE` lock-wait
    interleaving that holds under MySQL 8.4 (CI's engine) but not under local MariaDB, which
    surfaces a Sequelize optimistic-version "Record has changed since last read" error instead;
    both auto-detect the engine via `SELECT VERSION()` (`backend/test/dbEngine.js`) and skip
    themselves, with a visible reason, when running on MariaDB — they still run and pass on CI
    (MySQL 8.4). Severity: N/A — not a product defect, a test-infrastructure limitation.
    Documented by test: the two tests named above. Do not alter the file's existing intro
    paragraph or the "Reset-token exposure" entry — only append the new section.
  </action>
  <acceptance_criteria>
    - `grep -c "^## MariaDB-only skip" KNOWN-ISSUES.md` equals `1`.
    - The new section contains all five field labels (`**Location:**`, `**Expected:**`,
      `**Actual:**`, `**Severity:**`, `**Documented by test:**`).
    - The existing "Reset-token exposure" section is byte-identical to before this task (verify
      via `git diff KNOWN-ISSUES.md` shows only an addition, no deletion/modification of existing
      lines).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## MariaDB-only skip" KNOWN-ISSUES.md</automated>
  </verify>
  <done>KNOWN-ISSUES.md documents the MariaDB-vs-MySQL caveat in the established format, discoverable by anyone reading the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test process → local/CI DB | `dbEngine.js` opens a raw `mysql2` connection using the same test-environment credentials (`env/test.env`) already used identically by `verifyEmail.test.js`'s existing `rawConnection()` — no new credential-handling surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-29-01 | Repudiation | `ctx.skip(...)` guard on VERIFY-04/REL-06 | mitigate | The skip must be visible and reason-documented (not a silent `.skip`), and CI (MySQL 8.4) must still run and pass both tests unconditionally — this is the mitigation itself, verified by Task 2's acceptance criteria (`grep -c "ctx.skip"` == 1 per file, no test count change). A skip that silently weakened CI coverage would be a real defect-masking risk; this design explicitly prevents that by scoping the skip to the local-MariaDB branch only. |
| T-29-02 | Tampering | `backend/test/dbEngine.js` engine-detection query | accept | Read-only `SELECT VERSION()` against the test DB using existing test credentials; no write, no new attack surface beyond what `rawConnection()` already exercises in the same file. |
| T-29-SC | Tampering | npm/pip/cargo installs | N/A this plan | No package installs in this plan (jest-axe/wcag-contrast install happens in Plan 02). |

</threat_model>

<verification>
Run `npm test --workspace backend -- --run verifyEmail.test.js familyMember.dedup.test.js` and
confirm exit 0. Run `npm test --workspace backend` (full backend suite) and confirm exit 0 with no
new failures beyond the two intentionally-skipped-on-MariaDB tests. Inspect the Vitest reporter
output to confirm the skip reason string is visible for both guarded tests (on MariaDB) or that
both pass normally (on MySQL).
</verification>

<success_criteria>
- `backend/test/dbEngine.js` exports `isMariaDB()`.
- Exactly one test each in `verifyEmail.test.js` and `familyMember.dedup.test.js` carries the
  `ctx.skip(await isMariaDB(), reason)` guard; every other test in both files is untouched.
- `KNOWN-ISSUES.md` documents the caveat.
- `npm test --workspace backend` exits 0 on the current local engine.
- No product/resolver/service code was modified.
</success_criteria>

<output>
Create `.planning/phases/29-accessibility-responsive-quality-gate/29-01-SUMMARY.md` when done.
</output>
