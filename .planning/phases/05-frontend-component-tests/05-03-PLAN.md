---
phase: 05-frontend-component-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/pages/Login.test.jsx
  - frontend/src/pages/Register.test.jsx
autonomous: true
requirements: [FE-03, FE-04]

must_haves:
  truths:
    - "Submitting valid Login credentials navigates to /dashboard"
    - "Submitting Login credentials that the backend rejects shows an error alert and does not navigate"
    - "Submitting valid Register input navigates to /dashboard"
    - "Submitting Register input that the backend rejects shows an error alert and does not navigate"
  artifacts:
    - path: "frontend/src/pages/Login.test.jsx"
      provides: "Login page test coverage (FE-03)"
      min_lines: 50
    - path: "frontend/src/pages/Register.test.jsx"
      provides: "Register page test coverage (FE-04)"
      min_lines: 50
  key_links:
    - from: "frontend/src/pages/Login.test.jsx"
      to: "frontend/src/api/graphqlClient.js"
      via: "vi.mock module replacement"
      pattern: "vi\\.mock\\(['\"]\\.\\./\\.\\./api/graphqlClient\\.js['\"]"
    - from: "frontend/src/pages/Register.test.jsx"
      to: "frontend/src/api/graphqlClient.js"
      via: "vi.mock module replacement"
      pattern: "vi\\.mock\\(['\"]\\.\\./\\.\\./api/graphqlClient\\.js['\"]"
    - from: "frontend/src/pages/Login.test.jsx"
      to: "react-router-dom useNavigate"
      via: "partial vi.mock keeping the rest of react-router-dom real"
      pattern: "useNavigate: \\(\\) => navigateSpy"
---

<objective>
Add component test coverage for the Login page (FE-03) and Register page (FE-04): both submit their form's input through the real `AuthProvider` and assert both the success (navigate to `/dashboard`) and error (alert shown, no navigation) states.

Purpose: Login and Register are the two auth entry points a real user interacts with; per CONTEXT.md D-03 they are tested through the real `AuthProvider` with only the network (`graphqlRequest`) and navigation (`useNavigate`) mocked, giving the highest confidence per line without a second isolation strategy.
Output: `frontend/src/pages/Login.test.jsx` and `frontend/src/pages/Register.test.jsx` (new, co-located specs). Register reuses the exact recipe from Login with field/mutation names swapped (structurally identical source components per 05-PATTERNS.md).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-component-tests/05-CONTEXT.md
@.planning/phases/05-frontend-component-tests/05-PATTERNS.md
</context>

<interfaces>
<!-- Real contracts the executor must satisfy exactly. No further codebase exploration needed. -->

From frontend/src/pages/Login.jsx (full file, 76 lines — the subject under test):
```javascript
const handleSubmit = async (event) => {
  event.preventDefault();
  setError('');
  setLoading(true);
  try {
    await login(form.email, form.password);
    navigate('/dashboard');
  } catch (err) {
    setError(err.message);
  } finally {
    setLoading(false);
  }
};
// Fields: TextField label="Email address" type="email"; TextField label="Password" type="password"
// Submit button: {loading ? 'Signing in…' : 'Sign in'}
// error && <Alert severity="error">{error}</Alert>
```

From frontend/src/pages/Register.jsx (full file, 79 lines — structurally identical `handleSubmit` calling `register(form.name, form.email, form.password)`):
```javascript
// Fields: TextField label="Full name"; TextField label="Email address" type="email"; TextField label="Password" type="password"
// Submit button: {loading ? 'Creating account…' : 'Create account'}
// error && <Alert severity="error">{error}</Alert>
```

From frontend/src/api/graphqlClient.js (the mock target contract, D-01/D-02):
```javascript
export async function graphqlRequest(query, variables = {}) {
  // success: returns response.data.data (e.g. { login: { token, user } }, { register: { token, user } })
  // failure: throws new Error(<message>)
}
```

From frontend/src/context/AuthContext.jsx (real provider these pages are rendered inside, per D-03 — no seeded token means the me-on-mount effect is inert):
```javascript
export function AuthProvider({ children }) { /* see 05-01-PLAN.md interfaces block for full contract */ }
```

From frontend/src/components/AuthShell.jsx (presentational wrapper both pages render inside; uses `Link` from react-router-dom — must stay real under the partial mock, D-04):
```javascript
export default function AuthShell({ eyebrow, title, subtitle, children, footer, maxWidth }) { /* renders Paper + Stack around children */ }
```

From frontend/src/harness.test.jsx (render/query style to mirror):
```javascript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Login.test.jsx — success navigates, error shows alert</name>
  <files>frontend/src/pages/Login.test.jsx</files>
  <read_first>
    frontend/src/pages/Login.jsx (full file, shown in interfaces above); frontend/src/api/graphqlClient.js (mock target contract); frontend/src/context/AuthContext.jsx (real `AuthProvider` wrapper this page renders inside per D-03); frontend/src/components/AuthShell.jsx (what must stay real under the react-router-dom partial mock); frontend/src/harness.test.jsx (render/query style); 05-PATTERNS.md section "frontend/src/pages/Login.test.jsx" (exact `vi.mock` recipes for `graphqlClient.js` and `react-router-dom`); 05-CONTEXT.md D-01 through D-05.
  </read_first>
  <behavior>
    - Test 1 (success): fill `Email address` and `Password` fields via `userEvent.type`, click `Sign in`; `graphqlRequest` resolves `{ login: { token: 'tok-abc', user: { id: 1, name: 'Ada', email: 'ada@example.com', role: 'USER' } } }` -> `navigateSpy` is called with `'/dashboard'`.
    - Test 2 (error): fill the same fields, click `Sign in`; `graphqlRequest` rejects with `new Error('Invalid email or password.')` -> an element with role `alert` shows the text `'Invalid email or password.'`, and `navigateSpy` is NOT called.
  </behavior>
  <action>
    Create `frontend/src/pages/Login.test.jsx` with explicit imports (`import { describe, it, expect, vi, beforeEach } from 'vitest'`, `import { render, screen, waitFor } from '@testing-library/react'`, `import userEvent from '@testing-library/user-event'`, `import { MemoryRouter } from 'react-router-dom'`, `import { AuthProvider } from '../context/AuthContext.jsx'`, `import Login from './Login.jsx'`). Mock the network seam: `vi.mock('../api/graphqlClient.js', () => ({ graphqlRequest: vi.fn() }))` then `import { graphqlRequest } from '../api/graphqlClient.js'`. Mock navigation using `vi.hoisted` to avoid a temporal-dead-zone error on the outer spy reference: `const { navigateSpy } = vi.hoisted(() => ({ navigateSpy: vi.fn() }))` then `vi.mock('react-router-dom', async (importOriginal) => ({ ...(await importOriginal()), useNavigate: () => navigateSpy }))`. Add `beforeEach(() => vi.clearAllMocks())`. Render with `render(<AuthProvider><MemoryRouter><Login /></MemoryRouter></AuthProvider>)` — do not seed `localStorage` so the `me`-on-mount effect is inert. Use `userEvent.type(screen.getByLabelText('Email address'), 'ada@example.com')` and the equivalent for `'Password'`, then `userEvent.click(screen.getByRole('button', { name: 'Sign in' }))`. For Test 1, `graphqlRequest.mockResolvedValueOnce({ login: {...} })` before submit, then `await waitFor(() => expect(navigateSpy).toHaveBeenCalledWith('/dashboard'))`. For Test 2, `graphqlRequest.mockRejectedValueOnce(new Error('Invalid email or password.'))` before submit, then `expect(await screen.findByRole('alert')).toHaveTextContent('Invalid email or password.')` and `expect(navigateSpy).not.toHaveBeenCalled()`.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/Login.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/pages/Login.test.jsx` exits 0 with 2 passing tests.
    - Success test asserts `navigateSpy` called with `'/dashboard'` exactly once.
    - Error test asserts `screen.getByRole('alert')` has text content `'Invalid email or password.'` and `navigateSpy` was never called in that test.
  </acceptance_criteria>
  <done>Login.test.jsx exists, covers both the success (navigate) and error (alert, no navigate) states per FE-03, and passes under vitest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Register.test.jsx — reuse Login recipe, swap fields/mutation</name>
  <files>frontend/src/pages/Register.test.jsx</files>
  <read_first>
    frontend/src/pages/Register.jsx (full file, shown in interfaces above — structurally identical to Login.jsx); frontend/src/pages/Login.test.jsx (the file just created in Task 1 of this plan — copy its recipe verbatim, same relative import depth since both pages live in `frontend/src/pages/`); 05-PATTERNS.md section "frontend/src/pages/Register.test.jsx".
  </read_first>
  <behavior>
    - Test 1 (success): fill `Full name`, `Email address`, `Password` via `userEvent.type`, click `Create account`; `graphqlRequest` resolves `{ register: { token: 'tok-def', user: { id: 2, name: 'Ada', email: 'ada@example.com', role: 'USER' } } }` -> `navigateSpy` is called with `'/dashboard'`.
    - Test 2 (error): fill the same fields, click `Create account`; `graphqlRequest` rejects with `new Error('A user with this email already exists.')` -> an element with role `alert` shows that text, and `navigateSpy` is NOT called.
  </behavior>
  <action>
    Create `frontend/src/pages/Register.test.jsx` following the exact same structure as `Login.test.jsx` (same `vi.mock('../api/graphqlClient.js', ...)` and `vi.hoisted` + `vi.mock('react-router-dom', ...)` recipes, same `AuthProvider` + `MemoryRouter` render wrapper, same `beforeEach(() => vi.clearAllMocks())`), importing `Register` from `./Register.jsx` instead of `Login`. Swap field queries to `screen.getByLabelText('Full name')`, `screen.getByLabelText('Email address')`, `screen.getByLabelText('Password')`, and the submit button to `screen.getByRole('button', { name: 'Create account' })`. Resolve/reject `graphqlRequest` with the `{ register: {...} }` success payload or an `Error` for the failure path as specified in the behavior block above.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/pages/Register.test.jsx</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx vitest run src/pages/Register.test.jsx` exits 0 with 2 passing tests.
    - Success test asserts `navigateSpy` called with `'/dashboard'` exactly once.
    - Error test asserts `screen.getByRole('alert')` has text content `'A user with this email already exists.'` and `navigateSpy` was never called in that test.
  </acceptance_criteria>
  <done>Register.test.jsx exists, covers both the success (navigate) and error (alert, no navigate) states per FE-04, and passes under vitest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process <-> mocked graphqlRequest / useNavigate | Both mocked seams are `vi.fn()` stubs inside the test process — no real HTTP, no real routing history, no secrets. The rest of `react-router-dom` (`Link`, `MemoryRouter`) stays real per D-04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-05-04 | Tampering | mocked `graphqlRequest` diverging from the real success/failure contract on the Login/Register pages | mitigate | Stub shapes mirror `response.data.data` success (`{ login: {...} }` / `{ register: {...} }`) and `Error` rejection exactly, per D-02, cross-checked against `graphqlClient.js` in `<read_first>`. |
| T-05-05 | Repudiation | asserting only that `<Alert>` renders text, not that the exact backend error string is preserved | accept | Per CONTEXT.md Claude's Discretion, exact error message strings are representative, not contractual — the point is that the rejected `.message` reaches the `<Alert>`, which both tests verify. |

No high-severity threats identified. This plan adds zero new runtime code paths, inputs, or auth logic — only two test files.
</threat_model>

<verification>
`cd frontend && npx vitest run` exits 0 across the whole suite with `Login.test.jsx` and `Register.test.jsx` included and no regressions.
</verification>

<success_criteria>
Login page test passes: submits credentials and handles both the success and error response states (Roadmap Phase 5 Success Criterion 3 / FE-03).
Register page test passes: submits registration input and handles both the success and error response states (Roadmap Phase 5 Success Criterion 4 / FE-04).
</success_criteria>

<output>
Create `.planning/phases/05-frontend-component-tests/05-03-SUMMARY.md` when done
</output>
