---
phase: 24-backend-read-layer-for-detail
plan: 03
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - backend/src/schemas/familyMember.schema.js
  - backend/src/resolvers/familyMember.resolver.js
  - backend/src/resolvers/familyMember.canEdit.test.js
  - backend/src/resolvers/familyMember.cardFieldCoverage.test.js
autonomous: true
requirements: [API-01]

must_haves:
  truths:
    - "A GraphQL field exposes whether the current caller may edit/add relatives, reusing the existing admin check — true only for an ADMIN viewer, false for a non-admin or anonymous viewer."
    - "A GraphQL query returns a single person by id with every field the person card needs (Latin + Ge'ez name, gender, birth/death info, photo) in one operation, without rebuilding the existing familyMember(id) query."
  artifacts:
    - path: "backend/src/schemas/familyMember.schema.js"
      provides: "canEdit: Boolean! field on FamilyMember"
      contains: "canEdit: Boolean!"
    - path: "backend/src/resolvers/familyMember.resolver.js"
      provides: "FamilyMember.canEdit field resolver, mirroring createdBy/updatedBy's admin-check shape"
      contains: "canEdit:"
    - path: "backend/src/resolvers/familyMember.canEdit.test.js"
      provides: "SC-5 integration coverage (ADMIN true, non-admin false, anonymous rejected upstream)"
    - path: "backend/src/resolvers/familyMember.cardFieldCoverage.test.js"
      provides: "SC-2 field-coverage verification (no rebuild — confirms familyMember(id) already returns every card field)"
  key_links:
    - from: "backend/src/resolvers/familyMember.resolver.js FamilyMember.canEdit"
      to: "context.user.role"
      via: "pure synchronous derivation, no I/O, no loader"
      pattern: "Boolean\\(user\\?\\.role === 'ADMIN'\\)"
---

<objective>
Add the third and final genuinely-new API surface this phase requires: a per-person `canEdit: Boolean!` field on `FamilyMember` (SC-5, D-07/D-08), implemented as a one-line mirror of the existing `createdBy`/`updatedBy` admin-check precedent. Also verify, without rebuilding, that the existing `familyMember(id)` query already returns every field the person card needs (SC-2, D-09).

Purpose: `PersonCard` (Phase 25/28) needs a uniform, per-person signal to gate edit/add-relative controls without threading a global admin flag through props; SC-2's person-by-id read is already met by existing code and must not be duplicated.
Output: `canEdit: Boolean!` field, resolved as `Boolean(user?.role === 'ADMIN')`, plus a passing SC-2 field-coverage integration test proving no rebuild is needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/24-backend-read-layer-for-detail/24-CONTEXT.md
@.planning/phases/24-backend-read-layer-for-detail/24-RESEARCH.md
@.planning/phases/24-backend-read-layer-for-detail/24-01-SUMMARY.md

<locked_decisions>
- D-07: canEdit reuses the existing admin check — `user.role === 'ADMIN'` — exactly as `requireAdmin` and the `linkedUser`/`createdBy`/`updatedBy` field gating already do. No new scope logic (no `computeEditableScope` call for this field).
- D-08: Shape it as a per-person `canEdit: Boolean!` field on FamilyMember, resolved as `Boolean(user?.role === 'ADMIN')`. Uniform today (admin-only); the per-person shape leaves room for scoped editing later (deferred, out of scope).
- D-09: `familyMember(id)` already returns every card field — do not add a parallel query. Verify field coverage with a test, don't rebuild.
</locked_decisions>

<interfaces>
Existing admin-gated field precedent to copy verbatim in shape (backend/src/resolvers/familyMember.resolver.js:304-311) — canEdit is the identical boolean condition, just returned instead of gating a loader call:
  createdBy: (member, _args, { user, loaders }) => {
    if (user?.role !== 'ADMIN' || member.createdByUserId == null) return null;
    return loaders.userById.load(Number(member.createdByUserId));
  },
  updatedBy: (member, _args, { user, loaders }) => {
    if (user?.role !== 'ADMIN' || member.updatedByUserId == null) return null;
    return loaders.userById.load(Number(member.updatedByUserId));
  }

Existing FamilyMember type fields to extend (backend/src/schemas/familyMember.schema.js:13-41), including the provenance-field comment convention at line 36 to follow for the new field's own comment:
  linkedUser: User
  # Provenance — resolved to the user only for an ADMIN viewer, else null.
  createdBy: User
  updatedBy: User
  createdAt: String
  updatedAt: String

Existing admin-vs-non-admin two-case test structure to mirror exactly (backend/src/resolvers/familyMember.provenance.test.js:104-138) — canEdit's test adds a third anonymous-caller case:
  it('exposes createdBy/updatedBy to an ADMIN viewer', async () => { ... const admin = await createTestUser({ role: 'ADMIN', ... }); ... });
  it('hides createdBy/updatedBy from a non-admin viewer', async () => { ... const viewer = await createTestUser({ role: 'USER', familyMemberId: base.id }); ... });

Full current FamilyMember card-field list to select from for the SC-2 coverage test (backend/src/schemas/familyMember.schema.js:13-41): id, firstname, lastname, fullname, gender, mothersname, geezFirstname, geezLastname, geezMothersname, geezFullname, email, birthdate, isAlive, phone, address, photoUrl, mother, father, spouses, children, siblings, linkedUser, createdBy, updatedBy, createdAt, updatedAt — plus canEdit added by Task 1 of this plan.

photoUrl is a computed field, never a raw filename passthrough (backend/src/resolvers/familyMember.resolver.js:253):
  photoUrl: (member) => (member.profilePicture ? `/api/family-members/${member.id}/photo` : null),
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: canEdit field (SC-5, D-07/D-08)</name>
  <files>backend/src/schemas/familyMember.schema.js, backend/src/resolvers/familyMember.resolver.js, backend/src/resolvers/familyMember.canEdit.test.js</files>
  <read_first>
    backend/src/resolvers/familyMember.resolver.js lines 300-312 (createdBy/updatedBy — the exact pattern to mirror)
    backend/src/schemas/familyMember.schema.js lines 13-41 (FamilyMember type, provenance-field comment convention)
    backend/src/resolvers/familyMember.provenance.test.js (admin vs non-admin two-case test structure — the exact analog for canEdit's test)
    .planning/phases/24-backend-read-layer-for-detail/24-CONTEXT.md D-07/D-08
  </read_first>
  <behavior>
    - Test 1: canEdit resolves true when queried by an ADMIN actor.
    - Test 2: canEdit resolves false when queried by a linked non-admin USER actor.
    - Test 3: an anonymous (user: null) caller is rejected at the parent familyMember/familyMembers query by requireFamilyAccess before canEdit is ever reached — asserted as an error response, not as canEdit: false.
  </behavior>
  <action>
    Add `canEdit: Boolean!` to `type FamilyMember` in familyMember.schema.js, placed after the existing provenance fields with a one-line comment following the same convention as the `createdBy`/`updatedBy` comment (e.g. noting D-07/D-08 and that it is admin-only today). Add `canEdit: (_member, _args, { user }) => Boolean(user?.role === 'ADMIN')` to the `FamilyMember:` field-resolver map in familyMember.resolver.js, placed near `createdBy`/`updatedBy` — no loader, no I/O, ignore the `member` argument (leading underscore per this file's convention), and do NOT call `computeEditableScope` (D-07 — no new scope logic). Write `backend/src/resolvers/familyMember.canEdit.test.js` mirroring `familyMember.provenance.test.js`'s exact `createTestUser({ role: 'ADMIN' })` vs. `createTestUser({ role: 'USER', familyMemberId: ... })` two-case structure, plus the anonymous-caller case, covering all 3 behaviors above.
  </action>
  <acceptance_criteria>
    - familyMember.schema.js `type FamilyMember` contains `canEdit: Boolean!`
    - familyMember.resolver.js `FamilyMember.canEdit` resolver body is exactly `Boolean(user?.role === 'ADMIN')` — no loader call, no async I/O, no `computeEditableScope` import added for this field
    - `npm test --workspace backend -- familyMember.canEdit.test.js` exits 0
    - test asserts canEdit resolves true for an ADMIN actor
    - test asserts canEdit resolves false for a linked non-admin USER actor
    - test asserts an anonymous caller is rejected at the parent query (errors defined, data null) rather than reaching canEdit
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- familyMember.canEdit.test.js</automated>
  </verify>
  <done>canEdit resolves true only for ADMIN actors and false for non-admin/anonymous actors, mirroring createdBy/updatedBy's exact admin-check shape with no new authorization logic; test file green.</done>
</task>

<task type="auto">
  <name>Task 2: SC-2 person-by-id field-coverage verification (D-09 — no rebuild)</name>
  <files>backend/src/resolvers/familyMember.cardFieldCoverage.test.js</files>
  <read_first>
    backend/src/schemas/familyMember.schema.js lines 13-41 (full FamilyMember type field list, including canEdit added by Task 1 — the card-field contract to verify)
    .planning/ROADMAP.md § "Phase 24: Backend Read Layer for /detail" SC-2 ("returns a single person by id with every field the person card needs — Latin + Ge'ez name, gender, birth/death info, photo")
    backend/src/resolvers/familyMember.geez.test.js (Ge'ez field query/assertion shape to reuse)
    backend/src/resolvers/familyMember.resolver.js lines 250-253 (photoUrl computed-field shape — a route path, not a raw filename)
  </read_first>
  <action>
    Do NOT rebuild `familyMember(id)` (D-09). Write a new, additive integration test file `familyMember.cardFieldCoverage.test.js` that queries `familyMember(id)` in ONE operation selecting every SC-2 card field: id, firstname, lastname, fullname, gender, geezFirstname, geezLastname, geezFullname, birthdate, isAlive, photoUrl, canEdit. Create one `models.FamilyMember` fixture row with every one of those source fields populated (including a `profilePicture` value so `photoUrl` resolves non-null), query it as an ADMIN actor, and assert each selected field round-trips a non-null, correctly-shaped value: `photoUrl` matches the `/api/family-members/{id}/photo` pattern, `fullname`/`geezFullname` derive correctly from their parts, `gender` is one of the enum values, `canEdit` is true for the ADMIN actor. This is a read-only coverage assertion, not new production logic — if a field is genuinely found missing from the schema/resolver during this task, stop and report the gap rather than silently adding a new production field (none is expected per RESEARCH.md's confirmation).
  </action>
  <acceptance_criteria>
    - `npm test --workspace backend -- familyMember.cardFieldCoverage.test.js` exits 0
    - the test queries id, firstname, lastname, fullname, gender, geezFirstname, geezLastname, geezFullname, birthdate, isAlive, photoUrl, canEdit in a single familyMember(id) operation and asserts each has the expected value
    - no production schema/resolver file is modified by this task (Task 1 already added canEdit in this same plan; this task is verification-only)
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- familyMember.cardFieldCoverage.test.js</automated>
  </verify>
  <done>SC-2 confirmed met — familyMember(id) returns every field the person card needs in one query, proven by a passing integration test, with no query rebuilt or duplicated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GraphQL API | The caller's JWT-derived identity (`context.user`) crosses into the `canEdit` field resolver; the client cannot influence `canEdit`'s value through any argument |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Elevation of Privilege (canEdit spoofing / incorrect privilege signal) | `FamilyMember.canEdit` | mitigate | Purely server-derived from `context.user.role` (already resolved from a verified JWT upstream in `server.js`); no user-supplied argument reaches this resolver; proven false for non-admin/anonymous and true only for ADMIN by `familyMember.canEdit.test.js` |
| T-24-08 | Information Disclosure (parent query bypass) | `familyMember(id)` (existing, unmodified) | accept | Already gated by `requireFamilyAccess` before Task 1/Task 2's changes are reached — unchanged by this plan, confirmed by the anonymous-caller test in Task 1 |
| T-24-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new dependencies added by this plan — nothing to install or verify |
</threat_model>

<verification>
Run `npm test --workspace backend -- familyMember.canEdit.test.js` and `npm test --workspace backend -- familyMember.cardFieldCoverage.test.js` individually, then `npm test --workspace backend` for the full backend suite — this closes Phase 24's phase-gate requirement (full backend suite green before `/gsd:verify-work`).
</verification>

<success_criteria>
- `canEdit: Boolean!` resolves true only for ADMIN callers, false for non-admin/anonymous (SC-5).
- `familyMember(id)` is confirmed, by a passing test, to already return every field the person card needs — no rebuild (SC-2).
- No existing query, field, or mutation is modified beyond the additive `canEdit` field.
- `npm test --workspace backend` is green.
</success_criteria>

<output>
Create `.planning/phases/24-backend-read-layer-for-detail/24-03-SUMMARY.md` when done.
</output>
