---
phase: 24-backend-read-layer-for-detail
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/familyMember.queryCount.test.js
autonomous: true
requirements: [PERF-02, API-01]

must_haves:
  truths:
    - "A GraphQL query returns a person's direct children (each annotated with its own nested children ids and spouses) in a bounded/flat set of SQL statements regardless of child or grandchild count — proven by an integration test, not merely asserted."
  artifacts:
    - path: "backend/src/services/familyMember.queryCount.test.js"
      provides: "SC-4/PERF-02 bounded-SQL-statement-count proof for the direct-children + grandchild-ids + spouses read shape (D-05: no new childCount field, nested children { id } reused; D-06: bounded/flat SQL proof), extending the existing file"
      contains: "spouses { id }"
  key_links:
    - from: "familyMember(id) { children { id children { id } spouses { id } } }"
      to: "loaders.childrenByParentId / loaders.spousesByMemberId"
      via: "per-request DataLoader batching (existing, unmodified)"
      pattern: "childrenByParentId|spousesByMemberId"
---

<objective>
Prove, via a new test in the existing bounded-SQL test file, that the exact D-06 nested-children read shape — `familyMember(id) { children { id children { id } spouses { id } } }` — resolves in a flat, bounded SQL-statement count regardless of how many direct children or grandchildren exist. This is the mandatory SC-4/PERF-02 Nyquist gate for this phase.

Purpose: `/detail`'s direct-children-with-counts requirement (SC-4) depends on the already-existing `childrenByParentId`/`spousesByMemberId` DataLoaders being proven bounded for THIS specific nesting shape — not just the deeper 8-generation shape the existing test already covers.
Output: An extended `familyMember.queryCount.test.js` with a passing test that asserts the SQL statement count stays constant as the child/grandchild fixture size grows. No production code changes — everything this test exercises already exists (D-05/D-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/24-backend-read-layer-for-detail/24-CONTEXT.md
@.planning/phases/24-backend-read-layer-for-detail/24-RESEARCH.md

<locked_decisions>
- D-05: No new `childCount` field. Reuse the existing `children { id }` shape — the direct-children read nests one additional `children { id }` level so the client derives counts from array length. Do NOT add a `childCount` field to satisfy this task.
- D-06: The query `familyMember(id) { children { id children { id } spouses { id } } }` (or equivalent) is the N+1 proof target. It MUST resolve in a bounded, flat SQL-statement count regardless of child/grandchild count, reusing the existing `childrenByParentId` and `spousesByMemberId` DataLoaders. Prove it with the `countQueries()` recipe already established in this file.
- D-09: `children`/`spouses` fields already exist and are unchanged by this phase — this task is proof-only, no production code is modified.
</locked_decisions>

<interfaces>
Existing `countQueries()` helper to reuse verbatim, do not duplicate (backend/src/services/familyMember.queryCount.test.js:9-24):
  async function countQueries(fn) {
    const original = sequelize.options.logging;
    let count = 0;
    sequelize.options.logging = () => { count += 1; };
    try { await fn(); } finally { sequelize.options.logging = original; }
    return count;
  }

Existing bounded-SQL assertion pattern to mirror the style of (backend/src/services/familyMember.queryCount.test.js:64-79):
  it('resolves the same nested deep-tree query with a flat, bounded SQL query count (not proportional to the 255-node fixture size)', async () => {
    const { root } = await buildGenerationFixture({ depth: 8, childrenPerNode: 2 });
    const admin = await createTestUser({ role: 'ADMIN', familyMemberId: null });
    let result;
    const queryCount = await countQueries(async () => {
      result = await graphql(buildDeepChildrenQuery(7), { id: root.id }, admin);
    });
    expect(result.errors).toBeUndefined();
    expect(countNodes(result.data.familyMember)).toBe(255);
    expect(queryCount).toBeLessThan(20);
  });

`buildGenerationFixture` (backend/test/familyTreeFactory.js) builds a single-lineage branching tree with NO spouses attached — it cannot produce the spouse-bearing fixture this task needs. Build the new fixture directly via `models.FamilyMember.create(...)` and `models.Spouse.create({ memberAId, memberBId })` (mirroring `familyMember.head.test.js`'s Plan 24-01 convention and the Spouse-row creation convention already used in `familyMember.service.js`'s `createOrFindSpouseRow`), NOT the factory helper.

`childrenByParentId`/`spousesByMemberId` DataLoader batching to prove bounded, unmodified (backend/src/loaders/familyMember.loaders.js:26-59) — one flat `WHERE motherId/fatherId IN (...)` per nesting level, one flat `WHERE memberAId/memberBId IN (...)` for spouses, regardless of how many parent/member ids are in that level's batch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC-4/PERF-02 bounded-SQL N+1 proof for direct-children + spouses (D-06)</name>
  <files>backend/src/services/familyMember.queryCount.test.js</files>
  <read_first>
    backend/src/services/familyMember.queryCount.test.js (full file — countQueries() helper, existing describe block, buildDeepChildrenQuery/countNodes helpers)
    backend/src/loaders/familyMember.loaders.js (childrenByParentId, spousesByMemberId — the batching mechanism being proven bounded, unmodified)
    backend/src/resolvers/familyMember.resolver.js lines 258-259 (children/spouses field resolvers, confirm no change needed)
    backend/test/helpers.js (createTestUser, graphql, resetTables)
    .planning/phases/24-backend-read-layer-for-detail/24-CONTEXT.md D-05/D-06
  </read_first>
  <action>
    Add a new test (or describe block) to familyMember.queryCount.test.js. Build a SMALL fixture directly via `models.FamilyMember.create(...)`: one root member with 3 direct children, each child having 2 of their own children (grandchildren), and at least one `models.Spouse.create({ memberAId, memberBId })` row pairing one child with a married-in spouse. Run the exact D-06 query shape — `familyMember(id) { children { id children { id } spouses { id } } }` — through `countQueries()` as an ADMIN caller (`createTestUser({ role: 'ADMIN', familyMemberId: null })`), asserting `errors` is undefined, the returned shape has the expected child/grandchild/spouse counts, and `queryCount` stays below a small explicit constant (e.g. `toBeLessThan(10)`) matching this file's existing generous-headroom style (do not assert an exact query count). Then build a LARGER fixture with the same shape but 10 direct children instead of 3 (each still with 2 grandchildren, at least one spouse), run the identical query and `countQueries()` call, and assert the resulting `queryCount` does NOT increase relative to the small fixture's count (same small bound, or an explicit equality/near-equality assertion) — this is the concrete proof that the SQL-statement count is flat/bounded rather than proportional to child count as it scales from ~3 to ~10.
  </action>
  <acceptance_criteria>
    - backend/src/services/familyMember.queryCount.test.js contains a new test exercising `children { id children { id } spouses { id } }`
    - the test builds at least two fixture sizes (small: ~3 direct children, large: ~10 direct children, each with grandchildren and at least one spouse) and asserts the countQueries() result does not increase between them
    - test asserts errors is undefined and the returned data shape matches each fixture's actual child/grandchild/spouse counts
    - `npm test --workspace backend -- familyMember.queryCount.test.js` exits 0
    - no production source file is modified — git diff for this plan touches only the test file
  </acceptance_criteria>
  <verify>
    <automated>npm test --workspace backend -- familyMember.queryCount.test.js</automated>
  </verify>
  <done>SC-4/PERF-02 N+1 proof lands in the existing queryCount test file; the bounded SQL-statement count is asserted constant as the child-fixture size grows from ~3 to ~10 direct children, reusing the existing countQueries() recipe with zero production code changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan makes zero production code changes. It adds one integration test proving already-existing, already-authorized (`requireFamilyAccess`-gated `familyMember(id)`) read behavior stays bounded under a new fixture shape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-06 | Denial of Service (N+1 query amplification) | `familyMember(id) { children { ... spouses } }` (existing, unmodified) | mitigate | Proven bounded by this plan's new test — `countQueries()` assertion stays flat as child/grandchild fixture size scales 3x, reusing the already-batched `childrenByParentId`/`spousesByMemberId` DataLoaders |
| T-24-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | Zero new dependencies added by this plan — nothing to install or verify |
</threat_model>

<verification>
Run `npm test --workspace backend -- familyMember.queryCount.test.js`, then `npm test --workspace backend` for the full backend suite.
</verification>

<success_criteria>
- The direct-children read (`children { id children { id } spouses { id } }`) is proven, by an automated test, to resolve in a bounded/flat SQL-statement count regardless of child/grandchild count (SC-4, PERF-02).
- No production code is modified.
- `npm test --workspace backend` is green.
</success_criteria>

<output>
Create `.planning/phases/24-backend-read-layer-for-detail/24-02-SUMMARY.md` when done.
</output>
