---
phase: 04-frontend-test-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - package-lock.json
  - frontend/vitest.config.js
  - frontend/test/setup.js
  - frontend/src/harness.test.jsx
autonomous: true
requirements: [SETUP-02]

must_haves:
  truths:
    - "Running `npm test --workspace frontend` executes the Vitest runner in a jsdom environment and reports pass/fail (exit 0 when green)."
    - "A React component can be rendered and queried by role/text via React Testing Library inside the suite."
    - "MUI/React-Router browser globals (window.matchMedia) are stubbed so rendering a component does not throw a runtime error."
    - "jest-dom matchers (e.g. toBeInTheDocument) are available in specs without per-file imports."
    - "Rendered trees are unmounted between tests via RTL afterEach(cleanup)."
  artifacts:
    - path: "frontend/vitest.config.js"
      provides: "Standalone Vitest config: react() plugin, jsdom environment, setupFiles wiring"
      contains: "environment: 'jsdom'"
    - path: "frontend/test/setup.js"
      provides: "Per-test setup: jest-dom matchers, matchMedia stub, RTL afterEach(cleanup)"
      contains: "matchMedia"
    - path: "frontend/src/harness.test.jsx"
      provides: "Throwaway proof spec that renders an inline component and queries it by role/text"
      contains: "getByRole"
    - path: "frontend/package.json"
      provides: "test script + RTL/jsdom/vitest devDependencies"
      contains: "\"test\": \"vitest run\""
  key_links:
    - from: "frontend/vitest.config.js"
      to: "frontend/test/setup.js"
      via: "test.setupFiles array"
      pattern: "setupFiles.*test/setup"
    - from: "frontend/vitest.config.js"
      to: "@vitejs/plugin-react"
      via: "plugins: [react()]"
      pattern: "plugins:\\s*\\[react\\(\\)\\]"
    - from: "frontend/src/harness.test.jsx"
      to: "@testing-library/react"
      via: "render + screen import"
      pattern: "from '@testing-library/react'"
---

<objective>
Stand up the frontend test harness so the `frontend` workspace has a working Vitest
runner that can render and query React components in a simulated DOM — mirroring the
Phase 1 backend harness and adapting it for jsdom + JSX. Delivers SETUP-02.

Purpose: This is the foundation Phase 5 consumes to write the real auth-surface
component tests (AuthContext, ProtectedRoute, Login, Register). Phase 4 proves the
chain end-to-end with one throwaway spec; it writes no real component tests.

Output: `frontend/vitest.config.js` (standalone, react plugin + jsdom + setupFiles),
`frontend/test/setup.js` (jest-dom matchers + matchMedia stub + RTL cleanup), a
throwaway proof spec at `frontend/src/harness.test.jsx`, and the `test` script plus the
full RTL/jsdom/vitest devDependencies added to `frontend/package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-test-tooling/04-CONTEXT.md
@.planning/phases/04-frontend-test-tooling/04-PATTERNS.md

<interfaces>
<!-- Backend harness shapes to mirror. Executor uses these directly — no exploration needed. -->

Backend standalone config to mirror (backend/vitest.config.js) — copy the
`defineConfig({ test: { ... } })` shape, but register the infra file via
`test.setupFiles` (per-test), NOT `test.globalSetup` (per-run):
- import { defineConfig } from 'vitest/config'   (NOT from 'vite')
- export default defineConfig({ test: { ... } })

Frontend React plugin to re-declare (frontend/vite.config.js lines 1-2, 7) — the
standalone test config must add `plugins: [react()]` so JSX transforms in tests.
Do NOT copy the server.proxy block (dev/build only). Do NOT modify frontend/vite.config.js.

Backend proof-spec shape to mirror (backend/src/smoke.test.js):
- import { describe, it, expect } from 'vitest'   (explicit imports, globals OFF)

Backend test script to mirror (backend/package.json line 14):
- "test": "vitest run"

Current frontend/package.json state:
- scripts: dev/start/build/preview only — NO test script yet
- devDependencies already present: "@vitejs/plugin-react": "^4.3.4", "vite": "^6.0.7"
- dependencies include: react ^18.3.1, react-dom ^18.3.1, @mui/material ^6.3.1, react-router-dom ^6.28.2
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test script and RTL/jsdom/vitest devDependencies to frontend/package.json</name>
  <files>frontend/package.json, package-lock.json</files>
  <read_first>
    - frontend/package.json (the file being modified — current scripts + devDependencies)
    - backend/package.json (analog — `"test": "vitest run"` script at line 14, vitest ^4.1.10 devDep at line 30)
    - .planning/phases/04-frontend-test-tooling/04-PATTERNS.md (devDependency list under the `frontend/package.json (modified)` section)
    - .planning/codebase/STACK.md (version constraints: React 18.3, Vite 6, @vitejs/plugin-react 4.3)
  </read_first>
  <action>
    Add `"test": "vitest run"` to the `scripts` block of frontend/package.json (mirror
    backend/package.json line 14 exactly). Add the full RTL kit (per D-02) to
    `devDependencies`, alongside the existing `@vitejs/plugin-react` and `vite` entries:
    `vitest` pinned to the `^4.x` line matching the backend (backend uses `^4.1.10`);
    `jsdom` (^26 line); `@testing-library/react` (the v16 line — React 18/19 compatible,
    `^16.x`); `@testing-library/jest-dom` (`^6.x`); `@testing-library/user-event` (`^14.x`).
    Preserve existing keys, ordering style, 2-space indentation, and double-quoted JSON.
    Then install so package-lock.json updates: run `npm install --workspace frontend` from
    the repo root. If npm reports a peer/resolution conflict on a caret pin, bump that one
    package to the nearest version npm resolves cleanly against React 18.3 / Vite 6 /
    @vitejs/plugin-react 4.3 and record the resolved version in the SUMMARY — do NOT add
    `--legacy-peer-deps` or `--force`. Do NOT touch frontend/vite.config.js (D-01).
  </action>
  <verify>
    <automated>node -e "const p=require('/Users/bisrat/Projects/portofolio/frontend/package.json'); const d=p.devDependencies||{}; const need=['vitest','jsdom','@testing-library/react','@testing-library/jest-dom','@testing-library/user-event']; const missing=need.filter(k=>!d[k]); if(p.scripts.test!=='vitest run'){console.error('bad test script');process.exit(1)} if(missing.length){console.error('missing devDeps: '+missing.join(', '));process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json `scripts.test` equals `vitest run`.
    - frontend/package.json `devDependencies` contains all five: vitest, jsdom, @testing-library/react, @testing-library/jest-dom, @testing-library/user-event.
    - `npm install --workspace frontend` completes with exit 0 and no `--force`/`--legacy-peer-deps` flags.
    - `node -e "require('jsdom'); require('@testing-library/react')"` run from frontend resolves without error (packages physically installed).
    - frontend/vite.config.js is unchanged (git diff shows no modification).
  </acceptance_criteria>
  <done>The frontend workspace declares the `test` script and all five test devDependencies, and they are installed (package-lock.json updated), with no forced resolution flags.</done>
</task>

<task type="auto">
  <name>Task 2: Create standalone frontend/vitest.config.js and the shared frontend/test/setup.js</name>
  <files>frontend/vitest.config.js, frontend/test/setup.js</files>
  <read_first>
    - backend/vitest.config.js (analog — the standalone `defineConfig({ test: {...} })` shape to mirror; note it uses `test.globalSetup`, this plan uses `test.setupFiles` instead)
    - frontend/vite.config.js (source of the `import react from '@vitejs/plugin-react'` + `plugins: [react()]` lines to re-declare; do NOT modify this file)
    - backend/test/globalSetup.js (analog for placement/import convention only — dedicated `test/` dir, explicit `import { ... } from 'vitest'`; do NOT copy its DB logic)
    - frontend/src/main.jsx (shows the MUI `ThemeProvider`/`CssBaseline` usage that reads `window.matchMedia` — the reason the stub is required)
    - .planning/phases/04-frontend-test-tooling/04-PATTERNS.md (the `frontend/vitest.config.js` and `frontend/test/setup.js` pattern sections and the three setup responsibilities)
  </read_first>
  <action>
    Create frontend/vitest.config.js (D-01): `import { defineConfig } from 'vitest/config'`
    (NOT from 'vite') and `import react from '@vitejs/plugin-react'`; export a single
    `defineConfig` object with `plugins: [react()]` and a `test` block that sets
    `environment: 'jsdom'` and `setupFiles: ['./test/setup.js']`. Do NOT set `globals`
    (leave OFF — explicit vitest imports, backend convention per D-03 note). Do NOT copy
    the `server.proxy` block. Use 2-space indent and single quotes.

    Create frontend/test/setup.js (D-03) with the three responsibilities, all as ES module
    imports/statements: (1) register jest-dom matchers via `import '@testing-library/jest-dom/vitest'`;
    (2) stub `window.matchMedia` — assign a function returning an object shaped with
    `matches: false`, `media` (the passed query string), `onchange: null`, and the legacy
    `addListener`/`removeListener` plus modern `addEventListener`/`removeEventListener`/
    `dispatchEvent` no-op methods (MUI reads matchMedia; jsdom omits it); (3) wire RTL
    cleanup with `import { cleanup } from '@testing-library/react'` + an explicit
    `import { afterEach } from 'vitest'` (globals are OFF, so `afterEach` is NOT ambient)
    + `afterEach(() => { cleanup(); })`. Add ONLY the matchMedia stub now — do not
    preemptively stub ResizeObserver or others (D-03). Do NOT modify frontend/vite.config.js.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('/Users/bisrat/Projects/portofolio/frontend/vitest.config.js','utf8'); const s=fs.readFileSync('/Users/bisrat/Projects/portofolio/frontend/test/setup.js','utf8'); const cfg=/environment:\s*'jsdom'/.test(c)&&/setupFiles/.test(c)&&/plugins:\s*\[\s*react\(\)\s*\]/.test(c)&&/vitest\/config/.test(c)&&!/globals\s*:\s*true/.test(c); const setup=/@testing-library\/jest-dom/.test(s)&&/matchMedia/.test(s)&&/afterEach/.test(s)&&/cleanup/.test(s)&&/from 'vitest'/.test(s); if(!cfg){console.error('vitest.config.js missing required keys');process.exit(1)} if(!setup){console.error('setup.js missing a required responsibility');process.exit(1)} console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/vitest.config.js imports `defineConfig` from `vitest/config`, declares `plugins: [react()]`, sets `test.environment` to `'jsdom'`, and sets `test.setupFiles` to include `./test/setup.js`.
    - frontend/vitest.config.js does NOT set `globals: true` and does NOT include a `server`/`proxy` block.
    - frontend/test/setup.js imports `@testing-library/jest-dom/vitest`, defines a `window.matchMedia` stub, and wires `afterEach(() => cleanup())` with `afterEach` explicitly imported from `vitest` and `cleanup` from `@testing-library/react`.
    - frontend/vite.config.js is unchanged (git diff shows no modification).
  </acceptance_criteria>
  <done>A standalone Vitest config exists with the react plugin, jsdom environment, and setupFiles pointing at a setup file that registers jest-dom matchers, stubs matchMedia, and runs RTL cleanup between tests — mirroring the backend harness shape with the frontend jsdom deltas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add throwaway proof spec and prove the suite runs green in jsdom</name>
  <files>frontend/src/harness.test.jsx</files>
  <read_first>
    - backend/src/smoke.test.js (analog — the throwaway proof-spec pattern and explicit `import { describe, it, expect } from 'vitest'` header)
    - frontend/vitest.config.js (created in Task 2 — confirms jsdom env + setupFiles wiring the spec relies on)
    - frontend/test/setup.js (created in Task 2 — provides jest-dom matchers + matchMedia stub the spec exercises)
    - .planning/phases/04-frontend-test-tooling/04-PATTERNS.md (the throwaway proof-spec section — inline component, render + query by role/text)
  </read_first>
  <behavior>
    - Test: rendering a throwaway inline component and querying it by role/text with RTL succeeds and the jest-dom matcher `toBeInTheDocument()` passes.
    - Test: the spec runs in the jsdom environment (a DOM `document`/`window` is present) without a matchMedia runtime error, proving the setup file loaded.
  </behavior>
  <action>
    Create frontend/src/harness.test.jsx (D-04 — throwaway, inline, self-contained; do NOT
    import a real app component like BrandMark — real components are Phase 5). Mirror
    backend/src/smoke.test.js: start with `import { describe, it, expect } from 'vitest'`,
    add `import { render, screen } from '@testing-library/react'`. Define a small throwaway
    function component INSIDE the spec file that returns JSX with an identifiable landmark
    (e.g. an `<h1>` with fixed text). In the test, `render(<Component />)`, then assert via a
    jest-dom matcher — query by role/text (e.g. `screen.getByRole('heading', { name: ... })`)
    and assert `.toBeInTheDocument()`. This exercises the full chain: react plugin (JSX
    transform) + jsdom env + RTL render/query + jest-dom matcher + setup-file wiring. Use
    2-space indent and single quotes. Then run the suite to prove it green:
    `npm test --workspace frontend`. If it fails, fix the harness (config/setup/spec) until
    it exits 0 — the failing signal must come from a real green/red run, not assumption.
  </action>
  <verify>
    <automated>cd /Users/bisrat/Projects/portofolio && npm test --workspace frontend</automated>
  </verify>
  <acceptance_criteria>
    - `npm test --workspace frontend` exits 0 and reports at least one passing test.
    - frontend/src/harness.test.jsx defines its rendered component inline (no import of a real app component) and queries it by role or text.
    - The run produces no `window.matchMedia is not a function` (or equivalent) runtime error, confirming the setup stub is active.
    - The spec uses a jest-dom matcher (`toBeInTheDocument`), confirming matcher registration works.
    - `import { describe, it, expect } from 'vitest'` is present (globals stay off).
  </acceptance_criteria>
  <done>Running `npm test --workspace frontend` executes the jsdom Vitest runner and passes, with the throwaway spec rendering a component and querying it by role/text through the full harness chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → dev machine / CI | Newly added devDependencies (vitest, jsdom, @testing-library/*) are fetched and their install/postinstall scripts run in the dev/CI environment. |

Note: This phase adds dev-only test tooling (devDependencies, a jsdom test config, a
throwaway proof spec) and modifies NO application runtime code or production surface. The
harness code never ships to users and never runs against production data or the live
server. The realistic threat surface is therefore limited to supply-chain risk from the
newly added devDependencies — there is no new authentication, input, or network surface to
attack. No high-severity threats are fabricated where none exist.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | npm install of vitest/jsdom/@testing-library/* devDeps | mitigate | Pin all five packages to caret ranges of known-good major lines (vitest ^4.x matching backend, @testing-library/react v16 line, jsdom ^26, jest-dom ^6, user-event ^14); install updates package-lock.json for reproducible resolution; forbid `--force`/`--legacy-peer-deps` so peer conflicts surface rather than being masked. |
| T-04-02 | Elevation of Privilege | test config / setup executing in CI | accept | Test tooling runs only in dev/CI, never in the production runtime path; no secrets or prod DB are exposed to these packages in this phase (CI provisioning is Phase 6). Low-value, dev-only target. |
| T-04-SC | Tampering | npm installs (all newly added packages) | mitigate | All five packages are first-party, widely-used OSS on the official npm registry (vitest, jsdom, @testing-library/react, @testing-library/jest-dom, @testing-library/user-event); none are `[ASSUMED]`/`[SUS]`/`[SLOP]`. Resolved versions recorded in SUMMARY; package-lock.json pins the tree. No blocking legitimacy checkpoint required. |
</threat_model>

<verification>
- `npm test --workspace frontend` exits 0 and reports at least one passing test (SETUP-02).
- The suite runs in the jsdom environment: `frontend/vitest.config.js` sets `test.environment` to `'jsdom'`.
- React Testing Library renders a component and queries it by role/text in the proof spec.
- The setup file stubs `window.matchMedia` so no matchMedia runtime error occurs when a component renders.
- `frontend/vite.config.js` is unmodified (git diff clean for that file) — dev/build config left untouched per D-01.
</verification>

<success_criteria>
- Running `npm test --workspace frontend` executes the configured Vitest runner (jsdom) and reports pass/fail — SETUP-02 satisfied.
- React Testing Library is installed and configured; the sample spec renders a component and queries it by role/text.
- Test setup handles the browser globals MUI/React Router need in jsdom (window.matchMedia) without runtime errors.
- All locked decisions honored: standalone `frontend/vitest.config.js` re-declaring the react plugin (D-01), full RTL kit as frontend devDependencies (D-02), single shared setup file wiring jest-dom + matchMedia stub + RTL afterEach(cleanup) with explicit vitest imports (D-03), throwaway inline proof component (D-04). No `globals: true`; `frontend/vite.config.js` untouched.
</success_criteria>

<output>
Create `.planning/phases/04-frontend-test-tooling/04-01-SUMMARY.md` when done.
Record the exact resolved version pins for all five test devDependencies (in case npm
adjusted any caret during resolution).
</output>
